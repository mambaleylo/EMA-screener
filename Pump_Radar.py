#!/usr/bin/env python3
"""
Pump Radar v0.30.96 (fork of EMA Invert Experiment v0.1.10, itself a fork of
EMA Bounce Dossier v3.6.14 / SMC Optimizer v3.52.96)
- v0.30.96: серьёзный реальный баг, найден по прямому вопросу "стоп по
  bill и magma, что скажешь по винрейту" — BILL_USDT дал 483
  continuation-записи, но ТОЛЬКО 34 уникальные комбинации (tf, период,
  вход, стоп, тейк). Причина: дедуп блокировал новую запись только пока
  предыдущая ещё открыта (track_done=False). У continuation узкий стоп
  (1%) резолвится быстро — как только резолвится, ключ освобождается, а
  сама закрытая 4h-свеча ещё не сменилась (следующая через часы).
  Следующий скан-цикл видел ТОТ ЖЕ триггер и создавал НОВУЮ запись с
  ИДЕНТИЧНЫМИ entry/stop/take, та тоже резолвилась быстро — и по кругу
  каждый скан-цикл, пока свеча реально не закроется. Портило не только
  статистику (одно событие считалось десятками раз), но и было риском
  для автоторговли — мог открывать реальные сделки по кругу на одном и
  том же сетапе. Теперь ключ дедупа включает bar_close_ts и проверяется
  по ВСЕМ записям истории (не только открытым) — одна и та же свеча
  даёт сигнал ровно один раз, независимо от того, как быстро он
  резолвится. Старые дубликаты в истории фиксом не чистятся — стоит
  очистить историю вручную (кнопка на /ema_stretch), чтобы статистика
  дальше копилась честно.
- v0.30.95: по прямому запросу — три отдельных доработки.
  (1) Скан "Отрыва от EMA" теперь можно ограничить выбранными параметрами
  ЕМА+ТФ — на странице /ema_stretch новый блок с галочками напротив
  каждого периода (5/10/20) и каждого ТФ (15m/30m/1h/4h), настройка
  /stretch_scan_settings, персистится на диск. По умолчанию выбрано всё
  (поведение не меняется, пока пользователь явно не снимет галочки) —
  пустой список с любой из сторон трактуется как "выбрано всё", чтобы
  случайно снятые все галочки не остановили скан молча.
  (2) Автоторговля для "Отрыва от EMA" раньше была намертво привязана к
  топ-связке по винрейту (_stretch_diag_maybe_alert_top_combo вызывала
  её только для сигналов текущего лидера) — то есть по факту всегда
  "топ 1", список для торговли менялся сам без участия пользователя.
  Теперь отвязано: в настройках автоторговли — своя матрица галочек
  (ТФ×период, отдельно для фейда и продолжения), торговля идёт СТРОГО
  по выбранным пользователем конфигам, независимо от того, кто сейчас
  лидер по винрейту и ушёл ли по этой конкретной связке алерт в
  Telegram. Секция настроек также показывает, по каким конфигам сейчас
  реально ведётся автоторговля. Проверка на устаревание сигнала
  (staleness, было только у алертов) вынесена в отдельную функцию
  _stretch_rec_is_stale и применяется теперь и здесь — не торгуем по
  сигналу, который уже неактуален к моменту обработки, даже если он не
  идёт в Telegram.
  (3) Лог-файл на диске (~/pumpradar.log, до этого писался только в
  память для веб-страницы и терялся при рестарте) — с ротацией по
  размеру, не более 5 МБ: при превышении обрезается снизу (отбрасывается
  старая часть), в файле остаётся хвост.
- v0.30.94: по прямому сообщению "раньше баланс видело без проблем" —
  автосделка не открылась из-за RuntimeError "Нет баланса: 0.0", хотя в
  логе видно margin_mode=3 (не обычный cross), available=0.0 и
  cross_margin_balance=None — оба поля, что проверяет _gate_get_balance,
  пришли пустыми. Похоже, режим маржи на аккаунте сменился на что-то не
  cross/isolated, и баланс при этом режиме лежит в другом поле ответа
  API, которое код не проверял. Вместо гадания — теперь при получении
  0/пустого баланса печатается ВЕСЬ сырой ответ /futures/usdt/accounts
  целиком в лог, чтобы увидеть точно, в каком поле баланс на самом деле,
  и поправить код прицельно на следующей проверке, а не наугад.
- v0.30.93: финальная проверка безопасности автоторговли (v0.30.92) перед
  тем, как ей пользоваться — нашёл реальную гонку: между проверкой "нет
  ли уже открытой live-позиции по этой монете" и самим открытием ордера
  проходит сетевое время (запрос к Gate.io), а скан теперь параллельный
  (48 потоков) — теоретически два потока могли пройти проверку
  одновременно и открыть ДВЕ позиции по одной монете. Добавлена
  отдельная блокировка (_stretch_autotrade_open_lock), сериализующая
  ВЕСЬ check-then-open как единый атомарный блок — не только чтение
  списка live-позиций, но и сам вызов _gate_open_position. Открытие
  сделок теперь строго по одной за раз, не параллельно — это осознанный
  компромисс скорости ради безопасности реальных денег.
- v0.30.92: по прямому запросу — автоторговля для "Отрыва от EMA", Long
  или Short на выбор, % депозита на сделку, вход/стоп/тейк из самого
  сигнала. Инфраструктура (подписанные запросы к Gate.io Futures,
  расчёт размера позиции и плеча, market-вход, TP/SL price_orders,
  ретраи, алерты на провал) уже была в файле от старой системы EMA
  INVERT (_gate_open_position и вся цепочка вокруг неё) — просто не была
  подключена к новому движку. Плечо теперь ФИКСИРОВАННОЕ (по памяти
  проекта — план 10х), не выводится из %риска: risk_pct для
  _gate_open_position вычисляется на лету как leverage×stop_pct, чтобы
  формула round(risk_pct/stop_pct) детерминированно давала ровно
  заданное плечо, независимо от того, у фейда стоп 0.5% или у
  продолжения 1%. Автосделка открывается ТОЛЬКО когда алерт реально
  отправлен (не подавлен фильтром устаревания v0.30.79) — не торгуем по
  сигналу, который уже неактуален. Новый фоновый цикл
  _stretch_autotrade_watch_loop следит за реально открытыми
  live-позициями и шлёт алерт с настоящим PnL при закрытии (переиспользует
  _check_position_closed_and_alert). Новая страница-панель на /ema_stretch:
  режим (выкл/long/short), % депозита, плечо, вкл/выкл с подтверждением.
  По умолчанию enabled=False — не активна без явного включения.
  ⚠️ ВАЖНО: реальные деньги. Функции сигнатур/подписи Gate.io API уже
  были в файле (предположительно рабочие, использовались старой
  системой), но саму НОВУЮ цепочку (расчёт риск_pct под фикс. плечо,
  gating по режиму/направлению, обработчики настроек) вживую против
  Gate.io не проверял — нет доступа к их API из этой среды. Настоятельно
  тестировать сначала с минимальным % депозита.
- v0.30.91: по прямому вопросу "что можно добавить для лонга" — trend_pct
  (v0.30.90) уже собирается на обеих стратегиях, гипотеза для
  continuation обратная фейду (стретч ПОВЕРХ уже идущего тренда —
  подтверждение реального движения, не разовый спайк из затишья), сможем
  сравнить, когда накопится статистика. Добавлена вторая, новая метрика,
  специфичная для тезиса продолжения — объём триггерной свечи
  относительно фона (trigger_vol_ratio): настоящий пробой обычно идёт с
  ростом объёма, случайный шпиль на тонком объёме скорее выдыхается. Не
  требует доп. сетевых запросов — считается из уже загруженных свечей.
  Добавлена разбивка по объёму в сводку (STRETCH_VOL_BUCKETS: низкий/
  нормальный/высокий) — как и с трендом, сравним винрейт по сегментам,
  когда накопится статистика.
- v0.30.90: по прямому запросу — "если всплеск на затишье, шанс на откат
  выше, чем на тренде, сравнить потом винрейт". Добавлено новое поле
  trend_pct на каждой записи (и фейд, и continuation) — измеряет силу
  движения цены за 20 баров ДО триггерной свечи (не включая её саму,
  иначе сам отрыв исказил бы измерение). Не требует доп. сетевых
  запросов — считается из уже загруженных свечей в ctx. Добавлена
  разбивка по силе тренда в сводку (STRETCH_TREND_BUCKETS: затишье
  <3%, умеренный тренд 3-8%, сильный тренд 8%+) — как только накопится
  статистика, можно будет честно сравнить винрейт фейда в затишье
  против винрейта на тренде через экспорт JSON.
- v0.30.89: реальная находка по прямому вопросу "топ-1 продолжение, а
  сигнал шортом" — таблица сводки сортировалась ЕДИНЫМ списком (фейд и
  continuation вперемешку), из-за чего выглядело так, будто есть только
  ОДИН общий лидер — хотя на деле у каждой стратегии свой топ-1, и
  алерты идут по нему независимо (логика алертов и раньше была верной,
  проблема была чисто в отображении сводки, вводила в заблуждение).
  Теперь сортировка сначала группирует по стратегии (фейд, потом
  continuation), внутри группы — как раньше по винрейту. На странице
  добавлены заголовки-разделители между блоками и корона 👑 у реального
  лидера каждой стратегии — теперь сразу видно, что их два, не один.
- v0.30.88: реальная находка на первых 1491 решённых continuation-сигналах
  (дедуп-фикс v0.30.86 наконец дал данные) — у continuation ОБРАТНАЯ
  зависимость от фейда: чем сильнее отрыв, тем ЛУЧШЕ результат (2-2.5%=
  винрейт 27%/PnL -0.18%, 2.5-3%=25%/-0.26%, 3-4%=26%/-0.21%, 4-5%=
  35%/+0.04%, 5%+=40%/+0.21%). Зона 2-5% — "ничья земля", не работает ни
  как фейд (уже отсекается потолком 2.0%), ни как продолжение — а
  continuation раньше триггерился сразу с >2.0%, тем же порогом, что и
  потолок фейда. Добавлен свой, отдельный порог STRETCH_CONTINUATION_
  MIN_PCT=5.0 — теперь между стратегиями настоящий зазор, а не общая
  граница. Заодно подтвердилось на свежих данных: фейд (то, что реально
  алертится, отрыв ≤2%) — винрейт 44.5%, PnL +0.148%, в плюсе; фейд на
  отрывах >2% (не алертится, но записи копятся) — 13.2%/-0.337%, тот же
  паттерн, что и раньше.
- v0.30.87: по прямому запросу "снова рваный график, ужесточаем" —
  четвёртый живой промах подряд (MUFG_USDT, после RLS/FUTUON/MEGA)
  показал, что прежние пороги фильтра неликвида всё ещё слишком мягкие.
  Ужесточил все три числа разом: допуск на "плоскую" цену 0.02%→0.08%,
  относительный порог "мёртвого" бара 2%→5% от пика объёма в окне, доля
  мёртвых баров для срабатывания 35%→25% (нужно меньше мёртвых баров,
  чтобы сработало). Если после этого паттерн всё ещё проскакивает —
  вероятно, нужна отдельная проверка не по объёму, а по ДИАПАЗОНУ бара
  (high-low), раз именно "спайк + плоский хвост" повторяется так упрямо.
- v0.30.86: серьёзный реальный баг, найден по прямому вопросу "когда уже
  появятся лонги?" — проверил свежий экспорт: 2217 fade-записей с
  отрывом >2% (то есть событий, достаточных для continuation, было
  полно), но continuation-записей — 0, ни одной. Причина — дедупликация
  в цикле сканирования сравнивала кандидатов по ключу symbol|tf|period,
  БЕЗ стратегии. Fade и continuation для одной и той же связки
  создаются в ОДНОМ батче кандидатов с одинаковым ключом — fade
  добавлялась первой, помечала ключ "уже отслеживается", и continuation
  для того же самого события тут же отбрасывалась как "дубликат" на
  следующей итерации того же цикла. Добавил strategy в ключ
  дедупликации — теперь fade и continuation не конфликтуют.
- v0.30.85: по прямому вопросу "для лонга понятно, а для шорта?" — тот же
  диагноз, что и у продолжения до правки: старый фейд-стоп 2.0% при
  обычно МЕНЬШЕМ тейке (уровень EMA, часто 1-2%) означал риск больше
  награды — ровно убыточную конфигурацию (нужен винрейт 66%+ на
  брейкивен). Симуляция на 2969 решённых сигналах подтвердила: внутри
  лучшего сегмента (отрыв 1-2%, n=1458) — стоп 2.0% даёт винрейт 66%, но
  средний PnL всего +0.19%/сделку; стоп 0.5% — винрейт ниже (55%), но
  средний PnL +0.48%/сделку, почти в 2.5 раза лучше матожидание. Более
  узкие стопы (0.1-0.3%) в бэктесте чуть лучше, но уже опасно близко к
  обычному спреду/проскальзыванию — 0.5% разумный компромисс. Сузили
  STRETCH_REF_STOP_PCT 2.0→0.5 — это меняет и реальный стоп новых
  фейд-сигналов, и линейку "выживаемости" в ранжировании (используют одну
  и ту же константу). Обновлены текст на странице и заголовок таблицы,
  убраны устаревшие хардкод-упоминания "2%".
- v0.30.84: по прямому предложению — у фейда логика была "стоп 2% /
  тейк ~1%" (широкий стоп, узкая цель — ждём слабый откат). Для
  продолжения тренда это перевёрнуто зеркально: узкий стоп 1% (настоящий
  тренд не должен откатываться против нас сильно) и цель 2% (ловим сам
  импульс) — RR тот же 2:1, но абсолютные числа меньше и логика
  соответствует самой идее тренда, а не скопирована с фейда бездумно.
  Всё ещё НЕ откалибровано по историческим данным (v0.30.83) — стартовая
  прикидка, копим статистику с нуля.
- v0.30.83: по прямому запросу — потолок отрыва для алерта вернули
  обратно на 2.0% (было временно занижено до 1.5 в v0.30.82), и добавлена
  вторая, отдельная стратегия для отрывов больше потолка: "продолжение"
  (LONG на импульс, а не SHORT на возврат к EMA) — раз данные показали,
  что сильные отрывы почти всегда оказываются реальным трендом, имеет
  смысл торговать это направление, а не просто отбрасывать сигнал.
  ⚠️ Экспериментально — стоп 2%/тейк 4% (RR 2:1) НЕ откалиброваны по
  историческим данным (своей статистики по продолжению ещё нет), это
  стартовая прикидка. Реализация: новое поле "strategy" (fade/
  continuation) на каждой записи, сводка теперь группируется по
  (ТФ, период, стратегия) — не мешает фейд и продолжение в одну
  статистику. Алерты идут отдельно по топ-связке КАЖДОЙ стратегии
  параллельно. Трек-луп получил отдельную, более простую ветку для
  continuation (чистый вход/стоп/тейк по цене, без EMA — там сама идея
  не про возврат к уровню, а про движение дальше). Обновлены сводная и
  сырая таблицы на странице (колонка "Стратегия"), чарт-рендерер и
  подписи в Telegram — везде видно, какая это стратегия.
- v0.30.82 (короткоживущая, тут же уточнена до отдельной стратегии
  в v0.30.83): пробовал сузить потолок отрыва до 1.5% по прямому вопросу
  "разделить на 1% и 2% отдельно" — разобрал реальный PnL по силе отрыва (979 решённых сигналов,
  все связки): 1-2%=винрейт 62%/средний PnL +0.07% (положительно), 2-3%=
  23%/-1.02%, 3-5%=14%/-1.19%, 5%+=2%/-1.86%. Чем сильнее исходный отрыв,
  тем реже цена откатывается ДО того, как заденет фиксированный стоп
  2% — сильные отрывы почти всегда оказываются реальным трендом, а не
  перегревом; идея фейда работает только на мелких отрывах. Добавлен
  потолок STRETCH_SIGNAL_MAX_PCT=2.0% — алерт не шлётся на отрывы больше
  2%, хотя запись всё равно копится в истории для дальнейшего анализа.
  Известное ограничение: сама сводка/ранжирование топ-связки ещё
  считается по ВСЕМ отрывам вперемешку (хорошим 1-2% и плохим 2%+) — это
  можно уточнить отдельно, если понадобится более честное ранжирование.
- v0.30.80: реальный промах фильтра неликвида, найден на живом примере
  FUTUON_USDT ("монета с тире" — та же картина, что и у RLS_USDT раньше).
  Проверка на "бары без объёма" требовала СТРОГО нулевой объём (vol==0),
  а "мёртвые" бары на тонком рынке обычно не буквально нулевые — одна
  крошечная сделка уже даёт vol>0 и проскакивает мимо проверки. Теперь
  бар считается "мёртвым", если его объём меньше 2% от МАКСИМАЛЬНОГО
  объёма в этом же окне — относительный порог, не абсолютный ноль, ловит
  и почти-нулевые бары, не только буквально пустые.
- v0.30.79: по прямому запросу — "задержка просто по факту запуска
  позднего, такие сигналы неактуальны, зачем их слать" (RIF/ESPORTS,
  16-17 мин задержки при debug-разбивке очередь+лимит+запрос всего
  ~11-13 сек). Раньше уже пробовали резать по ИТОГОВОЙ задержке — не
  годилось, тогда она была большой почти у всех сигналов. Теперь режем
  точнее: смотрим, сколько времени прошло ДО постановки в очередь
  (submitted_ts - bar_close_ts, новое поле debug_submitted_ts) — если
  событие уже было старым, когда его только поставили на обработку, это
  типичный след пропущенного цикла/рестарта, а не нормальная рабочая
  задержка (та обычно секунды, судя по debug-полям). Порог — 5 минут
  (STRETCH_ALERT_MAX_STALENESS_SEC). Запись всё равно остаётся в истории
  для статистики — режем только отправку алерта в Telegram.
- v0.30.78: по прямому запросу — приоритет ранжирования топ-связки
  (та, по которой шлются алерты) отдан винрейту тейк/стоп
  (signal_win_rate), не stop_survival_rate, как было с v0.30.60.
  stop_survival_rate остался вторым критерием (тай-брейк) — полезен как
  более широкая, менее шумная выборка риска. Отсутствие данных (None)
  по-прежнему считается худшим (0), чтобы связка без решённых сигналов
  не всплыла в топ случайно. Обновлены текст алерта в Telegram и
  описание на странице.
- v0.30.77: реальный баг, найден по прямому вопросу "стоп словил, а в
  логах нет" (EPIC_USDT, 4h EMA20). Проверил живую цену на скрине —
  локальный пик 0.6142 реально выше стопа 0.610368, но в записи
  max_adverse_stretch_pct как был равен исходному отрыву при создании
  (16.103), так и остался — наша система вообще не увидела этот скачок.
  Причина — _gate_get_price() берёт ОДНУ точку цены раз в 60 секунд
  (STRETCH_DIAG_TRACK_POLL_SEC): если цена дёрнулась выше стопа и
  вернулась быстрее минуты между опросами, точечная выборка её просто не
  видит, хотя на бирже стоп сработал бы по факту касания. Добавлена
  проверка по последним ~15 минутам 1-минутных свечей (high/low, не одна
  точка) — со своим коротким кэшем (30с), чтобы несколько записей одного
  symbol не дублировали запрос. Заодно нашёл и починил соседний реальный
  баг в _fetch_candles — при дробном days (мой новый вызов передаёт
  days=0.015) since могло остаться float, а Gate.io отклоняет дробный
  `from` (та же болезнь, что уже чинили под offset_days в v0.30.17).
- v0.30.76: реальный дедлок, найден по прямому сообщению "нажал удалить,
  подвисло, после рестарта история на месте". В обработчике
  /ema_stretch_history_clear (v0.30.75) держали _stretch_diag_lock и
  ВНУТРИ него звали _stretch_diag_save(), которая САМА пытается взять тот
  же лок — threading.Lock не реентерабелен, поток блокируется сам на
  себе намертво. Запрос никогда не завершался, файл на диске не
  перезаписывался (только очистка в памяти произошла), поэтому после
  перезапуска Termux подхватилась старая история с диска. Проверил все
  остальные вызовы _stretch_diag_save() — там лок уже был отпущен до
  вызова, баг был только в этом одном обработчике.
- v0.30.75: по прямому запросу — старая история "Отрыв от EMA" копилась
  в период, когда трек-луп был сломан (v0.30.74) и не проверял открытые
  записи вообще — их реальный исход (задело ли стоп/тейк за то время,
  пока не проверялось) неизвестен, статистика по ним ненадёжна. Добавлена
  кнопка "Очистить историю" на странице /ema_stretch (эндпоинт
  /ema_stretch_history_clear, тот же паттерн, что уже был у
  /weekly_ema_history_clear) — дальше копим статистику честно, с рабочим
  параллельным трек-лупом.
- v0.30.74: серьёзный реальный баг, найден по прямому сообщению "стоп
  был, а в логах не нашёл, где-то баг". Проверил конкретную запись
  (BLUAI_USDT, вход 0.012482, стоп 0.012732) — цена на графике реально
  доходила до 0.012925 (выше стопа), но у записи last_checked_ts точно
  равен времени создания, хотя прошло уже 34 минуты. Проверил ВСЕ 1413
  открытых записей в файле — у 100% last_checked_ts == ts, трек-луп НИ
  РАЗУ не обновил ни одну из них. Причина — _stretch_diag_track_loop был
  ОДНОПОТОЧНЫМ: шёл по открытым записям по одной, с сетевым запросом на
  каждую. Когда открытых записей накопилось за тысячу, один проход
  просто не успевал закончиться до следующего цикла, а список тем
  временем только рос — ровно та же болезнь, что чинили в скане монет
  (v0.30.62), только тут пропустил при рефакторинге под тейк/стоп
  (v0.30.67). Теперь трек-луп параллельный, пул из 32 потоков — та же
  схема, что и у скана. Добавлено логирование длительности прохода.
- v0.30.73: реальная находка по новой разбивке задержки (v0.30.72) —
  медиана ожидания в самом рейт-лимите всего 0.4с, максимум времени
  запроса 13.2с (не 25с+, как было бы при повторных попытках после 429)
  — до реального лимита Gate.io мы пока не достаём. Настоящее узкое
  место: 24 потока × ~3.1с медианный запрос = максимум ~7.7 запросов/сек
  пропускной способности, что почти совпадает со старым лимитом 8/сек —
  воркеры упирались в СВОЮ ЖЕ мощность раньше, чем в лимит скорости.
  Подняли оба разом: STRETCH_DIAG_SCAN_WORKERS 24→48,
  STRETCH_RATE_LIMIT_PER_SEC 8→16 — должно примерно вдвое ускорить
  разгребание всплеска при синхронном закрытии свечей. Продолжаем
  следить за debug_gate_wait_sec — если начнёт расти, это будет
  сигналом, что настоящий потолок API где-то рядом.
- v0.30.72: по прямому запросу — "нужно точно понять, откуда задержка,
  9 минут это много!!". Хватит гадать (rate limit? очередь? рестарт?) —
  добавлена честная разбивка задержки по стадиям вместо одного общего
  числа. Три новых поля в каждой записи: debug_queue_wait_sec (сколько
  ждала своей очереди у 24 потоков после того как задача понадобилась),
  debug_gate_wait_sec (сколько ждала в ограничителе скорости 8 запросов/
  сек), debug_fetch_sec (сколько занял сам сетевой запрос к Gate.io).
  Сумма трёх плюс исходное "ожидание закрытия свечи до начала цикла"
  и даёт итоговую задержку — теперь видно, какая стадия реально виновата,
  не приходится сравнивать косвенные признаки между экспортами. Показано
  и в тексте алерта Telegram, и в сыром экспорте JSON.
- v0.30.71: по прямому вопросу "10 минут задержки, надо решать" — оба
  сигнала (AIO_USDT/ZEST_USDT) пришли почти одновременно, оба с ~11мин
  задержкой: явный признак затора именно в момент, когда ~700 15m-свечей
  закрываются разом. Фильтр v0.30.68 убрал конкуренцию с 30m/1h/4h, но
  сам всплеск в ~700 одновременных запросов остался — похоже, упирается
  в rate limit Gate.io, и встроенный в _fetch_candles retry-with-sleep(5с)
  превращает это в многоминутный затор. Вместо очередного подбора числа
  потоков — честный ограничитель СКОРОСТИ запросов (не одновременности):
  не больше 8 фетчей/сек суммарно по всем потокам. Всплеск в 700
  запросов теперь займёт ~88 сек предсказуемо, без ретрай-штормов.
- v0.30.70: реальный промах фильтра неликвида (v0.30.69), найден на живом
  примере RLS_USDT — паттерн "вперемешку мёртвые бары без объёма + редкий
  одиночный скачок" не ловился ни проверкой на плоскость (не ВСЕ
  последние 8 баров одинаковые — там как раз есть скачок), ни проверкой
  на временной разрыв (скачок сам по себе не создаёт дыру во времени,
  бары идут подряд). Добавлена третья проверка — доля баров БЕЗ ОБЪЁМА
  ВООБЩЕ в окне последних 20 баров: если >35% — считаем тонким рынком с
  редкими одиночными сделками, независимо от временных разрывов. Именно
  такой одиночный скачок среди мёртвых баров и создавал ложный "отрыв".
- v0.30.69: по прямому запросу — "отсеивать монеты, где супер неликвид".
  Две новых проверки в _stretch_get_closed_ctx (до любых расчётов): (1)
  цена стоит на месте последние 8 закрытых баров (диапазон <0.02%) —
  почти наверняка мёртвая котировка, не настоящий отрыв; (2) разрыв между
  соседними барами больше 1.5×ожидаемого интервала (свеча не смогла
  закрыться, потому что сделок не было вообще) — тонкий рынок, дыры в
  истории. Обе переиспользуют тот же механизм, что и "недостаточно
  истории" (кэшируется как insufficient, перепроверяется через
  EMA_TOUCH_INSUFFICIENT_HISTORY_RECHECK_SEC) — хорошо сочетается с
  фильтром "не проверять раньше срока" из v0.30.68, лишние проверки не
  добавляются.
- v0.30.68: по прямому возражению — "тогда почти все сигналы будут
  исключены, это не решение" (справедливо: реальная медиана задержки на
  15m — 5.1 мин, порог в 3-5 мин резал бы 50-89% сигналов). Убрал идею
  фильтрации по задержке, вместо этого — настоящая находка причины:
  КАЖДЫЙ цикл в очередь уходили ВСЕ ~700 монет × 4 ТФ, даже те, чей кэш
  свечи ещё не истёк (в среднем это 95%+ проверок впустую — кэш просто
  вернул бы то же самое). Эти бесполезные проверки всё равно занимали
  место в очереди у ограниченных 24 потоков, оттягивая внимание от
  немногих, кому СЕЙЧАС реально нужен свежий фетч. Теперь заранее
  фильтруем — в пул уходят только (symbol, tf), у которых кэш реально
  просрочен или не создавался вообще. В обычном цикле задач должно стать
  на порядки меньше (кроме моментов синхронного закрытия у всех разом),
  и внимание потоков будет сосредоточено именно там, где нужно.
- v0.30.67: по прямому запросу — "может уже можно сигналы придумать с
  тейком и стопом". "Отрыв от EMA" из чисто наблюдательного инструмента
  стал настоящим сигналом: Вход = цена триггера, Стоп = вход+2%
  (STRETCH_REF_STOP_PCT, та же линейка, что уже использовалась для
  ранжирования связок), Тейк = уровень EMA. Реальная находка при
  реализации: раньше (v0.30.56/60) "плохой случай" фиксировался просто
  как рекорд худшего захода, БЕЗ выхода из отслеживания — то есть даже
  если цена реально прошла бы стоп, мы продолжали ждать и потом могли
  засчитать touch=true, если она ВСЁ РАВНО потом дошла до EMA. Это была
  задняя дата, не честный сигнал: в реальности позиции бы уже не было
  после стопа. Теперь трек-луп проверяет СТОП ПЕРВЫМ (в порядке реальных
  событий) — как только цена достигает уровня стопа, запись сразу
  закрывается как "stop", даже если позже вернулась бы к EMA. Новое поле
  signal_outcome (open/take/stop/timeout) и статистика "Винрейт
  тейк/стоп" в сводке и в тексте алерта Telegram. Картинка (и PIL, и
  stdlib-версия) теперь показывает все три линии — вход/стоп/тейк.
- v0.30.66: по прямому вопросу "сколько времени уходит на цикл" — раньше
  никак не измерялось, только косвенно оценивалось по лагу. Добавлен
  честный замер длительности каждого полного скана (в секундах), пишется
  в лог ("скан завершён за Xс") и видно прямо на странице /ema_stretch
  ("последний скан занял Xс") — не нужно больше гадать.
- v0.30.65: по прямому запросу — вернул STRETCH_DIAG_SCAN_WORKERS обратно
  на 24 (было 8 в v0.30.64). Честно: измерил реальный лаг по bar_close_ts
  (добавлено в v0.30.63) на присланном экспорте — медиана 4.9 мин на 15m,
  но 80+ МИНУТ на 4h. Это совпадает с моментами, когда 15m/30m/1h/4h
  закрываются ОДНОВРЕМЕННО у всех монет разом (раз в 4 часа) — то есть
  похоже на затор именно на самых массовых одновременных всплесках
  запросов к Gate.io, а не просто "мало потоков". У меня нет прямого
  доступа к консольным логам живого запуска — если после возврата на 24
  лаг на 4h не уменьшится или вырастет — это подтвердит рейт-лимит, и
  тогда правильный фикс — не подбор числа потоков, а размазывание
  запросов по времени (джиттер) вместо одновременного удара по всем ~700
  монетам разом на каждой синхронной границе закрытия.
- v0.30.64: по прямому вопросу "задержки по 5-6 минут" (после того как
  v0.30.62/63 должны были их сократить) — вероятная причина: 24
  параллельных потока на публичный Gate.io API без ключа могли упираться
  в rate limit (429), а в _fetch_candles уже была логика до 5 попыток с
  паузой 5с между ними на каждый неудачный запрос — если много монет
  разом ловят лимит, суммарная задержка утекает в минуты, ровно
  противоположно ожидаемому от параллелизации ускорению. Снижено
  STRETCH_DIAG_SCAN_WORKERS с 24 до 8 — всё ещё на порядок быстрее
  последовательного обхода, но заметно бережнее к лимитам API. Не
  подтверждено окончательно (нет прямого доступа к логам живого запуска)
  — стоит проверить логи на "⚠ Gate.io 429" примерно во время задержанных
  сигналов, чтобы удостовериться в причине.
- v0.30.63: по прямому запросу — "сигнал приходит, а новая свеча уже
  откатилась, и её нет на графике". Два действия: (1) STRETCH_DIAG_
  SCAN_POLL_SEC снижен со 180с до 45с — теперь, когда скан параллельный
  (v0.30.62) и завершается на порядок быстрее, можно опрашивать заметно
  чаще без перегрузки API, это прямо сокращает "ожидание следующего
  прогона". (2) Добавлена прозрачность вместо тихого допущения "картинка
  свежая": каждая запись хранит bar_close_ts (реальный момент закрытия
  свечи, не момент обнаружения), и в сам текст алерта теперь попадает
  строка "Задержка от закрытия свечи: Xс/Yм" — видно напрямую в
  сообщении, насколько снимок устарел к моменту доставки, без гаданий.
- v0.30.62: два прямых замечания разом.
  1) "картинка рисовалась без Pillow в других проектах" — добавлен свой
  PNG-рендерер на чистом stdlib (struct+zlib, оба всегда доступны, PIL
  не ставится намертво на Termux — знакомая история). Рисует свечи +
  линию входа + линию EMA-цели вручную, без текстовых подписей на
  картинке (нет растрового шрифта без PIL — но все числа и так есть в
  подписи сообщения). Теперь используется как основной путь, PIL (если
  вдруг есть) — как более красивый вариант с подписями, а не наоборот.
  2) "сигналы приходят позже, чем надо" — реальная находка: скан
  проверял ~700 монет × 4 ТФ ПОСЛЕДОВАТЕЛЬНО. Свечи одного ТФ у всех
  монет закрываются ОДНОВРЕМЕННО — в момент закрытия почти всем сразу
  нужен свежий фетч, и последовательный обход этой пачки реально мог
  занимать много времени — монета в конце очереди обнаруживалась с
  опозданием на многие минуты после настоящего закрытия свечи. Теперь
  проверка идёт ПАРАЛЛЕЛЬНО пулом из 24 потоков (ThreadPoolExecutor) —
  на порядок быстрее последовательного обхода.
- v0.30.61: по прямому вопросу "нет графика" — раньше любая причина
  провала рендера (PIL не установлен, мало свечей, что угодно) тихо
  схлопывалась в один и тот же None, видимый только в консольных логах,
  до которых на телефоне неудобно добираться. Теперь причина попадает
  ПРЯМО В ТЕКСТ самого алерта в Telegram ("⚠️ график не приложен: ..."),
  не нужно лезть в логи, чтобы понять, что произошло — включая явную
  проверку доступности PIL (частая причина на Termux).
- v0.30.60: по прямому уточнению — "буду торговать с фикс. 10х плечом,
  учти на будущее". Пересчитал риск с этой поправкой: у 4h/EMA20 (прежний
  лидер по чистому % отката) при стопе 2% (=20% маржи на 10х) сносило бы
  стопом ~24% случаев ДО разворота, у 15m/EMA5 — почти никогда (~4%).
  Ранжирование топ-связки (та, по которой шлются алерты) теперь по
  НОВОЙ метрике stop_survival_rate — доле случаев, где цена НЕ прошла
  STRETCH_REF_STOP_PCT=2.0% против входа прежде чем начать откат, а не
  по абстрактному "% отката когда-нибудь". Добавлена колонка в сводку,
  обновлён текст алерта в Telegram. Сохранена память проекта (пользователь
  планирует фикс. 10х плечо) — учитывать во всех будущих расчётах
  риска/стопа для этого движка.
- v0.30.59: по прямому запросу — к алерту по топ-связке добавлен кусочек
  графика (последние свечи + горизонтальная линия уровня EMA — это и
  есть "куда планируется движение", цель возврата). Переиспользован
  _render_signal_chart_png — уже был в файле, но нигде не был подключён.
  Если PIL недоступен или рендер не удался — тихий откат на обычный
  текстовый алерт (как было раньше), ничего не сломается.
- v0.30.58: реальный баг, найден по прямому вопросу "почему алерты
  пачками и потом тишина" — подтверждено по факту: у лидера (4h EMA20)
  событие AKE_USDT создалось в 10:27:02 UTC, а последний алерт в Telegram
  был почти на час раньше (09:44-09:49 UTC, доставлен как единая пачка).
  Причина — _stretch_diag_maybe_alert_top_combo() вызывалась ОДИН раз,
  ПОСЛЕ полного прохода по всем символам×ТФ (реально может занимать
  десятки минут сетевого времени на ~700+ монет × 4 ТФ) — все новые
  события копились весь проход, алерты по ним улетали разом пачкой в
  конце, с задержкой в десятки минут от реального момента события и
  тишиной на весь следующий проход. Теперь алерт проверяется СРАЗУ по
  каждой найденной записи внутри цикла, без накопления — не привязано к
  длительности всего прохода.
- v0.30.57: по прямому запросу — "нужны пары только по криптовалютам".
  У Gate.io нет отдельного API-поля "это акция/товар, не крипта" — весь
  список исключений строится ТОЛЬКО ручным пополнением по факту находки
  (уже 2 пробела было раньше: TQQQX, SOXL). Найдены живьём в данных
  "Отрыв от EMA" ещё 3 утечки: XAUT_USDT (Tether Gold — токенизированное
  золото), SKHYNIX_USDT и SKHY_USDT (акция SK Hynix, видны оба варианта
  тикера). Добавлены в исключения (переименован в GATE_NON_CRYPTO_SYMBOLS
  для ясности, старое имя GATE_XSTOCKS_SYMBOLS оставлено алиасом).
  Гарантии полноты списка НЕТ — если попадётся что-то ещё нерыночное
  (акции/ETF/товары), нужно будет добавлять по факту находки.
- v0.30.56: по прямому запросу — "Отрыв от EMA" раньше показывал только
  шанс вернуться (closest_stretch_pct/retrace%), но не худший случай на
  пути. Добавлено новое поле max_adverse_stretch_pct (пик удаления от
  EMA, пока не откатилась) — считается по ВСЕМ записям (не только
  оценённым, это живой риск-профиль). В сводке — avg/max
  extra_adverse_pct (насколько % ДАЛЬШЕ в среднем/по максимуму ушло от
  исходного отрыва, прежде чем откатиться или протухнуть). Показано и в
  таблице сводки, и в сырых записях на странице. Цель — видеть не только
  "вернулась ли", но и "насколько плохо было по пути", раз уже сама идея
  ловли затухания импульса требует понимать оба хвоста, не только
  благоприятный.
- v0.30.55: по прямому уточнению — "Отрыв от EMA" должен триггериться
  ИМЕННО на закрытии свечи, не на живой цене. Раньше _stretch_check_symbol
  считал stretch_pct от живой цены на каждом опросе (раз в 3 минуты) —
  мог поймать внутрисвечной всплеск, откатившийся сам ещё ДО закрытия
  бара (событие, которого "по свече" не было). Теперь используется цена
  и EMA именно последней ЗАКРЫТОЙ свечи — ctx закэширован до следующего
  закрытия, так что повторные опросы внутри одного бара безопасны
  (вернут то же самое, переоценки не будет). Живая цена осталась только
  в трек-лупе — там она нужна, чтобы поймать момент возврата к EMA.
  Отдельно: пока НИ ОДНА связка не набрала 10 оценённых событий для
  алерта по топ-связке (это не баг — данные копятся первый день).
- v0.30.54: реальный баг, найден по прямому вопросу "может это круто
  сработало?" — 11 "тейков" в свежем экспорте оказались НЕ новой часовой
  стратегией: все 200 записей имели tf="1w", а _migrate_open_signals_to_
  fixed_take() (v0.30.39, задумана для другого перехода) продолжала на
  КАЖДЫЙ рестарт слепо перетирать take любой открытой записи под ТЕКУЩЕЕ
  значение константы — древним зависшим сделкам с недельным ATR-based
  стопом (до 971% от входа!) тихо подрезало тейк до нового часового 1.8%,
  что и давало случайные "победы", не имеющие отношения к реальной
  стратегии. Теперь миграция трогает только записи с tf==SLOW_IMPULSE_TF
  (актуальная схема) — более старые вместо этого закрываются как
  "stale_retired" и не резолвятся/не мешаются дальше.
- v0.30.53: по прямому запросу — два новых переключателя.
  1) Алерт по топ-связке "Отрыв от EMA" в Telegram: каждый скан заново
  считает текущий рейтинг связок ТФ×EMA (тот же _stretch_diag_summary, что
  на странице), и если новый отрыв совпал с лидером (не меньше 10
  оценённых событий — иначе рано делать выводы по горстке случайных) —
  шлёт алерт. Лидер не зафиксирован — по мере накопления данных может
  смениться, алерты пойдут уже по нему без ручного вмешательства.
  2) Выключатель на slow_impulse (часовые EMA-сигналы) — раньше был
  жёстко закодирован (SLOW_IMPULSE_ENABLED=True, менялся только правкой
  кода), теперь персистится через тот же scan_settings.json, что и
  порог объёма/спреда, переключается чекбоксом на главной без
  редеплоя. Выключение останавливает поиск НОВЫХ касаний — уже открытые
  сделки резолвер по-прежнему доводит до конца.
- v0.30.52: по прямому уточнению — "Отрыв от EMA" (v0.30.51) ловил оба
  направления симметрично (вверх/вниз). Нужно было только вверх (цена
  выше EMA) → откат вниз к EMA. Убрано отслеживание отрыва вниз.
- v0.30.51: по прямому запросу — новая кнопка "Отрыв от EMA" (/ema_stretch).
  Отдельный, независимый от EMA Diagnostics/slow_impulse движок: триггер —
  ОТРЫВ цены от короткой EMA (5/10/20) на закрытии свечи на ≥1%, ТФ
  15м/30м/1h/4h. Дальше следит либо до касания той же EMA обратно (как и
  предложили — логичнее фиксированного окна), либо до таймаута (свой на
  каждый ТФ), считает % отката от исходного отрыва. Сводка разбита по
  ТФ×период EMA И по величине самого отрыва (1-3%/3-6%/6%+) — цель ровно
  та, что просили: "какой фрейм и EMA лучше всего откатывает". Два новых
  фоновых цикла (_stretch_diag_scan_loop, _stretch_diag_track_loop), своя
  история на диске (~/pumpradar_ema_stretch_diag.json), свой export.
  Полностью параллельно существующим движкам — ничего из slow_impulse/
  EMA Diagnostics/бэктеста не тронуто.
- v0.30.50: перевёл slow_impulse с недельного ТФ на часовой (SLOW_IMPULSE_TF
  = "1h") — по находке v0.30.49: единственный ТФ, где во всех 3 периодах
  avg_mfe>avg_mae в своей диагностике. По пути нашёл и починил реальный
  баг в _ema_touch_get_closed_ctx — функция ВСЕГДА фетчила ДНЕВНЫЕ свечи и
  для любого tf кроме "1w" просто использовала их как есть (не ресемплила
  и не фетчила нужный интервал) — то есть при простой смене tf="1h" код
  молча посчитал бы EMA/ATR по дневным барам, выдавая их за часовые.
  Теперь для tf не "1w"/"1d" фетчится РЕАЛЬНЫЙ нативный интервал через
  Gate.io API (уже используется в других местах, напр. поиск аномалий).
  Заодно поправил interval_sec для кэша (был захардкожен на 86400 сек для
  всего, кроме 1w — контекст обновлялся бы раз в сутки вместо раза в час).
  Take/stop пересчитаны под часовой масштаб (EMA_TOUCH_TAKE_FIXED_PCT=1.8%,
  EMA_TOUCH_STOP_FIXED_PCT=1.5%, RR=1.2 — по avg_mfe/avg_mae на 1h).
  Кулдаун — новый SLOW_IMPULSE_TOUCH_COOLDOWN_SEC=4ч (старые 24ч были под
  недельные бары, для часовых неоправданно редко) — не откалиброван по
  диагностике, осторожный компромисс, можно пересмотреть по факту.
  Известный нерешённый вопрос: гейт по аномалии объёма (14-30 дней
  lookback) был откалиброван под НЕДЕЛЬНЫЙ масштаб (искали "было ли
  когда-нибудь давление продаж" за пару недель до недельного касания) —
  для часового сетапа это может быть слишком широкое окно (почти всегда
  найдётся хоть один всплеск за 30 дней). Не трогаю сейчас — сначала
  смотрим, что реально даёт часовой ТФ на практике, потом пересматриваем.
- v0.30.49: перестали гнаться исключительно за копированием референс-бота,
  посчитали свою статистику. Реальная находка #1 — RR: по всем 200 живым
  сигналам медианный RR оказался 0.103 (ни одного ≥0.5!) — стоп ATR-based
  на недельном ТФ на волатильных альтах улетал на 30-70%+ от входа против
  фиксированного тейка 4.5%. При таком RR брейкивен требует ~90%+
  винрейта, а собственная диагностика (ema_diagnostics, 8180 касаний)
  даёт bounce_rate 36-43% на 1w. Заменили стоп на фикс. % (свой выбор, не
  копирование — стоп референс-бота никогда не был виден), калибровано по
  своей же диагностике (median_mae 0.77-1.07% на 1w) — новый
  EMA_TOUCH_STOP_FIXED_PCT=3.0, RR=1.5. Затрагивает только НОВЫЕ сигналы,
  199 уже открытых остаются на старом ATR-based стопе (не трогаем чужие
  открытые позиции задним числом).
  Находка #2 — по той же диагностике недельный ТФ (1w, все 3 периода:
  7/14/28) статистически ХУДШИЙ: во всех трёх avg_mae > avg_mfe
  (неблагоприятное движение в среднем больше благоприятного). А часовой
  (1h, особенно период 28: bounce_rate 43.5%, n=604, avg_mfe 1.76% >
  avg_mae 1.64%) — единственный ТФ с явным позитивным перекосом. Пока не
  трогаем (slow_impulse остаётся на 1w) — фиксируем находку, обсуждаем
  дальше отдельно, не меняем сразу всё разом.
- v0.30.48: по скриншоту — v0.30.47 остановила фоновые циклы, но забыл
  панели на странице настроек: переключатель "голых" сигналов
  (памп/дамп/аномалия без EMA), таблицы "Живые пампы"/"Живые дампы"/
  "Аномальный объём продаж". Убрал весь блок из HTML + связанный JS
  (loadPumpDumpToggle/togglePumpDumpAlerts, refreshPumps/refreshDumps/
  refreshVolAnomaly и их setInterval-опрос, записи в CLEAR_ENDPOINTS) —
  иначе JS падал бы на document.getElementById(null) при попытке
  обновить несуществующие элементы (ловилось try/catch, но зря дёргало
  /pump_status,/dump_status,/vol_anomaly_status каждые 8 сек без толку).
  Таблица "Касания EMA7/14/28" (slow_impulse) и настройки алертов
  (Telegram/ntfy, общие для slow_impulse) не тронуты. Backend-эндпоинты
  (/pump_status и т.д.) оставлены как есть — не мешают, просто больше
  никем не вызываются.
- v0.30.47: по прямому запросу — убрано лишнее относительно цели
  "скопировать его бота". Проверено по ВСЕМ собранным сравнениям: ни
  один из его 22+ сигналов ни разу не пришёл через памп/дамп-детектор
  или Volume Peak Watcher (аномалия объёма как ИСТОЧНИК алерта, не
  путать с гейтом внутри slow_impulse — тот остался, использует другую,
  независимую функцию _manual_scan_find_anomalies). EMA28W-трекинг
  зависел ИСКЛЮЧИТЕЛЬНО от них как источников событий. Остановлены
  фоновые циклы: _pump_detect_loop, _pump_dump_track_loop,
  _vol_peak_find_loop, _vol_peak_watch_loop, _vol_anomaly_track_loop,
  _ema28w_touch_track_loop — тем же способом, что раньше в файле уже
  останавливали _ema_signal_loop (не запускается, код остаётся в файле
  нетронутым). Обновлено описание на главной странице (было
  устаревшее "детектит памп"). slow_impulse, ручной скан, бэктест,
  диагностика, Pump Match (калибровка отрисовки — не про памп-сигналы)
  не тронуты, ничего из этого не зависит от убранных циклов.
- v0.30.46: разгадал, почему MITO/NAORIS/AVAAI из ручного скана выглядели
  так, будто должны были сработать, а не сработали. Ручной скан считает
  dist_atr от цены ПОСЛЕДНЕЙ ЗАКРЫТОЙ свечи (bar_close_ts — у недельного
  ТФ это может быть 5-6 дней в прошлом), а реальный движок
  (_ema_touch_check_symbol) сравнивает с ЖИВОЙ ценой прямо сейчас — за
  дни между закрытием бара и моментом скана цена вполне могла уйти от
  уровня. Ручной скан теперь ДОПОЛНИТЕЛЬНО прогоняет ВСЕ те же гейты,
  что и slow_impulse (живая дистанция, cooldown, гейт по аномалии
  объёма, потолок/пол объёма) и пишет единый вердикт "live_engine_check"
  с полем would_fire_now — теперь видно НАПРЯМУЮ, сработает движок
  прямо сейчас или нет, и если нет — на каком именно гейте застряло.
- v0.30.45: реальный баг от своей же правки (v0.30.44) — экспорт
  показал 191 skipped_cooldown / 9 open за ОДИН цикл сканирования (10
  мин), и все 191 — РАЗНЫЕ монеты, не дубли. Значит на каждый цикл
  уходит ~190+ новых diag-записей в общий _weekly_ema_recent (кап 200)
  — буфер целиком перезаписывается ОДНИМ циклом, и реально открытые
  сделки ("open"), которые резолвер ждёт для проверки тейка/стопа,
  вымывались из истории быстрее, чем успевали разрешиться. Заведён
  отдельный, НЕперсистируемый _weekly_ema_skipped (свой кап 200) — на
  экспорте (/weekly_ema_export) оба буфера склеиваются в один файл
  (как и просили — не два разных файла), а резолвер и сохранение на
  диск по-прежнему трогают только _weekly_ema_recent.
- v0.30.44: 9 из его 22 сигналов совпали с нашими за всё время (HEMI,
  PIEVERSE, SAPIEN, COAI, XPIN, HAEDAL, JCT, EVAA, HANA) — остальные 13
  (B, SAGA, TURTLE, UB, ERA, BLESS, BANANA, TREE, USELESS, ME, BLUAI,
  1INCH, ROAM) НИ РАЗУ не сработали у нас, хотя дистанция до недельной
  EMA и объём у 10 из этих 13 в норме — то есть где-то теряются уже
  ПОСЛЕ прохождения проверки касания. Раньше это было не разглядеть: в
  weekly_ema_signals попадали только УЖЕ отправленные алерты — если
  монета терялась на cooldown или на гейте по аномалии, в файле не
  оставалось вообще никакого следа. По прямому запросу — не отдельный
  файл/лог, а те же записи в тот же _weekly_ema_recent (тот же
  weekly_ema_export/weekly_ema_signals файл), с outcome=
  "skipped_cooldown" / "skipped_no_anomaly" вместо "open", плюс новые
  поля: anomaly_z, anomaly_count, cooldown_remaining_sec,
  weekly_chg_7bar_pct. Резолвер исхода фильтрует строго по outcome==
  "open" — новые записи не тронет. Теперь при следующем экспорте будет
  прямо видно, почему конкретно каждая из этих 13 не дошла до алерта.
- v0.30.43: сверил новую пачку его сигналов (28 ручных сканов, включая
  свежие ERA/HANA) с тем, что реально прислал наш weekly_ema после
  гейта по аномалии (v0.30.42) — прислал BTC/ETH/SOL/DOGE, а среди ВСЕХ
  20 его уникальных сигналов (за всё время сбора) — ни одного мажора,
  максимальный объём $17.5М (B_USDT), 19 из 20 сильно ниже. Гейт по
  аномалии объёма это не ловит — у крупных монет тоже случаются
  z>=4.0 всплески на общерыночных распродажах, просто это другая
  механика, не то, что торгует референс-бот. Добавлен потолок
  SLOW_IMPULSE_MAX_VOLUME_USD=$20М (запас ~15% над его максимумом) —
  теперь slow_impulse исключает мажоры/крупные альты, не трогая его
  реальный диапазон объёмов.
- v0.30.42: разобрал 18 ручных сканов его сигналов (сравнимых по истории,
  ROAM исключён — недостаточно данных). Свёл в таблицу три поля: вход
  (ref_entry), тейк (ref_take), аномалии объёма из того же ручного скана.
  Тейк — везде ровно 4.500-4.503% от входа, кроме одного явного выброса
  (UB_USDT, похоже опечатка при ручном занесении) — наша формула
  EMA_TOUCH_TAKE_FIXED_PCT уже верна, тут менять нечего. Вход у всех 18
  ложится в пределах 0-5% на одну из EMA7/14/28 (в основном недельных) —
  наша логика "вход=уровень EMA" тоже уже верна. А вот что было ОБЩИМ у
  ВСЕХ 18 и чего у slow_impulse не проверялось вообще — красная
  (распродажная) аномалия объёма (z>=VOL_PEAK_ZSCORE_THRESHOLD=4.0) на 1h
  или 4h где-то в недавней истории монеты: минимальный найденный max_z
  среди 18 — 5.0, у остальных 6.3-11.4, то есть с хорошим запасом выше
  порога, не "почти не прошло". slow_impulse раньше стрелял по чистому
  касанию EMA (dist_atr<=0.25) без этого условия — отсюда и разрыв: мы
  ловим ЛЮБОЕ касание почти по всему универсуму, он — только те монеты,
  где касанию предшествовала заметная распродажа. Добавлен блокирующий
  гейт SLOW_IMPULSE_REQUIRE_SELL_ANOMALY, использует ту же
  _manual_scan_find_anomalies (тот же порог/lookback, что и ручной скан) —
  теперь как проверку перед отправкой алерта, не только для диагностики.
- v0.30.41: ещё один реальный найденный баг, по прямому вопросу
  "отслеживается ли вообще был ли вход в сделку". Ответ был — нет: вход
  (лимитка на EMA-уровне) вообще не проверялся, просто ПРЕДПОЛАГАЛОСЬ,
  что раз алерт сработал (касание в пределах допуска WEEKLY_EMA_TOUCH_ATR
  от уровня, не точно НА уровне), значит лимитка исполнилась. Цена вполне
  могла развернуться, так и не дойдя до точной цены входа — "сделки" по-
  настоящему не было бы вообще, но мы всё равно засчитывали бы тейк или
  стоп. Теперь сначала ищем первую свечу, где цена реально дошла до
  entry (high/low, не close) — новое поле entry_filled_at — и ТОЛЬКО С
  ЭТОГО МОМЕНТА начинаем считать стоп/тейк. Если вход не подтверждается
  за WEEKLY_EMA_ENTRY_FILL_TIMEOUT_SEC (3 дня) — запись помечается
  "no_fill" и не участвует в винрейте (честно: сделки не было). Новая
  метка видна и в разбивке по источнику (/weekly_ema_source_stats), и на
  главной странице. Проверено тестом: цена, никогда не дошедшая до
  входа, корректно даёт no_fill по таймауту; цена, дошедшая до входа и
  ЗАТЕМ пробившая стоп в следующей свече, корректно даёт stop.
- v0.30.40: реальный, серьёзный найденный баг — прямой вопрос "точно ли
  всё так хорошо, отслеживается ли очерёдность стоп/тейк" оказался
  обоснован. _weekly_ema_resolve_loop раньше проверял ТОЛЬКО живую цену
  раз в 2 минуты, не весь путь цены между проверками — если цена сначала
  пробивала стоп, а потом отскакивала выше тейка ДО следующего опроса,
  это оставалось незамеченным, записывался тейк, хотя по-настоящему
  сделка закрылась бы в минус раньше. Системная погрешность в пользу
  тейка, влияла на ВСЮ статистику с самого начала фичи. Переписано на
  проверку по свечам (1m, high/low) в хронологическом порядке с момента
  последней проверки (новое поле last_checked_ts на каждой записи) —
  первая свеча, где пробит хоть один уровень, определяет исход. Если
  свеча накрывает оба уровня разом — консервативно считается стоп (не
  можем знать точный порядок внутри одной свечи без тиковых данных). Для
  старых открытых записей (без last_checked_ts) при первом проходе
  честно перепроверяется ВЕСЬ путь с момента сигнала. Проверено тестом на
  точном сценарии из вопроса: стоп пробит раньше, потом отскок выше
  тейка — теперь корректно засчитывается стоп, не тейк.
- v0.30.39: по прямому запросу — тейк переведён на фиксированные 4.5% от
  входа (EMA_TOUCH_TAKE_FIXED_PCT), подтверждено на 23 чистых наблюдениях
  внешнего референс-бота (4.499%-4.503%, практически идеальная точность,
  не совпадение) — раньше был ATR-based (RR 1.5 × 1.5×ATR стопа),
  переменный по волатильности монеты. Стоп остался ATR-based — данных на
  его стоп нет вообще (ref_stop ни разу не был виден на скринах). RR
  теперь не константа, а фактическое отношение (тейк-дистанция/стоп-
  дистанция), считается для каждого сигнала отдельно. По прямому
  уточнению — уже ОТКРЫТЫЕ (нерешённые) сигналы пересчитываются на новую
  формулу автоматически при старте (_migrate_open_signals_to_fixed_take),
  чтобы не остаться зависшими на старой цели; уже РЕШЁННЫЕ (take/stop) не
  трогаются — история результатов не переписывается задним числом.
  Проверено тестом: формула точна для LONG/SHORT, миграция трогает
  только открытые записи.
- v0.30.38: реальная находка, третья по счёту на эту же тему —
  BANANA_USDT (объём $21,643) снова оказался НИЖЕ нашего уже сниженного
  порога slow_impulse ($150к, v0.30.29). Опущено до почти нулевого уровня
  ($5к, отсеивает только полностью мёртвые листинги) — реальную защиту
  от проскальзывания даёт проверка СПРЕДА (общий max_spread_pct), это
  более прямой показатель тордегуемости, чем сырой объём. Отдельно —
  ROAM_USDT встретился БЕЗ истории вообще (insufficient_history и по
  дню, и по неделе, объём 0) — это не вопрос порога, монета физически
  слишком новая для надёжного расчёта EMA (не хватает баров даже для
  EMA7), форсировать расчёт на пустых данных было бы небезопасно —
  зафиксировано как честный, пока не закрываемый разрыв.
- v0.30.37: по прямому запросу — "гораздо больше монет алертят, чем у
  него". Реальная находка: slow_impulse слал ОТДЕЛЬНЫЙ алерт на КАЖДЫЙ
  период EMA, подходящий по расстоянию одновременно (пример из логов:
  TLM сработал на EMA7+14+28 разом, WLD и NEAR — по 2 раза каждая) — это
  раздувало число "лишних" алертов относительно референс-бота, который
  шлёт один сигнал на монету, по ближайшему уровню. Теперь берём только
  САМОЕ БЛИЗКОЕ касание (наименьший dist_atr), не все подряд — ровно один
  алерт на монету за цикл. Проверено тестом: монета, одновременно близкая
  к трём уровням (как реальный случай TLM), теперь даёт 1 алерт вместо 3,
  выбирая корректно самый близкий период.
- v0.30.36: по прямому вопросу — "отслеживаем ли статистику этих
  сигналов, и где она". Данные УЖЕ были (каждая запись хранит
  triggered_by и итог take/stop/open), просто не было готовой сводки.
  Новая _weekly_ema_source_stats() разбивает по источнику (памп/дамп
  напрямую, аномалия объёма, накопленный импульс) с винрейтом среди уже
  решённых. Новый эндпоинт /weekly_ema_source_stats + блок на главной
  странице над сводкой EMA-касаний, обновляется раз в 15с вместе с
  остальным. Проверено тестом: разбивка считается верно, эндпоинт
  отдаёт корректный JSON.
- v0.30.35: реальная находка на расширенной выборке (19 сигналов
  референс-бота суммарно за две партии) — порог по накопленному
  недельному движению (SLOW_IMPULSE_MIN_WEEKLY_CHG_PCT=15%, v0.30.28)
  резал БОЛЬШЕ реальных сигналов, чем помогал: 6 из 9 их сигналов,
  прошедших по расстоянию до EMA, не набирали 15% по моментуму (ACE
  +0.9%, PROVE -3.9%, EPIC -5.8% — далеко ниже, не просто "чуть не
  дотянули"). Расстояние до EMA само по себе оказалось надёжным
  критерием (9 из 11 прошли, кроме повторяющегося ERA — там у них,
  похоже, допуск шире нашего). Убран как БЛОКИРУЮЩИЙ фильтр — остался
  только как информационная пометка в тексте алерта. Проверено тестом:
  слабый момент (2%) больше не блокирует отправку, но виден в тексте.
- v0.30.34: по запросу — общая панель навигации в шапке всех пяти
  активных страниц (Главная /ema, Ручной скан /manual_scan, EMA
  Диагностика /ema_diag, Pump Match /pump_match, Бэктест EMA
  /weekly_ema_backtest) — переход между ними теперь в один клик, не через
  ручной ввод адреса. Старая /ema_full (нерабочий EMA Invert движок,
  сейчас не запущен) намеренно не тронута — незачем вести туда. Проверено
  тестом на всех пяти страницах.
- v0.30.33: реальная находка на живых скринах — ntfy получил WLD_USDT и
  ADA_USDT (07:48), а Telegram по тем же сигналам молчал вообще. Причина
  не в коде-асимметрии (проверено ранее — оба канала действительно
  вызываются симметрично), а в лимите скорости самого Telegram Bot API
  (~1 сообщение/сек в один чат): при частых сериях алертов (залп
  slow_impulse и подобное) каждый алерт отправлялся Telegram СВОИМ
  отдельным потоком одновременно — часть сообщений тонула молча (429),
  пока ntfy (без такого жёсткого лимита) получал всё нормально. Теперь
  Telegram-отправка (и текст, и фото) идёт через ОДНУ общую очередь
  (_telegram_send_queue + _telegram_sender_worker), последовательно, с
  паузой ~1.1с между сообщениями — не теряем сообщения из-за собственного
  всплеска. ntfy остаётся полностью независимым и немедленным, как и
  раньше. Проверено тестом: залп из 5 алертов уходит в Telegram
  последовательно с паузой ≥1с между каждым, ни один не теряется; ntfy
  при этом отправляет все 5 почти мгновенно, не дожидаясь очереди.
- v0.30.32: реальный пробел, найден на живом скрине — TQQQX_USDT
  (ProShares UltraPro QQQ, плечевой ETF) не был в списке исключений
  xStocks (v0.30.30). Заодно вскрылось: SOXL_USDT был упомянут в ТЕКСТЕ
  changelog'а v0.30.30 как "исключённый", но по факту в само множество
  GATE_XSTOCKS_SYMBOLS так и не попал — забыт при реализации, хотя в
  описании версии значился. Оба добавлены. Проверено тестом.
- v0.30.31: по прямому запросу — приоритет "как у референс-бота" (только
  сигналы, реально дошедшие до недельной EMA). AKE_USDT сработал 18 раз
  за экспорт (13 памп + 5 аномалия объёма), НИ РАЗУ не дойдя до
  полноценного сигнала с EMA — референс-бот такое в принципе не может
  увидеть, у него критерий "рядом с уровнем" первичный. Пампы/дампы уже
  были приглушены (v0.30.7), аномалия объёма — нет, слалась без разбора.
  Применён тот же PUMP_DUMP_ALERTS_ENABLED флаг к _vol_peak_fire_alert —
  теперь ОБЕ "голые" (без подтверждённого касания EMA) категории
  уведомлений подавлены одним и тем же переключателем (по умолчанию
  выключено). Детект, история и проверка EMA-триггера продолжают
  работать в фоне без изменений — переключатель влияет только на
  отправку голого текста. Обновлена подпись переключателя на странице.
  Проверено тестом: при выключенном флаге аномалия объёма не уходит в
  Telegram, но EMA-проверка всё равно вызывается; включение обратно
  восстанавливает отправку.
- v0.30.30: по прямому запросу — исключены токенизированные акции/ETF
  Gate.io ("xStocks": MSTRX, CRCLX, AAPLX, GOOGLX, TSLAX, AMZNX, COINX,
  NVDAX, METAX, HOODX, DFDVX, QQQX, SPYX) из отбора монет для ВСЕХ
  детекторов. Найдено при сравнении с внешним крипто-ботом — MSTRX
  (MicroStrategy), CRCLX (Circle) и SOXL (leveraged ETF) оказались среди
  "лишних" наших сигналов; это не крипта вообще, другой класс актива,
  которого физически нет на Bybit/MEXC — референс-бот их никогда не
  увидит, сколько бы наши критерии ни совпадали. Исключены из ОБОИХ
  независимых источников монет — _fetch_all_symbols() (используется
  почти всем: slow_impulse, диагностика, дамп/vol_anomaly-триггер) и
  отдельного _pump_fetch_tickers_snapshot() (свой фетч у пампа/дампа,
  v0.30.3). Список стартовый, может расти по мере новых листингов Gate.
  Проверено тестом на обоих источниках.
- v0.30.29: реальная находка — 4 сигнала референс-бота (HAEDAL/ERA/BLESS/
  XPIN) НИ РАЗУ не появились ни в живом slow_impulse-сканере, ни даже в
  "широкой" EMA-диагностике, хотя цифры (дистанция до EMA, накопленный
  моментум) подходили под наши пороги для 3 из 4. Причина: обе системы
  теперь используют ОДИН И ТОТ ЖЕ _fetch_all_symbols() с общим порогом
  ликвидности $3млн (объединено в v0.30.1) — у этих монет объём
  $180-810к, они физически невидимы для обеих систем, независимо от
  правильности остальных цифр. _fetch_all_symbols() теперь принимает
  необязательный min_volume_usd_override — slow_impulse использует свой,
  более низкий порог ($150к, с запасом ниже наблюдаемого диапазона), не
  трогая общий /scan_settings (быстрые памп/дамп/аномалия объёма
  по-прежнему требуют $3млн — там вход рыночным ордером, риск
  проскальзывания реальный; у slow_impulse вход лимиткой на EMA-уровне,
  риск меньше). Проверено тестом: с обычным порогом монета с объёмом
  $206к отсеивается, с override для slow_impulse — проходит, монета с
  объёмом $50к по-прежнему отсеивается в обоих случаях.
- v0.30.28: НОВЫЙ, третий источник сигналов, по прямому запросу после
  сравнения с внешним референс-ботом. Находка: 6 из 8 их сигналов НЕ
  имели резкого пампа/дампа — движение было растянуто на недели (|chg 7
  недельных баров| 18-109%), наш быстрый (20/60/120-минутный) детектор
  физически не мог их поймать. Новый _slow_impulse_scan_loop — широкий
  периодический скан (раз в 10 мин, тот же принцип, что EMA Diagnostics,
  но с реальной отправкой) — недельная EMA touch (та же логика, что
  везде, включая "вход=сам уровень EMA" — уже совпадала с их ботом ДО
  этого сравнения) + фильтр на заметное накопленное недельное движение
  (порог 15%, консервативно ниже диапазона их сигналов). Использует ТОТ
  ЖЕ cooldown/state, что памп/дамп/аномалия объёма — не задублирует, если
  та же связка уже сработала через другой источник. Только недельный ТФ,
  по прямому уточнению. Проверено тестом: слабый импульс не проходит
  фильтр, сильный проходит, повторный вызов той же связки не дублирует.
- v0.30.27: по запросу — на страницу /manual_scan добавлены необязательные
  поля со скрина ИХ бота: сторона (LONG/SHORT), вход, стоп, тейк.
  Записываются и в полный результат скана, и в простую хронологию
  (v0.30.25) — теперь можно будет проверить, коррелирует ли направление
  ИХ сигнала с нашими данными (ладдер, дистанция до EMA, момент), не
  только "какая монета была близко к уровню". Поле "Сторона" добавлено и
  в таблицу накопленных сканов на странице. Поля очищаются после каждого
  успешного скана — удобнее заносить следующую монету. Проверено тестом
  end-to-end.
- v0.30.26: реальный баг, найден по свежим логам — volume_24h_usd был
  null абсолютно во всех записях, хотя funding_rate из того же вызова
  получался нормально. Причина: /futures/usdt/contracts/{contract}
  (эндпоинт, который дёргался) вообще не содержит полей объёма — по
  официальной схеме Gate.io там только quanto_multiplier/funding_rate/
  fee-параметры и т.п., volume_24h_* существуют ТОЛЬКО в
  /futures/usdt/tickers. Переключено на правильный эндпоинт — заодно
  funding_rate там тоже есть, двух запросов больше не нужно. Проверено
  тестом на точном формате ответа из официальной документации Gate.io.
- v0.30.25: по прямому запросу — отдельный, простой лог "тикер + время
  сигнала" (MANUAL_SCAN_CHRONOLOGY_FILE), независимый от полного расчёта.
  Смысл — даже если за неделю доступа к референс-боту не разгадаем логику
  отбора, останется чистый список наблюдений, по которому потом можно
  прогнать уже УЛУЧШЕННЫЕ индикаторы задним числом, не теряя сами
  наблюдения. Пишется ПЕРВЫМ в _manual_symbol_scan, до любых вычислений —
  сохранится, даже если детальный скан ниже упадёт. Новый эндпоинт
  /manual_scan_chronology_export + ссылка на странице. Проверено тестом:
  корректно пишется и для "сейчас", и для указанной даты/времени.
- v0.30.24: по прямому замечанию — "сигнал в 3 ночи, тикер ввёл в 9 утра,
  за первые 6 часов не будет данных по объёму?" Именно так и было бы:
  тикер Gate.io всегда даёт ТЕКУЩЕЕ скользящее 24ч окно (на момент запроса,
  9 утра), а не окно, заканчивающееся в указанный at_ts (3 ночи) — окна
  реально смещены на разницу во времени. Добавлена
  _manual_scan_historical_volume_24h — считает объём честно по 1h-свечам
  за 24 часа ДО указанного at_ts, не по текущему тикеру. При указанной
  дате результат теперь содержит оба значения:
  contract.volume_24h_usd (текущее, помечено live_only) и
  contract.volume_24h_historical (за нужное окно). Проверено тестом —
  корректно исключает свечи ПОСЛЕ at_ts из суммы.
- v0.30.23: по прямому вопросу — "если сигнал был ночью, а тикет загружаю
  в обед, какие свечи скачаются?" (раньше всегда "последние к текущему
  моменту", не "на момент сигнала"). Добавлено необязательное поле
  дата/время на странице /manual_scan — если указано, все свечи (день,
  неделя, 1h/4h для всплесков) качаются со сдвигом в прошлое (offset_days)
  и обрезаются по факту только барами, закрывшимися ДО указанного
  момента — можно заносить данные задним числом, не только для монет,
  увиденных прямо сейчас, статистика соберётся быстрее. Оговорка: объём
  за 24ч и funding rate у Gate.io доступны только текущими — при
  указанной дате помечается явно (contract.live_only). Проверено тестом:
  скан "на 5 дней назад" даёт другую (более раннюю) цену, чем "сейчас";
  будущая дата корректно отклоняется.
- v0.30.22: по запросу — расширен /manual_scan (v0.30.21), на всякий
  случай, пока не знаем, что реально важно для сравнения с внешним
  референс-ботом. Теперь на КАЖДЫЙ таймфрейм (день и неделя, было только
  неделя) собирается: EMA7/14/28, SMA7/14/28 (вдруг у них не EMA, а
  просто похоже названо), ATR + перцентиль (насколько сейчас волатильно
  относительно последних 100 баров), лесенка (структура тренда), моментум
  (изменение цены за 1/3/7 последних баров), положение относительно
  хая/лоя за 30 баров. Плюс объём за 24ч и funding rate (общий контекст
  монеты, не по конкретному ТФ). Все новые метрики берутся из УЖЕ
  выполняемых, кэшируемых запросов свечей — доп. сетевых вызовов почти не
  добавляет (один лёгкий запрос на funding/объём). Проверено тестом на
  реалистичных многодневных/часовых данных — все поля собираются верно.
- v0.30.21: по прямому запросу — ручной инструмент сравнения с внешним
  референс-ботом. Вместо разбора видимой части скринов (внутренняя
  математика чужого бота всё равно не видна на скрине) — вводишь тикер
  на новой странице /manual_scan, бот сам качает свечи (1h и 4h,
  разумный охват по каждому — 14 и 30 дней) и ищет ВСЕ доминирующие
  всплески объёма в окне (не только самый крупный, как в живом детекторе
  — тут это исследовательский скан, не алертинг), плюс смотрит статус
  недельных EMA7/14/28 (по прямому уточнению — единственные уровни,
  которые реально используются для отскока). Всё копится в отдельный
  файл (/manual_scan_export), не влияет на живые алерты вообще. Простая
  форма на странице + таблица накопленных сканов. Проверено тестом:
  функция находит EMA-статус и всплески; все четыре HTTP-эндпоинта
  работают end-to-end, включая нормализацию тикера (FF -> FF_USDT).
- v0.30.20: по запросу — таймаут на запросы к Gate.io (тикеры/свечи, 4
  места) увеличен с 15 до 20 секунд. Не про надёжность (обрывы и так уже
  не валят весь скан, см. v0.29.14) — просто меньше ложных таймаутов в
  логах при нестабильной, но рабочей сети. ntfy/Telegram не тронуты —
  это была жалоба конкретно про Gate.io.
- v0.30.19: реальная находка — v0.29.5 намеренно рисовала зелёный маркер
  по ТОЧНОЙ цене сигнала (signal_price), а не по последней свече из
  отдельного, чуть более раннего запроса свечей для картинки — это
  правильно по сути (точнее), но иногда создавало видимый РАЗРЫВ между
  концом чёрной линии цены и зелёной точкой, если эти две цены слегка
  разошлись. Выглядело как будто точка "оторвана" от графика. Теперь
  дорисовывается соединительный отрезок той же чёрной линией — маркер
  визуально доходит до графика, а точность позиции (по signal_price)
  сохраняется. Проверено тестом с намеренным расхождением 2% между
  последней свечой и сигнальной ценой.
- v0.30.18: по прямому запросу — проверил попиксельно (не на глаз):
  красный столбик (спайк на vmax) и зелёный маркер (сигнал на pmax) были
  математически на одном уровне ПО ДАННЫМ, но зелёный маркер рисовался
  кругом радиусом 7px — его верхний КРАЙ визуально выступал выше самой
  точки данных, тогда как у столбика такого отступа не было. Уменьшил
  радиус маркера (7 -> 4px) — разрыв между верхними краями сократился с
  5px до 2px, практически незаметно на глаз.
- v0.30.17: реальный баг, найден сразу же на первом запуске нового
  backfill'а (v0.30.16) — Gate.io 400 INVALID_PARAM_VALUE на дробный
  `from`. Причина: в _fetch_candles int() применялся к time.time() ДО
  вычитания offset_days*86400, а не после — если offset_days сам дробный
  (наш backfill передаёт именно такой), результат снова становился float
  и утекал в запрос к бирже как есть. Обёрнуто int() вокруг всего
  выражения. Проверено тестом: дробный offset_days=3.456789 теперь даёт
  целочисленный `from` в запросе.
- v0.30.16: по прямому вопросу — "можно ли для старых ема проверок
  посмотреть, где оказалась цена по новым 12-часовым ожиданиям". Обычный
  трек-луп старые, уже завершённые записи не трогает — их отслеживание
  закончилось на старом 4-часовом окне, "продолжить" живой ценой нельзя
  (пропущенный кусок времени потерян для live-данных). Но есть
  исторические свечи — новая _diag_backfill_extended_checkpoints()
  честно восстанавливает, что РЕАЛЬНО было в момент каждой новой отметки
  (360/480/600/720 мин), используя _fetch_candles с offset_days (запрос
  "на тот момент", не "сейчас"), плюс честно продлевает MFE/MAE по всему
  восстановленному диапазону, не только по отдельным точкам-чекпоинтам.
  Отметки, которые ещё не наступили даже сейчас, корректно пропускаются
  (не выдумываются). Новый эндпоинт /ema_diag_backfill_extended (POST,
  фоновый поток) + кнопка "Досчитать 12ч для старых" на странице
  диагностики. Ограничение — глубина истории на бирже (не бесконечная);
  для совсем старых записей данных может не быть, это не ошибка. Проверено
  тестом: отметки в пределах прошедшего времени досчитываются по реальным
  историческим ценам, ещё не наступившие — корректно не трогаются, MFE/MAE
  продлеваются по всему диапазону.
- v0.30.15: по запросу — окно чекпоинтов EMA Diagnostics расширено с 240
  минут (4ч) до 720 минут (12ч), добавлены промежуточные точки на 360/480/
  600 мин. Раньше при симуляции стоп/тейк большинство сделок (в прошлый
  раз — 76%) не успевали решиться за 4 часа и считались "по времени", что
  сильно смещало результат. Дольше следим — меньше сделок останется
  нерешёнными, честнее будет следующая симуляция.
- v0.30.14: по прямому вопросу — "есть ли разница памп/дамп/аномалия
  объёма, может после аномалии шанс на отскок вниз ещё выше". Сейчас
  ответить нечем — событие "цена подошла к EMA28(W)" (v0.30.13) просто
  слалось и забывалось, без отслеживания дальнейшей цены и без метки
  источника. Теперь при каждом срабатывании создаётся отдельная
  отслеживаемая запись (со своим source — pump/dump/vol_anomaly) в новом
  EMA28W_TOUCH_HISTORY_FILE, свой трек-луп (_ema28w_touch_track_loop)
  следит за ней ещё 4 часа (max_follow/max_reverse + чекпоинты, тот же
  принцип, что у остальных). Плюс новый эндпоинт /ema28w_touch_export.
  Как накопится достаточно данных по каждому источнику — можно будет
  честно посчитать разницу, не гадая. Проверено тестом: все три источника
  корректно создают отдельные, помеченные записи.
- v0.30.13: два изменения по прямому запросу. (1) Реальный баг —
  многострочный текст алерта в HTTP-заголовке Message ломал весь запрос
  к ntfy (InvalidHeader, HTTP-заголовки не поддерживают переносы строк) —
  картинка тихо не доходила НИКОГДА, с самого v0.30.11. Перенёс текст в
  query-параметр (там перенос строки нормально кодируется). Проверено
  тестом на реальном многострочном тексте — запрос больше не падает.
  (2) Новое отдельное уведомление — по подтверждённой находке (1w EMA28,
  60.5% отскок для шорта на n=76, самый заметный уровень из всех
  проверенных): пока мы продолжаем отслеживать уже сработавший сигнал
  (памп/дамп/аномалия объёма), на каждом опросе цена ТАКЖЕ проверяется на
  близость к EMA28(W) — не только в момент исходного сигнала, а в течение
  всего времени слежения. Если цена подходит близко (тот же допуск, что
  и у обычного касания) — шлётся отдельный алерт, один раз на запись (не
  спамит на каждом опросе). Проверено тестом: первое приближение шлёт
  алерт и помечает запись, повторные опросы — тишина.
- v0.30.12: по прямому запросу "сделай как у него" — сверил три свежих
  скрина референса (PTB, TLM, SUSHI) пиксель в пиксель. Подтверждено: у
  него цена и объём ТОЖЕ делят одну высоту графика (та же независимая
  двойная шкала, что была у нас до v0.30.10) — совпадение по-прежнему
  чисто математическое (обе шкалы растянуты 0->свой максимум в одной
  высоте, поэтому последняя точка цены на своём максимуме и самый
  крупный столбик объёма всегда окажутся рядом, у кого угодно, кто так
  рендерит), не означает никакой реальной связи. Но раз для привычности
  при сравнении со скринами чувака нужен тот же визуальный вид — вернул
  общую высоту, отменив суб-панель из v0.30.10. Проверено тестом:
  столбик объёма снова достаёт почти до самого верха графика.
- v0.30.11: по прямому вопросу "а в ntfy нет картинки, это нормально?" —
  нет, не специально. v0.30.9 чинила только "ntfy вообще молчит", отправляя
  туда голый текст caption — сама картинка не доходила. Теперь ntfy
  получает настоящее вложение (через заголовок Filename — так ntfy
  понимает, что тело запроса это файл, не текст), с текстом в заголовке
  Message. Заодно почищен HTML — наши тексты рассчитаны на Telegram
  (parse_mode=HTML, теги <b>), а ntfy такую разметку не понимает и
  показывал бы теги буквально; новая _strip_html_for_ntfy убирает их
  перед отправкой в оба канала (текстовый _send_alert и фото). Отправка в
  ntfy теперь полностью самостоятельный блок (не через _send_alert) —
  меньше риска задеть чужую логику дублей при следующей правке. Проверено
  тестом: HTML корректно убирается; ntfy получает именно байты картинки в
  теле запроса с верными заголовками Filename/Message.
- v0.30.10: реальная, ПОВТОРЯЮЩАЯСЯ путаница (уже несколько раз за
  разговор) — объём и цена делили ОДНУ И ТУ ЖЕ высоту графика, поэтому
  высокий по объёму столбик и высокая по цене точка визуально оказывались
  рядом друг с другом, хотя это совершенно не связанные величины, чистое
  совпадение шкал (не путь цены к "уровню всплеска" — такого уровня
  вообще не существует, объём меряется в других единицах). Объяснял это
  несколько раз в тексте — не помогало, путаница возникала снова на
  следующем скрине. Теперь исправлено в самом рендере, не в объяснении:
  объём живёт ТОЛЬКО в нижних 35% графика (отдельная суб-панель), цена —
  по-прежнему на всю высоту для своего диапазона. Даже максимальный по
  объёму столбик в окне физически не может дотянуться туда, где рисуется
  цена — путаница структурно исключена. Проверено тестом попиксельно:
  верхняя граница объёма на картинке (Y=333) практически точно совпадает
  с расчётной границей суб-панели (Y=333.4).
- v0.30.9: реальный баг по замечанию "разные уведомления в Telegram и
  ntfy". ДВЕ причины разом. (1) _send_alert: не-сетевое исключение (напр.
  битая HTML-разметка) на ОДНОМ канале делало `break`, выходящий из ВСЕГО
  цикла попыток — включая ещё не выполненную отправку в ДРУГОЙ канал в
  этой же попытке. (2) _send_alert_photo (используется почти ВСЕМИ
  алертами — памп/дамп, аномалия объёма, EMA-сигналы): ntfy получал
  уведомление ТОЛЬКО если Telegram не настроен или отправка фото не
  удалась — пока Telegram работал нормально, ntfy молчал по всем
  картиночным алертам вообще. Оба переписаны — каждый канал теперь
  независим: сбой (сетевой или нет) одного больше не задевает другой;
  _send_alert_photo при успехе TG теперь ОТДЕЛЬНО уведомляет ntfy (раньше
  не уведомлял вообще), при неудаче TG — как и раньше, но без дублей.
  Проверено тестом на трёх сценариях: не-сетевая ошибка TG не блокирует
  ntfy в _send_alert; успех TG теперь доводит до ntfy в _send_alert_photo
  (раньше не доводил); при сбое TG ntfy получает ровно одно уведомление,
  не дубль.
- v0.30.8: по запросу — фильтр по объёму в момент касания EMA, на
  основании диагностики (5000 записей, разбор с пользователем): при
  vol_ratio > 1.2 отскок случается заметно чаще (SHORT 45.5% против
  27.7%, n=995/1893; LONG 43.6% против 24.7%, n=599/1454) — эффект
  держится симметрично в обе стороны, самый чёткий из всех проверенных
  факторов (структура тренда "лесенка" при повторной проверке оказалась
  контринтуитивной — не используется как фильтр). _ema_touch_check_symbol
  теперь считает vol_ratio для КАЖДОГО касания (переиспользует уже
  существующую _vol_ratio, ту же, что в диагностике — методология
  идентична), _weekly_ema_fire_alert добавляет предупреждение в текст
  сигнала, если объём в моменте не подтверждён порогом. Не блокирует
  сигнал полностью (обычный объём даёт ~27%, не ноль) — явно помечает.
  vol_ratio сохраняется в саму запись сигнала для дальнейшего анализа.
  Проверено тестом: пометка появляется при vol_ratio≤1.2, отсутствует
  при vol_ratio>1.2.
- v0.30.7: по запросу — уведомления о голых пампах/дампах отключены по
  умолчанию (PUMP_DUMP_ALERTS_ENABLED=False), "пока", легко включить
  обратно (переключатель на главной странице + через /alert_cfg). Сам
  детект, запись в историю и проверка EMA-триггера продолжают работать в
  фоне без изменений — они нужны и для сигналов, приходящих от аномалии
  объёма, и для накопления статистики. Меняется только сама отправка
  текста "Pump: X%"/"Dump: X%" в Telegram — она пропускается, если
  выключено. Проверено тестом: при выключенном флаге в Telegram ничего
  не уходит, но EMA-проверка всё равно вызывается; включение обратно
  восстанавливает отправку немедленно.
- v0.30.6: четыре улучшения Volume Peak Watcher по запросу.
  (1, функционально) Более длинный горизонт для z-score:
  VOL_PEAK_BASELINE_LOOKBACK_MIN=24ч — среднее/std теперь считаются по
  суткам, не по тому же коротком 4-часовом окне поиска, где сам поиск
  мог оказаться случайно тихим и завысить z-score на рядовом объёме.
  Кандидат на "самый крупный" по-прежнему ищется только в свежем окне.
  Проверено тестом: объём, аномальный для тихих последних 4ч, но обычный
  для суток — больше не проходит порог.
  (2, функционально) Скоростная пометка — та же логика, что подтвердилась
  на обычных пампах (быстрое окно 20/60мин даёт 85% в пользу шорта,
  медленное 120мин — только 44-57%): если движение от всплеска до
  подтверждения заняло больше 60 минут, помечаем "медленный характер",
  та же формулировка попадает и в текст, и в поле slow_move для анализа.
  (3, функционально) Обратная связь по накопленной статистике —
  _vol_peak_historical_winrate считает реальный винрейт по УЖЕ решённым
  (take/stop) сигналам от аномалии объёма для ТОЙ ЖЕ связки ТФ/период
  EMA, добавляет в текст, если сделок набралось от 10 — иначе молчит, не
  гадает на 2-3 наблюдениях.
  (4, только диагностика) Дивергенция объём/цена — классический признак
  климакса (объём затухает, пока цена ещё растёт последним рывком).
  Считается по уже имеющимся свечам (без доп. запроса), пишется в поле
  divergence, НЕ влияет на отправку алерта — накопление данных для
  будущего разбора. Проверено тестом на явном сценарии затухания.
- v0.30.5: по запросу — отслеживание цены после сигнала расширено до 4
  часов для обоих (пампа/дампа — было 1ч, аномалии объёма — было 2ч),
  унифицировано. Чекпоинты (v0.29.8/9, для симуляции стоп/тейк задним
  числом) тоже расширены — были только до 120 минут, добавлены 180 и
  240, иначе новый 3-4-часовой участок окна остался бы без точек для
  восстановления порядка событий.
- v0.30.4: ещё один проход аудита по запросу — на этот раз в основном
  подтверждение, что всё в порядке, а не новые баги. Проверено и НЕ
  подтвердилось проблемой: порядок захвата блокировок в replace-логике
  Volume Peak Watcher (_vol_peak_lock снаружи, _log_lock внутри через
  olog — везде в одном порядке, дедлок невозможен), устойчивость
  _has_sufficient_ema_history к временным сетевым сбоям (fail-open — не
  выкидывает монету из отбора зря), парсинг JSON в POST-запросах (битое
  тело корректно ловится, сервер не падает), корректность
  _pump_update_history с новым источником монет (v0.30.1). Единственная
  находка — не баг, а лишний код: в _weekly_ema_touch_loop (спящая,
  сейчас не запускается) и _diag_scan_loop список монет обрезался
  повторно тем же потолком, который _fetch_all_symbols() уже применяет
  внутри себя — безвредно, но лишне, убрано для ясности.
- v0.30.3: реальная находка на вопрос "а не часто ли 60 секунд" — v0.30.1
  случайно удвоила сетевые запросы в _pump_detect_loop: и
  _pump_fetch_tickers_snapshot(), и добавленный тогда _fetch_all_symbols()
  оба дёргают один и тот же /tickers каждый цикл — при нестабильном
  интернете лишний риск обрыва на пустом месте, хотя докстринг функции
  прямо обещал "один запрос". Починено: _pump_fetch_tickers_snapshot()
  теперь захватывает ещё и bid/ask (не только price/volume), и новая
  _filter_liquid_symbols_from_snapshot() считает фильтр ликвидности на
  ТОМ ЖЕ снапшоте — без второго похода в сеть. Снова ровно один запрос за
  цикл, как и было изначально задумано. Проверено тестом — ровно один
  вызов requests.get() за цикл.
- v0.30.2: по прямому вопросу — "недостаточно истории" (новый листинг)
  кэшируется на 3 дня (EMA_TOUCH_INSUFFICIENT_HISTORY_RECHECK_SEC), чтобы
  не дёргать биржу зря на заведомо молодых монетах — само перепроверяется
  через 3 дня, без участия пользователя. Но если ждать не хочется (монета
  только что перевалила за нужный возраст) — новый /recheck_history
  (POST, symbol=XXX или пусто для всех) сбрасывает кэш "недостаточно" для
  конкретной монеты или сразу для всех — следующий скан перепроверит
  заново. Кнопка на главной странице рядом с настройками отбора. Не
  трогает монеты, для которых история УЖЕ подтверждена достаточной —
  только записи именно "insufficient". Проверено тестом.
- v0.30.1: ПОЛНАЯ ПЕРЕРАБОТКА логики отбора монет по прямому запросу.
  Найдена настоящая причина, почему настройка top_n "непонятно что
  делает": в _fetch_all_symbols() было ДВА отдельных, зашитых прямо в код
  потолка (candidates = valid[:150] на входе, top50 = filtered[:100] на
  выходе), которые не имели ОТНОШЕНИЯ к top_n вообще — сколько её ни
  увеличивай (хоть до 500), больше 100 монет оттуда физически не
  выходило. Переделано под запрошенную логику: универсум — ВСЕ фьючерсы
  USDT на бирже (~700 пар, без обрезки на входе); фильтр — ПОРОГ
  ликвидности (объём/спред — защита от проскальзывания на неликвиде), не
  ранг "топ-N по объёму" — сколько монет пройдёт порог, столько и берём,
  может быть и больше 100. _scan_settings теперь {min_volume_usd,
  max_spread_pct, max_symbols_cap} вместо {top_n} — первые два
  настраиваются через ту же форму на главной странице (это и есть реальные
  рычаги "сколько бумаг в итоге пройдёт"), третий — не основной фильтр, а
  страховочный потолок на случай аномалии (дефолт 800, выше размера всей
  биржи, обычно не срабатывает). _pump_detect_loop и _get_tradeable_symbols
  теперь используют ОДИН источник истины (_fetch_all_symbols) вместо
  дублирующей, рассинхронизированной логики отбора. Обратная совместимость
  со старым файлом настроек (top_n) — трактуется как max_symbols_cap.
  Проверено тестом: 180 из 250 смоделированных монет, прошедших порог
  ликвидности, возвращаются ВСЕ (не обрезаются до 100); старый файл с
  top_n корректно превращается в новый потолок; оба HTTP-эндпоинта и
  форма на странице работают end-to-end.
- v0.30.0: по прямому запросу — все логи переведены на понятный русский.
  Раньше текст самого исключения вставлялся как есть ({e}) — а Python
  формирует его на английском (ConnectionError, Timeout, JSONDecodeError...)
  даже когда весь остальной текст лога уже на русском, непонятно, в чём
  проблема, не зная английских терминов. Новая _explain_error(e)
  переводит частые типы ошибок (нет связи с интернетом, сервер не ответил
  вовремя, нечитаемый ответ вместо данных, не хватает поля в ответе,
  пустое значение вместо числа и т.д.) в понятную причину — но НЕ теряет
  техническую деталь, она остаётся в скобках, пригодится, если понадобится
  показать мне для разбора. Применено массово во ВСЕХ 125 местах, где лог
  вставлял текст исключения (включая варианты e2/e_tp/e_tp2). Проверено
  тестом на реальных типах ошибок (сеть, таймаут, битый JSON, отсутствующее
  поле) и сквозным тестом через реальную функцию с сетевым сбоем.
- v0.29.14: по запросу — нестабильный интернет, частые обрывы, нужна
  "бесшовность" (не стартовать проход заново из-за одного сбоя). Аудит
  всех фоновых циклов на предмет "обрыв на одной монете валит весь
  оставшийся проход": _vol_peak_find_loop, _vol_peak_watch_loop,
  _diag_scan_loop, _diag_track_loop, _pump_dump_track_loop,
  _vol_anomaly_track_loop — уже все были с защитой на каждую монету/запись
  отдельно (внутренние try/except в задействованных функциях или прямо в
  цикле). Единственный реальный пробел — _pump_detect_loop: обход монет
  шёл БЕЗ своей защиты на каждую; если сеть оборвётся именно во время
  _pump_fire_alert/_dump_fire_alert (свечи для картинки + отправка в
  Telegram — оба сетевые), исключение улетало во внешний try/except и
  обрывало ВЕСЬ оставшийся проход — монеты дальше по списку в этом цикле
  просто пропускались, не только та, что вызвала сбой. Добавлена защита
  на каждую монету отдельно — сосед по списку больше не страдает из-за
  чужого сбоя. Проверено тестом: имитация обрыва сети на одной из пяти
  монет — обработка доходит до всех пяти, не останавливается на второй.
- v0.29.13: реальная находка (AKE_USDT) — "утром был сигнал где пик более
  большой появился, но как ориентир не применился". Причина: проверка
  "обнаружено задним числом" (v0.28.1) полностью ОТБРАСЫВАЛА кандидата
  (return []) из _find_volume_peaks, если цена уже ушла на 7.5%+ с
  момента всплеска. Побочный эффект: более значимый (реально самый
  крупный) всплеск, случившийся ПОЗЖЕ уже отслеживаемого, мог никогда не
  попасть даже на сравнение "не крупнее ли он текущего" в
  _vol_peak_find_loop — если цена после него уже успела заметно
  сдвинуться (а у по-настоящему значимых всплесков так обычно и бывает),
  кандидат отбрасывался ДО того, как вообще мог начать конкурировать за
  замену tracked-записи (v0.29.6). Убрано — своя, более надёжная защита
  от "протухшей" цены есть в _vol_peak_verify_at_peak (v0.29.2, проверяет
  ВЕСЬ путь с момента всплеска прямо перед отправкой) и она не мешает
  replace-логике. Проверено тестом на обоих сценариях: крупный поздний
  всплеск (цена уже ушла на 10%+) теперь корректно проходит поиск
  (раньше отбрасывался); SOXL-сценарий (цена уже прошла пик и откатила)
  по-прежнему корректно НЕ проходит — просто защита сработала на этапе
  отправки, а не поиска.
- v0.29.12: по запросу — полный аудит проекта. Найдено и исправлено два
  реальных бага: (1) _watchdog_loop был ЕДИНСТВЕННЫМ из всех фоновых
  циклов без try/except вокруг тела — необработанное исключение тихо
  убило бы поток навсегда, без единой строчки в лог, и мониторинг связи с
  интернетом просто перестал бы работать до следующего перезапуска, никто
  бы не узнал почему. Добавлена та же защита, что у всех остальных
  циклов. Проверено тестом: цикл переживает исключения внутри тела и
  продолжает работать. (2) _weekly_ema_fire_alert мутировал
  _weekly_ema_recent (append + обрезка до 200) БЕЗ _weekly_ema_touch_lock,
  хотя чтение/очистка/сохранение этого же списка отовсюду блокировку
  используют — функция может вызываться параллельно из пампа, дампа и
  аномалии объёма одновременно, реальный риск гонки на структурной
  мутации общего списка. Обёрнуто в тот же лок. Проверены и НЕ подтвердились
  как баги: _ema_touch_ctx_cache (лок уже был, просто не сразу заметен),
  _pump_price_history (везде корректно под _pump_detect_lock), _diag_records
  (везде корректно под _diag_lock), все числовые хелперы (_ema/_atr — с
  защитой от коротких списков).
- v0.29.11: доп. защита от дублей поверх v0.29.10 — "один активный на
  монету" (v0.28.6/v0.29.6) проверяется только при добавлении НОВОГО
  всплеска, не чистит watchlist, если там УЖЕ оказалось несколько записей
  на одну монету по любой другой причине (старые баги, ручное
  редактирование файла, будущие пока не найденные пути). Теперь при
  каждой загрузке с диска watchlist принудительно схлопывается до ОДНОЙ —
  самой крупной по объёму — записи на монету, независимо от того, как
  там оказалось несколько. Проверено тестом на точной реконструкции
  случая TAC_USDT (две разные записи, обе технически ещё "живые" по
  сохранённому expires_at) — после загрузки остаётся ровно одна, с
  большим объёмом.
- v0.29.10: реальный баг, подтверждённый на АКТУАЛЬНОЙ версии (TAC_USDT,
  всплеск 29-часовой давности при текущем лимите 6 часов — пользователь
  явно подтвердил, что скрин со свежей версии, не гадание про старый
  билд). Причина: expires_at считается и сохраняется на диск ОДИН раз в
  момент добавления записи, с ТЕМ значением VOL_PEAK_EXPIRY_SEC, что было
  в коде на тот момент. За этот разговор константа менялась несколько раз
  (12ч -> 90мин -> 6ч) — но уже сохранённые на диск записи продолжали
  жить со СТАРЫМ, более длинным сроком, потому что _load_vol_peak_watchlist
  при подхвате с диска после каждого перезапуска (что происходит при
  каждом обновлении бота) просто доверяла сохранённому значению, не
  пересчитывала его под ТЕКУЩУЮ константу. Теперь при загрузке КАЖДАЯ
  запись пересчитывается заново (expires_at = spike_ts + текущий
  VOL_PEAK_EXPIRY_SEC) — если по новым правилам она уже должна была
  истечь, отбрасывается сразу при старте, не ждёт следующего скана.
  Проверено тестом: запись, ещё "живая" по старому 12-часовому лимиту (её
  всплеску 8 часов), после загрузки корректно отбрасывается по текущему
  6-часовому.
- v0.29.9: по запросу — добавлены ещё и чекпоинты цены (5/15/30/60/90/120
  минут после сигнала) в историю пампов/дампов, и в файл аномалий объёма
  рядом с уже существующим полным path (для единообразия между файлами).
  _track_record_checkpoint пишет значение (та же система знаков, что у
  max_follow_pct) на каждой фиксированной отметке — компактная версия той
  же идеи, что и path (знать порядок событий для честной симуляции
  стоп/тейк), но без роста файла на каждый опрос — фиксированные 6 точек
  на сделку, для пампа/дампа (у которых полного path не было). Проверено
  тестом: чекпоинты 5/15/30 минут записываются с верными значениями и не
  перезаписываются повторно при следующих проходах.
- v0.29.8: по запросу — записи Volume Peak Watcher теперь несут поле
  "path": весь путь цены по времени (каждая проверка трек-цикла, ~24
  точки на сделку за 2 часа при опросе раз в 5 минут), не только крайние
  max_follow_pct/max_reverse_pct. Крайние точки сами по себе не говорят,
  В КАКОМ ПОРЯДКЕ случились просадка и разворот в прибыль — а это
  критично для честной симуляции "стоп -X% / тейк +Y%" (стоп мог
  сработать раньше, чем случился бы разворот в прибыль, и по одним
  крайним точкам это было не отличить от честного выигрыша). Первая точка
  пути — цена входа (момент самого алерта). Проверено тестом: путь
  корректно накапливается по времени в правильном порядке.
- v0.29.7: по запросу — два добавления в логи для будущего анализа
  (разбор реального экспорта показал два пробела). (1) Записи Volume Peak
  Watcher теперь несут schema_version — не нужно вручную вычислять "это
  точно старая логика" по возрасту/дублям, как пришлось делать при разборе
  текущего экспорта; поднимать при следующих изменениях порога/выбора
  пика/подтверждения. (2) source_id — уникальный идентификатор всплеска
  (символ+время), передаётся через _pump_or_dump_maybe_trigger_ema_signal
  как triggered_by и сохраняется в итоговую запись EMA-сигнала (если
  всплеск довёл до полноценного сигнала со входом/стопом/тейком). Теперь
  можно честно связать "какой именно всплеск объёма привёл к какому именно
  сигналу" и посчитать реальный винрейт по цепочке целиком, а не только
  видеть, куда пошла цена в целом. Проверено тестом: source_id корректно
  передаётся и совпадает по формату symbol|spike_ts.
- v0.29.6: два фикса по реальному скрину (AKE_USDT). (1) Подсветка
  всплеска была слишком широкой и непрозрачной — столбик высотой близко к
  максимуму окна объёма (общая высота с ценовой осью) визуально выглядел
  как будто перекрывает весь график. Теперь узкая (фиксированные 6px, не
  растущая от количества свечей) полупрозрачная линия — видно и её, и что
  под ней; проверено попиксельно (ширина подтверждена 6-7px, чёрная линия
  цены видна поверх). (2) Реальный баг выбора пика — пока шло слежение за
  одним всплеском, объективно БОЛЕЕ КРУПНЫЙ новый всплеск на той же
  монете полностью игнорировался (защита "один активный на монету" из
  v0.28.6 не различала размер). Теперь при обнаружении нового всплеска,
  если он крупнее уже отслеживаемого — ЗАМЕНЯЕТ его (не добавляется
  вторым — по-прежнему один активный, просто "активный" означает "самый
  крупный из известных"). Проверено тестом: всплеск 1000000 корректно
  заменяет ранее отслеживаемый 300000.
- v0.29.5: ДВА реальных бага, найденных на живых примерах (RLC пропал из
  веб-таблицы, BILL_USDT сработал дважды на одном и том же всплеске).
  (1) Гонка данных: _pump_dump_history_append (дозапись одной записи) и
  track-циклы (читают ВЕСЬ файл, обрабатывают, перезаписывают ЦЕЛИКОМ)
  работали на одних файлах без всякой синхронизации между потоками. Если
  дозапись алерта попадала между чтением и перезаписью трек-цикла — трек-
  цикл в конце перезаписывал файл своей устаревшей версией без только что
  добавленной записи, и она терялась целиком: алерт в Telegram уходил, но
  в истории/реестре сработавших его не было — отсюда и пропажа из
  веб-таблицы, И повторное срабатывание того же всплеска (реестр
  "уже сработавших" читает ту же историю и не видел потерянную запись).
  Добавлен единый замок на файл (_get_history_file_lock) — обе операции
  (дозапись и полная перезапись) теперь взаимно исключают друг друга,
  для всех трёх файлов истории и обеих кнопок "Очистить". Проверено
  тестом: параллельные track-loop (с задержкой, имитирующей медленную
  обработку) и append — обе записи сохраняются, ничего не теряется.
  (2) Зелёный маркер сигнала рисовался по последней свече из отдельного,
  чуть более позднего запроса свечей для картинки — не по точной цене,
  что реально вызвала алерт (небольшой зазор по времени между проверкой
  цены и фетчем свечей). Теперь точная цена передаётся явно
  (signal_price) и используется и для маркера, и для масштаба графика —
  картинка показывает ровно ту цену, что в тексте. Проверено попиксельно.
- v0.29.4: по прямому запросу — VOL_PEAK_EXPIRY_SEC расширен обратно с 90
  минут до 6 часов. Изначально короткое окно (v0.28.3) было ГЛАВНОЙ
  защитой от протухшего всплеска — но с тех пор появились более надёжные,
  не завязанные на жёсткий таймер защиты: _vol_peak_verify_at_peak
  (v0.29.2) проверяет ВЕСЬ путь цены с момента самого всплеска перед
  каждой отправкой, постоянный реестр сработавших (v0.28.3) не даёт
  переоткрыть уже сработавшее, один активный на монету (v0.28.6) не даёт
  копить несколько одновременно. Не каждый памп успевает подтвердиться за
  90 минут — по обратной связи, сигналы с разницей в несколько часов
  бывают нормальными, короткий жёсткий таймер отсеивал часть из них
  напрасно, раз реальную защиту теперь даёт проверка по существу, а не по
  времени.
- v0.29.3: по прямому запросу — на графике Volume Peak Watcher теперь
  ДВА новых визуальных элемента, чтобы не гадать по тексту: (1) сам
  столбик объёма всплеска, на который ориентируется детектор, закрашен
  ОТДЕЛЬНЫМ ЯРКИМ АЛЫМ цветом (плюс подпись "ВСПЛЕСК") — на графике с
  десятками похожих фиолетовых пиков раньше было невозможно на глаз
  понять, какой из них реально используется; (2) точка на линии цены, где
  сработал сигнал (последняя свеча — момент отправки алерта), теперь
  крупный зелёный маркер с тёмной обводкой (плюс подпись "СИГНАЛ"), не
  такая же мелкая точка, как все остальные на линии. Проверено попиксельно
  — оба новых элемента (алый столбик, зелёный маркер) корректно
  присутствуют на тестовом рендере.
- v0.29.2: реальный баг — v0.28.5 добавила самостоятельную проверку "цена
  сейчас и есть максимум", но с ФИКСИРОВАННЫМ окном 20 минут. Если истинный
  пик случился ЧАСЫ назад (реальный случай: пик дня был в 15:55, алерт
  пытался уйти на цене из ~19:00, намного ниже) — 20-минутное окно этот
  пик просто не видит, смотрит только на последний обрывок времени и
  ошибочно решает, что текущая цена — "максимум" (в рамках этого узкого
  окна). Теперь окно проверки — ВЕСЬ период с момента самого всплеска
  (spike_ts), а не фиксированные 20 минут: сколько бы времени ни прошло
  с начала слежения, сверяем полный путь цены за это время. Проверено
  тестом на точной реконструкции сценария (истинный пик 6 часов назад,
  текущая цена локально "максимум за 20 минут", но далеко от истинного
  пика дня) — теперь корректно отсекается; нормальный случай (цена
  реально на максимуме) по-прежнему проходит.
- v0.29.1: по запросу — фильтр по истории (v0.29.0) применялся ПОСЛЕ среза
  топ-N по объёму: если среди топ-N оказывалось много молодых монет,
  реально просканированных получалось меньше запрошенного N (список
  "усыхал"). Теперь порядок обратный — сначала фильтр по истории на
  расширенном пуле (топ-N с запасом ×3 для Pump/Dump Detector'а, не вся
  биржа целиком — лишней нагрузки на сотнях нерелевантных низколиквидных
  контрактов не добавляет; у Volume Peak Watcher'а буфер не нужен —
  _fetch_all_symbols() уже сам ограничен ~100 монетами), потом топ-N по
  объёму среди прошедших фильтр. Список честно добирается до
  запрошенного N за счёт следующих по объёму монет, а не обрезается.
  Проверено тестом: 3 молодых монеты в начале списка по объёму — итоговый
  список всё равно наполняется до полных запрошенных 5.
- v0.29.0: по запросу — монеты с недостаточной торговой историей (пара
  дней/недель) теперь фильтруются из поиска пампов/дампов/аномалий объёма
  целиком, не только из EMA-триггера. Если дневной EMA7/14/28(D)
  физически невозможно посчитать — не от чего оттолкнуться, чтобы зайти в
  сделку, даже обычный памп/дамп на такой монете бесполезен для торговли.
  Новая _has_sufficient_ema_history(symbol) переиспользует уже
  существовавший негативный кэш (_ema_touch_get_closed_ctx, 3 дня между
  перепроверками) — почти бесплатно после первого раза, не гоняет сеть
  заново каждую минуту на одну и ту же свежую монету. Применено к Pump/Dump
  Detector'у (топ-N по тикерам) и Volume Peak Watcher'у (общий
  _get_tradeable_symbols()). EMA Diagnostics (широкий исследовательский
  сканер) намеренно НЕ фильтруется — там ценность именно в охвате всех
  монет для анализа, не в готовности к сделке прямо сейчас. Проверено
  тестом: монета с 10 днями истории отсеивается, с 400 днями — проходит.
- v0.28.6: реальный случай (SKHY_USDT, три разных всплеска — z-score 2.84,
  2.72, 6.72 — выстрелили почти одним пакетом за 1 минуту, все на одну и
  ту же цену 165.13). Причина: по мере того, как 4-часовое окно сдвигается
  вперёд во времени, "самый крупный всплеск в окне" МЕНЯЕТСЯ — старый
  гигантский всплеск устаревает и выходит из окна, освобождая место
  следующему по величине. Каждая такая смена добавляла ОТДЕЛЬНУЮ, новую
  запись в watchlist (v0.28.0 гарантировал только "один самый крупный НА
  СКАН", но не "один активный НА МОНЕТУ") — на одну монету могло скопиться
  сразу несколько ожидающих всплесков, и если цена вырастала достаточно,
  все они срабатывали почти одновременно. Теперь — одна монета, не больше
  ОДНОГО активного отслеживания одновременно: если уже что-то отслеживается,
  новый (пусть даже другой) всплеск на ту же монету игнорируется, пока
  текущий не сработает или не истечёт. Проверено тестом на точной
  реконструкции сценария SKHY (3 скана, 3 разных всплеска) — в watchlist
  остаётся ровно одна запись, не три.
- v0.28.5: по прямому запросу — хватит гадать про сроки жизни, нужна
  прямая надёжная проверка. Добавлена _vol_peak_verify_at_peak —
  САМОСТОЯТЕЛЬНАЯ, независимая от накопленного за много опросов
  running_max проверка ПРЯМО ПЕРЕД отправкой: свежий запрос последних 20
  минут реальных свечей и явное сравнение — текущая цена и есть максимум
  за это время, а не что-то уже откатившееся от более высокой точки.
  Не зависит от возможных багов в накоплении состояния между опросами —
  каждый раз пересчитывается заново из реальных рыночных данных. Если
  формальный порог (VOL_PEAK_CONFIRM_PCT + бегущий максимум) пройден, но
  эта свежая проверка показывает, что цена НЕ на максимуме последних 20
  минут — алерт не шлётся, ждём дальше. Проверено тестом на точных цифрах
  из реального алерта SKHYNIX_USDT (пик 1225, попытка алерта на 1213) —
  корректно отсекается; на реальном максимуме — корректно проходит.
- v0.28.4: по наводке пользователя (SKHYNIX_USDT, 511 мин при лимите 90) —
  реальный баг, найден повторным разбором собственного кода. Срок жизни
  всплеска (VOL_PEAK_EXPIRY_SEC=90 мин) отсчитывался от момента, КОГДА МЫ
  ЕГО ОБНАРУЖИЛИ (discovery time), а не от времени самого всплеска
  (spike_ts). Если всплеск долго "тихо сидел" в 4-часовом окне (найден
  только когда стал самым крупным на каком-то из сканов раз в 5 минут) —
  он получал ещё полные 90 минут ПОВЕРХ уже прошедшего с него времени,
  реальный возраст к моменту срабатывания мог заметно превышать заявленный
  лимит. Теперь expires_at = spike_ts + VOL_PEAK_EXPIRY_SEC — не зависит
  от того, когда мы его нашли. Проверено тестом: всплеск, которому уже
  100 минут на момент обнаружения (при лимите 90), корректно НЕ
  добавляется в watchlist — он уже истёк независимо от того, что мы его
  только что "нашли".
- v0.28.3: реальный БАГ ЦИКЛА, пойманный на живом примере (SOXL_USDT,
  один и тот же z-score/объём/mean/std: сначала "182 мин назад", потом
  "423 мин назад" — буквально одно и то же событие срабатывало дважды).
  Причина: когда алерт срабатывает, запись удаляется из watchlist — но
  на следующем скане (раз в 5 минут) _find_volume_peaks находит ТОТ ЖЕ
  исторически крупнейший в окне всплеск заново (он никуда не делся из
  реальных свечей) и добавляет его как будто новый — бесконечный цикл
  переоткрытия одного и того же события. Добавлен постоянный реестр
  _vol_peak_get_fired_keys() — читает уже сработавшие алерты из истории
  (независимо от текущего watchlist, который чистится при срабатывании) и
  не даёт повторно добавить то, что уже отработало. Плюс по прямому
  запросу — VOL_PEAK_EXPIRY_SEC сокращён с 12 часов до 90 минут: старый
  всплеск за много часов теряет всякую связь с текущей ценой, использовать
  его как базу для % расчёта через 4-7 часов бессмысленно само по себе,
  не только из-за цикла переоткрытия. Проверено тестом: всплеск,
  зафиксированный как уже сработавший в истории, корректно НЕ добавляется
  в watchlist повторно при повторном обнаружении.
- v0.28.2: по прямому диагностическому наблюдению пользователя — "у него
  график всегда обрывается на максимальной цене, у меня идёт откат" —
  это прямое доказательство, что наш алерт стабильно опаздывает
  относительно истинного пика (график рисуется данными на момент
  отправки; если виден откат — значит отправили уже после пика).
  v0.28.1 (защита от "обнаружения задним числом") подтверждённо не
  покрывала весь случай — баг оставался даже на свежих, вовремя найденных
  всплесках. Добавлена защита на уровне самого момента срабатывания:
  каждая запись watchlist теперь отслеживает running_max — бегущий
  максимум живой цены с начала слежения, обновляется на КАЖДОМ опросе
  (раз в 30с, без доп. стоимости — цена и так уже фетчится). Алерт шлётся
  ТОЛЬКО если одновременно: цена выросла от всплеска на
  VOL_PEAK_CONFIRM_PCT И текущая цена — это и есть (в пределах 0.3%)
  бегущий максимум, а не уже откатившееся от него значение. Так как
  срабатывание происходит на ПЕРВОМ пересечении порога (само по себе
  всегда являющемся новым максимумом на тот момент), пропуск истинного
  пика между двумя опросами структурно исключён — если между опросами
  цена улетела выше и уже откатывает к моменту следующей проверки, алерт
  просто не уйдёт вообще (лучше промолчать, чем прислать протухший
  сигнал), дождавшись либо нового реального максимума, либо угасания
  движения. Проверено тестом на нескольких сценариях: мгновенное
  срабатывание на первом пересечении, полный пропуск истинного пика между
  опросами (сработало сразу на своём первом наблюдении, не на отложенном
  более позднем), молчание на явном "опросе с откатом".
- v0.28.1: реальный баг по живому алерту (SOXL_USDT) — монета часто
  попадает в топ-N по объёму ИЗ-ЗА того же пампа, который мы пытаемся
  поймать. К моменту, когда монета наконец входит в топ-N и мы находим её
  всплеск объёма (в примере — 182 минуты спустя), цена уже успевает
  дойти до истинного пика (+8.5% от всплеска) И ОТКАТИТЬ ОБРАТНО почти
  до порога подтверждения (+5.47%) — алерт формально "срабатывает", но
  реальный момент сигнала давно упущен, ловится уже мёртвая, отыгранная
  информация. Проверка одной живой цены в момент обнаружения этого не
  ловит (текущая цена может быть "только-только" у порога). Теперь
  _find_volume_peaks при каждом обнаружении всплеска проверяет МАКСИМУМ
  цены по всем свечам ПОСЛЕ всплеска — если он уже превышал порог более
  чем в 1.5 раза, движение уже состоялось и отыграно назад до обнаружения,
  всплеск отбрасывается целиком (не добавляется в watchlist). Проверено
  тестом на точной реконструкции сценария SOXL (0 вместо ложного сигнала)
  и на свежем, ещё не отыгранном случае (проходит нормально).
- v0.28.0: по прямому уточнению — ориентир только на САМЫЙ КРУПНЫЙ всплеск
  объёма в отображаемом окне (4 часа), не на любой, что просто перевалил
  порог z-score. Раньше _find_volume_peaks возвращала ВСЕ red-свечи,
  проходящие порог — теперь берёт red-свечу с максимальным объёмом во
  всём окне и проверяет z-score только для неё одной. Дедуп по spike_ts
  в watchlist сохранён — если в окне позже появляется ЕЩЁ БОЛЕЕ крупный
  всплеск (новый максимум), он тоже начнёт отслеживаться отдельно; но на
  каждый конкретный проход сканирования из нескольких одновременно
  квалифицирующихся всплесков остаётся только реально самый большой.
  Проверено тестом: из трёх всплесков, каждый по отдельности проходящих
  порог z>=4.0, в watchlist попадает только один — с максимальным объёмом.
- v0.27.1: попробовал добавить фильтр "всплеск объёма должен совпадать по
  времени с локальным пиком цены" (по разбору скрина ESP0RTS, где как
  показалось, крупный всплеск посреди ровного роста был "неправильным"
  ориентиром) — по прямому уточнению это была ошибка, полностью откачено.
  Ориентир — сам факт доминирующего всплеска (z-score >= 4.0), без
  дополнительных условий по совпадению с ценой. Логика функционально
  идентична v0.27.0 (z-score порог + VOL_PEAK_CONFIRM_PCT=5% рост цены от
  цены всплеска, без ожидания отката). Также обратил внимание: тот
  конкретный скрин (z-score 3.17) показывал алерт НИЖЕ порога 4.0 —
  скорее всего это была ещё не обновлённая версия на устройстве, не
  реальный баг текущей логики.
- v0.27.0: УПРОЩЕНО по прямому подтверждению ("сразу слать, всё как на
  примерах"). Оба предыдущих подхода (v0.25 — ждать возврата к цене
  всплеска; v0.26 — ждать подтверждённого отката от бегущего максимума)
  были неверны. Разобрав ESP0RTS_USDT — референсный алерт оказался просто
  нашим ЖЕ форматом обычного Pump Detector ("Pump: 6.9%"), никакого
  ожидания отката вообще, график обрывается ровно на пике без единой
  точки после него. Логика теперь: (1) находим ДОМИНИРУЮЩИЙ всплеск
  red-объёма (z-score порог поднят с 2.5 до 4.0 по подтверждению "только
  высокие, в самом верху графика") — запоминаем цену В МОМЕНТ всплеска;
  (2) как только живая цена выросла от неё на VOL_PEAK_CONFIRM_PCT=5% —
  СРАЗУ шлём алерт, без дальнейшего ожидания. Убрано отслеживание
  бегущего максимума целиком. Проверено тестом на точных цифрах из
  реального скрина ESP0RTS (0.0212->0.02262, +6.7%): не срабатывает
  раньше порога, срабатывает сразу на реальном движении.
- v0.26.1: по реальному скрину (SNDK_USDT) — сигнал пришёл с откатом уже
  0.9% при пороге 0.4%, потому что проверка цены была завязана на тот же
  5-минутный цикл, что и дорогой поиск новых всплесков (фетч свечей) — за
  эти 5 минут цена успевала укатиться далеко за порог, прежде чем мы
  вообще замечали. Разделено на два независимых цикла: _vol_peak_find_loop
  (5 минут, дорогой — только поиск новых всплесков) и _vol_peak_watch_loop
  (30 секунд, дешёвый — только живая цена уже отслеживаемых монет, без
  фетча свечей). Проверено тестом: то же самое срабатывание теперь ловит
  откат ровно у порога (0.4%), а не убежавший до 0.9%.
- v0.26.0: ИСПРАВЛЕНА суть логики Volume Peak Watcher по разбору
  аннотированных скринов — v0.25.0 была основана на неверном понимании:
  "пик" это не цена свечи со всплеском объёма, а НОВЫЙ ЛОКАЛЬНЫЙ МАКСИМУМ
  ЦЕНЫ, который формируется ПОСЛЕ всплеска (пользователь обвёл именно
  точку на самой линии цены в момент её локального пика, не место
  всплеска объёма). Всплеск объёма — просто триггер "начать следить",
  сигнал шлётся когда цена РЕАЛЬНО сделала пик и НАЧАЛА ОТКАТЫВАТЬ.
  Логика теперь: (1) всплеск найден — начинаем следить, бегущий максимум
  = цена всплеска; (2) пока живая цена растёт — просто двигаем бегущий
  максимум дальше, без алерта; (3) как только цена откатила от максимума
  на VOL_PEAK_ROLLOVER_PCT=0.4% — пик подтверждён, вот тогда алерт с
  ценой/временем этого пика. Проверено тестом с реальными числами из
  скрина (SOL_USDT, пик на 75.28 — та же цифра, что была на графике):
  рост цены НЕ вызывает алерт, только подтверждённый откат.
- v0.25.0: ПОЛНАЯ ПЕРЕРАБОТКА Sell-Volume Anomaly Watcher → Volume Peak
  Level Watcher, по прямому запросу после разбора ~10 реальных скринов
  стороннего бота. Обнаружилась ОДНА постоянная структура на каждом
  примере: пик объёма происходит в прошлом на определённой ЦЕНЕ, а сигнал
  шлётся НЕ в момент пика, а ПОЗЖЕ — когда цена, двигаясь дальше,
  ВОЗВРАЩАЕТСЯ на ценовой уровень этого пика (пример RARE: красный
  всплеск на уровне ~0.0140, сигнал пришёл когда цена вернулась туда же,
  ~0.01399 — воспроизведено тестом с точностью до этих же цифр). Раньше
  алерт слался МГНОВЕННО в момент самого пика — двухэтапная механика
  теперь: (1) _find_volume_peaks сканирует ВСЁ окно 4 часа (не только
  последнюю свечу, как раньше) и кладёт УРОВЕНЬ ЦЕНЫ пика в watchlist, не
  алертя сразу; (2) _vol_peak_scan_loop на каждом проходе проверяет,
  вернулась ли живая цена к уровню (допуск VOL_PEAK_TOUCH_TOLERANCE_PCT=
  0.5%) — и только тогда шлёт алерт + запускает EMA-триггер, как раньше;
  (3) записи watchlist старше 12 часов забываются (цена так и не
  вернулась). Watchlist переживает перезапуск процесса (атомарная запись,
  как и остальные файлы состояния), новый эндпоинт
  /vol_peak_watchlist_status показывает, что сейчас отслеживается. Картинка
  к алерту теперь показывает достаточно широкое окно, чтобы был виден и
  сам пик (в прошлом), и текущая точка возврата. Таблица на главной
  странице получила колонки "Уровень пика"/"Пик был". Проверено тестом на
  всех этапах: поиск пика в середине окна (не только последняя свеча),
  отсутствие алерта пока цена далеко от уровня, срабатывание при возврате,
  персистентность watchlist через "падение процесса".
- v0.24.0: по запросу — Sell-Volume Anomaly Watcher теперь тоже шлёт
  картинку, но ДРУГОГО стиля, чем памп/дамп (визуально отличать в списке
  Telegram-уведомлений). Новый _render_vol_anomaly_chart_png: та же общая
  структура (белый фон, чёрная линия цены, синие пунктирные уровни,
  красная базовая линия), но заливка — ФИОЛЕТОВАЯ и это РЕАЛЬНЫЙ ряд
  объёма именно на red-свечах (то, что детектор реально считает для
  z-score), не directional split зелёное/красное, как у пампа/дампа —
  тот прежний режим остался нетронутым. Сама аномальная свеча подсвечена
  отдельным оранжевым маркером сверху — тот же дух, что стрелка на
  референс-скрине RARE. Проверено попиксельно (фиолетовая заливка,
  оранжевый маркер, чёрная линия — все на месте) и сквозным тестом
  через реальный _vol_anomaly_fire_alert.
- v0.23.0: два фикса по прямому запросу и разбору реального сигнала.
  (1) БАГ, пойманный на реальном скрине (ASTEROID_USDT) — side/classic_bias
  считается по ПРЕДЫДУЩЕМУ закрытому бару, а entry=level сравнивается с
  ЖИВОЙ ценой; если цена внутри текущего (ещё не закрытого) бара уже
  прошла уровень насквозь, они рассогласуются — получался SHORT с входом
  НИЖЕ текущей цены (бессмысленно: для шорта лимитка должна быть выше
  цены, продаём дороже — сверено на всех реальных примерах стороннего
  бота, там эта связь строго соблюдается). Добавлен guard: для SHORT вход
  обязан быть >= живой цены, для LONG <= — иначе тихо пропускаем касание,
  сторона и уровень уже рассогласованы внутри дня. Проверено тестом на
  точной реконструкции сценария со скрина (0 вместо ложного сигнала) и на
  нормальных случаях (не сломался, проходят как раньше, для обоих
  направлений). (2) По данным реальной статистики (см. предыдущий анализ)
  — пампы, пойманные 120-минутным окном, дают исход в пользу шорта только
  в 44% случаев против ~85% у 20/60-минутных. Не отключаем (выборка
  13-16 на группу, не железобетонно), но помечаем такие сигналы в тексте
  алерта ("⚠️ памп пойман медленным окном — ниже уверенность") и сохраняем
  пометку в историю для дальнейшего анализа. Дамп пока не трогаем — там
  аналогичная выборка (n=8) искажена одним тикером, недостаточно надёжно.
- v0.22.1: по запросу — кнопки "Скачать" (💾) на всех четырёх таблицах
  главной страницы (Живые пампы / Живые дампы / Аномальный объём продаж /
  Касания EMA), не только на /ema_diag как раньше. Новые эндпоинты
  /pump_export, /dump_export, /vol_anomaly_export, /weekly_ema_export — с
  тем же Content-Disposition: attachment, что и у ema_diag_export (общий
  метод _json_download на Handler). Это именно те данные, что реально
  проходят через изменения v0.20-0.22 (мультиокно пампа/дампа, аномалия
  объёма) — в отличие от EMA Diagnostics (независимый широкий сканер),
  сравнение ДО/ПОСЛЕ по этим экспортам покажет реальный эффект. Проверено
  HTTP-тестом на все четыре эндпоинта и кнопки на странице.
- v0.22.0: автор стороннего бота подтвердил напрямую (не по пикселям) — red
  это объём на red-свечах (продажи), по симметрии зелёное — объём на
  green-свечах (покупки). Это ровно volume_mode="directional" +
  source="volume", уже бывший дефолтом в коде — но старый /pump_match
  (слепой визуальный грид-серч по десяткам гипотез: dual_period,
  dual_source, разные source) мог сохранить ДРУГОЙ режим, и тот файл имел
  приоритет над дефолтом. _load_pump_render_params и _save_pump_render_params
  теперь форсируют подтверждённые volume_mode/source при каждой загрузке
  И сохранении — не зависит от того, что лежит в файле, не потеряется при
  следующем обновлении, и будущий прогон /pump_match больше не сможет
  случайно переключить обратно на неверную гипотезу. Заодно упростил сам
  /pump_match: раз источник/режим больше не гипотеза, а факт — перебор
  сократился с тысяч комбинаций (перебирал ещё и source/dual_period/
  dual_source вслепую) до перебора только способа/периода сглаживания
  поверх подтверждённых directional/volume — быстрее и не тратит время на
  заведомо неверные варианты. Проверено тестом: сохранённый файл с
  "неверным" режимом (dual_source/price_change) корректно переписывается
  на directional/volume при загрузке, а неподтверждённые параметры
  сглаживания сохраняются как были.
- v0.21.0: по прямой цитате автора стороннего бота ("Первый фильтр — это
  большие объёмы на продажу, дисперсия отклонений от среднего", скрин RARE
  со стрелкой на всплеск red-объёма за 30-40 минут до пробоя цены) — новый
  Sell-Volume Anomaly Watcher, с тем же статусом триггера, что у Pump/Dump
  Detector (по прямому запросу — не отдельный экспериментальный слой, а
  полноценная часть пайплайна). _detect_sell_volume_anomaly считает
  z-score объёма ПОСЛЕДНЕЙ red-свечи относительно среднего/std всех
  red-свечей за 2 часа — при z>=2.5 шлёт алерт И сразу проверяет EMA-
  уровень (expected_bias="short", та же логика, что у пампа — в примере
  RARE аномалия предшествовала именно росту цены в сопротивление), может
  дать полный сигнал раньше, чем цена вообще успеет заметно сдвинуться.
  Известен только "первый шаг" его алгоритма (сам автор пишет "второй шаг
  анализа кидать?") — реализован он один, трекинг MFE/MAE (тот же принцип,
  что у пампа/дампа) оставлен, чтобы дальше было видно, насколько он сам
  по себе полезен. Новая таблица "Аномальный объём продаж" на главной
  странице, /vol_anomaly_status, кнопка очистки. Проверено тестом: детект
  корректно ловит выброс и корректно молчит на обычном объёме; полная
  цепочка аномалия→EMA-проверка→полный сигнал отработала на синтетике.
- v0.20.1: по запросу — скринсейвер теперь ещё и уходит в полноэкранный
  режим (Fullscreen API, с кроссбраузерными префиксами webkit/moz/ms) —
  весь экран чёрный, включая адресную строку/статус-бар, не только
  видимая область страницы. Если полноэкранный режим сняли снаружи
  (системный жест/кнопка назад), а не через тап по экрану — состояние
  синхронизируется само (wake lock освобождается, оверлей скрывается),
  не остаётся повисшим.
- v0.20.0: по реальным скринам — Pump/Dump Detector ловил только РЕЗКИЕ
  движения (одно окно 20 минут / 5%). Медленный разгон (час-полтора, как
  на присланных примерах) не давал 5%+ ни в одном 20-минутном сегменте —
  алерт либо опаздывал почти к самому пику, либо не срабатывал вовсе.
  Симулировал синтетикой той же формы, что на скрине: старая логика на
  88-минутном разгоне НЕ СРАБОТАЛА ВООБЩЕ; новая сработала на 69-й минуте,
  за 18 минут до истинного пика, при цене заметно ниже максимума.
  _pump_detect_check/_dump_detect_check теперь проверяют НЕСКОЛЬКО окон
  одновременно (PUMP_MOVE_WINDOWS_MIN=[120,60,20], пороги по убыванию
  длины окна выше: 8%/6%/5% — PUMP_THRESHOLD_PCT_BY_WINDOW), от длинного к
  короткому, первое сработавшее и даёт алерт. Резкие пампы по-прежнему
  ловятся так же быстро коротким 20-минутным окном (RR не изменился,
  проверено тестом отдельно). История price теперь хранится на 120+15
  минут (было 120+10 — небольшой запас под самое длинное окно). Алерт и
  запись истории показывают, каким именно окном поймали ("окно 120м").
- v0.19.4: по вопросу "после изменения алгоритма старые и новые данные не
  смешаются молча?" — раньше действительно смешались бы, никакой пометки
  версии не было. Добавлен DIAG_SCHEMA_VERSION (сейчас 1) — каждая новая
  запись помечается им при создании. Поднимать эту версию нужно ТОЛЬКО при
  изменении того, что делает записи несопоставимыми (формула детекта,
  сетка чекпоинтов, счёт MFE/MAE) — НЕ при изменении бизнес-порогов вроде
  DIAG_BOUNCE_THRESHOLD_PCT (те применяются при анализе поверх сырых
  данных, старые записи пересчитываются под них одинаково корректно).
  Старые записи без поля (собранные до этого фикса) считаются "версия 0" —
  не теряются, просто отдельно помечены. Сводная таблица на /ema_diag
  показывает список версий в каждой строке и подсвечивает ⚠, если группа
  смешала несколько версий — та же идея, что и предупреждение про среднее/
  медиану. У сырых записей — фильтр по версии + колонка "Схема". Проверено
  тестом: легаси-данные (без поля) корректно определяются как v0,
  смешение новой и старой версии в одной группе ловится.
- v0.19.3: разбор первой реальной ночи диагностики выявил, что среднее
  (mean) может быть полностью искажено 1-2 экстремальными выбросами на
  тонких монетах — конкретный случай: AKE_USDT дал +250% за 15 минут на
  недельном касании, единолично утащив среднюю всей связки 1w-EMA7-SHORT
  с честных +0.04% (без него) до -3.7% — при том что медиана всё время
  показывала правильные +0.27%. _diag_summary теперь считает median_mfe_pct
  /median_mae_pct рядом со средним (сырые данные НЕ отсекаются — экстремальный
  исход тоже реальный, просто мера отдельно), таблица на /ema_diag
  подсвечивает ⚠ жёлтым, если среднее разошлось с медианой больше чем в 2
  раза — сразу видно, где выброс, без ручного разбора.
- v0.19.2: по запросу — кнопка "Скринсейвер" на главной странице. Чисто
  клиентская фича (никакого сервера): полноэкранный чёрный div поверх
  всего + Screen Wake Lock API (navigator.wakeLock), не даёт экрану
  заблокироваться, пока страница открыта. Тап по чёрному экрану — выход.
  На visibilitychange перезапрашивает лок, если браузер сам его снял при
  уходе со страницы (так делают некоторые мобильные браузеры). Требует
  secure context — на localhost (как здесь, раз Termux и браузер на одном
  устройстве) работает и по http.
- v0.19.1: по запросу — кнопка "Скачать JSON" на /ema_diag (GET
  /ema_diag_export, с Content-Disposition: attachment — телефон сохранит
  файл, а не просто покажет JSON текстом в браузере). Экспорт включает и
  сырые записи, и уже готовую сводку (_diag_summary) в одном файле.
- v0.19.0: по запросу — целая система сбора диагностических данных для
  анализа/улучшения алгоритма, новая страница /ema_diag. В отличие от
  live-вотчера (узкий порог 0.25 ATR, только под алерты) — здесь широкий
  DIAG_TOUCH_ATR=1.0, собирает ВСЕ приближения к EMA7/14/28, не только
  сигнальные. Таймфреймов теперь 4 (1h/4h добавлены к 1d/1w). Для каждого
  касания: MFE/MAE (макс. движение за/против), срезы через 15/30/60/120/240
  минут, флаг просадки ПОСЛЕ уже начавшегося движения по тренду (не просто
  любой MAE — это нормальный шум входа), Volume Profile (POC + доля объёма
  в пиковой зоне), funding rate. Честно про "карту ликвидации" — настоящей
  нет и не будет без платного источника типа Coinglass, funding rate это
  единственный реально доступный (и куда более грубый) прокси. Сводная
  таблица /ema_diag_summary: по каждой связке период×таймфрейм — bounce
  rate, средний MFE/MAE, отдельно по long/short — отвечает на "откуда
  отскакивало чаще всего". Два новых фоновых цикла (_diag_scan_loop раз в
  15 минут, _diag_track_loop раз в 2 минуты), та же архитектура защиты от
  сбоев, что и у остального (атомарная запись, восстановление истории при
  старте — не наступаем на грабли v0.13/v0.15). Пойман и починен реальный
  дедлок при разработке (POST /ema_diag_clear держал _diag_lock и внутри
  вызывал _diag_save(), которая тоже берёт этот лок — Lock не реентерабельный).
  Проверено полным HTTP-тестом: скан находит касания, статус/сводка/очистка
  работают, очистка не виснет.
- v0.18.0: по запросу — (1) количество монет, участвующих в поиске сигналов
  (было жёстко зашитое PUMP_DETECT_TOP_N=100), вынесено в настройку
  /scan_settings — меняется прямо на странице (поле + кнопка "Сохранить"
  сверху), сохраняется на диск, применяется со следующего цикла сканирования
  без рестарта процесса. (2) Недостаточность истории на новых листингах
  теперь кэшируется — раньше монета без достаточной недельной/дневной
  истории заново дёргала полный фетч (до 400 дней) на КАЖДОЙ проверке после
  каждого пампа/дампа и каждый раз получала один и тот же отрицательный
  результат впустую. Теперь _ema_touch_get_closed_ctx запоминает "у этой
  монеты недостаточно истории" на 3 дня и не тратит сеть повторно. Проверено
  тестом: настройка сохраняется/применяется через реальный HTTP, повторный
  fetch для новой монеты не дублируется.
- v0.17.0: по запросу — кнопки очистки истории с подтверждением на все три
  таблицы главной страницы (Живые пампы / Живые дампы / Касания EMA7/14/28),
  не только одна. Каждая — свой confirm() и свой POST-эндпоинт
  (/pump_history_clear, /dump_history_clear, /weekly_ema_history_clear),
  зеркалит уже существовавший паттерн (/ema_signal_history_clear в старой
  версии страницы). Проверено сквозным HTTP-тестом на все три.
- v0.16.2: реальный баг — для очень волатильных дешёвых монет ATR может
  быть настолько большим относительно самой цены (видели >45%), что
  stop_dist*RR (2.25×ATR) превышает саму цену — тейк (для SHORT) или стоп
  (для LONG) уходил в ОТРИЦАТЕЛЬНУЮ цену, что и было видно в сигналах.
  Добавлен guard в _ema_touch_check_symbol: если расчётный entry/stop/take
  <= 0, касание тихо пропускается (лог с причиной), сигнал не шлётся.
  Проверено юнит-тестом на обе уязвимые ветки формулы (SHORT→тейк,
  LONG→стоп) — обе корректно отсекаются при экстремальной волатильности,
  нормальные случаи проходят как раньше.
- v0.16.1: по запросу — трекинг цены после пампа/дампа теперь раз в минуту
  (было раз в 5 минут) в течение часа (было 2 часа) — PUMP_TRACK_POLL_SEC
  60, PUMP_TRACK_WINDOW_SEC 3600.
- v0.16.0: по запросу — памп/дамп алерты тоже теперь отслеживают, куда
  пошла цена ПОСЛЕ сигнала и насколько далеко (в отличие от EMA-сигналов у
  памп/дампа нет явных стоп/тейк уровней — здесь просто трекинг движения
  цены, не гонка take-vs-stop). Новый фоновый _pump_dump_track_loop: раз в
  5 минут, в течение 2 часов после срабатывания, обновляет max_follow_pct
  (максимальное движение цены В ТУ ЖЕ сторону, что и сигнал — "разогналось
  дальше") и max_reverse_pct (максимальное движение В ОБРАТНУЮ — "откатило
  назад"). Таблицы "Живые пампы"/"Живые дампы" получили колонку "После
  сигнала". Заодно вынес дублировавшийся код записи истории (был отдельно
  в _pump_fire_alert и _dump_fire_alert) в общие _pump_dump_history_append/
  _pump_dump_history_save_all — с той же атомарной записью (tmp+os.replace),
  что и остальные файлы состояния. Проверено тестом на обе стороны для
  памп и для дамп: и продолжение движения, и откат корректно фиксируются.
- v0.15.0: защита от потери отслеживания тейка/стопа при сбоях (интернет,
  падение Termux и т.п.) — по прямому запросу. НАШЛИ И ПОЧИНИЛИ реальный
  баг по пути: история EMA-сигналов подхватывалась с диска ТОЛЬКО внутри
  _weekly_ema_touch_loop; когда в v0.13.0 этот цикл остановили (заменили на
  триггер от пампа/дампа), подгрузка при старте молча перестала
  происходить вообще — после любого перезапуска процесса все ОТКРЫТЫЕ
  сигналы теряли отслеживание (на диске оставались, в памяти пропадали).
  Теперь _load_weekly_ema_history() вызывается прямо в main(), не зависит
  от того, какие циклы включены. Плюс: (1) запись истории стала атомарной
  (tmp-файл + os.replace — битый файл при обрыве записи посреди процесса
  больше не возможен, тот же приём, что уже был у _save_ema_history);
  (2) одна повреждённая запись в резолвере не срывает проверку остальных
  открытых сигналов в том же проходе; (3) serve_forever() теперь ловит
  любое исключение, а не только KeyboardInterrupt, и процесс сам себя
  перезапускает изнутри (внешний while в if __name__=="__main__") при
  падении потока; (4) отдельный run_pump_radar.sh — внешний супервизор на
  случай, если умрёт вся Termux-сессия целиком (это уже не починить
  изнутри процесса), с termux-wake-lock и автоперезапуском python3.
  Проверено тестом: сигнал переживает имитацию "падения процесса"
  (сохранение → обнуление памяти → загрузка) и виден для резолвера снова;
  битый JSON-файл не роняет процесс, просто стартует с пустой историей.
- v0.14.0: по запросу — в список сигналов добавлено поле "Исход". Каждый
  сработавший сигнал теперь получает outcome="open" при создании, фоновый
  _weekly_ema_resolve_loop раз в 2 минуты проверяет живую цену всех ещё
  открытых сигналов (группирует по монете — одна проверка цены на монету
  за проход, даже если открытых сигналов у неё несколько) и закрывает по
  тейку или стопу (упрощённая гонка take-vs-stop, вход отдельно не
  моделируется — та же логика, что в Weekly EMA Backtest). При закрытии —
  дополнительный Telegram-алерт (✅ ТЕЙК / ❌ СТОП). Таблица на главной
  странице получила колонку "Исход" + сводку (закрыто X, винрейт Y%).
  Проверено тестом на обе стороны (SHORT→тейк, LONG→стоп).
- v0.13.0: АРХИТЕКТУРНАЯ ПЕРЕСТРОЙКА по прямому запросу — "эти пампы и есть
  триггер сигнала". Разобрали по числам: LONG-сигнал = подход к EMA сверху
  вниз = ровно то, что происходит при резком ДАМПЕ; SHORT-сигнал = подход
  снизу вверх = ровно то, что происходит при резком ПАМПЕ. Раньше EMA-
  касания сканировались ОТДЕЛЬНЫМ постоянным опросом топ-100 монет каждую
  минуту, независимо от того, происходит там что-то или нет — теперь
  полностью заменено: постоянный опрос убран (_weekly_ema_touch_loop
  оставлена в файле, просто не запускается), вместо неё —
  _pump_or_dump_maybe_trigger_ema_signal(symbol, expected_bias),
  вызывается ТОЛЬКО в момент, когда реально сработал памп или дамп (изнутри
  _pump_fire_alert/_dump_fire_alert). expected_bias — safety-фильтр: если
  сторона подхода к уровню не совпала с направлением самого движения (памп
  → ожидаем short, дамп → ожидаем long), полный сигнал не шлётся, тишина
  лучше нелогичного сочетания направлений.
  Добавлен Dump Detector — зеркало Pump Detector: та же общая скользящая
  история цен (_pump_price_history, один запрос /tickers на двоих, не
  дублируем сеть), просто ищет движение от МАКСИМУМА окна вниз, а не от
  минимума вверх. _pump_detect_loop теперь проверяет каждую монету И на
  памп, И на дамп за один проход. Новая таблица "Живые дампы" на главной
  странице (зеркало пампов), эндпоинт GET /dump_status.
  Проверено сквозным тестом: памп с совпадающей стороной корректно даёт и
  обычный алерт, и полный сигнал по каждому совпавшему уровню EMA (обоим
  ТФ), памп с несовпадающей стороной — только обычный алерт, без полного
  сигнала (safety-фильтр отработал).
- v0.12.0: по реальным скринам стороннего бота (формат "ВХОД: $X (лимитка,
  ожидаем откат к уровню)" + список "Уровни: • EMAn(W): $Y — роль") — Вход
  теперь САМ УРОВЕНЬ EMA (лимитка на откат), а не текущая живая цена.
  Раньше entry=price означало "по рынку прямо сейчас", что путало: к
  моменту просмотра алерта цена уже могла отойти от уровня. Стоп/Тейк
  считаются от этого же уровня (RR не изменился, 1.5). Добавлено поле
  "levels" — все три EMA(7/14/28) с ролью (поддержка/сопротивление,
  определяется по тому, выше или ниже текущей цены), выводится в алерте
  списком, в том же стиле, что у референса.
- v0.11.0: по реальному скрину алерта — сигнал приходил ПОЗДНО (цена уже
  заметно ушла от EMA к моменту срабатывания). Две причины, обе починены:
  (1) опрос был раз в 30 минут (WEEKLY_EMA_SCAN_POLL_SEC), потому что на
  каждой проверке заново перекачивалась вся дневная история — дорого.
  Теперь дорогая часть (свечи + EMA/ATR по закрытым барам) кэшируется в
  _ema_touch_ctx_cache и пересчитывается только когда должен закрыться
  новый бар (тот же приём, что _ema_get_closed_ctx в исходном движке), а
  живая цена (дешёвый тикер-запрос) опрашивается раз в минуту вместо
  получаса. (2) график в алерте обрывался на ПОСЛЕДНЕЙ ЗАКРЫТОЙ свече
  (вчера/прошлая неделя) — белая точка стояла там, а не на живой цене,
  создавая ложное впечатление "цена ушла от уровня" даже когда касание
  было валидным. Теперь _render_ema_touch_chart_png дорисовывает
  виртуальную "живую" точку (touch_price + экстраполированные EMA на
  сейчас) поверх исторических свечей — белая точка и линии EMA доходят до
  реального момента касания. Проверено попиксельно: Y-координата точки
  теперь точно совпадает с живой ценой, а не с историческим закрытием.
- v0.10.0: по скринам стороннего "AI EMA SIGNALS" — добавлены Вход/Стоп/
  Тейк (ATR-based, RR=1.5 по умолчанию — подсмотрено на их примере APE, где
  риск/прибыль ≈ 1:1.5) в touch-записи _ema_touch_check_symbol, и картинка
  к алерту вместо голого текста: _render_ema_touch_chart_png рисует свечи +
  EMA7/14/28 линиями (синий/фиолетовый/золотой, как у референса) + пункти-
  рные уровни Вход/Стоп/Тейк + белая точка на текущей цене, тёмная тема.
  Алерт теперь уходит через _send_alert_photo вместо текстового _send_alert
  (с тем же текстом в подписи). Таблица на главной странице получила
  колонки Вход/Стоп/Тейк.
- v0.9.0: по запросу (сообщение пришло со съехавшей раскладкой, расшифровано
  как "касания надо отслеживать как на недельке, так и на дневке? работаем
  только в шорт") — Weekly EMA Touch Watcher переименован в Weekly/Daily,
  сканирует ОБА таймфрейма (EMA_TOUCH_TIMEFRAMES = ["1w","1d"]), и в LIVE-
  алертах фильтрует только SHORT-касания (EMA_TOUCH_LIVE_ONLY_SHORT=True —
  подход снизу вверх, тест сопротивления); long-касания больше не алертят.
  _weekly_ema_check_symbol обобщена в _ema_touch_check_symbol(symbol, tf) —
  общая логика на оба ТФ, отдельный кулдаун для дневного (короче недельного,
  раз дневной график быстрее меняется). Weekly EMA Backtest тоже получил
  параметр tf ("1w"/"1d", выбирается на странице) — там long НЕ фильтруется
  (обе стороны видны в by_bias для сравнения), результаты по разным ТФ
  сохраняются в отдельные файлы, не перезаписывают друг друга.
- v0.8.0: ВАЖНАЯ поправка методики Weekly EMA Backtest по прямому запросу —
  сигнал ищется на недельном графике, но сделка НЕ держится неделями:
  реальная торговля — мгновенная реакция на 3-4%, иногда 6-10%, сделки на
  минуты-час максимум. Прежняя версия (v0.7.0) оценивала исход по недельным
  закрытиям через 1-12 недель — это измеряло НЕ ТО, что реально
  торгуется. Переписано: детект сигнала остался прежним (недельное
  касание EMA7/14/28), но оценка исхода теперь — по 1-минутным свечам в
  первые W минут (дефолт 180) после момента касания: достигнут ли каждый
  из порогов (3/4/6/10%, настраиваемые) в сторону classic_bias, и за
  сколько минут (переиспользует _pm_fetch_candles_window из Pump Match).
  Учтено практическое ограничение: у биржи почти наверняка нет 1m-истории
  на годы назад — реакция проверяется только для сигналов не старше
  max_age_days (дефолт 90), для более старых считается только сам факт
  касания (total_signals), без участия в винрейте по реакции — это явно
  видно в таблице по числу "n=" (evaluable) у каждой ячейки.
- v0.7.0: добавлен Weekly EMA Backtest (/weekly_ema_backtest) — по запросу
  "прогнать исторически все сигналы, посмотреть куда шла цена, составить
  рейтинг угадывания направления". Проходит всю доступную недельную
  историю по монетам (не только текущий момент, как живой вотчер), находит
  ВСЕ прошлые касания EMA7/14/28 без заглядывания вперёд, и оценивает
  классическую bounce-трактовку на 5 горизонтах разом (1/2/4/8/12 недель) —
  НЕ на одном произвольном горизонте: на синтетике винрейт скакал от 0% до
  56% просто от смены горизонта 8->15 недель (совпадение фазы с любой
  циклической компонентой цены) — один горизонт был бы вводящим в
  заблуждение числом, а не честной оценкой. Асинхронная задача (тот же
  паттерн, что у /pump_match — POST возвращает job_id, прогресс через GET),
  результат сохраняется на диск и переживает рестарт. Таблица на странице:
  монета/кол-во сигналов/винрейт на каждом горизонте, с фильтром по
  минимальному числу сигналов (отсеивает шум от 1-2 случайных касаний) и
  сортировкой по выбранному горизонту.
- v0.6.1: Weekly EMA Touch Watcher теперь определяет СТОРОНУ подхода к
  уровню (по предыдущей закрытой недельной свече — была она выше или ниже
  EMA) и классическую bounce-трактовку: подход снизу вверх = тест
  сопротивления = SHORT-контекст (ждём отскок вниз); сверху вниз = тест
  поддержки = LONG-контекст (ждём отскок вверх). Это ИНФОРМАЦИЯ о
  классическом прочтении, не гарантированный сигнал — уровень может и
  пробиться вместо отскока (та же развилка bounce/breakout, что и во всём
  остальном движке). Добавлено в текст Telegram-алерта и колонку таблицы.
- v0.6.0: добавлен Weekly EMA Touch Watcher — отдельный, независимый ни от
  Pump Detector, ни от (остановленного) EMA-инверт движка сканер. Раз в
  30 минут проверяет топ-100 монет по объёму на касание ценой EMA7/14/28
  на НЕДЕЛЬНОМ графике (переиспользует чистые функции _ema/_atr/
  _resample_to_weekly/_ema_live_value/_ladder_order, уже бывшие в файле —
  ничего заново не писалось) и шлёт текстовый алерт в Telegram + пишет в
  историю. Секция "Касания EMA7/14/28" добавлена на главную страницу
  (таблица последних срабатываний), эндпоинт GET /weekly_ema_status.
  Чисто информационно — НЕ сигнал на вход, автоторговли не касается.
- v0.5.0: главная страница ("/", "/ema") упрощена — по запросу "уходим от
  прошлой реализации, оставляем только поиск пампов". Убраны с UI: скан
  EMA-досье, живые EMA-сигналы, история сигналов, автоторговля — остались
  только живые пампы, настройки алертов и ссылка на /pump_match. Старая
  полная страница никуда не делась, доступна на /ema_full (таблицы будут
  пустые, т.к. сам EMA-движок теперь не запускается — см. ниже), код всего
  движка остался в файле нетронутым, просто не в фокусе.
  _ema_signal_loop() остановлен в main() (закомментирован запуск потока) —
  раз им не пользуемся, незачем ему фоном дёргать Gate.io API и держать
  включённой возможность реальной автоторговли без видимого в UI контроля
  над ней. Возврат — раскомментировать одну строку в main().
- v0.4.0: /pump_match переведён на асинхронную схему — сетка перебора
  выросла до тысяч комбинаций (несколько минут), один блокирующий HTTP-
  запрос на это время выглядел бы как зависание браузера. Теперь POST
  /pump_match_run сразу возвращает job_id, сама работа идёт в фоновом
  потоке, прогресс — через GET /pump_match_status?job_id=... (опрос раз в
  2с с индикатором "готово X/Y"). Плюс третья независимая гипотеза формулы
  сигналов — volume_mode="dual_source": зелёная и красная волна вообще из
  ДВУХ РАЗНЫХ рядов (не просто по-разному сглаженный объём), и несколько
  сырых источников на выбор (объём/изменение цены/диапазон бара/тени
  свечи/тело) — по прямому запросу "не привязывайся к одной идее, юзай всё
  что есть, пока не найдём формулу". Дефолты формы — T_USDT/120 минут.
- v0.3.0: приоритет /pump_match переставлен на МАКСИМАЛЬНОЕ совпадение формы
  красной/зелёной волны (по явному запросу — "прям очень много параметров
  добавить пока не найдём формулу сигналов референса, приоритет максимальная
  схожесть волн"). combined_score теперь 0.8×стиль + 0.2×время (было 50/50) —
  позиция окна во времени уже надёжно находится сама (0.98-0.99 на тестах),
  так что вес отдан подбору формы. Внутри стиля тоже сдвинуто в пользу
  позиционного совпадения волн: 0.75×позиция + 0.25×баланс (было 0.65/0.35).
  Сетка перебора сильно расширена: periods [1..20] вместо [2,3,5,8], floor_frac
  9 значений вместо 4, dual_period пар ×4 больше, добавлена ТРЕТЬЯ независимая
  гипотеза volume_mode="price_change" (зелёная/красная волна — не из объёма
  вообще, а из сглаженных прибавок/потерь цены закрытия, как внутренние
  gain/loss в RSI — принципиально другой источник данных, не просто другое
  сглаживание объёма). PUMP_MATCH_STYLE_SEARCH_WINDOWS снижен до 1 (позиция
  времени уже надёжна) — бюджет по времени выполнения переброшен в глубину
  сетки стиля вместо ширины по времени.
- APP_VERSION теперь поднимается на каждое реальное изменение — раньше
  застряла на 0.1.0 несколько итераций подряд, из-за чего нельзя было по
  `grep APP_VERSION` отличить, обновился ли реально файл после докачки.
- Отдельный, самостоятельный проект-песочница. Взят как копия рабочего
  EMA_Invert_Experiment.py v0.1.10 (весь EMA-инверт движок ниже перенесён
  1:1, без изменений в логике входа/SL/TP/time-stop/диагностики) + добавлен
  независимый Live Pump Detector (см. секцию "Live Pump Detector" ниже —
  детектит резкий рост цены за короткое окно и шлёт в Telegram картинку в
  стиле стороннего скринера: чёрная линия цены с точками, красно-зелёная
  гистограмма объёма, сплошная красная база, синяя штрихованная сетка) +
  веб-страница /pump_match — загружаешь скрин-эталон, бот сам ищет и
  подходящее время (по форме ценовой линии), и параметры отрисовки
  гистограммы (по форме и балансу красной/зелёной волны), лучшее сразу
  применяется в реальных алертах.
  Полная история изменений (SMC Optimizer v1.0 → v3.52.96 → EMA Bounce
  Dossier v1.0 → v3.6.14 → EMA Invert Experiment v0.1.0 → v0.1.10) — в
  докстринге EMA_Invert_Experiment.py, здесь сознательно не дублируется
  построчно, чтобы не раздувать файл историей другого проекта; сама логика
  унаследована без потерь, только докстринг сокращён.
  Независимая идентичность на случай, если этот файл и оригинальный
  EMA_Invert_Experiment.py (PORT 8766) когда-нибудь понадобится гонять
  ОДНОВРЕМЕННО на одном устройстве: свой PORT (8767) и свои файлы
  состояния/логов/диагностики (префикс "pumpradar_" вместо "ema_invert_").
  Telegram/ntfy и Gate.io ключи по-прежнему в общих ~/.smc_alert_cfg.json /
  ~/.smc_gate_cfg.json — это осознанно (одни и те же реквизиты для всех
  ботов на устройстве, как и было в родительском проекте), не отдельные.
  Дальше этот файл — открытая песочница: логику EMA-инверта можно смело
  резать/переделывать/выключать, ничего в оригинальном EMA_Invert_
  Experiment.py от этого не пострадает.
"""
import os, sys, json, time, math, random, threading, base64, hashlib, subprocess, io, gc, uuid, statistics, re, queue
import struct, zlib
import multiprocessing
import http.server, urllib.request, urllib.parse
from functools import lru_cache
from concurrent.futures import ThreadPoolExecutor, ProcessPoolExecutor, wait as _fw, as_completed as _as_completed
import sys as _sys
# python3.14t (free-threaded, no GIL) не поддерживает ProcessPoolExecutor
if hasattr(_sys, '_is_gil_enabled') and not _sys._is_gil_enabled():
    _PoolExecutor = ThreadPoolExecutor
    _POOL_TYPE = "thread"
else:
    _PoolExecutor = ProcessPoolExecutor
    _POOL_TYPE = "process"

try:
    import requests
except ImportError:
    os.system(f"{sys.executable} -m pip install requests -q")
    import requests

APP_VERSION  = "0.30.96"

# ── Проверка консистентности версии (защита от забытого обновления) ──────────
def _check_version():
    import re as _re
    _src = open(__file__).read()
    _m1  = _re.search(r'APP_VERSION\s+=\s+"([^"]+)"', _src)
    _m2  = _re.search(r'Pump Radar v([\d.]+)', _src)
    if not _m1 or not _m2:
        raise RuntimeError("VERSION CHECK: не найдены строки версии в файле!")
    if _m1.group(1) != _m2.group(1):
        raise RuntimeError(
            f"VERSION MISMATCH: APP_VERSION={_m1.group(1)} "
            f"но docstring={_m2.group(1)} — обновите оба!"
        )
_check_version()
# ─────────────────────────────────────────────────────────────────────────────

GATE_API     = "https://api.gateio.ws/api/v4"
# автоопределение по числу ядер устройства (одно ядро оставляем ОС/интерфейсу)
_cpu_count   = multiprocessing.cpu_count() or 2
_env_workers = os.environ.get("SMC_NUM_WORKERS")
NUM_WORKERS  = max(1, int(_env_workers)) if _env_workers else max(1, _cpu_count - 1)
_ema_dossier_lock = threading.Lock()
_ema_dossier_running = {"v": False}

PORT         = 8767   # отдельный порт — можно держать запущенным одновременно
                       # с EMA_Invert_Experiment.py (8766) и оригинальным
                       # EMA-screener (8765) без коллизий
TG_TOKEN     = os.environ.get("TG_TOKEN", "")
TG_CHAT      = os.environ.get("TG_CHAT", "")
NTFY_URL     = os.environ.get("NTFY_URL", "")
WATCHDOG_ENABLED      = True   # увед. в TG/ntfy, если пропал интернет
PUMP_DUMP_ALERTS_ENABLED = False   # v0.30.7: по запросу — по умолчанию ВЫКЛЮЧЕНО, "пока" (легко включить обратно). Детект и трекинг пампов/дампов продолжают работать в фоне (нужны для EMA-триггера), просто без отправки текста в Telegram
WATCHDOG_TIMEOUT_MIN  = 60     # порог простоя (мин) до первого алерта
HC_URL       = os.environ.get("HC_URL", "")  # heartbeat healthchecks.io
ALERT_CFG_PATH   = os.path.expanduser("~/.smc_alert_cfg.json")
GATE_CFG_PATH    = os.path.expanduser("~/.smc_gate_cfg.json")
GATE_KEY         = os.environ.get("GATE_KEY", "")
GATE_SECRET      = os.environ.get("GATE_SECRET", "")

_C_GRN = "\033[92m"; _C_YEL = "\033[93m"; _C_RED = "\033[91m"
_C_GREY = "\033[90m"; _C_RST = "\033[0m"

TF_SECONDS = {
    "1m":60,"5m":300,"15m":900,"30m":1800,
    "1h":3600,"4h":14400,"1d":86400
}

# ─── Глобальное состояние ───────────────────────────────────────────────────
opt_lock   = threading.Lock()
_log_lock  = threading.Lock()  # отдельный лок для olog

opt_state = {"logs": [], "logs_dropped": 0}

import hmac, hashlib, urllib.parse as _uparse

def _gate_req(method, path, params=None, body=None, _retries=3, _retry_delay=2.0):
    """Подписанный запрос к Gate.io Futures USDT API.
    При сетевых ошибках (timeout, connection) делает до _retries попыток."""
    if not GATE_KEY or not GATE_SECRET:
        raise RuntimeError("Gate.io ключи не настроены")
    import hashlib as _hl
    query_str = _uparse.urlencode(params) if params else ""
    body_str  = json.dumps(body) if body else ""
    url_path  = f"/api/v4{path}"
    body_hash = _hl.sha512(body_str.encode()).hexdigest()
    ts = str(int(time.time()))
    msg = "\n".join([method, url_path, query_str, body_hash, ts])
    sig = hmac.new(GATE_SECRET.encode(), msg.encode(), _hl.sha512).hexdigest()
    headers = {
        "Content-Type": "application/json",
        "KEY":       GATE_KEY,
        "Timestamp": ts,
        "SIGN":      sig,
    }
    url = f"https://fx-api.gateio.ws{url_path}"
    if query_str: url += "?" + query_str

    last_exc = None
    for attempt in range(1, _retries + 1):
        try:
            r = requests.request(method, url, headers=headers,
                                 data=body_str if body_str else None, timeout=10)
            if r.status_code == 429:
                last_exc = RuntimeError(f"Gate {method} {path} → 429: {r.text[:200]}")
                if attempt < _retries:
                    olog(f"⚠ Gate 429 rate limit (попытка {attempt}/{_retries}) — повтор через {_retry_delay*2}с")
                    time.sleep(_retry_delay * 2)
                    continue
                raise last_exc
            if not r.ok:
                raise RuntimeError(f"Gate {method} {path} → {r.status_code}: {r.text[:200]}")
            return r.json()
        except (requests.exceptions.ConnectionError,
                requests.exceptions.Timeout,
                requests.exceptions.ReadTimeout) as e:
            last_exc = e
            if attempt < _retries:
                olog(f"⚠ Gate сеть (попытка {attempt}/{_retries}): {_explain_error(e)} — повтор через {_retry_delay}с")
                time.sleep(_retry_delay)
        except RuntimeError:
            raise
        except Exception as e:
            last_exc = e
            if attempt < _retries:
                olog(f"⚠ Gate ошибка (попытка {attempt}/{_retries}): {_explain_error(e)} — повтор через {_retry_delay}с")
                time.sleep(_retry_delay)
    raise last_exc

def _gate_get_position(symbol):
    """Текущая позиция по символу (None если ПОДТВЕРЖДЁННО нет)."""
    try:
        data = _gate_req("GET", f"/futures/usdt/positions/{symbol}")
    except RuntimeError as e:
        if "POSITION_NOT_FOUND" in str(e):
            return None
        raise
    size = float(data.get("size", 0))
    if size == 0: return None
    return {
        "dir":    "long" if size > 0 else "short",
        "size":   abs(size),
        "entry":  float(data.get("entry_price", 0)),
    }

def _gate_get_price(symbol):
    """Текущая цена фьючерса (mark price)."""
    try:
        data = requests.get(f"{GATE_API}/futures/usdt/contracts/{symbol}", timeout=5).json()
        return float(data.get("mark_price") or data.get("last_price", 0))
    except:
        return 0.0

def _gate_get_quanto(symbol):
    """Размер одного контракта в USDT (quanto_multiplier)."""
    return _gate_get_contract_info(symbol)["quanto_multiplier"]

_gate_contract_cache      = {}
_gate_contract_cache_lock = threading.Lock()

def _gate_get_contract_info(symbol, force_refresh=False):
    """Спецификация контракта Gate.io (quanto_multiplier, шаг цены
    order_price_round) с кэшем."""
    contract = symbol.replace("/", "_").upper()
    if not force_refresh:
        with _gate_contract_cache_lock:
            cached = _gate_contract_cache.get(contract)
        if cached:
            return cached
    r = requests.get(f"{GATE_API}/futures/usdt/contracts/{contract}", timeout=5)
    if r.status_code != 200:
        raise RuntimeError(f"Gate.io contracts/{contract} → {r.status_code}: {r.text[:200]}")
    data = r.json()
    if not isinstance(data, dict) or "quanto_multiplier" not in data:
        raise RuntimeError(f"Gate.io contracts/{contract}: неожиданный ответ {data}")
    info = {
        "quanto_multiplier": float(data["quanto_multiplier"]),
        "order_price_round": str(data.get("order_price_round", "0.01")),
    }
    with _gate_contract_cache_lock:
        _gate_contract_cache[contract] = info
    return info

def _gate_get_balance():
    """Свободный баланс USDT (для расчёта размера новой позиции)."""
    try:
        data = _gate_req("GET", "/futures/usdt/accounts")
        cross_bal = data.get("cross_margin_balance")
        avail     = data.get("available")
        try:
            cross_bal = float(cross_bal) if cross_bal not in (None, "") else None
        except (TypeError, ValueError):
            cross_bal = None
        try:
            avail = float(avail) if avail not in (None, "") else 0.0
        except (TypeError, ValueError):
            avail = 0.0
        olog(f"🔍 gate_balance: margin_mode={data.get('margin_mode')} "
             f"available={avail} cross_margin_balance={cross_bal}")
        result = cross_bal if (cross_bal is not None and cross_bal > 0) else avail
        if result <= 0:
            # v0.30.94: реальная находка по прямому сообщению "раньше баланс
            # видело без проблем" — при margin_mode=3 (не обычный cross)
            # оба поля, что мы проверяем (available/cross_margin_balance),
            # пришли пустыми, хотя деньги на аккаунте, по словам
            # пользователя, реально есть. Вместо гадания — печатаем ВЕСЬ
            # сырой ответ API целиком, чтобы увидеть, в каком поле баланс
            # на самом деле лежит при этом режиме маржи, и поправить точно,
            # не наугад
            olog(f"🔍 gate_balance: баланс вышел 0/пусто при margin_mode="
                 f"{data.get('margin_mode')} — полный сырой ответ API: {json.dumps(data)}")
        return result
    except Exception as e:
        olog(f"⚠ gate_get_balance: {_explain_error(e)}")
        return 0.0

def _gate_get_equity():
    """Баланс аккаунта С УЧЁТОМ открытой позиции (для отображения в UI/AMOLED)."""
    try:
        data = _gate_req("GET", "/futures/usdt/accounts")
        def _f(key):
            v = data.get(key)
            if v in (None, ""):
                return None
            try:
                return float(v)
            except (TypeError, ValueError):
                return None
        cross_bal = _f("cross_margin_balance")
        total     = _f("total")
        upnl      = _f("unrealised_pnl") or 0.0
        avail     = _f("available")
        olog(f"🔍 gate_equity: margin_mode={data.get('margin_mode')} "
             f"enable_credit={data.get('enable_credit')} total={total} "
             f"available={avail} unrealised_pnl={upnl} cross_margin_balance={cross_bal}")
        if cross_bal is not None and cross_bal > 0:
            return cross_bal
        if total is not None:
            return total + upnl
        return avail
    except Exception as e:
        olog(f"⚠ gate_equity: {_explain_error(e)}")
        return None

def _gate_cancel_orders(symbol):
    """Отменить все открытые ордера по символу — лимитные/рыночные и триггерные."""
    contract = symbol.replace("/", "_").upper()
    try:
        _gate_req("DELETE", "/futures/usdt/orders",
                  params={"contract": contract, "status": "open"})
    except Exception as e:
        olog(f"⚠ gate_cancel_orders (orders) {contract}: {_explain_error(e)}")
    try:
        _gate_req("DELETE", "/futures/usdt/price_orders",
                  params={"contract": contract, "status": "open"})
    except Exception as e:
        olog(f"⚠ gate_cancel_orders (price_orders) {contract}: {_explain_error(e)}")

def _gate_get_pnl_from_account_book(symbol, since_ts, until_ts=None, max_lookback_sec=3600):
    """Fallback, когда _gate_get_last_pnl_from_position_close не смог
    сматчить закрытие. Использует /futures/usdt/account_book — общий леджер
    счёта (изменения баланса: pnl/комиссии/фандинг) как ВТОРОЙ, независимый
    от /position_close источник результата закрытия."""
    contract = symbol.replace("/", "_").upper()
    until_ts = until_ts or (since_ts + max_lookback_sec)
    try:
        entries = _gate_req("GET", "/futures/usdt/account_book", params={
            "contract": contract,
            "from": int(since_ts) - 30,
            "to":   int(until_ts) + 30,
            "type": "pnl",
            "limit": 50,
        })
    except Exception as e:
        olog(f"[gate_pnl_fallback] {symbol}: account_book запрос не удался: {_explain_error(e)}")
        return None
    if not isinstance(entries, list) or not entries:
        return None
    matched = [e for e in entries if contract in str(e.get("text", ""))] or entries
    if not matched:
        return None
    total = 0.0
    for e in matched:
        try:
            total += float(e.get("change", 0))
        except (TypeError, ValueError):
            continue
    olog(f"[gate_pnl_fallback] {symbol}: {len(matched)} записей в account_book, "
         f"суммарный pnl={total:.4f} (использован как fallback — /position_close не дал результата)")
    return {"pnl": total, "pnl_pct": None, "close_price": None,
            "pnl_fee": None, "pnl_fund": None, "pnl_price": None,
            "source": "account_book_fallback"}

def _gate_get_last_pnl(symbol, max_age_sec=1800, fallback_since_ts=None):
    """Тонкая обёртка. Основная логика — в
    _gate_get_last_pnl_from_position_close. Если она вернула None и передан
    fallback_since_ts — пробуем _gate_get_pnl_from_account_book как второй
    источник, вместо того чтобы молча терять $-результат сделки."""
    result = _gate_get_last_pnl_from_position_close(symbol, max_age_sec)
    if result is not None:
        return result
    if fallback_since_ts:
        return _gate_get_pnl_from_account_book(symbol, fallback_since_ts)
    return None

def _gate_get_last_pnl_from_position_close(symbol, max_age_sec=1800):
    """Возвращает PnL последней закрытой позиции по символу (USDT).
    Проверяет rec["time"] — если запись закрытия старше max_age_sec от
    момента вызова, считаем её НЕ нашей и возвращаем None вместо того,
    чтобы приписать чужой PnL."""
    try:
        contract = symbol.replace("/", "_").upper()
        data = _gate_req("GET", "/futures/usdt/position_close",
                         params={"contract": contract, "limit": 1})
        if not isinstance(data, list) or not data:
            return None
        rec = data[0]
        rec_time = float(rec.get("time", 0) or 0)
        age = time.time() - rec_time
        if rec_time <= 0 or age > max_age_sec:
            olog(f"⚠ gate_get_last_pnl({symbol}): последняя запись закрытия "
                 f"старше {max_age_sec}с (age={age:.0f}с) — похоже на ЧУЖОЕ "
                 f"закрытие (старая ручная сделка/прошлая сессия), а не "
                 f"наше только что закрытое — игнорирую, PnL не приписываю")
            return None
        pnl       = float(rec.get("pnl",       0))
        pnl_fee   = float(rec.get("pnl_fee",   0) or 0)
        pnl_fund  = float(rec.get("pnl_fund",  0) or 0)
        pnl_price = float(rec.get("pnl_pnl",   0) or 0)
        olog(f"[gate_pnl] {symbol}: pnl={pnl:.4f} (price={pnl_price:.4f} "
             f"fee={pnl_fee:.4f} fund={pnl_fund:.4f} сумма_частей="
             f"{pnl_price+pnl_fee+pnl_fund:.4f})")
        side      = rec.get("side")
        long_px   = float(rec.get("long_price", 0) or 0)
        short_px  = float(rec.get("short_price", 0) or 0)
        if side == "long":
            entry_px, close_px = long_px, short_px
            pnl_pct = (close_px - entry_px) / entry_px * 100.0 if entry_px > 0 else 0.0
        else:
            entry_px, close_px = short_px, long_px
            pnl_pct = (entry_px - close_px) / entry_px * 100.0 if entry_px > 0 else 0.0
        return {"pnl": pnl, "pnl_pct": pnl_pct, "close_price": close_px,
                "pnl_fee": pnl_fee, "pnl_fund": pnl_fund, "pnl_price": pnl_price}
    except Exception as e:
        olog(f"⚠ gate_get_last_pnl: {_explain_error(e)}")
        return None


def _check_position_closed_and_alert(sym, our_pos_now):
    """Проверяет, не закрылась ли отслеживаемая позиция на бирже; если
    закрылась — шлёт алерт с реальным PnL."""
    try:
        still_open = _gate_get_position(sym)
    except Exception as e:
        olog(f"⚠ Не удалось проверить статус позиции {sym} ({_explain_error(e)}) — "
             f"считаем ещё открытой, проверим на следующем тике")
        return False
    if still_open:
        return False
    dirru = our_pos_now.get("dir", "?").upper()
    entry = our_pos_now.get("entry", 0)
    olog(f"✅ Позиция {dirru} закрыта на бирже (TP/SL или вручную)")
    pnl_info = _gate_get_last_pnl(sym)
    if pnl_info:
        pnl     = pnl_info["pnl"]
        pnl_pct = pnl_info["pnl_pct"]
        close_p = pnl_info["close_price"]
        sign    = "+" if pnl >= 0 else ""
        result_emoji = "✅" if pnl >= 0 else "❌"
        _send_alert(
            f"{result_emoji} <b>{sym}</b> — позиция {dirru} закрыта\n"
            f"Entry {_fmt_px(entry)} → Close {_fmt_px(close_p)}\n"
            f"PnL: <b>{sign}{pnl:.2f} USDT ({sign}{pnl_pct:.2f}%)</b>"
        )
    else:
        _send_alert(
            f"{'🟢' if dirru=='LONG' else '🔴'} <b>{sym}</b> — "
            f"позиция {dirru} закрыта\n"
            f"TP/SL сработал или закрыта вручную. Жду следующего сигнала."
        )
    return True

def _gate_close_position(symbol):
    """Закрыть позицию рыночным ордером."""
    try:
        contract = symbol.replace("/", "_").upper()
        pos = _gate_get_position(contract)
        if not pos: return
        _gate_cancel_orders(contract)
        close_size = -int(pos["size"]) if pos["dir"] == "long" else int(pos["size"])
        _gate_req("POST", "/futures/usdt/orders", body={
            "contract":    contract,
            "size":        close_size,
            "price":       "0",
            "tif":         "ioc",
            "reduce_only": True,
            "text":        "t-smc-close",
        })
        olog(f"📤 Позиция закрыта: {contract} {pos['dir']}")
    except Exception as e:
        olog(f"⚠ gate_close_position {symbol}: {_explain_error(e)}")

def _gate_round_price(price, contract):
    """Округляет цену TP/SL до реального шага цены (order_price_round)
    контракта на Gate.io."""
    try:
        info = _gate_get_contract_info(contract)
        tick_str = info.get("order_price_round", "0.01")
        tick = float(tick_str)
        decimals = len(tick_str.split(".")[1]) if "." in tick_str else 0
        rounded = round(round(price / tick) * tick, decimals)
        return f"{rounded:.{decimals}f}"
    except Exception as e:
        olog(f"⚠ _gate_round_price: не удалось получить шаг цены для {contract} "
             f"({_explain_error(e)}) — использую грубое округление по диапазону цены")
        if price > 1000:  return f"{price:.1f}"
        if price > 10:    return f"{price:.2f}"
        if price > 1:     return f"{price:.4f}"
        return f"{price:.6f}"

SL_SLIPPAGE_BUFFER_PCT = 0.3

def _gate_place_protective_orders(symbol, direction, close_size, sl_px, tp_px, text_prefix="smc"):
    """Выставляет TP/SL price_orders (reduce_only) для уже открытой позиции."""
    contract = symbol.replace("/", "_").upper()
    is_long = (direction == "long")
    _gate_req("POST", "/futures/usdt/price_orders", body={
        "initial": {
            "contract":    contract,
            "size":        close_size,
            "price":       "0",
            "tif":         "ioc",
            "reduce_only": True,
            "text":        f"t-{text_prefix}-tp",
        },
        "trigger": {
            "strategy_type": 0,
            "price_type":    0,
            "price":         _gate_round_price(tp_px, contract),
            "rule":          1 if is_long else 2,
            "expiration":    86400,
        },
    })
    sl_limit_px = sl_px * (1 - SL_SLIPPAGE_BUFFER_PCT/100) if is_long else sl_px * (1 + SL_SLIPPAGE_BUFFER_PCT/100)
    _gate_req("POST", "/futures/usdt/price_orders", body={
        "initial": {
            "contract":    contract,
            "size":        close_size,
            "price":       _gate_round_price(sl_limit_px, contract),
            "tif":         "gtc",
            "reduce_only": True,
            "text":        f"t-{text_prefix}-sl",
        },
        "trigger": {
            "strategy_type": 0,
            "price_type":    0,
            "price":         _gate_round_price(sl_px, contract),
            "rule":          2 if is_long else 1,
            "expiration":    86400,
        },
    })

def _gate_has_protective_orders(symbol, text_prefix):
    """Смотрит открытые price_orders по контракту и проверяет, есть ли
    среди них НАШИ TP/SL (по тегу t-{prefix}-tp/-sl)."""
    contract = symbol.replace("/", "_").upper()
    try:
        r = _gate_req("GET", "/futures/usdt/price_orders",
                       params={"contract": contract, "status": "open"})
        orders = r if isinstance(r, list) else []
    except Exception as e:
        olog(f"⚠ _gate_has_protective_orders {symbol}: {_explain_error(e)}")
        return None
    tags = {(o.get("initial") or {}).get("text", "") for o in orders}
    return (f"t-{text_prefix}-tp" in tags, f"t-{text_prefix}-sl" in tags)

def _ema_rearm_missing_protection():
    """Watchdog — для каждого live-сигнала, у которого позиция на бирже
    РЕАЛЬНО ещё открыта, проверяет, что там же висят наши TP/SL. Если нет —
    позиция голая; перевыставляет TP/SL по сохранённым в истории sl/tp."""
    with _ema_history_lock:
        state = _load_ema_history()
    live_open = [(k, v) for k, v in state["items"].items()
                 if v.get("status") == "open" and v.get("live")]
    for key, item in live_open:
        symbol = item["symbol"]
        try:
            pos = _gate_get_position(symbol.replace("/", "_").upper())
        except Exception:
            continue
        if not pos:
            continue
        check = _gate_has_protective_orders(symbol, "emainv")
        if check is None:
            continue
        has_tp, has_sl = check
        if has_tp and has_sl:
            continue
        olog(f"[ema_rearm] ⚠ {symbol}: позиция открыта, но TP/SL отсутствуют "
             f"(has_tp={has_tp} has_sl={has_sl}) — перевыставляю по item sl/tp")
        close_size = -int(pos["size"]) if pos["dir"] == "long" else int(pos["size"])
        try:
            _gate_place_protective_orders(symbol, pos["dir"], close_size,
                                           item["sl"], item["tp"], text_prefix="emainv")
            olog(f"[ema_rearm] ✓ {symbol}: TP/SL перевыставлены")
            _send_alert(f"🛡 <b>{symbol} EMA INVERT</b> — TP/SL отсутствовали на голой "
                        f"позиции, перевыставил автоматически "
                        f"(SL {_fmt_px(item['sl'])} / TP {_fmt_px(item['tp'])})")
        except Exception as e:
            olog(f"[ema_rearm] 🚨 {symbol}: не смог перевыставить TP/SL: {_explain_error(e)}")
            _send_alert(f"🚨 <b>{symbol} EMA INVERT</b> — позиция ГОЛАЯ (без TP/SL), "
                        f"автоперевыставление не удалось: {_explain_error(e)}\nВЫСТАВЬ ВРУЧНУЮ!")

EMA_INVERT_TIME_STOP_BARS = {"1m": 6, "5m": 6, "15m": 5, "1h": 3}
EMA_INVERT_TIME_STOP_DEFAULT_BARS = 5
EMA_INVERT_SAFETY_RR = 1.1
EMA_INVERT_MAX_VOL_RATIO = 1.5
EMA_INVERT_REJECT_CONFIRMED_PATTERN = True
_ema_invert_filter_stats = {"vol_ratio": 0, "candle_pattern": 0}

def _ema_invert_time_limit_sec(tf):
    bars = EMA_INVERT_TIME_STOP_BARS.get(tf, EMA_INVERT_TIME_STOP_BARS.get(
        "1h" if tf in ("4h", "1d", "1w") else tf, EMA_INVERT_TIME_STOP_DEFAULT_BARS))
    return bars * TF_SECONDS.get(tf, 3600)

def _ema_invert_timestop_watchdog():
    """Раз в цикл проверяет все реально живые сигналы этого форка — если с
    момента открытия прошло больше time_limit_sec, закрывает позицию по
    рынку и сам помечает историю status='time_stop'."""
    with _ema_history_lock:
        state = _load_ema_history()
    live_open = [(k, v) for k, v in state["items"].items()
                 if v.get("status") == "open" and v.get("live")]
    now = time.time()
    for key, item in live_open:
        symbol = item["symbol"]
        opened_at = item.get("opened_at")
        limit_sec = item.get("time_limit_sec") or _ema_invert_time_limit_sec(item["tf"])
        if opened_at is None or now - opened_at < limit_sec:
            continue
        contract = symbol.replace("/", "_").upper()
        try:
            pos = _gate_get_position(contract)
        except Exception as e:
            olog(f"[ema_invert_timestop] {symbol}: не смог проверить биржу ({_explain_error(e)}) — пропуск")
            continue
        if not pos:
            continue
        live_price = _gate_get_price(symbol)
        try:
            _gate_close_position(symbol)
        except Exception as e:
            olog(f"[ema_invert_timestop] 🚨 {symbol}: не удалось закрыть по времени: {_explain_error(e)}")
            _ema_event_log_write("timestop_close_failed", symbol=symbol, error=str(e))
            continue
        held_sec = now - opened_at
        olog(f"[ema_invert_timestop] ✓ {symbol}: время вышло ({held_sec:.0f}с "
             f">= {limit_sec:.0f}с лимита) — закрыто по рынку")
        pnl_info = None
        try:
            pnl_info = _gate_get_last_pnl(symbol, fallback_since_ts=opened_at)
        except Exception as e:
            olog(f"[ema_invert_timestop] {symbol}: не смог получить PnL закрытия ({_explain_error(e)})")
        _ema_event_log_write("timestop_closed", symbol=symbol, held_sec=round(held_sec, 1),
                              limit_sec=limit_sec, tf=item["tf"],
                              pnl=pnl_info["pnl"] if pnl_info else None)
        _send_alert(f"⏱ <b>{symbol} EMA INVERT</b> — вышло время ({held_sec:.0f}с), "
                    f"закрыто по рынку")
        with _ema_history_lock:
            state2 = _load_ema_history()
            it2 = state2["items"].get(key)
            if it2:
                it2["status"] = "time_stop"
                it2["closed_at"] = int(now)
                it2["close_price"] = (pnl_info["close_price"] if pnl_info else live_price) or it2.get("price")
                it2["closed_externally"] = False
                if pnl_info:
                    it2["live_pnl"]     = pnl_info["pnl"]
                    it2["live_pnl_pct"] = pnl_info["pnl_pct"]
                    it2["live_pnl_fee"]   = pnl_info.get("pnl_fee")
                    it2["live_pnl_fund"]  = pnl_info.get("pnl_fund")
                    it2["live_pnl_price"] = pnl_info.get("pnl_price")
                it2["diag_status"] = "pending"
                _save_ema_history(state2)
                _ema_invert_diag_schedule(it2)

EMA_INVERT_DIAG_FILE = os.path.expanduser("~/pumpradar_invert_diagnostics.jsonl")
EMA_INVERT_DIAG_WAIT_BARS = 10
_ema_invert_diag_lock = threading.Lock()

def _ema_invert_diag_log_write(record):
    try:
        with _ema_invert_diag_lock:
            with open(EMA_INVERT_DIAG_FILE, "a") as f:
                f.write(json.dumps(record, ensure_ascii=False) + "\n")
    except Exception as e:
        olog(f"[ema_invert_diag] ошибка записи лога: {_explain_error(e)}")

def _ema_invert_diag_schedule(item):
    item["diag_status"] = "pending"

def _ema_invert_diagnose_one(item):
    """Разбирает одну ЗАКРЫТУЮ (любым исходом) сделку инвертированной
    логики."""
    symbol, tf, ema_period = item["symbol"], item["tf"], item["ema_period"]
    direction = item["dir"]
    entry, tp, safety_sl = item["price"], item["tp"], item.get("sl")
    exit_reason = item["status"]
    exit_price  = item.get("close_price")
    opened_at, closed_at = item["opened_at"], item["closed_at"]
    fetch_tf = "1d" if tf == "1w" else tf
    span_sec = max(1, int(time.time()) - closed_at)
    days = math.ceil((int(time.time()) - opened_at) / 86400) + EMA_DIAG_LOOKBACK_PAD
    raw = _fetch_candles(symbol, fetch_tf, days)
    candles = _resample_to_weekly(raw) if tf == "1w" else raw
    if not candles:
        return None
    during = [c for c in candles if opened_at <= c["t"] < closed_at]
    after  = [c for c in candles if c["t"] >= closed_at]
    tf_sec = TF_SECONDS.get(fetch_tf, 3600)
    if len(after) < EMA_INVERT_DIAG_WAIT_BARS and \
       (int(time.time()) - closed_at) < EMA_INVERT_DIAG_WAIT_BARS * tf_sec:
        return None
    risk_tp = abs(entry - tp)
    mfe_toward_tp = 0.0
    if during:
        if direction == "long":
            mfe_toward_tp = max([c["high"] for c in during], default=entry) - entry
        else:
            mfe_toward_tp = entry - min([c["low"] for c in during], default=entry)
    mfe_r = round(mfe_toward_tp / risk_tp, 3) if risk_tp else None
    post_would_hit_tp = None
    if exit_reason == "time_stop" and after:
        window = after[:EMA_INVERT_DIAG_WAIT_BARS]
        if direction == "long":
            post_would_hit_tp = max(c["high"] for c in window) >= tp
        else:
            post_would_hit_tp = min(c["low"] for c in window) <= tp
    held_sec = closed_at - opened_at
    bars_held = round(held_sec / tf_sec, 2) if tf_sec else None
    pnl_pct = None
    if exit_price and entry:
        raw_pnl = (exit_price - entry) / entry if direction == "long" else (entry - exit_price) / entry
        pnl_pct = round(raw_pnl * 100.0, 4)
    record = {
        "ts": int(time.time()), "symbol": symbol, "tf": tf,
        "ema_period": ema_period, "dir": direction,
        "entry": entry, "tp": tp, "safety_sl": safety_sl,
        "exit_reason": exit_reason, "exit_price": exit_price,
        "held_sec": round(held_sec, 1), "bars_held": bars_held,
        "time_limit_sec": item.get("time_limit_sec") or _ema_invert_time_limit_sec(tf),
        "mfe_r_to_tp": mfe_r,
        "post_timestop_would_hit_tp": post_would_hit_tp,
        "pnl_pct": pnl_pct,
        "dist_atr_at_entry": item.get("dist_atr"), "ladder_at_entry": item.get("ladder"),
        "rsi_at_entry": item.get("rsi"), "adx_at_entry": item.get("adx"),
        "plus_di_at_entry": item.get("plus_di"), "minus_di_at_entry": item.get("minus_di"),
        "vol_ratio_at_entry": item.get("vol_ratio"),
        "atr_percentile_at_entry": item.get("atr_percentile"),
        "ribbon_spread_atr_at_entry": item.get("ribbon_spread_atr"),
        "candle_pattern_at_entry": item.get("candle_pattern"),
        "session_at_entry": item.get("session"),
        "htf_trend_at_entry": item.get("htf_trend"),
    }
    return record

def _ema_invert_run_diagnostics():
    """Аналог _ema_run_diagnostics для ЛЮБОГО исхода (tp/sl/time_stop)
    закрытых сделок этого форка."""
    with _ema_history_lock:
        state = _load_ema_history()
    pending = [(k, v) for k, v in state["items"].items()
               if v.get("status") in ("tp", "sl", "time_stop")
               and v.get("diag_status") == "pending"]
    if not pending:
        return
    done = {}
    for key, item in pending:
        try:
            record = _ema_invert_diagnose_one(item)
        except Exception as e:
            olog(f"[ema_invert_diag] {item.get('symbol')} ошибка разбора: {_explain_error(e)}")
            continue
        if record is None:
            continue
        _ema_invert_diag_log_write(record)
        olog(f"[ema_invert_diag] {record['symbol']} {record['tf']} EMA{record['ema_period']} "
             f"{record['dir']} closed={record['exit_reason']} held={record['bars_held']}bar "
             f"pnl={record['pnl_pct']}%")
        item["diag_status"] = "done"
        done[key] = item
    if done:
        with _ema_history_lock:
            state2 = _load_ema_history()
            for key, item in done.items():
                state2["items"][key] = item
            _save_ema_history(state2)

def _gate_open_position(symbol, direction, entry_px, sl_px, tp_px, risk_pct, **kwargs):
    """
    Полный цикл открытия позиции:
      leverage = round(risk_pct / sl_pct)
      margin   = balance * position_pct%
      size     = (margin * leverage) / (entry_px * qm)
      TP/SL    — price_orders с price="0" (маркет при триггере)
    """
    try:
        sl_pct_val   = abs(entry_px - sl_px) / entry_px * 100.0
        position_pct = kwargs.get("position_pct", risk_pct)
        label       = kwargs.get("label", "SMC AUTO")
        text_prefix = kwargs.get("text_prefix", "smc")

        balance = _gate_get_balance()
        if not balance or balance <= 0:
            raise RuntimeError(f"Нет баланса: {balance}")

        leverage_raw = round(risk_pct / sl_pct_val)
        leverage = max(1, min(MAX_LEVERAGE, leverage_raw))
        if leverage_raw > MAX_LEVERAGE:
            olog(f"⚠ [{symbol}] расчётное плечо {leverage_raw}× превышает "
                 f"MAX_LEVERAGE={MAX_LEVERAGE} (risk_pct={risk_pct}%, "
                 f"sl_pct={sl_pct_val:.4f}%) — ограничено до {leverage}×")

        applied_leverage = leverage
        contract = symbol.replace("/", "_").upper()
        try:
            r = _gate_req("POST", f"/futures/usdt/positions/{contract}/leverage",
                          params={"leverage": str(leverage)})
            applied_leverage = int(r.get("leverage", leverage)) if isinstance(r, dict) else leverage
            olog(f"✓ Плечо: {applied_leverage}×")
        except Exception as e:
            olog(f"⚠ Плечо попытка 1: {_explain_error(e)} — повтор через 1с")
            time.sleep(1.0)
            try:
                r = _gate_req("POST", f"/futures/usdt/positions/{contract}/leverage",
                              params={"leverage": str(leverage)})
                applied_leverage = int(r.get("leverage", leverage)) if isinstance(r, dict) else leverage
                olog(f"✓ Плечо (попытка 2): {applied_leverage}×")
            except Exception as e2:
                olog(f"⚠ Плечо не применено: {_explain_error(e2)} — используем leverage=1")
                applied_leverage = 1

        qm = _gate_get_quanto(symbol)
        margin   = balance * (position_pct / 100.0)
        size_raw = (margin * applied_leverage) / (entry_px * qm)
        size     = round(size_raw)
        forced_min_size = False
        if size < 1:
            forced_size_max_multiple = kwargs.get("forced_size_max_multiple", 3.0)
            max_forced_margin_pct = kwargs.get("max_forced_margin_pct", 20.0)
            margin_for_one = (entry_px * qm) / applied_leverage
            margin_cap = balance * (max_forced_margin_pct / 100.0)
            multiple = margin_for_one / margin if margin > 0 else float("inf")
            if margin_for_one <= margin_cap and multiple <= forced_size_max_multiple:
                size = 1
                forced_min_size = True
                olog(f"[ema_auto_trade] {symbol}: size_raw={size_raw:.3f}<1 при "
                     f"номинальной марже {margin:.2f}U — форсирую size=1 "
                     f"(нужно {margin_for_one:.2f}U маржи = {multiple:.1f}× номинала, потолок "
                     f"{margin_cap:.2f}U={max_forced_margin_pct}% депо, баланс {balance:.2f}U)")
            else:
                reason = (f"{margin_for_one:.2f}U > потолка {margin_cap:.2f}U ({max_forced_margin_pct}% депо)"
                          if margin_for_one > margin_cap else
                          f"нужно {multiple:.1f}× номинальной маржи > лимита {forced_size_max_multiple}×")
                raise RuntimeError(
                    f"Недостаточно средств: balance={balance:.2f} margin={margin:.2f} "
                    f"lev={applied_leverage}× ep={entry_px} qm={qm} → size={size_raw:.3f} < 1, "
                    f"даже 1 контракт требует {margin_for_one:.2f}U маржи — {reason} — пропуск"
                )
        notional = size * entry_px * qm
        olog(f"🔓 Открываем {direction.upper()} {symbol}: "
             f"balance={balance:.2f} × {position_pct}%(маржа) × {applied_leverage}×(плечо) → позиция~{notional:.2f}U | "
             f"риск={risk_pct}% | size={size} контр{' [форс-минимум]' if forced_min_size else ''} | "
             f"entry≈{_fmt_px(entry_px)} SL={_fmt_px(sl_px)} TP={_fmt_px(tp_px)}")

        _gate_cancel_orders(symbol)

        is_long = (direction == "long")
        try:
            _gate_req("POST", "/futures/usdt/orders", body={
                "contract": contract,
                "size":     size if is_long else -size,
                "price":    "0",
                "tif":      "ioc",
                "text":     f"t-{text_prefix}-open",
            }, _retries=1)
        except (requests.exceptions.ConnectionError,
                requests.exceptions.Timeout,
                requests.exceptions.ReadTimeout) as _e_net:
            olog(f"⚠ Таймаут/сеть при отправке ордера на вход ({_e_net}) — "
                 f"проверяю на бирже, прошёл ли ордер, прежде чем считать "
                 f"попытку неудачной (чтобы не задвоить позицию повтором)")
            time.sleep(2.0)
            try:
                _check = _gate_get_position(contract)
            except Exception:
                _check = None
            if not _check:
                raise
            olog(f"✓ Ордер на вход всё же исполнился на бирже несмотря на "
                 f"таймаут ответа — продолжаю как при успехе, повтор не нужен")
        time.sleep(1.0)

        close_size = -size if is_long else size
        pos_info   = {"dir": direction, "entry": entry_px, "sl": sl_px, "tp": tp_px,
                      "size": size, "leverage": applied_leverage, "notional": round(notional, 2)}
        tp_sl_ok   = False

        def _place_tp_sl():
            _gate_place_protective_orders(symbol, direction, close_size, sl_px, tp_px, text_prefix)

        try:
            _place_tp_sl()
            tp_sl_ok = True
        except Exception as e_tp:
            olog(f"⚠ TP/SL попытка 1 упала: {_explain_error(e_tp)} — повтор через 2с")
            time.sleep(2.0)
            try:
                _place_tp_sl()
                tp_sl_ok = True
                olog("✓ TP/SL выставлены со второй попытки")
            except Exception as e_tp2:
                olog(f"🚨 TP/SL НЕ выставлены после 2 попыток: {_explain_error(e_tp2)}")
                _send_alert(
                    f"🚨 <b>{symbol}</b> — позиция открыта, но TP/SL НЕ выставлены!\n"
                    f"Причина: {_explain_error(e_tp2)}\nВыставь стоп вручную! "
                    f"TP={_fmt_px(tp_px)} SL={_fmt_px(sl_px)}"
                )

        emoji = "🟢" if is_long else "🔴"
        status = "✅ открыт + TP/SL выставлены" if tp_sl_ok else "⚠️ открыт, TP/SL — см. выше"
        olog(f"{status}: {symbol} {direction.upper()} [{label}]")
        _send_alert(
            f"{emoji} <b>{symbol} {label}</b> — {direction.upper()}\n"
            f"Entry ≈ {_fmt_px(entry_px)} | TP {_fmt_px(tp_px)} | SL {_fmt_px(sl_px)}\n"
            f"Size {size} контр | ~{notional:.1f}U | {applied_leverage}×плечо"
            + ("" if tp_sl_ok else "\n⚠️ TP/SL не выставлены — проверь вручную!")
        )
        return pos_info
    except Exception as e:
        olog(f"⚠ gate_open_position {symbol}: {_explain_error(e)}")
        _send_alert(f"🚨 <b>{symbol}</b> — ошибка открытия позиции:\n{_explain_error(e)}")
        return None

def _load_gate_cfg():
    global GATE_KEY, GATE_SECRET
    try:
        with open(GATE_CFG_PATH) as f:
            cfg = json.load(f)
        GATE_KEY    = cfg.get("gate_key",    GATE_KEY)    or GATE_KEY
        GATE_SECRET = cfg.get("gate_secret", GATE_SECRET) or GATE_SECRET
    except: pass

def _save_gate_cfg():
    try:
        with open(GATE_CFG_PATH, "w") as f:
            json.dump({"gate_key": GATE_KEY, "gate_secret": GATE_SECRET}, f)
    except Exception as e:
        olog(f"⚠ Не удалось сохранить gate cfg: {_explain_error(e)}")

def _ts():
    return time.strftime("[%H:%M:%S]")

LOG_FILE = os.path.expanduser("~/pumpradar.log")
LOG_FILE_MAX_BYTES = 5 * 1024 * 1024   # v0.30.95: по прямому запросу — лог на диске не должен расти бесконечно (места на телефоне ограничено)
LOG_FILE_TRIM_TARGET_BYTES = int(LOG_FILE_MAX_BYTES * 0.8)   # обрезаем не ровно до лимита, а с запасом — иначе обрезка запускалась бы почти на каждой следующей строке


def _log_file_write(line):
    """Дописывает строку в постоянный лог-файл на диске (в дополнение к
    in-memory логу для веб-страницы — тот при рестарте процесса теряется,
    этот переживает). Если файл перерос LOG_FILE_MAX_BYTES — обрезается
    СНИЗУ (отбрасывается старая часть), остаётся хвост ~80% от лимита, не
    ровно по границе. Ошибка записи лога не должна ронять процесс —
    любое исключение здесь молча проглатывается."""
    try:
        with open(LOG_FILE, "a", encoding="utf-8") as f:
            f.write(line + "\n")
        try:
            size = os.path.getsize(LOG_FILE)
        except OSError:
            return
        if size > LOG_FILE_MAX_BYTES:
            with open(LOG_FILE, "rb") as f:
                f.seek(-LOG_FILE_TRIM_TARGET_BYTES, os.SEEK_END)
                tail = f.read()
            nl = tail.find(b"\n")   # не начинать файл с обрубленной посередине строки
            if nl != -1:
                tail = tail[nl + 1:]
            tmp_path = LOG_FILE + f".tmp{os.getpid()}"
            with open(tmp_path, "wb") as f:
                f.write(tail)
            os.replace(tmp_path, LOG_FILE)
    except Exception:
        pass


def olog(msg):
    with _log_lock:
        ts = time.strftime("%H:%M:%S")
        opt_state["logs"].append({"ts": ts, "msg": msg})
        if len(opt_state["logs"]) > 500:
            opt_state["logs"] = opt_state["logs"][-300:]
            opt_state["logs_dropped"] = opt_state.get("logs_dropped",0) + 200
        _log_file_write(f"[{ts}] {msg}")

def _explain_error(e):
    """v0.30.0: по прямому запросу — раньше в логах текст самого исключения
    вставлялся как есть (просто {e} в f-строке), а Python формирует его на английском
    (ConnectionError, Timeout, JSONDecodeError...), даже когда весь
    остальной текст лога уже на русском — непонятно, в чём проблема, не
    зная английских терминов. Переводит частые типы ошибок в понятную
    причину на русском, но НЕ теряет техническую деталь — она остаётся в
    скобках, пригодится, если понадобится показать мне для разбора."""
    name = type(e).__name__
    text = str(e).strip()
    low = (name + " " + text).lower()
    if "connect" in low or "resolve" in low or "dns" in low:
        hint = "нет связи с интернетом или сервер недоступен"
    elif "timeout" in low or "timed out" in low:
        hint = "сервер не ответил вовремя (медленная/нестабильная связь)"
    elif "jsondecodeerror" in low or "expecting value" in low:
        hint = "получен нечитаемый ответ вместо данных (не JSON — часто бывает при обрыве связи)"
    elif isinstance(e, KeyError):
        hint = "в ответе от биржи не хватает ожидаемого поля"
    elif isinstance(e, IndexError):
        hint = "данных пришло меньше, чем ожидалось"
    elif isinstance(e, TypeError):
        hint = "получено пустое значение там, где ожидалось число"
    elif isinstance(e, ValueError):
        hint = "не удалось распознать полученное значение"
    elif isinstance(e, FileNotFoundError):
        hint = "файл не найден на устройстве"
    elif isinstance(e, PermissionError):
        hint = "нет прав на чтение/запись файла"
    elif isinstance(e, OSError):
        hint = "ошибка сети или системы"
    else:
        hint = "непредвиденная ошибка"
    detail = f"{name}: {text[:150]}" if text else name
    return f"{hint} ({detail})"

def _strip_html_for_ntfy(text):
    """v0.30.11: наши тексты алертов сделаны под Telegram (parse_mode=HTML,
    теги <b>...</b> для жирного) — ntfy такую разметку не понимает и
    просто показывает теги буквально ("<b>SYMBOL</b>"). Убирает базовые
    теги перед отправкой в ntfy, чтобы текст читался нормально."""
    return re.sub(r"</?(?:b|i|u|s|code|pre|a)(?:\s[^>]*)?>", "", text)

# ─── Gate.io fetch ──────────────────────────────────────────────────────────
# v0.30.1: MAX_SPREAD_PCT/MIN_VOLUME_24H_USD как захардкоженные константы
# убраны — те же значения (0.15%, $3,000,000) теперь дефолты в
# _scan_settings, настраиваемые через /scan_settings без правки кода.

# v0.30.30: по прямому запросу — Gate.io с 2025 предлагает отдельный
# раздел "xStocks" (токенизированные акции/ETF: MicroStrategy, Circle,
# Apple, Tesla и т.п. под тикерами вида MSTRX/CRCLX/AAPLX) — это НЕ
# крипта, а совсем другой класс актива, которого физически нет на
# Bybit/MEXC (чисто крипто-биржах). Сравнение сигналов с внешним
# крипто-ботом эти монеты только зашумляли — он их никогда не увидит,
# сколько бы наши критерии ни совпадали. Список может расти со временем
# (Gate добавляет новые), это стартовый набор по официальным анонсам.
GATE_NON_CRYPTO_SYMBOLS = {
    "AAPLX_USDT", "GOOGLX_USDT", "TSLAX_USDT", "AMZNX_USDT", "MSTRX_USDT",
    "COINX_USDT", "NVDAX_USDT", "CRCLX_USDT", "METAX_USDT", "HOODX_USDT",
    "DFDVX_USDT", "QQQX_USDT", "SPYX_USDT",
    # v0.30.32: реальный пробел — TQQQX (плечевой ETF ProShares UltraPro
    # QQQ) увиден на живом скрине, не был в списке. SOXL (плечевой ETF на
    # полупроводники) был упомянут в тексте версии v0.30.30 как
    # "исключённый", но по факту забыт в самом множестве — тоже добавлен
    # только сейчас.
    "TQQQX_USDT", "SOXL_USDT",
    # v0.30.57: реальный пробел #3 — по прямому запросу "нужны пары
    # только по криптовалютам", найдены живьём в данных "Отрыв от EMA":
    # XAUT_USDT (Tether Gold — токенизированное золото, не крипто-актив
    # по сути) и SKHYNIX_USDT/SKHY_USDT (акция SK Hynix, похоже видны ОБА
    # варианта тикера одной и той же бумаги). У Gate.io нет отдельного
    # API-поля "это акция/товар" — весь список строится ТОЛЬКО на ручном
    # пополнении по факту находки, так что гарантии полноты нет: если
    # увидишь ещё что-то нерыночное (акции, ETF, товары) — присылай
    # тикер, добавлю сюда же.
    "XAUT_USDT", "SKHYNIX_USDT", "SKHY_USDT",
}
GATE_XSTOCKS_SYMBOLS = GATE_NON_CRYPTO_SYMBOLS   # обратная совместимость имени, ничего не переименовываю в местах использования без необходимости


def _fetch_all_symbols(min_volume_usd_override=None, max_volume_usd_override=None):
    """v0.30.1: ПОЛНАЯ ПЕРЕРАБОТКА — раньше здесь было ДВА зашитых прямо в
    код потолка (candidates = valid[:150], top50 = filtered[:100]) —
    настройка top_n к ним вообще не имела отношения, сколько её ни
    увеличивай, больше 100 монет отсюда никогда не выходило. Теперь:
    универсум — ВСЕ фьючерсы USDT на бирже (~700 пар, без обрезки на
    входе), фильтр — ПОРОГ ликвидности (объём/спред, настраиваемые через
    /scan_settings — защита от проскальзывания на неликвиде), не ранг
    "топ-N". Сколько монет реально пройдёт порог, столько и возвращаем —
    может быть и больше 100. max_symbols_cap применяется ТОЛЬКО как
    крайняя защита от перегрузки (дефолт 800, заведомо выше размера всей
    биржи, обычно не срабатывает вообще).
    v0.30.29: min_volume_usd_override — по прямому запросу, для
    slow_impulse (вход лимиткой на EMA-уровне, не срочный рыночный ордер —
    риск проскальзывания на неликвиде меньше, чем у пампа/дампа) — свой,
    более низкий порог, не трогая общий /scan_settings, используемый
    быстрыми детекторами.
    v0.30.43: max_volume_usd_override — по разбору 20 его сигналов
    (manual_scans) — максимальный объём среди ВСЕХ них $17.5М (B_USDT), у
    остальных 19 сильно ниже. slow_impulse без потолка стал стрелять по
    BTC/ETH/SOL/DOGE (объём — миллиарды) после добавления гейта по
    аномалии объёма (v0.30.42) — крупные монеты тоже иногда получают
    z>=4.0 на общерыночных распродажах, но это не то, что ловит референс-
    бот вообще ни разу за все собранные сигналы."""
    try:
        r = requests.get(f"{GATE_API}/futures/usdt/tickers", timeout=20)
        if r.status_code != 200: return []
        data = r.json()
        if not isinstance(data, list): return []
        valid = [t for t in data
                 if isinstance(t, dict) and "_USDT" in t.get("contract","")
                 and t.get("contract") not in GATE_XSTOCKS_SYMBOLS]
        valid.sort(key=lambda t: float(t.get("volume_24h_usd") or t.get("volume_24h_quote") or t.get("volume_24h") or 0), reverse=True)
        min_volume_usd, max_spread_pct = _get_liquidity_thresholds()
        if min_volume_usd_override is not None:
            min_volume_usd = min_volume_usd_override
        filtered = []
        skipped_spread = []
        skipped_volume = []
        for t in valid:
            try:
                vol24 = float(t.get("volume_24h_usd") or t.get("volume_24h_quote") or t.get("volume_24h") or 0)
                if vol24 < min_volume_usd:
                    skipped_volume.append(f"{t.get('contract')}(${vol24:,.0f})")
                    continue
                if max_volume_usd_override is not None and vol24 > max_volume_usd_override:
                    continue
                bid = float(t.get("highest_bid") or 0)
                ask = float(t.get("lowest_ask") or 0)
                if bid <= 0 or ask <= 0 or ask < bid:
                    continue
                spread_pct = (ask - bid) / ((ask + bid) / 2) * 100
                if spread_pct <= max_spread_pct:
                    filtered.append(t)
                else:
                    skipped_spread.append(f"{t['contract']}({spread_pct:.2f}%)")
            except (TypeError, ValueError):
                continue
        cap = _get_scan_top_n()   # max_symbols_cap — крайняя защита, не основной фильтр
        result = [t["contract"] for t in filtered[:cap]]
        olog(f"Ликвидных монет (объём ≥${min_volume_usd:,.0f}, спред ≤{max_spread_pct}%): "
             f"{len(result)} из {len(valid)} проверенных пар"
             + (f" (обрезано страховочным потолком {cap})" if len(filtered) > cap else ""))
        if skipped_spread:
            olog(f"⚠ Отсеяно по широкому спреду ({len(skipped_spread)}): {', '.join(skipped_spread[:15])}"
                 + (" ..." if len(skipped_spread) > 15 else ""))
        if skipped_volume:
            olog(f"⚠ Отсеяно по низкому объёму <${min_volume_usd:,.0f} ({len(skipped_volume)}): "
                 f"{', '.join(skipped_volume[:15])}" + (" ..." if len(skipped_volume) > 15 else ""))
        return result
    except Exception as e:
        olog(f"fetch_all_symbols error: {_explain_error(e)}")
        return []

def _fetch_candles(symbol, tf, days, _stop_event=None, offset_days=0):
    interval_sec = TF_SECONDS.get(tf, 3600)
    # v0.30.17: реальный баг — int() применялся ДО вычитания offset_days,
    # а не после. Если offset_days дробный (наш новый backfill передаёт
    # именно такой — "на столько-то дней в прошлое, не ровно сутками"),
    # результат снова становился float, и биржа отвечала 400
    # INVALID_PARAM_VALUE на дробный `from`. int() теперь оборачивает всё
    # выражение целиком.
    now   = int(time.time() - offset_days * 86400)
    since = now - days * 86400
    LIMIT = 999
    all_candles = []
    current_from = int(since)   # v0.30.77: реальная находка — since могло быть float при дробном days (мой новый _stretch_recent_1m_high_low передаёт days=0.015) — Gate.io отклоняет дробный `from`, та же болезнь, что уже чинили под offset_days в v0.30.17, просто с другой стороны
    fail_count = 0
    MAX_FAILS = 5
    while current_from < now:
        if _stop_event and _stop_event.is_set(): return []
        try:
            r = requests.get(f"{GATE_API}/futures/usdt/candlesticks",
                params={"contract":symbol,"interval":tf,"from":current_from,"limit":LIMIT},
                timeout=20)
            if r.status_code != 200:
                fail_count += 1
                olog(f"⚠ Gate.io {r.status_code} для {symbol}: {r.text[:200]}")
                if fail_count >= MAX_FAILS:
                    olog(f"❌ {symbol}: {fail_count} ошибок подряд — контракт не существует "
                         f"или недоступен на Gate.io Futures, прерываю загрузку")
                    break
                sleep_t = 1 if _stop_event else 5
                if _stop_event:
                    _stop_event.wait(timeout=sleep_t)
                else:
                    time.sleep(sleep_t)
                continue
            fail_count = 0
            raw = r.json()
            if not raw: break
            batch = []
            for c in raw:
                t = int(c.get("t",0))
                batch.append({
                    "t": t, "open": float(c.get("o",0)),
                    "high": float(c.get("h",0)), "low": float(c.get("l",0)),
                    "close": float(c.get("c",0)), "vol": float(c.get("v",0))
                })
            if not batch: break
            all_candles.extend(batch)
            last_t = batch[-1]["t"]
            if last_t >= now - interval_sec: break
            current_from = last_t + interval_sec
            if _stop_event:
                if _stop_event.is_set(): return []
                _stop_event.wait(timeout=0.12)
            else:
                time.sleep(0.12)
        except Exception as e:
            fail_count += 1
            olog(f"fetch error: {_explain_error(e)}")
            if fail_count >= MAX_FAILS:
                olog(f"❌ {symbol}: {fail_count} ошибок подряд, прерываю загрузку")
                break
            sleep_t = 1 if _stop_event else 5
            if _stop_event:
                _stop_event.wait(timeout=sleep_t)
            else:
                time.sleep(sleep_t)
    seen = set()
    result = []
    for c in sorted(all_candles, key=lambda x: x["t"]):
        if c["t"] not in seen:
            seen.add(c["t"]); result.append(c)
    result = [c for c in result if c["t"] + interval_sec <= now]
    return result

# ─── Индикаторы ─────────────────────────────────────────────────────────────
def _ema(arr, period):
    result = [None]*len(arr)
    if len(arr) < period: return result
    k = 2.0/(period+1)
    s = sum(arr[:period])/period
    result[period-1] = s
    for i in range(period, len(arr)):
        s = arr[i]*k + s*(1-k)
        result[i] = s
    return result

def _sma(arr, period):
    """v0.30.22: по запросу — простое скользящее среднее, для сравнения с
    EMA у внешнего референс-бота (вдруг у него на самом деле не EMA)."""
    result = [None]*len(arr)
    if len(arr) < period: return result
    for i in range(period-1, len(arr)):
        result[i] = sum(arr[i-period+1:i+1]) / period
    return result

def _atr(candles, period=14):
    trs = []
    for i in range(1, len(candles)):
        h = candles[i]["high"]; l = candles[i]["low"]; pc = candles[i-1]["close"]
        trs.append(max(h-l, abs(h-pc), abs(l-pc)))
    result = [None]*len(candles)
    if len(trs) < period: return result
    s = sum(trs[:period])/period
    result[period] = s
    for i in range(period+1, len(candles)):
        s = (s*(period-1) + trs[i-1])/period
        result[i] = s
    return result

def _rsi(closes, period=14):
    n = len(closes)
    result = [None]*n
    if n < period + 1:
        return result
    gains  = [max(closes[i]-closes[i-1], 0.0) for i in range(1, n)]
    losses = [max(closes[i-1]-closes[i], 0.0) for i in range(1, n)]
    avg_gain = sum(gains[:period])/period
    avg_loss = sum(losses[:period])/period
    def _val(ag, al):
        if al == 0: return 100.0
        rs = ag/al
        return 100.0 - 100.0/(1.0+rs)
    result[period] = round(_val(avg_gain, avg_loss), 2)
    for i in range(period+1, n):
        avg_gain = (avg_gain*(period-1) + gains[i-1])/period
        avg_loss = (avg_loss*(period-1) + losses[i-1])/period
        result[i] = round(_val(avg_gain, avg_loss), 2)
    return result

def _adx(candles, period=14):
    n = len(candles)
    adx_arr, plus_di_arr, minus_di_arr = [None]*n, [None]*n, [None]*n
    if n < period*2 + 1:
        return adx_arr, plus_di_arr, minus_di_arr
    tr, pdm, mdm = [None]*n, [None]*n, [None]*n
    for i in range(1, n):
        h, l = candles[i]["high"], candles[i]["low"]
        ph, pl, pc = candles[i-1]["high"], candles[i-1]["low"], candles[i-1]["close"]
        up_move, down_move = h - ph, pl - l
        pdm[i] = up_move if (up_move > down_move and up_move > 0) else 0.0
        mdm[i] = down_move if (down_move > up_move and down_move > 0) else 0.0
        tr[i]  = max(h-l, abs(h-pc), abs(l-pc))
    tr_s  = sum(tr[1:period+1])
    pdm_s = sum(pdm[1:period+1])
    mdm_s = sum(mdm[1:period+1])
    def _fill_di(i, tr_s, pdm_s, mdm_s):
        pdi = 100.0*pdm_s/tr_s if tr_s > 0 else 0.0
        mdi = 100.0*mdm_s/tr_s if tr_s > 0 else 0.0
        plus_di_arr[i]  = round(pdi, 2)
        minus_di_arr[i] = round(mdi, 2)
        return 100.0*abs(pdi-mdi)/(pdi+mdi) if (pdi+mdi) > 0 else 0.0
    dx_hist = [_fill_di(period, tr_s, pdm_s, mdm_s)]
    adx_prev = None
    for i in range(period+1, n):
        tr_s  = tr_s  - tr_s/period  + tr[i]
        pdm_s = pdm_s - pdm_s/period + pdm[i]
        mdm_s = mdm_s - mdm_s/period + mdm[i]
        dx = _fill_di(i, tr_s, pdm_s, mdm_s)
        dx_hist.append(dx)
        if len(dx_hist) == period:
            adx_prev = sum(dx_hist)/period
            adx_arr[i] = round(adx_prev, 2)
        elif len(dx_hist) > period:
            adx_prev = (adx_prev*(period-1) + dx)/period
            adx_arr[i] = round(adx_prev, 2)
    return adx_arr, plus_di_arr, minus_di_arr

def _vol_ratio(candles, i, lookback=20):
    if i < lookback or i >= len(candles):
        return None
    vols = [candles[j].get("vol") for j in range(i-lookback, i)]
    vols = [v for v in vols if v]
    if not vols:
        return None
    med = sorted(vols)[len(vols)//2]
    if med <= 0:
        return None
    cur = candles[i].get("vol") or 0
    return round(cur/med, 3)

def _atr_percentile(atr_arr, i, lookback=100):
    if i >= len(atr_arr) or atr_arr[i] is None:
        return None
    window = [v for v in atr_arr[max(0, i-lookback):i] if v is not None]
    if len(window) < 10:
        return None
    cur = atr_arr[i]
    rank = sum(1 for v in window if v <= cur)
    return round(100.0*rank/len(window), 1)

def _ribbon_spread_atr(ladder_vals, atr_v):
    vals = [v for v in ladder_vals if v is not None]
    if len(vals) < 2 or not atr_v:
        return None
    return round((max(vals)-min(vals))/atr_v, 3)

def _candle_reaction_pattern(candles, i):
    if i < 1 or i >= len(candles):
        return False, False
    c, p = candles[i], candles[i-1]
    o, h, l, cl = c["open"], c["high"], c["low"], c["close"]
    rng = h - l
    if rng <= 0:
        return False, False
    body = abs(cl-o)
    upper_wick = h - max(o, cl)
    lower_wick = min(o, cl) - l
    bullish_pin = lower_wick >= body*2 and lower_wick >= rng*0.5
    bearish_pin = upper_wick >= body*2 and upper_wick >= rng*0.5
    po, pcl = p["open"], p["close"]
    bullish_engulf = cl > o and pcl < po and cl >= po and o <= pcl
    bearish_engulf = cl < o and pcl > po and cl <= po and o >= pcl
    return (bullish_pin or bullish_engulf), (bearish_pin or bearish_engulf)

def _session_bucket(ts=None):
    h = time.gmtime(ts if ts is not None else time.time()).tm_hour
    if 0  <= h < 8:  return "asia"
    if 8  <= h < 14: return "europe"
    if 14 <= h < 22: return "us"
    return "asia"

# ─── EMA-bounce dossier engine ──────────────────────────────────────────────
EMA_TF_PERIODS = {
    "1m":  [5, 8, 13, 21],
    "5m":  [5, 8, 13, 21],
    "15m": [9, 20, 21],
    "1h":  [9, 20, 21],
    "4h":  [21, 50, 100],
    "1d":  [7, 14, 28],
    "1w":  [50, 100, 200],
}
EMA_DOSSIER_TFS  = ["1m", "5m", "15m", "1h", "4h", "1d"]
EMA_DOSSIER_DAYS = {"1m": 3, "5m": 6, "15m": 10, "1h": 30, "4h": 90, "1d": 400}
EMA_WEEKLY_PERIODS  = EMA_TF_PERIODS["1w"]
PUMP_LOOKBACK_DAYS  = 7
PUMP_THRESHOLD_PCT_DOSSIER  = 20.0   # rename: не путать с PUMP_THRESHOLD_PCT детектора ниже
EMA_DOSSIER_TOUCH_ATR    = 0.25
EMA_TOUCH_CLOSE_MAX_ATR  = 1.5
EMA_DOSSIER_REACT_ATR    = 0.5
EMA_DOSSIER_REACT_BARS   = 5
EMA_DOSSIER_MIN_TOUCHES  = 5
EMA_DOSSIER_FILE = os.path.expanduser("~/pumpradar_dossier_state.json")
EMA_SIGNAL_SL_ATR = 0.6
EMA_SIGNAL_RR      = 2.0
MAX_LEVERAGE = 20
EMA_HTF_TREND_TF        = "1d"
EMA_HTF_TREND_PERIOD    = 28
EMA_HTF_TREND_FILTER_TFS = {"1m", "5m", "15m", "1h", "4h"}

def _ladder_periods_for(tf, ema_period):
    return sorted(EMA_TF_PERIODS.get(tf, EMA_TF_PERIODS["1d"]))


def _ladder_order(vals):
    if any(v is None for v in vals):
        return None
    if all(vals[k] >= vals[k+1] for k in range(len(vals)-1)):
        return "up"
    if all(vals[k] <= vals[k+1] for k in range(len(vals)-1)):
        return "down"
    return None

def _detect_ema_bounces(candles, ema_period, tf="1d",
                         touch_atr=EMA_DOSSIER_TOUCH_ATR,
                         react_atr=EMA_DOSSIER_REACT_ATR,
                         react_bars=EMA_DOSSIER_REACT_BARS):
    closes = [c["close"] for c in candles]
    ema_arr = _ema(closes, ema_period)
    atr_arr = _atr(candles, 14)
    ladder_periods = _ladder_periods_for(tf, ema_period)
    ladder_arrs = [_ema(closes, p) for p in ladder_periods]
    n = len(candles)
    touches = 0
    bounces = 0
    breaks  = 0
    bounce_up = 0
    bounce_dn = 0
    i = ema_period
    while i < n - react_bars:
        ema_v = ema_arr[i]
        atr_v = atr_arr[i] or 0.0
        if ema_v is None or atr_v <= 0:
            i += 1
            continue
        lo, hi = candles[i]["low"], candles[i]["high"]
        tol = touch_atr * atr_v
        touched = ((lo - tol) <= ema_v <= (hi + tol) and
                   abs(closes[i] - ema_v) <= EMA_TOUCH_CLOSE_MAX_ATR * atr_v)
        prev_close = closes[i-1]
        if touched and abs(prev_close - ema_v) > tol:
            side_above = prev_close > ema_v
            expected = "up" if side_above else "down"
            ladder = _ladder_order([closes[i]] + [arr[i] for arr in ladder_arrs])
            if ladder is not None and ladder != expected:
                i += 1
                continue
            touches += 1
            fwd_closes = closes[i+1:i+1+react_bars]
            if side_above:
                moved_up   = max((c - ema_v) for c in fwd_closes) if fwd_closes else 0
                moved_down = max((ema_v - c) for c in fwd_closes) if fwd_closes else 0
                if moved_up >= react_atr * atr_v and moved_up > moved_down:
                    bounces += 1; bounce_up += 1
                elif moved_down >= react_atr * atr_v:
                    breaks += 1
            else:
                moved_down = max((ema_v - c) for c in fwd_closes) if fwd_closes else 0
                moved_up   = max((c - ema_v) for c in fwd_closes) if fwd_closes else 0
                if moved_down >= react_atr * atr_v and moved_down > moved_up:
                    bounces += 1; bounce_dn += 1
                elif moved_up >= react_atr * atr_v:
                    breaks += 1
            i += react_bars
        else:
            i += 1
    bounce_rate = (bounces / touches) if touches else 0.0
    return {
        "ema_period": ema_period, "touches": touches, "bounces": bounces,
        "breaks": breaks, "bounce_up": bounce_up, "bounce_dn": bounce_dn,
        "bounce_rate": round(bounce_rate, 4),
    }

def _resample_to_weekly(daily_candles):
    if not daily_candles: return []
    weekly = []
    bucket = []
    bucket_start = None
    for c in daily_candles:
        day_idx = c["t"] // 86400
        week_idx = day_idx // 7
        if bucket_start is None:
            bucket_start = week_idx
        if week_idx != bucket_start:
            if bucket:
                weekly.append({
                    "t": bucket[0]["t"],
                    "open": bucket[0]["open"], "close": bucket[-1]["close"],
                    "high": max(b["high"] for b in bucket),
                    "low":  min(b["low"] for b in bucket),
                    "vol":  sum(b.get("vol", 0) for b in bucket),
                })
            bucket = [c]; bucket_start = week_idx
        else:
            bucket.append(c)
    if bucket:
        weekly.append({
            "t": bucket[0]["t"],
            "open": bucket[0]["open"], "close": bucket[-1]["close"],
            "high": max(b["high"] for b in bucket),
            "low":  min(b["low"] for b in bucket),
            "vol":  sum(b.get("vol", 0) for b in bucket),
        })
    return weekly

def _detect_recent_pump(daily_candles, lookback_days=PUMP_LOOKBACK_DAYS, threshold_pct=PUMP_THRESHOLD_PCT_DOSSIER):
    if len(daily_candles) < lookback_days + 1:
        return False, 0.0
    prev = daily_candles[-lookback_days-1]["close"]
    last = daily_candles[-1]["close"]
    if prev <= 0: return False, 0.0
    pct = (last - prev) / prev * 100.0
    return (pct >= threshold_pct), round(pct, 2)

def _build_coin_dossier(symbol, timeframes=None):
    timeframes  = timeframes or EMA_DOSSIER_TFS
    dossier = {"symbol": symbol, "by_tf": {}, "generated_at": int(time.time()),
               "recent_pump": False, "pump_pct": 0.0}
    daily_candles_cache = None
    for tf in timeframes:
        days = EMA_DOSSIER_DAYS.get(tf, 30)
        candles = _fetch_candles(symbol, tf, days)
        if tf == "1d":
            daily_candles_cache = candles
        ema_periods = EMA_TF_PERIODS.get(tf, EMA_TF_PERIODS["1d"])
        if len(candles) < max(ema_periods) + EMA_DOSSIER_REACT_BARS + 10:
            dossier["by_tf"][tf] = {"error": "недостаточно данных", "emas": []}
            continue
        stats = [_detect_ema_bounces(candles, p, tf=tf) for p in ema_periods]
        reliable = [s for s in stats if s["touches"] >= EMA_DOSSIER_MIN_TOUCHES]
        best = max(reliable, key=lambda s: s["bounce_rate"]) if reliable else None
        dossier["by_tf"][tf] = {
            "emas": stats,
            "best_ema": best["ema_period"] if best else None,
            "best_bounce_rate": best["bounce_rate"] if best else None,
        }
    if daily_candles_cache is None:
        daily_candles_cache = _fetch_candles(symbol, "1d", EMA_DOSSIER_DAYS.get("1d", 400))
    if daily_candles_cache:
        weekly_candles = _resample_to_weekly(daily_candles_cache)
        w_periods = EMA_TF_PERIODS["1w"]
        if len(weekly_candles) >= max(w_periods) + EMA_DOSSIER_REACT_BARS + 10:
            w_stats = [_detect_ema_bounces(weekly_candles, p, tf="1w") for p in w_periods]
            w_reliable = [s for s in w_stats if s["touches"] >= EMA_DOSSIER_MIN_TOUCHES]
            w_best = max(w_reliable, key=lambda s: s["bounce_rate"]) if w_reliable else None
            dossier["by_tf"]["1w"] = {
                "emas": w_stats,
                "best_ema": w_best["ema_period"] if w_best else None,
                "best_bounce_rate": w_best["bounce_rate"] if w_best else None,
            }
        else:
            dossier["by_tf"]["1w"] = {"error": "недостаточно недель истории", "emas": []}
        is_pump, pct = _detect_recent_pump(daily_candles_cache)
        dossier["recent_pump"] = is_pump
        dossier["pump_pct"] = pct
    return dossier

def _load_ema_dossier_state():
    try:
        with open(EMA_DOSSIER_FILE, "r") as f: return json.load(f)
    except Exception:
        return {"status": "idle", "results": {}, "updated_at": 0}

def _save_ema_dossier_state(state):
    try:
        with open(EMA_DOSSIER_FILE, "w") as f: json.dump(state, f)
    except Exception as e:
        olog(f"ema_dossier save error: {_explain_error(e)}")

def _run_ema_dossier_scan(top_n=50):
    symbols = _fetch_all_symbols()[:top_n]
    if len(symbols) < top_n:
        olog(f"⚠ [ema_dossier] после фильтра по спреду доступно только "
             f"{len(symbols)} монет из запрошенных {top_n} — список короче "
             f"ожидаемого (см. лог отсева выше)")
    state = {"status": "running", "results": {}, "total": len(symbols),
             "done": 0, "updated_at": int(time.time())}
    _save_ema_dossier_state(state)
    _before = _rss_mb()
    ex = _PoolExecutor(max_workers=NUM_WORKERS)
    olog(f"[ema_dossier] скан старт: {len(symbols)} монет, "
         f"{NUM_WORKERS} {'потоков' if _POOL_TYPE=='thread' else 'процессов'}"
         + (f", RSS {_before} МБ" if _before is not None else ""))
    try:
        futs = {ex.submit(_build_coin_dossier, sym): sym for sym in symbols}
        for fut in _as_completed(futs):
            sym = futs[fut]
            try:
                state["results"][sym] = fut.result()
            except Exception as e:
                state["results"][sym] = {"symbol": sym, "error": str(e)}
            state["done"] += 1
            state["updated_at"] = int(time.time())
            _save_ema_dossier_state(state)
    finally:
        _shutdown_pool_safely(ex)
    _after = _rss_mb()
    if _before is not None and _after is not None:
        olog(f"[ema_dossier] пул закрыт, RSS {_before}→{_after} МБ")
    state["status"] = "done"
    _save_ema_dossier_state(state)
    olog(f"[ema_dossier] скан завершён: {len(symbols)} монет")
    return state

# ─── Live EMA-сигналы ───────────────────────────────────────────────────────
EMA_LIVE_FILE      = os.path.expanduser("~/pumpradar_live_signals.json")
EMA_LIVE_POLL_SEC  = 15
_ema_live_lock  = threading.Lock()
_ema_live_state = {"signals": {}, "updated_at": 0}

EMA_HISTORY_FILE = os.path.expanduser("~/pumpradar_signal_history.json")
EMA_HISTORY_MAX  = 500
_ema_history_lock = threading.Lock()

def _load_ema_history():
    try:
        with open(EMA_HISTORY_FILE, "r") as f:
            state = json.load(f)
            state.setdefault("items", {})
            return state
    except Exception:
        return {"items": {}}

def _save_ema_history(state):
    try:
        tmp_path = EMA_HISTORY_FILE + f".tmp{os.getpid()}"
        with open(tmp_path, "w") as f:
            json.dump(state, f)
        os.replace(tmp_path, EMA_HISTORY_FILE)
    except Exception as e:
        olog(f"[ema_history] ошибка сохранения: {_explain_error(e)}")

def _ema_history_add(sig):
    key = f"{sig['symbol']}|{sig['tf']}|{sig['ema_period']}|{sig['bar_t']}"
    with _ema_history_lock:
        state = _load_ema_history()
        if key in state["items"]:
            return key
        item = dict(sig)
        item.update(status="open", opened_at=int(time.time()),
                    closed_at=None, close_price=None)
        state["items"][key] = item
        if len(state["items"]) > EMA_HISTORY_MAX:
            oldest = sorted(state["items"].items(), key=lambda kv: kv[1]["opened_at"])
            for k, _ in oldest[:len(state["items"]) - EMA_HISTORY_MAX]:
                del state["items"][k]
        _save_ema_history(state)
        return key

def _ema_reconcile_live_positions():
    with _ema_history_lock:
        state = _load_ema_history()
    live_open = [(k, v) for k, v in state["items"].items()
                 if v.get("status") == "open" and v.get("live")]
    if not live_open:
        return
    updated = {}
    for key, item in live_open:
        contract = item["symbol"].replace("/", "_").upper()
        try:
            still_open = _gate_get_position(contract)
        except Exception as e:
            olog(f"[ema_reconcile] {item['symbol']}: не смог проверить биржу ({_explain_error(e)}) — пропуск")
            continue
        if still_open:
            continue
        try:
            _gate_cancel_orders(contract)
        except Exception as e:
            olog(f"[ema_reconcile] {item['symbol']}: не смог снять старые ордера ({_explain_error(e)})")
        pnl_info = _gate_get_last_pnl(item["symbol"], fallback_since_ts=item["opened_at"])
        close_p  = (pnl_info["close_price"] if pnl_info and pnl_info.get("close_price") is not None
                    else item.get("sl"))
        won      = bool(pnl_info and pnl_info["pnl"] >= 0)
        item["status"]            = "tp" if won else "sl"
        item["closed_at"]         = int(time.time())
        item["close_price"]       = close_p
        item["closed_externally"] = True
        item["diag_status"]       = "pending"
        if pnl_info:
            item["live_pnl"]     = pnl_info["pnl"]
            item["live_pnl_pct"] = pnl_info["pnl_pct"]
            item["live_pnl_fee"]   = pnl_info.get("pnl_fee")
            item["live_pnl_fund"]  = pnl_info.get("pnl_fund")
            item["live_pnl_price"] = pnl_info.get("pnl_price")
        olog(f"[ema_reconcile] {item['symbol']}: реально закрыта на бирже "
             f"внешним путём (не по нашему TP/SL), close={close_p} pnl={pnl_info}")
        updated[key] = item
    if updated:
        with _ema_history_lock:
            state = _load_ema_history()
            state["items"].update(updated)
            _save_ema_history(state)

def _ema_history_update_open():
    with _ema_history_lock:
        state = _load_ema_history()
    open_items = [(k, v) for k, v in state["items"].items() if v["status"] == "open"]
    if not open_items:
        return
    updated = {}
    for key, item in open_items:
        try:
            outcome, outcome_price = None, None
            live_price = _gate_get_price(item["symbol"])
            if live_price:
                if item["dir"] == "long":
                    if live_price <= item["sl"]: outcome, outcome_price = "sl", item["sl"]
                    elif live_price >= item["tp"]: outcome, outcome_price = "tp", item["tp"]
                else:
                    if live_price >= item["sl"]: outcome, outcome_price = "sl", item["sl"]
                    elif live_price <= item["tp"]: outcome, outcome_price = "tp", item["tp"]
            if not outcome:
                fetch_tf = "1d" if item["tf"] == "1w" else item["tf"]
                days = _days_for_live_check(item["tf"], item["ema_period"])
                raw = _fetch_candles(item["symbol"], fetch_tf, days)
                candles = _resample_to_weekly(raw) if item["tf"] == "1w" else raw
                fwd = [c for c in candles if c["t"] > item["bar_t"]]
                for c in fwd:
                    if item["dir"] == "long":
                        hit_tp, hit_sl = c["high"] >= item["tp"], c["low"] <= item["sl"]
                    else:
                        hit_tp, hit_sl = c["low"] <= item["tp"], c["high"] >= item["sl"]
                    if hit_sl:
                        outcome, outcome_price = "sl", item["sl"]; break
                    if hit_tp:
                        outcome, outcome_price = "tp", item["tp"]; break
            if outcome:
                item["status"] = outcome
                item["closed_at"] = int(time.time())
                item["close_price"] = outcome_price
                item["diag_status"] = "pending"
                if item.get("live"):
                    try:
                        pnl_info = _gate_get_last_pnl(item["symbol"], fallback_since_ts=item["opened_at"])
                    except Exception as e:
                        olog(f"[ema_history] {item['symbol']}: не смог получить live_pnl ({_explain_error(e)})")
                        pnl_info = None
                    if pnl_info:
                        item["live_pnl"]       = pnl_info["pnl"]
                        item["live_pnl_pct"]   = pnl_info.get("pnl_pct")
                        item["live_pnl_fee"]   = pnl_info.get("pnl_fee")
                        item["live_pnl_fund"]  = pnl_info.get("pnl_fund")
                        item["live_pnl_price"] = pnl_info.get("pnl_price")
                updated[key] = item
        except Exception as e:
            olog(f"[ema_history] статус {item.get('symbol')} ошибка: {_explain_error(e)}")
    if updated:
        with _ema_history_lock:
            state2 = _load_ema_history()
            for key, item in updated.items():
                state2["items"][key] = item
            _save_ema_history(state2)
        for item in updated.values():
            if item.get("live"):
                _ema_finalize_live_position(item)

# ─── реальная автоторговля по EMA-сигналам ─────────────────────────────────
EMA_AUTO_TRADE_CFG_FILE = os.path.expanduser("~/pumpradar_auto_trade_cfg.json")
ema_auto_trade_lock  = threading.Lock()
ema_auto_trade_state = {
    "enabled": False,
    "position_pct":   3.0,
    "risk_pct":       5.0,
    "max_concurrent": None,
    "max_forced_margin_pct": 20.0,
    "forced_size_max_multiple": 3.0,
    "last_error": "",
}

def _load_ema_auto_trade_cfg():
    try:
        with open(EMA_AUTO_TRADE_CFG_FILE) as f:
            saved = json.load(f)
        with ema_auto_trade_lock:
            for k in ("enabled", "position_pct", "risk_pct", "max_concurrent", "max_forced_margin_pct", "forced_size_max_multiple"):
                if k in saved:
                    ema_auto_trade_state[k] = saved[k]
    except Exception:
        pass

def _save_ema_auto_trade_cfg():
    try:
        with ema_auto_trade_lock:
            snapshot = {k: ema_auto_trade_state[k] for k in
                        ("enabled", "position_pct", "risk_pct", "max_concurrent", "max_forced_margin_pct", "forced_size_max_multiple")}
        with open(EMA_AUTO_TRADE_CFG_FILE, "w") as f:
            json.dump(snapshot, f)
    except Exception as e:
        olog(f"[ema_auto_trade] ошибка сохранения настроек: {_explain_error(e)}")

def _ema_maybe_open_live_trade(symbol, sig, hist_key):
    with ema_auto_trade_lock:
        if not ema_auto_trade_state["enabled"]:
            return
        position_pct = ema_auto_trade_state["position_pct"]
        risk_pct     = ema_auto_trade_state["risk_pct"]
        max_c        = ema_auto_trade_state["max_concurrent"]
        max_forced_margin_pct = ema_auto_trade_state["max_forced_margin_pct"]
        forced_size_max_multiple = ema_auto_trade_state["forced_size_max_multiple"]
    with _ema_history_lock:
        state = _load_ema_history()
        live_items = [v for v in state["items"].values()
                      if v.get("status") == "open" and v.get("live")]
    if any(v["symbol"] == symbol for v in live_items):
        return
    if max_c and len(live_items) >= max_c:
        olog(f"[ema_auto_trade] лимит {max_c} одновр. позиций достигнут — пропуск {symbol}")
        return
    contract = symbol.replace("/", "_").upper()
    try:
        existing = _gate_get_position(contract)
    except Exception as e:
        olog(f"[ema_auto_trade] {symbol}: не смог проверить биржу перед входом ({_explain_error(e)}) — пропуск на этот раз")
        return
    if existing:
        olog(f"[ema_auto_trade] {symbol}: на бирже уже есть позиция вне нашего учёта — пропуск")
        return
    pos_info = _gate_open_position(
        symbol, sig["dir"], sig["price"], sig["sl"], sig["tp"], risk_pct,
        position_pct=position_pct, label="EMA INVERT", text_prefix="emainv",
        max_forced_margin_pct=max_forced_margin_pct,
        forced_size_max_multiple=forced_size_max_multiple,
    )
    if not pos_info:
        return
    with _ema_history_lock:
        state = _load_ema_history()
        item = state["items"].get(hist_key)
        if item:
            item["live"] = True
            item["live_size"]     = pos_info.get("size")
            item["live_leverage"] = pos_info.get("leverage")
            item["live_notional"] = pos_info.get("notional")
            _save_ema_history(state)

def _ema_finalize_live_position(item):
    symbol = item["symbol"]
    contract = symbol.replace("/", "_").upper()
    try:
        still_open = _gate_get_position(contract)
    except Exception as e:
        olog(f"[ema_auto_trade] {symbol}: не смог проверить биржу после закрытия сигнала: {_explain_error(e)}")
        return
    if not still_open:
        return
    olog(f"[ema_auto_trade] ⚠ {symbol}: сигнал закрыт ({item['status']}), но позиция "
         f"ещё висит на бирже — закрываю маркетом принудительно")
    try:
        _gate_close_position(symbol)
        _send_alert(f"⚠️ <b>{symbol} EMA INVERT</b> — TP/SL-ордер не сработал вовремя, "
                     f"позиция закрыта маркетом принудительно")
    except Exception as e:
        olog(f"[ema_auto_trade] {symbol}: ошибка принудительного закрытия: {_explain_error(e)}")
        _send_alert(f"🚨 <b>{symbol} EMA INVERT</b> — не удалось закрыть позицию "
                     f"принудительно: {_explain_error(e)}. ЗАКРОЙ ВРУЧНУЮ!")


EMA_DIAG_FILE          = os.path.expanduser("~/pumpradar_signal_diagnostics.jsonl")
EMA_DIAG_WAIT_BARS     = 20
EMA_DIAG_LOOKBACK_PAD  = 5
_ema_diag_lock = threading.Lock()

def _ema_diag_log_write(record):
    try:
        with _ema_diag_lock:
            with open(EMA_DIAG_FILE, "a") as f:
                f.write(json.dumps(record, ensure_ascii=False) + "\n")
    except Exception as e:
        olog(f"[ema_diag] ошибка записи лога: {_explain_error(e)}")

EMA_EVENTS_FILE = os.path.expanduser("~/pumpradar_events_diagnostics.jsonl")
_ema_events_lock = threading.Lock()

def _ema_event_log_write(event_type, **fields):
    record = {"ts": time.time(), "ts_h": time.strftime("%Y-%m-%d %H:%M:%S"), "type": event_type}
    record.update(fields)
    try:
        with _ema_events_lock:
            with open(EMA_EVENTS_FILE, "a") as f:
                f.write(json.dumps(record, ensure_ascii=False) + "\n")
    except Exception as e:
        olog(f"[ema_events] ⚠ ошибка записи в {EMA_EVENTS_FILE}: {_explain_error(e)}")

def _ema_diagnose_one(item):
    symbol, tf, ema_period = item["symbol"], item["tf"], item["ema_period"]
    direction = item["dir"]
    entry, tp = item["price"], item["tp"]
    sl_hit_price   = item["sl"]
    sl             = item.get("orig_sl", sl_hit_price)
    breakeven_hit  = bool(item.get("breakeven_done"))
    bar_t, closed_at = item["bar_t"], item["closed_at"]
    fetch_tf = "1d" if tf == "1w" else tf
    span_sec = max(1, int(time.time()) - bar_t)
    days = math.ceil(span_sec / 86400) + EMA_DIAG_LOOKBACK_PAD
    raw = _fetch_candles(symbol, fetch_tf, days)
    candles = _resample_to_weekly(raw) if tf == "1w" else raw
    if not candles:
        return None
    during = [c for c in candles if bar_t <= c["t"] < closed_at]
    after  = [c for c in candles if c["t"] >= closed_at]
    if len(after) < EMA_DIAG_WAIT_BARS:
        return None
    risk = abs(entry - sl)
    if risk <= 0:
        return None
    if direction == "long":
        mfe = max([c["high"] for c in during], default=entry) - entry
        post_high = max(c["high"] for c in after[:EMA_DIAG_WAIT_BARS])
        post_low  = min(c["low"]  for c in after[:EMA_DIAG_WAIT_BARS])
        would_hit_tp   = post_high >= tp
        continued_down = post_low <= sl - risk
    else:
        mfe = entry - min([c["low"] for c in during], default=entry)
        post_high = max(c["high"] for c in after[:EMA_DIAG_WAIT_BARS])
        post_low  = min(c["low"]  for c in after[:EMA_DIAG_WAIT_BARS])
        would_hit_tp   = post_low <= tp
        continued_down = post_high >= sl + risk
    mfe_r_atr = round(mfe / item["atr_v"], 2) if item.get("atr_v") else None
    bars_to_sl = len(during)
    if breakeven_hit:
        verdict = ("закрылась в безубыток: SL был переставлен watchdog'ом "
                    "после достижения профита, реальный убыток — только "
                    "комиссия/буфер, не полноценный стоп")
    elif would_hit_tp:
        verdict = ("стоп преждевременный: цена выбила SL, но в следующие "
                    f"{EMA_DIAG_WAIT_BARS} баров всё же дошла до уровня TP — "
                    "направление было угадано верно, не хватило запаса по стопу")
    elif continued_down:
        verdict = ("сетап был неверным: после SL цена продолжила движение "
                    "против сигнала ещё как минимум на такой же риск — "
                    "отскока от EMA не было, уровень был пробит трендом")
    elif mfe_r_atr is not None and mfe_r_atr < 0.15:
        verdict = ("шум/боковик: цена почти не двигалась в сторону сигнала "
                    "ни до, ни после стопа — касание EMA было случайным, "
                    "не реальным отскоком")
    else:
        verdict = ("смешанная картина: небольшое движение в пользу сигнала "
                    "было, но недостаточное ни для TP, ни для явного "
                    "продолжения тренда против — типичный шумовой стоп")
    record = {
        "ts": int(time.time()), "symbol": symbol, "tf": tf,
        "ema_period": ema_period, "dir": direction,
        "entry": entry, "sl": sl, "sl_hit_price": sl_hit_price, "tp": tp,
        "breakeven_hit": breakeven_hit,
        "bounce_rate": item.get("bounce_rate"), "touches": item.get("touches"),
        "dist_atr_at_entry": item.get("dist_atr"), "ladder_at_entry": item.get("ladder"),
        "bars_to_sl": bars_to_sl,
        "mfe_atr_before_sl": mfe_r_atr,
        "post_sl_hit_tp": would_hit_tp,
        "post_sl_continued_against": continued_down,
        "verdict": verdict,
        "rsi_at_entry": item.get("rsi"), "adx_at_entry": item.get("adx"),
        "plus_di_at_entry": item.get("plus_di"), "minus_di_at_entry": item.get("minus_di"),
        "vol_ratio_at_entry": item.get("vol_ratio"),
        "atr_percentile_at_entry": item.get("atr_percentile"),
        "ribbon_spread_atr_at_entry": item.get("ribbon_spread_atr"),
        "candle_pattern_at_entry": item.get("candle_pattern"),
        "session_at_entry": item.get("session"),
        "htf_trend_at_entry": item.get("htf_trend"),
    }
    return record

def _ema_run_diagnostics():
    with _ema_history_lock:
        state = _load_ema_history()
    pending = [(k, v) for k, v in state["items"].items()
               if v.get("status") == "sl" and v.get("diag_status") == "pending"]
    if not pending:
        return
    done = {}
    for key, item in pending:
        tf_sec = TF_SECONDS.get("1d" if item["tf"] == "1w" else item["tf"], 3600)
        if int(time.time()) - item["closed_at"] < EMA_DIAG_WAIT_BARS * tf_sec:
            continue
        try:
            record = _ema_diagnose_one(item)
        except Exception as e:
            olog(f"[ema_diag] {item.get('symbol')} ошибка разбора: {_explain_error(e)}")
            continue
        if record is None:
            continue
        _ema_diag_log_write(record)
        olog(f"[ema_diag] {record['symbol']} {record['tf']} EMA{record['ema_period']} "
             f"{record['dir']} SL → {record['verdict']}")
        item["diag_status"] = "done"
        done[key] = item
    if done:
        with _ema_history_lock:
            state2 = _load_ema_history()
            for key, item in done.items():
                state2["items"][key] = item
            _save_ema_history(state2)

EMA_LIVE_TFS = {"1m", "5m", "15m", "1h"}

def _pick_best_ema_for_symbol(dossier_entry):
    best = None
    for tf, d in (dossier_entry.get("by_tf") or {}).items():
        if tf not in EMA_LIVE_TFS:
            continue
        for s in d.get("emas", []):
            touches = s.get("touches", 0)
            if touches < EMA_DOSSIER_MIN_TOUCHES:
                continue
            break_rate = s.get("breaks", 0) / touches
            if best is None or break_rate > best["break_rate"]:
                best = {"tf": tf, "ema_period": s["ema_period"],
                        "bounce_rate": s["bounce_rate"],
                        "break_rate": round(break_rate, 4),
                        "touches": touches}
    return best

def _days_for_live_check(tf, ema_period):
    bars_needed = ema_period + 260
    src_tf = "1d" if tf == "1w" else tf
    days = math.ceil(bars_needed * (TF_SECONDS.get(src_tf, 3600) if tf != "1w" else 86400*7) / 86400)
    return max(3, days)

_ema_touch_state = {}
EMA_TOUCH_EXIT_MULT = 1.8
_ema_touch_pending  = {}
EMA_TOUCH_CONFIRM_SEC = 15
_ema_touch_confirm_stats = {"confirmed": 0, "aborted": 0}

_ema_open_keys_cache = {"at": 0, "keys": set()}
EMA_OPEN_KEYS_TTL = 10

def _ema_has_open_signal(symbol, tf, ema_period):
    now = time.time()
    if now - _ema_open_keys_cache["at"] > EMA_OPEN_KEYS_TTL:
        with _ema_history_lock:
            state = _load_ema_history()
        _ema_open_keys_cache["keys"] = {
            f"{v['symbol']}|{v['tf']}|{v['ema_period']}"
            for v in state["items"].values() if v.get("status") == "open"
        }
        _ema_open_keys_cache["at"] = now
    return f"{symbol}|{tf}|{ema_period}" in _ema_open_keys_cache["keys"]
_ema_ctx_cache   = {}

def _round_price(v):
    if v is None or v == 0:
        return v
    magnitude = math.floor(math.log10(abs(v)))
    decimals = max(0, 6 - magnitude - 1)
    return round(v, decimals)

def _ema_live_value(prev_ema, live_price, period):
    if prev_ema is None: return None
    k = 2.0 / (period + 1)
    return live_price * k + prev_ema * (1 - k)

def _ema_get_closed_ctx(symbol, tf, ema_period):
    key = f"{symbol}|{tf}|{ema_period}"
    now = time.time()
    cached = _ema_ctx_cache.get(key)
    if cached and now < cached["next_close_t"]:
        return cached
    days = _days_for_live_check(tf, ema_period)
    fetch_tf = "1d" if tf == "1w" else tf
    raw = _fetch_candles(symbol, fetch_tf, days)
    candles = _resample_to_weekly(raw) if tf == "1w" else raw
    if len(candles) < ema_period + 5:
        return None
    closes = [c["close"] for c in candles]
    ema_arr = _ema(closes, ema_period)
    atr_arr = _atr(candles, 14)
    i = len(candles) - 1
    ema_closed, atr_v = ema_arr[i], atr_arr[i]
    if ema_closed is None or not atr_v:
        return None
    ladder_periods = _ladder_periods_for(tf, ema_period)
    ladder_ema_closed = [_ema(closes, p)[i] for p in ladder_periods]
    interval_sec = TF_SECONDS.get("1d" if tf == "1w" else tf, 3600)
    next_close_t = candles[i]["t"] + interval_sec + 2
    rsi_arr = _rsi(closes, 14)
    adx_arr, plus_di_arr, minus_di_arr = _adx(candles, 14)
    bullish_reaction, bearish_reaction = _candle_reaction_pattern(candles, i)
    ctx = {
        "close_i": closes[i], "ema_closed": ema_closed, "atr_v": atr_v,
        "ladder_periods": ladder_periods, "ladder_ema_closed": ladder_ema_closed,
        "next_close_t": next_close_t,
        "rsi": rsi_arr[i], "adx": adx_arr[i],
        "plus_di": plus_di_arr[i], "minus_di": minus_di_arr[i],
        "vol_ratio": _vol_ratio(candles, i),
        "atr_percentile": _atr_percentile(atr_arr, i),
        "ribbon_spread_atr": _ribbon_spread_atr(ladder_ema_closed, atr_v),
        "bullish_reaction": bullish_reaction, "bearish_reaction": bearish_reaction,
    }
    _ema_ctx_cache[key] = ctx
    return ctx

def _get_htf_trend(symbol):
    ctx = _ema_get_closed_ctx(symbol, EMA_HTF_TREND_TF, EMA_HTF_TREND_PERIOD)
    if ctx is None:
        return None
    return "up" if ctx["close_i"] > ctx["ema_closed"] else "down"

def _ema_check_symbol_signal(symbol, pick):
    tf, ema_period = pick["tf"], pick["ema_period"]
    ctx = _ema_get_closed_ctx(symbol, tf, ema_period)
    if ctx is None: return None
    ema_closed, atr_v = ctx["ema_closed"], ctx["atr_v"]
    live_price = _gate_get_price(symbol)
    if not live_price: return None
    ema_v = _ema_live_value(ema_closed, live_price, ema_period)
    tol = EMA_DOSSIER_TOUCH_ATR * atr_v
    dist = abs(live_price - ema_v)
    key = f"{symbol}|{tf}|{ema_period}"
    state = _ema_touch_state.get(key, "out")
    side_above = ctx["close_i"] > ema_closed
    direction = "long" if side_above else "short"
    touched_now = dist <= tol if state == "out" else dist <= tol * EMA_TOUCH_EXIT_MULT
    if not touched_now:
        if state == "pending":
            _ema_touch_confirm_stats["aborted"] += 1
            waited = time.time() - _ema_touch_pending.get(key, time.time())
            olog(f"[ema_touch] {symbol} {tf} EMA{ema_period}: касание отменено — "
                 f"цена пробила уровень насквозь за {waited:.0f}с без подтверждения "
                 f"(всего отменено={_ema_touch_confirm_stats['aborted']}, "
                 f"подтверждено={_ema_touch_confirm_stats['confirmed']})")
            _ema_event_log_write("touch_aborted", symbol=symbol, tf=tf,
                                  ema_period=ema_period, waited_sec=round(waited, 1),
                                  aborted_total=_ema_touch_confirm_stats["aborted"],
                                  confirmed_total=_ema_touch_confirm_stats["confirmed"])
        _ema_touch_state[key] = "out"
        _ema_touch_pending.pop(key, None)
        return None
    if state == "out":
        _ema_touch_state[key] = "pending"
        _ema_touch_pending[key] = time.time()
        return None
    if state == "pending":
        if time.time() - _ema_touch_pending.get(key, 0) < EMA_TOUCH_CONFIRM_SEC:
            return None
        _ema_touch_state[key] = "done"
        _ema_touch_confirm_stats["confirmed"] += 1
        _ema_event_log_write("touch_confirmed", symbol=symbol, tf=tf,
                              ema_period=ema_period,
                              waited_sec=round(time.time() - _ema_touch_pending.get(key, time.time()), 1),
                              aborted_total=_ema_touch_confirm_stats["aborted"],
                              confirmed_total=_ema_touch_confirm_stats["confirmed"])
    else:
        return None
    if _ema_has_open_signal(symbol, tf, ema_period):
        return None
    ladder_vals_live = [_ema_live_value(v, live_price, p)
                         for v, p in zip(ctx["ladder_ema_closed"], ctx["ladder_periods"])]
    ladder = _ladder_order([live_price] + ladder_vals_live)
    expected = "up" if side_above else "down"
    if ladder is not None and ladder != expected:
        return None
    htf_trend = _get_htf_trend(symbol)
    if tf in EMA_HTF_TREND_FILTER_TFS and htf_trend is not None:
        if direction == "long" and htf_trend == "up":
            return None
        if direction == "short" and htf_trend == "down":
            return None
    price = live_price
    sl_buf = EMA_SIGNAL_SL_ATR * atr_v
    level = (ema_v - sl_buf) if direction == "long" else (ema_v + sl_buf)
    trade_dir = "short" if direction == "long" else "long"
    tp = level
    if trade_dir == "long":
        risk_to_tp = tp - price
        safety_sl = price - EMA_INVERT_SAFETY_RR * risk_to_tp
    else:
        risk_to_tp = price - tp
        safety_sl = price + EMA_INVERT_SAFETY_RR * risk_to_tp
    if risk_to_tp <= 0:
        return None
    if trade_dir == "long" and not (safety_sl < price < tp):
        return None
    if trade_dir == "short" and not (tp < price < safety_sl):
        return None
    ema_r, tp_r, safety_sl_r = _round_price(ema_v), _round_price(tp), _round_price(safety_sl)
    if trade_dir == "long" and not (safety_sl_r < price < tp_r):
        return None
    if trade_dir == "short" and not (tp_r < price < safety_sl_r):
        return None
    dist_atr = round(abs(live_price - ema_v) / atr_v, 3) if atr_v else None
    ladder_snapshot = {str(p): _round_price(v) for p, v in
                        zip(ctx["ladder_periods"], ladder_vals_live)}
    if direction == "long":
        candle_pattern = "confirmed" if ctx["bullish_reaction"] else "absent"
    else:
        candle_pattern = "confirmed" if ctx["bearish_reaction"] else "absent"

    if ctx["vol_ratio"] is not None and ctx["vol_ratio"] > EMA_INVERT_MAX_VOL_RATIO:
        _ema_invert_filter_stats["vol_ratio"] += 1
        _ema_event_log_write("invert_filter_rejected", symbol=symbol, tf=tf,
                              ema_period=ema_period, reason="vol_ratio",
                              value=ctx["vol_ratio"], threshold=EMA_INVERT_MAX_VOL_RATIO)
        return None
    if EMA_INVERT_REJECT_CONFIRMED_PATTERN and candle_pattern == "confirmed":
        _ema_invert_filter_stats["candle_pattern"] += 1
        _ema_event_log_write("invert_filter_rejected", symbol=symbol, tf=tf,
                              ema_period=ema_period, reason="candle_pattern")
        return None

    return {
        "symbol": symbol, "tf": tf, "ema_period": ema_period, "dir": trade_dir,
        "bounce_dir": direction,
        "price": price, "ema_value": ema_r,
        "sl": safety_sl_r, "tp": tp_r, "rr": None,
        "time_limit_sec": _ema_invert_time_limit_sec(tf),
        "bounce_rate": pick["bounce_rate"], "break_rate": pick.get("break_rate"),
        "bar_t": int(time.time()),
        "touches": pick.get("touches"), "atr_v": _round_price(atr_v),
        "dist_atr": dist_atr, "ladder": ladder_snapshot,
        "rsi": ctx["rsi"], "adx": ctx["adx"],
        "plus_di": ctx["plus_di"], "minus_di": ctx["minus_di"],
        "vol_ratio": ctx["vol_ratio"], "atr_percentile": ctx["atr_percentile"],
        "ribbon_spread_atr": ctx["ribbon_spread_atr"],
        "candle_pattern": candle_pattern,
        "session": _session_bucket(),
        "htf_trend": htf_trend,
    }

def _ema_signal_loop():
    time.sleep(30)
    _iter = 0
    while True:
        try:
            state = _load_ema_dossier_state()
            results = state.get("results") or {}
            if not results:
                time.sleep(EMA_LIVE_POLL_SEC); continue
            for symbol, entry in results.items():
                if "error" in entry: continue
                pick = _pick_best_ema_for_symbol(entry)
                if not pick: continue
                sig = _ema_check_symbol_signal(symbol, pick)
                if not sig: continue
                with _ema_live_lock:
                    _ema_live_state["signals"][symbol] = sig
                    _ema_live_state["updated_at"] = int(time.time())
                hist_key = _ema_history_add(sig)
                arrow = "🟢 LONG" if sig["dir"] == "long" else "🔴 SHORT"
                mins_limit = sig["time_limit_sec"] / 60.0
                msg = (f"📊 <b>{symbol}</b> {sig['tf']} — {arrow} (INVERT: пробой EMA{sig['ema_period']}, "
                       f"против bounce_rate {sig['bounce_rate']*100:.0f}%)\n"
                       f"Цена: {sig['price']} | EMA: {sig['ema_value']}\n"
                       f"Safety-SL: {sig['sl']} | TP: {sig['tp']} | time-stop: {mins_limit:.0f}мин")
                _send_alert(msg)
                olog(f"[ema_live] новый сигнал {symbol} {sig['tf']} EMA{sig['ema_period']} {sig['dir']}")
                _ema_maybe_open_live_trade(symbol, sig, hist_key)
            _ema_reconcile_live_positions()
            _ema_rearm_missing_protection()
            _ema_invert_timestop_watchdog()
            _ema_history_update_open()
            _ema_invert_run_diagnostics()
        except Exception as e:
            olog(f"[ema_live] ошибка цикла: {_explain_error(e)}")
        _iter += 1
        if _iter % 120 == 0:
            _rss = _rss_mb()
            if _rss is not None:
                olog(f"[ema_live] RSS {_rss} МБ (итерация {_iter}, ctx_cache={len(_ema_ctx_cache)})")
            gc.collect()
        time.sleep(EMA_LIVE_POLL_SEC)
PUMP_MAIN_HTML_PAGE = """<!DOCTYPE html>
<html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Pump Radar</title>
<style>
body{background:#0d1117;color:#c9d1d9;font-family:system-ui,sans-serif;margin:0;padding:16px}
h1{font-size:20px}
h3{font-size:15px;margin:0}
button{background:#238636;color:#fff;border:0;padding:10px 16px;border-radius:6px;font-size:14px;margin:6px 6px 6px 0;cursor:pointer}
table{width:100%;border-collapse:collapse;margin-top:0;font-size:13px}
th,td{padding:6px 8px;text-align:left;border-bottom:1px solid #21262d;white-space:nowrap}
th{color:#8b949e;position:sticky;top:0;background:#161b22}
tbody tr:hover{background:#1c2128}
.long{color:#3fb950}
#status{color:#8b949e;font-size:13px;margin-top:8px}
#alertModal{display:none;position:fixed;inset:0;background:rgba(0,0,0,.6);z-index:50;align-items:center;justify-content:center}
#alertModal .card{background:#161b22;border:1px solid #30363d;border-radius:10px;padding:20px;width:min(420px,92vw)}
#alertModal label{display:block;margin-top:12px;font-size:13px;color:#8b949e}
#alertModal input[type=text]{width:100%;background:#0d1117;color:#c9d1d9;border:1px solid #30363d;border-radius:6px;padding:8px;margin-top:4px;font-size:14px;box-sizing:border-box}
#alertModal .row{display:flex;justify-content:space-between;align-items:center;margin-top:12px}
.section-card{background:#161b22;border:1px solid #30363d;border-radius:10px;padding:14px;margin-top:14px}
.section-head{display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:8px}
.btn-danger-sm{background:#3a1414;border:1px solid #f85149;color:#f85149;padding:4px 10px;font-size:12px;border-radius:6px;margin:0;cursor:pointer}
.summary-line{font-size:13px;color:#8b949e;margin-top:6px}
.summary-line b{color:#c9d1d9}
.table-scroll{overflow-x:auto;margin-top:10px;border-radius:6px}
details summary{cursor:pointer;font-size:15px;list-style:none}
details summary::-webkit-details-marker{display:none}
details summary:before{content:"▸ ";color:#8b949e}
details[open] summary:before{content:"▾ "}
.log-box{background:#010409;border:1px solid #21262d;border-radius:4px;height:200px;overflow-y:auto;padding:6px;font-size:11px;font-family:monospace}
.log-line{padding:1px 0;border-bottom:1px solid #21262d}
@media (max-width:480px){
  body{padding:8px;font-size:13px}
  h1{font-size:17px}
  h3{font-size:14px}
  button{padding:8px 12px;font-size:13px;margin:4px 4px 4px 0}
  th,td{padding:4px 6px;font-size:12px}
  .section-card{padding:10px;margin-top:10px}
}
</style></head><body>
<div style="background:#161b22;border-bottom:1px solid #30363d;padding:8px 12px;display:flex;gap:6px;flex-wrap:wrap;font-size:12px">
  <a href="/ema" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#127968; Главная</a>
  <a href="/manual_scan" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128269; Ручной скан</a>
  <a href="/ema_diag" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128202; EMA Диагностика</a>
  <a href="/ema_stretch" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128201; Отрыв от EMA</a>
  <a href="/pump_match" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#127919; Pump Match</a>
  <a href="/weekly_ema_backtest" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128200; Бэктест EMA</a>
</div>

<h1>&#128225; Pump Radar <span style="font-size:12px;color:#8b949e;font-weight:normal">v__APP_VERSION__</span></h1>
<p style="color:#8b949e;font-size:13px;max-width:480px">Ищет часовые касания EMA(7/14/28) с гейтом по распродажной аномалии объёма — своя стратегия, откалиброванная по собственной диагностике (часовой ТФ показал реальный edge: avg MFE>avg MAE во всех 3 периодах, в отличие от недельного). Памп/дамп-детектор и Volume Peak Watcher отключены — код остался в файле, просто не запускается.</p>
<button onclick="openAlertSettings()" style="background:#21262d;border:1px solid #30363d">&#128276; Алерты</button>
<a href="/pump_match" style="text-decoration:none"><button style="background:#8250df">&#127919; Pump Match (подбор параметров отрисовки)</button></a>
<a href="/weekly_ema_backtest" style="text-decoration:none"><button style="background:#8250df">&#128202; Weekly EMA Backtest</button></a>
<a href="/ema_diag" style="text-decoration:none"><button style="background:#8250df">&#128300; EMA Diagnostics</button></a>
<a href="/ema_stretch" style="text-decoration:none"><button style="background:#8250df">&#128201; Отрыв от EMA</button></a>
<button onclick="startScreensaver()" style="background:#21262d;border:1px solid #30363d">&#128421; Скринсейвер</button>
<div id="screensaverOverlay" style="display:none;position:fixed;inset:0;background:#000;z-index:9999" onclick="stopScreensaver()"></div>
<div class="section-card" style="max-width:420px">
  <label style="display:block;font-size:13px;color:#8b949e">Отбор монет для поиска сигналов</label>
  <p style="font-size:11px;color:#6e7681;margin:4px 0 10px">Универсум — все фьючерсы USDT на бирже (~700 пар). Проходят порог — все и берутся, без обрезки по рангу (может быть и больше 100).</p>
  <div style="display:flex;flex-direction:column;gap:10px">
    <div>
      <label style="display:block;font-size:12px;color:#8b949e">Мин. объём за 24ч, $ (защита от неликвида/проскальзывания)</label>
      <input type="number" id="minVolInput" min="0" step="100000" style="width:140px;background:#0d1117;color:#c9d1d9;border:1px solid #30363d;border-radius:6px;padding:8px;font-size:14px">
    </div>
    <div>
      <label style="display:block;font-size:12px;color:#8b949e">Макс. спред, % (тоже защита от неликвида)</label>
      <input type="number" id="maxSpreadInput" min="0.01" max="10" step="0.01" style="width:100px;background:#0d1117;color:#c9d1d9;border:1px solid #30363d;border-radius:6px;padding:8px;font-size:14px">
    </div>
    <div>
      <label style="display:block;font-size:12px;color:#8b949e">Страховочный потолок (не основной фильтр — макс. монет на всякий случай)</label>
      <input type="number" id="maxCapInput" min="10" max="2000" style="width:100px;background:#0d1117;color:#c9d1d9;border:1px solid #30363d;border-radius:6px;padding:8px;font-size:14px">
    </div>
    <div style="display:flex;gap:8px;align-items:center">
      <button onclick="saveScanSettings()" style="margin:0">Сохранить</button>
      <span id="scanSettingsMsg" style="font-size:12px;color:#8b949e"></span>
    </div>
  </div>
  <p style="font-size:11px;color:#6e7681;margin:10px 0 0">Монеты без достаточной недельной/дневной истории (новые листинги) автоматически пропускаются при поиске EMA-сигналов, даже если прошли порог ликвидности — перепроверяется само раз в 3 дня.</p>
  <div style="display:flex;gap:8px;margin-top:8px;align-items:center;flex-wrap:wrap">
    <input type="text" id="recheckSymbolInput" placeholder="SYMBOL_USDT (пусто = все)" style="width:170px;background:#0d1117;color:#c9d1d9;border:1px solid #30363d;border-radius:6px;padding:8px;font-size:13px">
    <button onclick="recheckHistory()" style="margin:0;background:#21262d;border:1px solid #30363d">Перепроверить сейчас</button>
    <span id="recheckMsg" style="font-size:12px;color:#8b949e"></span>
  </div>
</div>

<div class="section-card" style="max-width:420px">
  <label style="display:flex;gap:8px;align-items:center;font-size:13px;color:#c9d1d9;cursor:pointer">
    <input type="checkbox" id="slowImpulseToggle" onchange="toggleFlag('slow_impulse_enabled','slowImpulseMsg')" style="width:16px;height:16px">
    Часовые EMA-сигналы (slow_impulse) включены
  </label>
  <p style="font-size:11px;color:#6e7681;margin:6px 0 0">Выключатель на уже существующие сигналы — если выключить, новые часовые касания EMA(7/14/28) не ищутся и алерты не шлются (уже открытые сделки резолвер по-прежнему доводит до конца).</p>
  <span id="slowImpulseMsg" style="font-size:12px;color:#8b949e"></span>
</div>

<div class="section-card" style="max-width:420px">
  <label style="display:flex;gap:8px;align-items:center;font-size:13px;color:#c9d1d9;cursor:pointer">
    <input type="checkbox" id="stretchTopAlertToggle" onchange="toggleFlag('stretch_top_alert_enabled','stretchTopAlertMsg')" style="width:16px;height:16px">
    Алерт по топ-связке "Отрыв от EMA" в Telegram
  </label>
  <p style="font-size:11px;color:#6e7681;margin:6px 0 0">Каждый скан заново считает текущий рейтинг связок ТФ×EMA по среднему откату (см. /ema_stretch) и шлёт алерт, если новый отрыв совпал с лидером рейтинга (не меньше 10 оценённых событий, иначе рано делать выводы). Лидер не зафиксирован навсегда — по мере накопления данных может смениться.</p>
  <span id="stretchTopAlertMsg" style="font-size:12px;color:#8b949e"></span>
</div>

<div id="status"></div>

<div id="alertModal">
  <div class="card">
    <h3 style="margin-top:0">&#128276; Алерты (Telegram / ntfy)</h3>
    <label>Telegram bot token</label>
    <input type="text" id="alTgToken" placeholder="123456:AAаа...">
    <label>Telegram chat id</label>
    <input type="text" id="alTgChat" placeholder="-1001234567890">
    <label>ntfy.sh URL (необязательно, запасной канал)</label>
    <input type="text" id="alNtfyUrl" placeholder="https://ntfy.sh/my-topic">
    <div class="row">
      <button onclick="closeAlertSettings()" style="background:#21262d;border:1px solid #30363d">Отмена</button>
      <div>
        <button onclick="testAlert()" style="background:#1f6feb">Тест</button>
        <button onclick="saveAlertSettings()">Сохранить</button>
      </div>
    </div>
    <div id="alertMsg" style="margin-top:8px;font-size:12px;color:#f85149"></div>
  </div>
</div>

<div class="section-card">
  <div class="section-head">
    <h3>&#128204; Касания EMA7/14/28 (slow_impulse)</h3>
    <div><a href="/weekly_ema_export" style="text-decoration:none"><button style="background:#21262d;border:1px solid #30363d;margin-right:6px">&#128190;</button></a><button onclick="clearHistory('weekly_ema')" class="btn-danger-sm">Очистить</button></div>
  </div>
  <div id="sourceStatsBox" style="font-size:12px;color:#8b949e;margin-bottom:8px"></div>
  <div id="weeklyEmaSummary" class="summary-line">пока не было срабатываний</div>
  <div class="table-scroll">
    <table id="weeklyEma"><thead><tr><th>Монета</th><th>ТФ</th><th>EMA</th><th>Цена</th><th>Вход</th><th>Стоп</th><th>Тейк</th><th>Исход</th><th>Расст. (ATR)</th><th>Подход</th><th>Тренд</th><th>Когда</th></tr></thead><tbody></tbody></table>
  </div>
</div>

<div class="section-card">
  <details id="logDetails">
    <summary><h3 style="display:inline">&#128221; Логи</h3></summary>
    <div class="card log-box" id="logBox" style="margin-top:8px"></div>
  </details>
</div>

<script>
function openAlertSettings(){ document.getElementById('alertModal').style.display='flex'; loadAlertSettings(); }
function closeAlertSettings(){ document.getElementById('alertModal').style.display='none'; }
async function loadAlertSettings(){
  try{
    const r = await fetch('/alert_cfg'); const d = await r.json();
    document.getElementById('alTgToken').value = d.tg_token || '';
    document.getElementById('alTgChat').value  = d.tg_chat  || '';
    document.getElementById('alNtfyUrl').value = d.ntfy_url || '';
  }catch(e){}
}
async function saveAlertSettings(){
  const msg = document.getElementById('alertMsg'); msg.innerText = '';
  const payload = {
    tg_token: document.getElementById('alTgToken').value.trim(),
    tg_chat:  document.getElementById('alTgChat').value.trim(),
    ntfy_url: document.getElementById('alNtfyUrl').value.trim(),
  };
  const r = await fetch('/alert_cfg', {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify(payload)});
  const d = await r.json();
  if(!d.ok){ msg.innerText = d.msg || 'Ошибка сохранения'; return; }
  closeAlertSettings();
}
async function saveAlertSettingsSync(){
  const payload = {
    tg_token: document.getElementById('alTgToken').value.trim(),
    tg_chat:  document.getElementById('alTgChat').value.trim(),
    ntfy_url: document.getElementById('alNtfyUrl').value.trim(),
  };
  const r = await fetch('/alert_cfg', {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify(payload)});
  return r.json();
}
async function testAlert(){
  const msg = document.getElementById('alertMsg'); msg.style.color = '#8b949e'; msg.innerText = 'Отправляю...';
  const saved = await saveAlertSettingsSync();
  if(!saved.ok){ msg.style.color = '#f85149'; msg.innerText = saved.msg || 'Ошибка сохранения'; return; }
  const r = await fetch('/alert_test', {method:'POST'}); const d = await r.json();
  msg.style.color = d.ok ? '#3fb950' : '#f85149';
  msg.innerText = d.ok ? '✅ Тестовое сообщение отправлено' : ('Ошибка: ' + (d.error||''));
}

async function loadScanSettings(){
  try{
    const r = await fetch('/scan_settings'); const d = await r.json();
    document.getElementById('minVolInput').value = d.min_volume_usd;
    document.getElementById('maxSpreadInput').value = d.max_spread_pct;
    document.getElementById('maxCapInput').value = d.max_symbols_cap;
    document.getElementById('slowImpulseToggle').checked = !!d.slow_impulse_enabled;
    document.getElementById('stretchTopAlertToggle').checked = !!d.stretch_top_alert_enabled;
  }catch(e){}
}
async function toggleFlag(field, msgElId){
  const msg = document.getElementById(msgElId);
  const checked = document.getElementById(field === 'slow_impulse_enabled' ? 'slowImpulseToggle' : 'stretchTopAlertToggle').checked;
  try{
    const r = await fetch('/scan_settings', {method:'POST', headers:{'Content-Type':'application/json'},
      body: JSON.stringify({[field]: checked})});
    const d = await r.json();
    msg.style.color = d.ok ? '#3fb950' : '#f85149';
    msg.innerText = d.ok ? (checked ? 'Включено' : 'Выключено') : (d.msg || 'Ошибка');
  }catch(e){ msg.style.color = '#f85149'; msg.innerText = 'Ошибка запроса'; }
}
async function saveScanSettings(){
  const msg = document.getElementById('scanSettingsMsg');
  const minVol = parseFloat(document.getElementById('minVolInput').value);
  const maxSpread = parseFloat(document.getElementById('maxSpreadInput').value);
  const maxCap = parseInt(document.getElementById('maxCapInput').value);
  if(isNaN(minVol) || minVol < 0){ msg.style.color = '#f85149'; msg.innerText = 'Мин. объём: введи число ≥ 0'; return; }
  if(isNaN(maxSpread) || maxSpread < 0.01 || maxSpread > 10){ msg.style.color = '#f85149'; msg.innerText = 'Спред: введи число от 0.01 до 10'; return; }
  if(isNaN(maxCap) || maxCap < 10 || maxCap > 2000){ msg.style.color = '#f85149'; msg.innerText = 'Потолок: введи число от 10 до 2000'; return; }
  try{
    const r = await fetch('/scan_settings', {method:'POST', headers:{'Content-Type':'application/json'},
      body: JSON.stringify({min_volume_usd: minVol, max_spread_pct: maxSpread, max_symbols_cap: maxCap})});
    const d = await r.json();
    msg.style.color = d.ok ? '#3fb950' : '#f85149';
    msg.innerText = d.ok ? 'Сохранено — применится со следующего цикла сканирования' : (d.msg || 'Ошибка');
  }catch(e){ msg.style.color = '#f85149'; msg.innerText = 'Ошибка запроса'; }
}
loadScanSettings();

async function recheckHistory(){
  const msg = document.getElementById('recheckMsg');
  const symbol = document.getElementById('recheckSymbolInput').value.trim();
  try{
    const r = await fetch('/recheck_history', {method:'POST', headers:{'Content-Type':'application/json'},
      body: JSON.stringify(symbol ? {symbol} : {})});
    const d = await r.json();
    msg.style.color = d.ok ? '#3fb950' : '#f85149';
    msg.innerText = d.ok ? `Сброшено записей: ${d.reset} — перепроверится на следующем скане` : (d.msg || 'Ошибка');
  }catch(e){ msg.style.color = '#f85149'; msg.innerText = 'Ошибка запроса'; }
}

let _wakeLock = null;
function _requestFullscreenCompat(el){
  const fn = el.requestFullscreen || el.webkitRequestFullscreen || el.mozRequestFullScreen || el.msRequestFullscreen;
  return fn ? fn.call(el) : Promise.reject(new Error('Fullscreen API недоступен'));
}
function _exitFullscreenCompat(){
  const fn = document.exitFullscreen || document.webkitExitFullscreen || document.mozCancelFullScreen || document.msExitFullscreen;
  return (document.fullscreenElement || document.webkitFullscreenElement) && fn ? fn.call(document) : Promise.resolve();
}
async function startScreensaver(){
  document.getElementById('screensaverOverlay').style.display = 'block';
  try{
    await _requestFullscreenCompat(document.documentElement);
  }catch(e){
    // не критично -- чёрный экран всё равно покажется, просто адресная
    // строка/статус-бар браузера останутся видны
  }
  try{
    if('wakeLock' in navigator){
      _wakeLock = await navigator.wakeLock.request('screen');
    }
  }catch(e){
    // не критично -- чёрный экран всё равно покажется, просто без гарантии от блокировки
  }
}
function stopScreensaver(){
  document.getElementById('screensaverOverlay').style.display = 'none';
  if(_wakeLock){ _wakeLock.release().catch(()=>{}); _wakeLock = null; }
  _exitFullscreenCompat().catch(()=>{});
}
document.addEventListener('visibilitychange', () => {
  // некоторые браузеры сами снимают wake lock при уходе со страницы —
  // если вернулись, а скринсейвер всё ещё показан, переберём лок заново
  if(document.visibilityState === 'visible' && document.getElementById('screensaverOverlay').style.display === 'block' && !_wakeLock && 'wakeLock' in navigator){
    navigator.wakeLock.request('screen').then(wl => { _wakeLock = wl; }).catch(()=>{});
  }
});
['fullscreenchange','webkitfullscreenchange','mozfullscreenchange','MSFullscreenChange'].forEach(evt => {
  document.addEventListener(evt, () => {
    // если полноэкранный режим сняли СНАРУЖИ (жест/кнопка назад системы),
    // а не через stopScreensaver() -- держать состояние в синхроне, иначе
    // wake lock продолжит висеть, а оверлей — нет
    const isFs = document.fullscreenElement || document.webkitFullscreenElement;
    if(!isFs && document.getElementById('screensaverOverlay').style.display === 'block'){
      stopScreensaver();
    }
  });
});

function fmtAgo(ts){
  const s = Math.floor(Date.now()/1000) - ts;
  if(s < 3600) return Math.floor(s/60)+'м назад';
  if(s < 86400) return Math.floor(s/3600)+'ч назад';
  return Math.floor(s/86400)+'д назад';
}

function fmtTrack(it){
  const hasData = it.max_follow_pct !== undefined;
  if(!hasData) return '—';
  const follow = `<span style="color:#3fb950">+${it.max_follow_pct}%</span>`;
  const reverse = `<span style="color:#f85149">-${it.max_reverse_pct}%</span>`;
  const status = it.track_done ? '' : ' <span style="color:#6e7681;font-size:11px">(ещё следим)</span>';
  return `дальше ${follow} / откат ${reverse}${status}`;
}

const CLEAR_ENDPOINTS = {
  weekly_ema: {url:'/weekly_ema_history_clear', label:'EMA-сигналов (включая открытые)', refresh:() => refreshWeeklyEma()},
};
async function clearHistory(kind){
  const cfg = CLEAR_ENDPOINTS[kind];
  if(!cfg) return;
  if(!confirm(`Точно очистить всю историю ${cfg.label}? Отменить нельзя.`)) return;
  try{
    await fetch(cfg.url, {method:'POST'});
    cfg.refresh();
  }catch(e){}
}

async function refreshWeeklyEma(){
  try{
    const r = await fetch('/weekly_ema_status'); const d = await r.json();
    const tbody = document.querySelector('#weeklyEma tbody'); tbody.innerHTML = '';
    const items = (d.recent || []).slice().reverse();
    const closed = items.filter(it => it.outcome === 'take' || it.outcome === 'stop');
    const takes = closed.filter(it => it.outcome === 'take').length;
    document.getElementById('weeklyEmaSummary').innerHTML = items.length
      ? `сработало касаний: <b>${items.length}</b>` + (closed.length
          ? ` &nbsp;·&nbsp; закрыто: <b>${closed.length}</b> (тейк ${takes} / стоп ${closed.length-takes}, винрейт ${Math.round(takes/closed.length*100)}%)`
          : ` &nbsp;·&nbsp; все ещё открыты`)
      : 'пока не было срабатываний';
    const ladderLabel = {up:'&#8593; вверх', down:'&#8595; вниз'};
    const biasLabel = {short:'<span style="color:#f85149">снизу вверх → SHORT</span>',
                        long:'<span style="color:#3fb950">сверху вниз → LONG</span>'};
    const tfLabel = {'1w':'Неделя', '1d':'День'};
    const outcomeLabel = {take:'<span style="color:#3fb950">&#9989; Тейк</span>',
                           stop:'<span style="color:#f85149">&#10060; Стоп</span>',
                           open:'<span style="color:#d29922">&#8987; Открыт</span>'};
    for(const it of items.slice(0,50)){
      const tr = document.createElement('tr');
      const outcome = outcomeLabel[it.outcome] || outcomeLabel.open;
      tr.innerHTML = `<td>${it.symbol}</td><td>${tfLabel[it.tf]||it.tf||'—'}</td><td>EMA${it.period}</td><td>${it.price}</td><td>${it.entry??'—'}</td><td style="color:#f85149">${it.stop??'—'}</td><td style="color:#3fb950">${it.take??'—'}</td><td>${outcome}</td><td>${it.dist_atr}</td><td>${biasLabel[it.classic_bias]||'—'}</td><td>${ladderLabel[it.ladder]||'—'}</td><td>${fmtAgo(it.ts)}</td>`;
      tbody.appendChild(tr);
    }
  }catch(e){}
}

let pumpLogsTotal = 0;
async function pollPumpLogs(){
  try{
    const r = await fetch('/ema_logs', {cache:'no-store'});
    const d = await r.json();
    const dropped = d.logs_dropped || 0;
    const totalNow = dropped + (d.logs||[]).length;
    const newFrom = Math.max(0, pumpLogsTotal - dropped);
    const newLogs = (d.logs||[]).slice(newFrom);
    const lb = document.getElementById('logBox');
    const wasOpen = document.getElementById('logDetails').open;
    const atBottom = !wasOpen || (lb.scrollTop + lb.clientHeight >= lb.scrollHeight - 4);
    newLogs.forEach(l => {
      const div = document.createElement('div');
      div.className = 'log-line';
      div.innerHTML = `<span style="color:#555">[${l.ts}]</span> ${l.msg}`;
      lb.appendChild(div);
    });
    while(lb.children.length > 300) lb.removeChild(lb.firstChild);
    if(newLogs.length && atBottom) lb.scrollTop = lb.scrollHeight;
    pumpLogsTotal = totalNow;
  }catch(e){}
}

pollPumpLogs(); setInterval(pollPumpLogs, 4000);
async function loadSourceStats(){
  try{
    const r = await fetch('/weekly_ema_source_stats'); const d = await r.json();
    const box = document.getElementById('sourceStatsBox');
    const stats = d.stats || [];
    if(!stats.length){ box.innerHTML = ''; return; }
    box.innerHTML = 'По источнику: ' + stats.map(s => {
      const wr = s.win_rate_pct != null ? `, винрейт ${s.win_rate_pct}%` : '';
      return `<b>${s.source}</b> — ${s.total} (тейк ${s.take}/стоп ${s.stop}/открыто ${s.open}/без входа ${s.no_fill||0}${wr})`;
    }).join(' &nbsp;·&nbsp; ');
  }catch(e){}
}
refreshWeeklyEma(); setInterval(refreshWeeklyEma, 15000);
loadSourceStats(); setInterval(loadSourceStats, 15000);
</script></body></html>"""


EMA_HTML_PAGE = """<!DOCTYPE html>
<html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Pump Radar</title>
<style>
body{background:#0d1117;color:#c9d1d9;font-family:system-ui,sans-serif;margin:0;padding:16px}
h1{font-size:20px}
h3{font-size:15px;margin:0}
button{background:#238636;color:#fff;border:0;padding:10px 16px;border-radius:6px;font-size:14px;margin:6px 6px 6px 0;cursor:pointer}
table{width:100%;border-collapse:collapse;margin-top:0;font-size:13px}
th,td{padding:6px 8px;text-align:left;border-bottom:1px solid #21262d;white-space:nowrap}
th{color:#8b949e;position:sticky;top:0;background:#161b22}
tbody tr:hover{background:#1c2128}
tr.row-tp{background:rgba(63,185,80,.06)}
tr.row-sl{background:rgba(248,81,73,.06)}
.long{color:#3fb950}.short{color:#f85149}
.tp{color:#3fb950}.sl{color:#f85149}.open{color:#8b949e}
#status{color:#8b949e;font-size:13px;margin-top:8px}
.livebadge{background:#1f6feb;color:#fff;font-size:10px;padding:1px 5px;border-radius:3px;margin-left:4px}
#atBox{background:#161b22;border:1px solid #30363d;border-radius:8px;padding:10px 14px;margin-top:10px;font-size:13px}
#atModal,#alertModal{display:none;position:fixed;inset:0;background:rgba(0,0,0,.6);z-index:50;align-items:center;justify-content:center}
#atModal .card,#alertModal .card{background:#161b22;border:1px solid #30363d;border-radius:10px;padding:20px;width:min(420px,92vw)}
#atModal label,#alertModal label{display:block;margin-top:12px;font-size:13px;color:#8b949e}
#atModal input[type=number],#alertModal input[type=number],#atModal input[type=text],#alertModal input[type=text]{width:100%;background:#0d1117;color:#c9d1d9;border:1px solid #30363d;border-radius:6px;padding:8px;margin-top:4px;font-size:14px;box-sizing:border-box}
#atModal .row,#alertModal .row{display:flex;justify-content:space-between;align-items:center;margin-top:12px}
#atModal hr{border:0;border-top:1px solid #30363d;margin:16px 0 4px}
.switch{position:relative;width:44px;height:24px}
.switch input{opacity:0;width:0;height:0}
.slider{position:absolute;inset:0;background:#30363d;border-radius:24px;cursor:pointer;transition:.2s}
.slider:before{content:"";position:absolute;height:18px;width:18px;left:3px;bottom:3px;background:#fff;border-radius:50%;transition:.2s}
input:checked + .slider{background:#238636}
input:checked + .slider:before{transform:translateX(20px)}
.section-card{background:#161b22;border:1px solid #30363d;border-radius:10px;padding:14px;margin-top:14px}
.section-head{display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:8px}
.btn-danger-sm{background:#3a1414;border:1px solid #f85149;color:#f85149;padding:4px 10px;font-size:12px;border-radius:6px;margin:0}
.summary-line{font-size:13px;color:#8b949e;margin-top:6px}
.summary-line b{color:#c9d1d9}
.filter-row{display:flex;gap:6px;margin-top:10px;flex-wrap:wrap}
.filter-btn{background:#21262d;border:1px solid #30363d;color:#8b949e;padding:4px 12px;font-size:12px;margin:0;border-radius:14px;cursor:pointer}
.filter-btn.active{background:#1f6feb;border-color:#1f6feb;color:#fff}
.table-scroll{overflow-x:auto;margin-top:10px;border-radius:6px}
.status-pill{display:inline-block;padding:2px 8px;border-radius:10px;font-size:11px;font-weight:600;white-space:nowrap}
.status-pill.tp{background:#0d2818;color:#3fb950;border:1px solid #23643a}
.status-pill.sl{background:#2a0f0f;color:#f85149;border:1px solid #6e2323}
.status-pill.open{background:#2a2410;color:#d29922;border:1px solid #6e5a1f}
.dur{font-size:11px;color:#6e7681;margin-top:2px}
.pager{display:flex;align-items:center;justify-content:center;gap:10px;margin-top:10px;font-size:12px;color:#8b949e}
.pager button{background:#21262d;border:1px solid #30363d;color:#c9d1d9;padding:4px 12px;font-size:12px;margin:0;border-radius:6px}
.pager button:disabled{opacity:.35;cursor:default}
details summary{cursor:pointer;font-size:15px;list-style:none}
details summary::-webkit-details-marker{display:none}
details summary:before{content:"▸ ";color:#8b949e}
details[open] summary:before{content:"▾ "}
.log-box{background:#010409;border:1px solid #21262d;border-radius:4px;height:200px;overflow-y:auto;padding:6px;font-size:11px;font-family:monospace}
.log-line{padding:1px 0;border-bottom:1px solid #21262d}
@media (max-width:480px){
  body{padding:8px;font-size:13px}
  h1{font-size:17px}
  h3{font-size:14px}
  button{padding:8px 12px;font-size:13px;margin:4px 4px 4px 0}
  th,td{padding:4px 6px;font-size:12px}
  .section-card{padding:10px;margin-top:10px}
  #atBox{padding:8px 10px;font-size:12px}
  .filter-btn{padding:3px 9px;font-size:11px}
  .status-pill{padding:1px 6px;font-size:10px}
}
</style></head><body>
<h1>&#128225; Pump Radar <span style="font-size:12px;color:#8b949e;font-weight:normal">v__APP_VERSION__</span></h1>
<button onclick="startScan()">Запустить скан EMA-досье (топ-50)</button>
<button onclick="openSettings()" style="background:#21262d;border:1px solid #30363d">&#9881;&#65039; Автоторговля</button>
<button onclick="openAlertSettings()" style="background:#21262d;border:1px solid #30363d">&#128276; Алерты</button>
<a href="/pump_match" style="text-decoration:none"><button style="background:#8250df">&#127919; Pump Match (подбор параметров)</button></a>
<div id="status"></div>
<div id="atBox"></div>

<div id="atModal">
  <div class="card">
    <h3 style="margin-top:0">&#9881;&#65039; Автоторговля EMA (Gate.io)</h3>
    <div class="row">
      <span>Включена</span>
      <label class="switch"><input type="checkbox" id="atEnabled"><span class="slider"></span></label>
    </div>
    <label>Маржа на ОДИН вход, % от депозита</label>
    <input type="number" id="atPositionPct" step="0.5" min="0.1" max="100">
    <label>Risk %, влияет на плечо (плечо = risk% / SL%)</label>
    <input type="number" id="atRiskPct" step="0.5" min="0.1" max="100">
    <label>Максимум одновременных позиций (пусто = без ограничений)</label>
    <input type="number" id="atMaxConcurrent" step="1" min="1">
    <label>Потолок маржи при форс-минимуме (% депозита)</label>
    <input type="number" id="atMaxForcedMarginPct" step="1" min="0.1" max="100">
    <label>Потолок форс-минимума по кратности номинала (×)</label>
    <input type="number" id="atForcedSizeMaxMultiple" step="0.5" min="1" max="20">
    <p style="font-size:12px;color:#8b949e;margin:2px 0 0">Если 1 мин. контракт требует больше этого потолка ИЛИ больше чем в N раз номинальной маржи — сигнал пропускается, а не открывается ценой почти всего депозита.</p>
    <hr>
    <label style="color:#c9d1d9;font-weight:bold">&#128273; Gate.io API ключи</label>
    <p style="font-size:12px;color:#8b949e;margin:2px 0 0">Нужны для реальных ордеров. Оставьте пустыми, чтобы не менять сохранённые. Оба поля заполняются вместе.</p>
    <label>API key</label>
    <input type="text" id="atGateKey" placeholder="">
    <label>API secret</label>
    <input type="text" id="atGateSecret" placeholder="">
    <div class="row">
      <button onclick="closeSettings()" style="background:#21262d;border:1px solid #30363d">Отмена</button>
      <button onclick="saveSettings()">Сохранить</button>
    </div>
    <div id="atMsg" style="margin-top:8px;font-size:12px;color:#f85149"></div>
  </div>
</div>

<div id="alertModal" style="display:none;position:fixed;inset:0;background:rgba(0,0,0,.6);z-index:50;align-items:center;justify-content:center">
  <div class="card">
    <h3 style="margin-top:0">&#128276; Алерты (Telegram / ntfy)</h3>
    <label>Telegram bot token</label>
    <input type="text" id="alTgToken" placeholder="123456:AAаа...">
    <label>Telegram chat id</label>
    <input type="text" id="alTgChat" placeholder="-1001234567890">
    <label>ntfy.sh URL (необязательно, запасной канал)</label>
    <input type="text" id="alNtfyUrl" placeholder="https://ntfy.sh/my-topic">
    <div class="row">
      <button onclick="closeAlertSettings()" style="background:#21262d;border:1px solid #30363d">Отмена</button>
      <div>
        <button onclick="testAlert()" style="background:#1f6feb">Тест</button>
        <button onclick="saveAlertSettings()">Сохранить</button>
      </div>
    </div>
    <div id="alertMsg" style="margin-top:8px;font-size:12px;color:#f85149"></div>
  </div>
</div>

<div class="section-card">
  <details id="logDetails">
    <summary><h3 style="display:inline">&#128221; Логи</h3></summary>
    <div class="card log-box" id="logBox" style="margin-top:8px"></div>
  </details>
</div>

<div class="section-card">
  <h3>&#128293; Живые пампы (Pump Radar)</h3>
  <div id="pumpSummary" class="summary-line">пока не было срабатываний</div>
  <div class="table-scroll">
    <table id="pumps"><thead><tr><th>Монета</th><th>%</th><th>База → сейчас</th><th>Когда</th></tr></thead><tbody></tbody></table>
  </div>
</div>

<div class="section-card">
  <h3>&#128276; Живые EMA-инверт сигналы</h3>
  <div class="table-scroll">
    <table id="live"><thead><tr><th>Монета</th><th>ТФ</th><th>EMA</th><th>Направление</th><th>Цена</th><th>SL</th><th>TP</th><th>Bounce rate</th></tr></thead><tbody></tbody></table>
  </div>
</div>

<div class="section-card">
  <div class="section-head">
    <h3>&#128203; История EMA-сигналов</h3>
    <button onclick="clearHistory()" class="btn-danger-sm">Очистить историю</button>
  </div>
  <div id="histSummary" class="summary-line"></div>
  <div class="filter-row" id="histFilterRow">
    <button class="filter-btn active" data-f="all" onclick="setHistFilter('all')">Все</button>
    <button class="filter-btn" data-f="open" onclick="setHistFilter('open')">&#8987; Открытые</button>
    <button class="filter-btn" data-f="tp" onclick="setHistFilter('tp')">&#9989; TP</button>
    <button class="filter-btn" data-f="sl" onclick="setHistFilter('sl')">&#10060; SL</button>
  </div>
  <div class="table-scroll">
    <table id="history"><thead><tr><th>Монета</th><th>ТФ</th><th>EMA</th><th>Направление</th><th>Вход</th><th>SL</th><th>TP</th><th>Статус</th><th>Когда</th></tr></thead><tbody></tbody></table>
  </div>
  <div class="pager" id="historyPager"></div>
</div>

<details id="dossierBlock" class="section-card">
<summary>Досье по монетам (лучшая EMA на каждом ТФ, включая недельный) — <span id="dossierCount">0</span> строк</summary>
<div class="table-scroll">
  <table id="dossier"><thead><tr><th>Монета</th><th>Взлёт</th><th>ТФ</th><th>Лучшая EMA</th><th>Bounce rate</th></tr></thead><tbody></tbody></table>
</div>
<div class="pager" id="dossierPager"></div>
</details>
<script>
async function startScan(){
  const r = await fetch('/ema_dossier_start', {method:'POST'}); const d = await r.json();
  document.getElementById('status').innerText = d.msg;
}
async function clearHistory(){
  if(!confirm('Точно очистить всю историю сигналов? Открытые сделки тоже сотрутся, отменить нельзя.')) return;
  await fetch('/ema_signal_history_clear', {method:'POST'});
  refresh();
}
async function openSettings(){
  document.getElementById('atModal').style.display='flex';
  document.getElementById('atEnabled').disabled = true;
  await loadSettingsIntoModal();
  document.getElementById('atEnabled').disabled = false;
}
function closeSettings(){ document.getElementById('atModal').style.display='none'; }
async function loadSettingsIntoModal(){
  try{
    const r = await fetch('/ema_auto_trade_status'); const d = await r.json();
    document.getElementById('atEnabled').checked = !!d.enabled;
    document.getElementById('atPositionPct').value = d.position_pct;
    document.getElementById('atRiskPct').value = d.risk_pct;
    document.getElementById('atMaxConcurrent').value = d.max_concurrent ?? '';
    document.getElementById('atMaxForcedMarginPct').value = d.max_forced_margin_pct ?? 20;
    document.getElementById('atForcedSizeMaxMultiple').value = d.forced_size_max_multiple ?? 3;
  }catch(e){}
  try{
    const rg = await fetch('/gate_cfg'); const dg = await rg.json();
    document.getElementById('atGateKey').value = '';
    document.getElementById('atGateSecret').value = '';
    document.getElementById('atGateKey').placeholder = dg.has_key ? (dg.gate_key + ' (сохранён)') : 'API key';
    document.getElementById('atGateSecret').placeholder = dg.has_key ? '*** (сохранён)' : 'API secret';
  }catch(e){}
}
async function saveSettings(){
  const msg = document.getElementById('atMsg'); msg.innerText = '';
  const gateKey = document.getElementById('atGateKey').value.trim();
  const gateSecret = document.getElementById('atGateSecret').value.trim();
  if((gateKey && !gateSecret) || (!gateKey && gateSecret)){
    msg.innerText = 'Введите и ключ, и секрет Gate.io вместе (или оставьте оба пустыми)';
    return;
  }
  if(gateKey && gateSecret){
    const rg = await fetch('/gate_cfg', {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({gate_key:gateKey, gate_secret:gateSecret})});
    const dg = await rg.json();
    if(!dg.ok){ msg.innerText = 'Ошибка сохранения ключей Gate.io'; return; }
  }
  const payload = {
    enabled: document.getElementById('atEnabled').checked,
    position_pct: parseFloat(document.getElementById('atPositionPct').value),
    risk_pct: parseFloat(document.getElementById('atRiskPct').value),
    max_concurrent: document.getElementById('atMaxConcurrent').value || null,
    max_forced_margin_pct: parseFloat(document.getElementById('atMaxForcedMarginPct').value),
    forced_size_max_multiple: parseFloat(document.getElementById('atForcedSizeMaxMultiple').value),
  };
  const r = await fetch('/ema_auto_trade_settings', {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify(payload)});
  const d = await r.json();
  if(!d.ok){ msg.innerText = d.msg || 'Ошибка сохранения'; return; }
  closeSettings();
  refreshAutoTradeBox();
}
async function closeLivePosition(sym){
  if(!confirm(`Закрыть реальную позицию ${sym} маркетом прямо сейчас?`)) return;
  await fetch('/ema_auto_trade_close', {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({symbol: sym})});
  refreshAutoTradeBox();
}

function openAlertSettings(){ document.getElementById('alertModal').style.display='flex'; loadAlertSettings(); }
function closeAlertSettings(){ document.getElementById('alertModal').style.display='none'; }
async function loadAlertSettings(){
  try{
    const r = await fetch('/alert_cfg'); const d = await r.json();
    document.getElementById('alTgToken').value = d.tg_token || '';
    document.getElementById('alTgChat').value  = d.tg_chat  || '';
    document.getElementById('alNtfyUrl').value = d.ntfy_url || '';
  }catch(e){}
}
async function saveAlertSettings(){
  const msg = document.getElementById('alertMsg'); msg.innerText = '';
  const payload = {
    tg_token: document.getElementById('alTgToken').value.trim(),
    tg_chat:  document.getElementById('alTgChat').value.trim(),
    ntfy_url: document.getElementById('alNtfyUrl').value.trim(),
  };
  const r = await fetch('/alert_cfg', {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify(payload)});
  const d = await r.json();
  if(!d.ok){ msg.innerText = d.msg || 'Ошибка сохранения'; return; }
  closeAlertSettings();
}
async function saveAlertSettingsSync(){
  const payload = {
    tg_token: document.getElementById('alTgToken').value.trim(),
    tg_chat:  document.getElementById('alTgChat').value.trim(),
    ntfy_url: document.getElementById('alNtfyUrl').value.trim(),
  };
  const r = await fetch('/alert_cfg', {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify(payload)});
  return r.json();
}
async function testAlert(){
  const msg = document.getElementById('alertMsg'); msg.style.color = '#8b949e'; msg.innerText = 'Отправляю...';
  const saved = await saveAlertSettingsSync();
  if(!saved.ok){ msg.style.color = '#f85149'; msg.innerText = saved.msg || 'Ошибка сохранения'; return; }
  const r = await fetch('/alert_test', {method:'POST'}); const d = await r.json();
  msg.style.color = d.ok ? '#3fb950' : '#f85149';
  msg.innerText = d.ok ? '✅ Тестовое сообщение отправлено' : ('Ошибка: ' + (d.error||''));
}

async function refreshAutoTradeBox(){
  try{
    const r = await fetch('/ema_auto_trade_status'); const d = await r.json();
    const box = document.getElementById('atBox');
    const stateTxt = d.enabled ? '🟢 включена' : '⚪ выключена';
    let rows = (d.live_positions||[]).map(p => {
      const cls = p.dir === 'long' ? 'long' : 'short';
      return `<tr><td>${p.symbol}</td><td class="${cls}">${p.dir.toUpperCase()}</td><td>${p.entry}</td><td>${p.sl}</td><td>${p.tp}</td><td>${p.size ?? ''}</td><td>${p.leverage ?? ''}×</td><td>${p.notional ?? ''}USDT</td>`
        + `<td><button style="margin:0;padding:3px 8px;font-size:11px;background:#3a1414;border:1px solid #f85149;color:#f85149" onclick="closeLivePosition('${p.symbol}')">Закрыть</button></td></tr>`;
    }).join('');
    box.innerHTML = `<b>Автоторговля:</b> ${stateTxt} &nbsp;|&nbsp; `
      + `маржа/вход ${d.position_pct}% &nbsp;|&nbsp; risk ${d.risk_pct}% &nbsp;|&nbsp; `
      + `потолок форс-маржи ${d.max_forced_margin_pct}% &nbsp;|&nbsp; `
      + `лимит позиций: ${d.max_concurrent ?? 'без ограничений'} &nbsp;|&nbsp; `
      + `открыто сейчас: <b>${d.live_count}</b>`
      + (d.balance != null ? ` &nbsp;|&nbsp; баланс: ${d.balance.toFixed(2)}USDT` : '')
      + (!d.gate_configured ? ` <span style="color:#f85149">— Gate.io ключи не настроены (/gate_cfg)</span>` : '')
      + (rows ? `<div class="table-scroll"><table style="margin-top:8px"><thead><tr><th>Монета</th><th>Напр.</th><th>Вход</th><th>SL</th><th>TP</th><th>Size</th><th>Плечо</th><th>~USDT</th><th></th></tr></thead><tbody>${rows}</tbody></table></div>` : '');
  }catch(e){}
}
function fmtAgo(ts){
  const s = Math.floor(Date.now()/1000) - ts;
  if(s < 3600) return Math.floor(s/60)+'м назад';
  if(s < 86400) return Math.floor(s/3600)+'ч назад';
  return Math.floor(s/86400)+'д назад';
}
function fmtDuration(s){
  if(s < 60) return s+'с';
  if(s < 3600) return Math.floor(s/60)+'м';
  if(s < 86400) return Math.floor(s/3600)+'ч '+Math.floor((s%3600)/60)+'м';
  return Math.floor(s/86400)+'д '+Math.floor((s%86400)/3600)+'ч';
}
function renderPager(elId, page, totalPages, onChange){
  const el = document.getElementById(elId);
  if(totalPages <= 1){ el.innerHTML = ''; return; }
  el.innerHTML = `<button id="${elId}_prev" ${page<=0?'disabled':''}>&#8249; Назад</button>`
    + `<span>стр. ${page+1} из ${totalPages}</span>`
    + `<button id="${elId}_next" ${page>=totalPages-1?'disabled':''}>Вперёд &#8250;</button>`;
  document.getElementById(elId+'_prev').onclick = () => onChange(page-1);
  document.getElementById(elId+'_next').onclick = () => onChange(page+1);
}

const HIST_PAGE_SIZE = 15;
let histFilter = 'all', histPage = 0, histAllItems = [];
function setHistFilter(f){
  histFilter = f; histPage = 0;
  document.querySelectorAll('#histFilterRow .filter-btn').forEach(b => b.classList.toggle('active', b.dataset.f === f));
  renderHistoryTable();
}
function renderHistoryTable(){
  const filtered = histFilter === 'all' ? histAllItems : histAllItems.filter(it => it.status === histFilter);
  const totalPages = Math.max(1, Math.ceil(filtered.length / HIST_PAGE_SIZE));
  if(histPage >= totalPages) histPage = totalPages - 1;
  if(histPage < 0) histPage = 0;
  const pageItems = filtered.slice(histPage*HIST_PAGE_SIZE, histPage*HIST_PAGE_SIZE + HIST_PAGE_SIZE);
  const tbody3 = document.querySelector('#history tbody'); tbody3.innerHTML = '';
  const statusLabel = {open:'&#9203; Открыт', tp:'&#9989; TP', sl:'&#10060; SL'};
  for(const it of pageItems){
    const tr = document.createElement('tr');
    const dcls = it.dir === 'long' ? 'long' : 'short';
    const liveBadge = it.live ? '<span class="livebadge">LIVE</span>' : '';
    let pctStr = '', whenCell = fmtAgo(it.opened_at);
    if((it.status === 'tp' || it.status === 'sl') && it.close_price != null){
      const pct = it.dir === 'long'
        ? (it.close_price - it.price) / it.price * 100
        : (it.price - it.close_price) / it.price * 100;
      pctStr = ' ' + (pct >= 0 ? '+' : '') + pct.toFixed(2) + '%';
      tr.classList.add('row-' + it.status);
      if(it.closed_at) whenCell += `<div class="dur">длилась ${fmtDuration(it.closed_at - it.opened_at)}</div>`;
    }
    const badge = `<span class="status-pill ${it.status}">${statusLabel[it.status]||it.status}${pctStr}</span>${liveBadge}`;
    tr.innerHTML = `<td>${it.symbol}</td><td>${it.tf}</td><td>EMA${it.ema_period}</td><td class="${dcls}">${it.dir.toUpperCase()}</td><td>${it.price}</td><td>${it.sl}</td><td>${it.tp}</td><td>${badge}</td><td>${whenCell}</td>`;
    tbody3.appendChild(tr);
  }
  renderPager('historyPager', histPage, totalPages, p => { histPage = p; renderHistoryTable(); });
}

const DOSSIER_PAGE_SIZE = 20;
let dossierPage = 0, dossierAllRows = [];
function renderDossierTable(){
  const totalPages = Math.max(1, Math.ceil(dossierAllRows.length / DOSSIER_PAGE_SIZE));
  if(dossierPage >= totalPages) dossierPage = totalPages - 1;
  if(dossierPage < 0) dossierPage = 0;
  const pageRows = dossierAllRows.slice(dossierPage*DOSSIER_PAGE_SIZE, dossierPage*DOSSIER_PAGE_SIZE + DOSSIER_PAGE_SIZE);
  const tbody = document.querySelector('#dossier tbody'); tbody.innerHTML = '';
  for(const row of pageRows){
    const tr = document.createElement('tr');
    tr.innerHTML = `<td>${row.sym}</td><td>${row.pumpLabel}</td><td>${row.tf}</td><td>EMA${row.ema}</td><td>${(row.rate*100).toFixed(0)}%</td>`;
    tbody.appendChild(tr);
  }
  renderPager('dossierPager', dossierPage, totalPages, p => { dossierPage = p; renderDossierTable(); });
}

async function refreshPumps(){
  try{
    const r = await fetch('/pump_status'); const d = await r.json();
    const tbody = document.querySelector('#pumps tbody'); tbody.innerHTML = '';
    const items = (d.recent || []).slice().reverse();
    document.getElementById('pumpSummary').innerHTML = items.length
      ? `отслеживается монет: <b>${d.tracked||0}</b> &nbsp;·&nbsp; сработало пампов: <b>${items.length}</b>`
      : `отслеживается монет: <b>${d.tracked||0}</b> &nbsp;·&nbsp; пока не было срабатываний`;
    for(const it of items.slice(0,30)){
      const tr = document.createElement('tr');
      tr.innerHTML = `<td>${it.symbol}</td><td class="long">+${it.pct}%</td><td>${it.base_price} -> ${it.last_price}</td><td>${fmtAgo(it.ts)}</td>`;
      tbody.appendChild(tr);
    }
  }catch(e){}
}

async function refresh(){
  try{
    const r2 = await fetch('/ema_live_signals'); const d2 = await r2.json();
    const tbody2 = document.querySelector('#live tbody'); tbody2.innerHTML = '';
    for(const [sym, sig] of Object.entries(d2.signals||{})){
      const tr = document.createElement('tr');
      const cls = sig.dir === 'long' ? 'long' : 'short';
      tr.innerHTML = `<td>${sym}</td><td>${sig.tf}</td><td>EMA${sig.ema_period}</td><td class="${cls}">${sig.dir.toUpperCase()}</td><td>${sig.price}</td><td>${sig.sl}</td><td>${sig.tp}</td><td>${(sig.bounce_rate*100).toFixed(0)}%</td>`;
      tbody2.appendChild(tr);
    }

    const r3 = await fetch('/ema_signal_history'); const d3 = await r3.json();
    document.getElementById('histSummary').innerHTML = d3.closed
      ? `закрыто <b>${d3.closed}</b> &nbsp;·&nbsp; <span class="tp">&#9989; TP ${d3.tp}</span> &nbsp;·&nbsp; <span class="sl">&#10060; SL ${d3.sl}</span> &nbsp;·&nbsp; винрейт <b>${d3.winrate}%</b>`
      : 'пока нет закрытых сигналов';
    histAllItems = d3.items || [];
    renderHistoryTable();

    const r = await fetch('/ema_dossier_status'); const d = await r.json();
    document.getElementById('status').innerText =
      `EMA-досье: ${d.status||'idle'}  |  ${d.done||0}/${d.total||0} монет`;
    const rows = [];
    for(const [sym, entry] of Object.entries(d.results||{})){
      if(entry.error){ continue; }
      const pumpLabel = entry.recent_pump ? ('&#128293; +'+entry.pump_pct+'%') : '';
      for(const [tf, tfd] of Object.entries(entry.by_tf||{})){
        if(!tfd.best_ema) continue;
        rows.push({sym, pumpLabel, tf, ema: tfd.best_ema, rate: tfd.best_bounce_rate});
      }
    }
    rows.sort((a,b) => b.rate - a.rate);
    document.getElementById('dossierCount').innerText = rows.length;
    dossierAllRows = rows;
    renderDossierTable();
  }catch(e){}
}
let emaLogsTotal = 0;
async function pollEmaLogs(){
  try{
    const r = await fetch('/ema_logs', {cache:'no-store'});
    const d = await r.json();
    const dropped = d.logs_dropped || 0;
    const totalNow = dropped + (d.logs||[]).length;
    const newFrom = Math.max(0, emaLogsTotal - dropped);
    const newLogs = (d.logs||[]).slice(newFrom);
    const lb = document.getElementById('logBox');
    const wasOpen = document.getElementById('logDetails').open;
    const atBottom = !wasOpen || (lb.scrollTop + lb.clientHeight >= lb.scrollHeight - 4);
    newLogs.forEach(l => {
      const div = document.createElement('div');
      div.className = 'log-line';
      div.innerHTML = `<span style="color:#555">[${l.ts}]</span> ${l.msg}`;
      lb.appendChild(div);
    });
    while(lb.children.length > 300) lb.removeChild(lb.firstChild);
    if(newLogs.length && atBottom) lb.scrollTop = lb.scrollHeight;
    emaLogsTotal = totalNow;
  }catch(e){}
}

pollEmaLogs(); setInterval(pollEmaLogs, 4000);
refresh(); setInterval(refresh, 5000);
refreshAutoTradeBox(); setInterval(refreshAutoTradeBox, 5000);
refreshPumps(); setInterval(refreshPumps, 8000);
</script></body></html>"""
# ─── конец live EMA-сигналов ────────────────────────────────────────────────
# ─── конец EMA-bounce dossier engine ────────────────────────────────────────
# ─── Telegram / ntfy ────────────────────────────────────────────────────────
_telegram_send_queue = queue.Queue()


def _telegram_sender_worker():
    """v0.30.33: реальная находка на реальных скринах — при частых сериях
    алертов (например, залп slow_impulse) каждый алерт спамил Telegram
    СВОИМ отдельным потоком ОДНОВРЕМЕННО — у Telegram Bot API реальный
    лимит около 1 сообщения/сек в один чат, часть сообщений тонула молча
    (429 Too Many Requests), пока ntfy (без такого жёсткого лимита)
    получал всё нормально — отсюда и расхождение между каналами, которое
    выглядело как баг кода, а на деле было лимитом скорости самого
    Telegram. Теперь отправка в Telegram идёт через ОДНУ очередь,
    последовательно, с паузой между сообщениями — не теряем сообщения
    из-за собственного всплеска. ntfy остаётся независимым и
    немедленным — у него такого жёсткого лимита нет."""
    while True:
        task = _telegram_send_queue.get()
        try:
            task()
        except Exception as e:
            olog(f"⚠ ошибка в очереди отправки Telegram: {_explain_error(e)}")
        time.sleep(1.1)   # с запасом чуть выше лимита Telegram ~1 сообщение/сек в один чат


def _send_alert(msg):
    """Отправка алерта в Telegram и/или ntfy. Telegram идёт через общую
    последовательную очередь (см. _telegram_sender_worker), ntfy —
    независимо и немедленно в своём потоке.
    v0.30.9: реальный баг — раньше generic-исключение (не сетевое, напр.
    из-за некорректной HTML-разметки в тексте) на ОДНОМ канале делало
    `break`, который выходил из ВСЕГО цикла попыток целиком — включая ещё
    не выполненную отправку в ДРУГОЙ канал в этой же попытке. Теперь
    каждый канал отслеживается и повторяется НЕЗАВИСИМО — сбой одного
    (сетевой или нет) больше не задевает другой."""
    def _do_telegram():
        if not (TG_TOKEN and TG_CHAT):
            return
        for attempt in range(1, 4):
            try:
                r = requests.post(
                    f"https://api.telegram.org/bot{TG_TOKEN}/sendMessage",
                    json={"chat_id": TG_CHAT, "text": msg, "parse_mode": "HTML"},
                    timeout=8)
                if r.ok:
                    return
                olog(f"⚠ TG алерт HTTP {r.status_code} (попытка {attempt}/3)")
            except (requests.exceptions.ConnectionError,
                    requests.exceptions.Timeout) as e:
                olog(f"⚠ TG алерт сеть (попытка {attempt}/3): {_explain_error(e)}")
            except Exception as e:
                olog(f"⚠ TG алерт: {_explain_error(e)} — не повторяю (не сетевая ошибка)")
                return
            if attempt < 3:
                time.sleep(5)

    def _do_ntfy():
        if not NTFY_URL:
            return
        for attempt in range(1, 4):
            try:
                requests.post(NTFY_URL, data=_strip_html_for_ntfy(msg).encode(), timeout=8)
                return
            except (requests.exceptions.ConnectionError,
                    requests.exceptions.Timeout) as e:
                olog(f"⚠ ntfy алерт сеть (попытка {attempt}/3): {_explain_error(e)}")
            except Exception as e:
                olog(f"⚠ ntfy алерт: {_explain_error(e)} — не повторяю (не сетевая ошибка)")
                return
            if attempt < 3:
                time.sleep(5)

    _telegram_send_queue.put(_do_telegram)
    threading.Thread(target=_do_ntfy, daemon=True).start()

def _png_encode_rgb(width, height, pixel_rows):
    """v0.30.62: по прямому запросу — "в других проектах картинка рисовалась
    без Pillow". Пишет валидный PNG вручную (сигнатура+IHDR+IDAT+IEND) —
    только struct и zlib (оба в стандартной библиотеке, всегда доступны,
    даже если PIL на Termux не ставится). pixel_rows — список строк
    сверху вниз, каждая bytes длиной width*3 (RGB без фильтр-байта, тот
    добавляется тут же)."""
    def chunk(tag, data):
        return (struct.pack(">I", len(data)) + tag + data +
                struct.pack(">I", zlib.crc32(tag + data) & 0xffffffff))
    sig = b"\x89PNG\r\n\x1a\n"
    ihdr = struct.pack(">IIBBBBB", width, height, 8, 2, 0, 0, 0)
    raw = bytearray()
    for row in pixel_rows:
        raw.append(0)   # filter type: None
        raw += row
    idat = zlib.compress(bytes(raw), 6)
    return sig + chunk(b"IHDR", ihdr) + chunk(b"IDAT", idat) + chunk(b"IEND", b"")


def _render_signal_chart_png_stdlib(candles, sig, sym, tf):
    """Без-Pillow версия _render_signal_chart_png — тот же смысл (свечи +
    линия входа + линия цели/EMA), но рисует пиксели вручную в bytearray
    и кодирует PNG сам. Без текста на картинке (нет доступной растровой
    шрифтовой библиотеки без PIL) — все числа и так есть в подписи
    (caption) сообщения, дублировать их пиксельным шрифтом не критично."""
    try:
        if not candles:
            return None
        entry_i = sig.get("entry_i")
        if entry_i is None or not (0 <= entry_i < len(candles)):
            entry_i = len(candles) - 1
        lo = max(0, entry_i - 50)
        hi = min(len(candles), entry_i + 16)
        window = candles[lo:hi]
        if len(window) < 2:
            return None

        def _g(c, *keys):
            for k in keys:
                if k in c: return c[k]
            return 0

        highs = [_g(c, "high", "h") for c in window]
        lows = [_g(c, "low", "l") for c in window]
        entry, tp, sl = sig.get("entry"), sig.get("tp"), sig.get("sl")
        vals = highs + lows + [v for v in (entry, tp, sl) if v]
        vmax, vmin = max(vals), min(vals)
        if vmax <= vmin:
            vmax = vmin + max(abs(vmin) * 0.001, 1e-9)

        W, H = 720, 400
        pad_l, pad_r, pad_t, pad_b = 6, 6, 6, 6
        bg, grid, green, red, white = (13, 13, 15), (40, 40, 44), (8, 153, 129), (242, 54, 69), (235, 235, 235)

        buf = bytearray(bytes(bg) * (W * H))

        def fill_rect(x0, y0, x1, y1, color):
            x0 = max(0, min(W, int(x0))); x1 = max(0, min(W, int(x1)))
            y0 = max(0, min(H, int(y0))); y1 = max(0, min(H, int(y1)))
            if x1 <= x0 or y1 <= y0:
                return
            row = bytes(color) * (x1 - x0)
            stride = W * 3
            for y in range(y0, y1):
                off = y * stride + x0 * 3
                buf[off:off + len(row)] = row

        def y_of(v):
            return pad_t + (vmax - v) / (vmax - vmin) * (H - pad_t - pad_b)

        for gy in range(5):
            yy = pad_t + gy * (H - pad_t - pad_b) / 4
            fill_rect(pad_l, yy, W - pad_r, yy + 1, grid)

        n = len(window)
        cw = (W - pad_l - pad_r) / n
        for i, c in enumerate(window):
            o, h, l, cl = _g(c, "open", "o"), _g(c, "high", "h"), _g(c, "low", "l"), _g(c, "close", "c")
            x = pad_l + i * cw
            w = max(cw * 0.6, 1)
            color = green if cl >= o else red
            xc = x + w / 2
            fill_rect(xc - 1, y_of(h), xc + 1, y_of(l), color)   # фитиль (тонкий прямоугольник вместо линии)
            ytop, ybot = sorted([y_of(o), y_of(cl)])
            if ybot - ytop < 1.5:
                ybot = ytop + 1.5
            fill_rect(x, ytop, x + w, ybot, color)

        def hline(v, color):
            if not v:
                return
            yy = y_of(v)
            xseg = pad_l
            while xseg < W - pad_r:
                fill_rect(xseg, yy, min(xseg + 8, W - pad_r), yy + 1, color)
                xseg += 14

        hline(entry, white)
        hline(tp, green)
        hline(sl, red)

        rows = [bytes(buf[y * W * 3:(y + 1) * W * 3]) for y in range(H)]
        return _png_encode_rgb(W, H, rows)
    except Exception as e:
        olog(f"⚠ Рендер скриншота (stdlib): {_explain_error(e)}")
        return None


def _render_signal_chart_png(candles, sig, sym, tf):
    """Скриншот сделки (EMA-сигнал) для Telegram — своя картинка, отдельная
    от Pump Radar (_render_pump_chart_png ниже)."""
    try:
        from PIL import Image, ImageDraw, ImageFont
    except ImportError:
        return None
    try:
        if not candles:
            return None
        entry_i = sig.get("entry_i")
        if entry_i is None or not (0 <= entry_i < len(candles)):
            entry_i = len(candles) - 1
        lo = max(0, entry_i - 50)
        hi = min(len(candles), entry_i + 16)
        window = candles[lo:hi]
        if len(window) < 2:
            return None

        def _g(c, *keys):
            for k in keys:
                if k in c: return c[k]
            return 0

        highs  = [_g(c, "high", "h") for c in window]
        lows   = [_g(c, "low", "l") for c in window]
        entry, sl, tp = sig.get("entry"), sig.get("sl"), sig.get("tp")
        vals = highs + lows + [v for v in (entry, sl, tp) if v]
        vmax, vmin = max(vals), min(vals)
        if vmax <= vmin:
            vmax = vmin + max(abs(vmin) * 0.001, 1e-9)

        W, H = 960, 540
        pad_l, pad_r, pad_t, pad_b = 16, 90, 46, 30
        bg, grid, txt = (13, 13, 15), (40, 40, 44), (220, 220, 220)
        green, red, white = (8, 153, 129), (242, 54, 69), (235, 235, 235)

        img = Image.new("RGB", (W, H), bg)
        d = ImageDraw.Draw(img)
        try:
            font = ImageFont.load_default()
        except Exception:
            font = None

        def y_of(v):
            return pad_t + (vmax - v) / (vmax - vmin) * (H - pad_t - pad_b)

        n = len(window)
        cw = (W - pad_l - pad_r) / n

        for gy in range(5):
            yy = pad_t + gy * (H - pad_t - pad_b) / 4
            d.line([(pad_l, yy), (W - pad_r, yy)], fill=grid, width=1)

        for i, c in enumerate(window):
            o, h, l, cl = _g(c, "open", "o"), _g(c, "high", "h"), _g(c, "low", "l"), _g(c, "close", "c")
            x = pad_l + i * cw
            w = max(cw * 0.6, 1)
            color = green if cl >= o else red
            xc = x + w / 2
            d.line([(xc, y_of(h)), (xc, y_of(l))], fill=color, width=2)
            ytop, ybot = sorted([y_of(o), y_of(cl)])
            if ybot - ytop < 1.5:
                ybot = ytop + 1.5
            d.rectangle([x, ytop, x + w, ybot], fill=color)

        def hline(v, color, label):
            if not v:
                return
            yy = y_of(v)
            xseg = pad_l
            while xseg < W - pad_r:
                d.line([(xseg, yy), (min(xseg + 8, W - pad_r), yy)], fill=color, width=1)
                xseg += 14
            d.text((W - pad_r + 4, yy - 6), label, fill=color, font=font)

        hline(entry, white, f"E {entry:.6g}" if entry else "")
        hline(sl,    red,   f"SL {sl:.6g}" if sl else "")
        hline(tp,    green, f"TP {tp:.6g}" if tp else "")

        title = f"{sym} {tf} — {str(sig.get('dir','')).upper()}"
        d.text((10, 8), title, fill=txt, font=font)

        buf = io.BytesIO()
        img.save(buf, format="PNG")
        return buf.getvalue()
    except Exception as e:
        olog(f"⚠ Рендер скриншота сигнала: {_explain_error(e)}")
        return None

def _send_alert_photo(png_bytes, caption):
    """Как _send_alert, но с картинкой (Telegram sendPhoto). Если png_bytes
    нет — тихо откатывается на обычный текстовый _send_alert.
    v0.30.9: реальный баг — раньше ntfy получал уведомление ТОЛЬКО если
    Telegram не настроен или отправка фото не удалась. Пока Telegram
    работал нормально — ntfy молчал по ВСЕМ картиночным алертам (а это
    почти все наши — памп/дамп, аномалия объёма, EMA-сигналы).
    v0.30.11: по прямому вопросу "а в ntfy нет картинки, это нормально?" —
    нет, не специально: раньше в ntfy уходил только текст. Теперь ntfy
    тоже получает настоящую картинку (через Filename-заголовок) — и
    делает это ПОЛНОСТЬЮ НЕЗАВИСИМО от исхода Telegram.
    v0.30.33: реальная находка на живых скринах — при частых сериях
    алертов Telegram-отправка каждого алерта шла СВОИМ отдельным потоком
    одновременно, упираясь в реальный лимит Telegram Bot API (~1
    сообщение/сек в один чат) — часть тонула молча, пока ntfy получал всё
    (у него такого жёсткого лимита нет). Теперь Telegram-часть идёт через
    ту же общую последовательную очередь, что и _send_alert."""
    if not png_bytes:
        _send_alert(caption)
        return

    def _do_telegram():
        if not (TG_TOKEN and TG_CHAT):
            return
        try:
            r = requests.post(
                f"https://api.telegram.org/bot{TG_TOKEN}/sendPhoto",
                data={"chat_id": TG_CHAT, "caption": caption, "parse_mode": "HTML"},
                files={"photo": ("signal.png", png_bytes, "image/png")},
                timeout=20)
            if not r.ok:
                olog(f"⚠ TG фото HTTP {r.status_code} — пробую текстом в TG")
                try:
                    requests.post(
                        f"https://api.telegram.org/bot{TG_TOKEN}/sendMessage",
                        json={"chat_id": TG_CHAT, "text": caption, "parse_mode": "HTML"},
                        timeout=8)
                except Exception as e2:
                    olog(f"⚠ TG текст (запасной вариант): {_explain_error(e2)}")
        except Exception as e:
            olog(f"⚠ TG фото: {_explain_error(e)} — пробую текстом в TG")
            try:
                requests.post(
                    f"https://api.telegram.org/bot{TG_TOKEN}/sendMessage",
                    json={"chat_id": TG_CHAT, "text": caption, "parse_mode": "HTML"},
                    timeout=8)
            except Exception as e2:
                olog(f"⚠ TG текст (запасной вариант): {_explain_error(e2)}")

    def _do_ntfy():
        if not NTFY_URL:
            return
        try:
            requests.post(NTFY_URL, data=png_bytes,
                          headers={"Filename": "chart.png"},
                          params={"message": _strip_html_for_ntfy(caption)[:4096]},
                          timeout=15)
        except Exception as e:
            olog(f"⚠ ntfy фото: {_explain_error(e)}")

    _telegram_send_queue.put(_do_telegram)
    threading.Thread(target=_do_ntfy, daemon=True).start()


# ─── v0.1.11: Live Pump Detector ────────────────────────────────────────────
# Отдельный, НЕЗАВИСИМЫЙ от EMA-инверт-сигналов детектор. Не ищет вход —
# только ловит резкий рост цены за короткое окно и шлёт в Telegram картинку
# в стиле стороннего скринера, который прислали как референс: чёрная линия
# цены с точками + красно-зелёная гистограмма объёма на заднем плане
# (зелёная свеча = объём зелёным, красная = красным), сплошная красная
# горизонтальная линия — базовая цена, от которой считается % пампа,
# синие штрихованные линии — сетка цены. Это первый этап будущего пайплайна
# "памп → откат → вход по EMA" — сам вход по EMA сюда сознательно не
# подключён, это отдельная следующая задача.
#
# Дешёвый источник цен: ОДИН запрос /futures/usdt/tickers на весь топ
# (как в _fetch_all_symbols) раз в PUMP_DETECT_POLL_SEC, а не по одному
# тикеру на монету — иначе скан топ-100 каждые 60с означал бы 100
# HTTP-запросов в минуту только на детектор, поверх всего остального.
# Скользящая история цен по каждой монете держится в памяти
# (_pump_price_history), 1m-свечи для самой картинки запрашиваются только
# В МОМЕНТ срабатывания алерта — не для всех монет постоянно.

PUMP_DETECT_TF            = "1m"
PUMP_CHART_WINDOW_MIN     = 120   # сколько минут истории показываем на картинке (как на референсе ~11:30-13:30)
# PUMP_MOVE_WINDOWS_MIN / PUMP_THRESHOLD_PCT_BY_WINDOW определены ниже, рядом
# с _pump_detect_check — несколько окон разной длины вместо одного (v0.20.0)
PUMP_DETECT_POLL_SEC      = 60    # как часто обновляем цены (один общий /tickers запрос)
PUMP_DETECT_COOLDOWN_SEC  = 30 * 60   # не повторять алерт по той же монете чаще, пока памп длится
PUMP_DETECT_HISTORY_FILE  = os.path.expanduser("~/pumpradar_pump_history.json")  # необязательный лог сработавших пампов
SCAN_SETTINGS_FILE = os.path.expanduser("~/pumpradar_scan_settings.json")
# v0.30.1: ПОЛНАЯ ПЕРЕРАБОТКА по прямому запросу — раньше "top_n" был
# рангом ("бери ровно N САМЫХ объёмных"), но на деле НИКОГДА не мог
# сработать больше 100, потому что _fetch_all_symbols() имела ДВА
# отдельных, зашитых прямо в код потолка (валид[:150] на входе,
# filtered[:100] на выходе) — настройка top_n к ним вообще не имела
# отношения, сколько её ни увеличивай. Теперь логика другая, как
# запрошено: универсум — ВСЕ фьючерсы USDT на бирже (~700 пар), фильтр —
# ПОРОГ ликвидности (объём/спред — защита от проскальзывания на
# неликвиде), не ранг "топ-N" — сколько монет пройдёт порог, столько и
# берём, пусть даже больше 100. max_symbols_cap — не основной фильтр, а
# страховочный потолок на случай, если почему-то пройдёт порога
# аномально много монет (защита от перегрузки), дефолт заведомо выше
# реального размера всей биржи, чтобы обычно не срабатывал вообще.
_scan_settings = {"min_volume_usd": 3_000_000, "max_spread_pct": 0.15, "max_symbols_cap": 800,
                  "slow_impulse_enabled": True, "stretch_top_alert_enabled": True}
_scan_settings_lock = threading.Lock()


def _load_scan_settings():
    global _scan_settings
    try:
        if os.path.exists(SCAN_SETTINGS_FILE):
            with open(SCAN_SETTINGS_FILE) as f:
                saved = json.load(f)
            with _scan_settings_lock:
                if "min_volume_usd" in saved:
                    _scan_settings["min_volume_usd"] = max(0, float(saved["min_volume_usd"]))
                if "max_spread_pct" in saved:
                    _scan_settings["max_spread_pct"] = max(0.01, min(10.0, float(saved["max_spread_pct"])))
                if "max_symbols_cap" in saved:
                    _scan_settings["max_symbols_cap"] = max(10, min(2000, int(saved["max_symbols_cap"])))
                if "slow_impulse_enabled" in saved:
                    _scan_settings["slow_impulse_enabled"] = bool(saved["slow_impulse_enabled"])
                if "stretch_top_alert_enabled" in saved:
                    _scan_settings["stretch_top_alert_enabled"] = bool(saved["stretch_top_alert_enabled"])
                # обратная совместимость со старым файлом настроек (top_n) —
                # если найден только он, трактуем как страховочный потолок
                elif "top_n" in saved:
                    _scan_settings["max_symbols_cap"] = max(10, min(2000, int(saved["top_n"])))
            olog(f"[scan_settings] загружено: {_scan_settings}")
    except Exception as e:
        olog(f"[scan_settings] ⚠ не смог загрузить: {_explain_error(e)}")


def _save_scan_settings(min_volume_usd=None, max_spread_pct=None, max_symbols_cap=None,
                        slow_impulse_enabled=None, stretch_top_alert_enabled=None):
    global _scan_settings
    with _scan_settings_lock:
        if min_volume_usd is not None:
            _scan_settings["min_volume_usd"] = max(0, float(min_volume_usd))
        if max_spread_pct is not None:
            _scan_settings["max_spread_pct"] = max(0.01, min(10.0, float(max_spread_pct)))
        if max_symbols_cap is not None:
            _scan_settings["max_symbols_cap"] = max(10, min(2000, int(max_symbols_cap)))
        if slow_impulse_enabled is not None:
            _scan_settings["slow_impulse_enabled"] = bool(slow_impulse_enabled)
        if stretch_top_alert_enabled is not None:
            _scan_settings["stretch_top_alert_enabled"] = bool(stretch_top_alert_enabled)
        snapshot = dict(_scan_settings)
    try:
        with open(SCAN_SETTINGS_FILE, "w") as f:
            json.dump(snapshot, f)
        olog(f"[scan_settings] сохранено и применено: {snapshot}")
    except Exception as e:
        olog(f"[scan_settings] ⚠ не смог сохранить: {_explain_error(e)}")


def _get_slow_impulse_enabled():
    with _scan_settings_lock:
        return _scan_settings["slow_impulse_enabled"]


def _get_stretch_top_alert_enabled():
    with _scan_settings_lock:
        return _scan_settings["stretch_top_alert_enabled"]


def _get_scan_top_n():
    """Сохранено под старым именем ради всех вызовов ниже по коду —
    теперь это СТРАХОВОЧНЫЙ ПОТОЛОК (max_symbols_cap), не ранг отбора."""
    with _scan_settings_lock:
        return _scan_settings["max_symbols_cap"]


def _get_liquidity_thresholds():
    with _scan_settings_lock:
        return _scan_settings["min_volume_usd"], _scan_settings["max_spread_pct"]


def _has_sufficient_ema_history(symbol):
    """По прямому запросу — монеты с маленькой историей (несколько дней
    или недель) фильтруются из поиска пампов/дампов/аномалий объёма:
    если дневной EMA7/14/28(D) физически невозможно посчитать (мало
    баров), то и полноценный сигнал со входом/стопом/тейком никогда не
    получится — не от чего оттолкнуться, чтобы зайти в сделку. Переиспользует
    _ema_touch_get_closed_ctx — тот же негативный кэш на 3 дня, что уже
    был для EMA-триггера, так что повторная проверка почти бесплатна
    после первого раза (не тратим сеть на одну и ту же свежую монету
    заново каждую минуту)."""
    try:
        return _ema_touch_get_closed_ctx(symbol, "1d") is not None
    except Exception:
        return True  # не блокируем при сетевой ошибке -- лучше пропустить лишний раз, чем упустить сигнал


def _get_tradeable_symbols():
    """Все ликвидные монеты, прошедшие порог объёма/спреда (настраиваемый,
    не топ-N по рангу — см. v0.30.1), отфильтрованные по достаточности
    истории — используется Volume Peak Watcher'ом, чтобы не гонять детект
    на монетах, где EMA-сигнал физически невозможен.
    v0.29.1: фильтр по истории теперь ДО применения любых дальнейших
    ограничений. v0.30.1: _fetch_all_symbols() уже сама применяет и порог
    ликвидности, и страховочный потолок — повторно обрезать здесь не
    нужно."""
    return [s for s in _fetch_all_symbols() if _has_sufficient_ema_history(s)]


_pump_detect_lock   = threading.Lock()

TRACK_CHECKPOINTS_MIN = [5, 15, 30, 60, 90, 120, 180, 240]   # v0.29.8, расширено под 4ч окно (было 90/120 макс.)

def _track_record_checkpoint(rec, now, follow_pct):
    """v0.29.8: по запросу — раньше хранился только ИТОГОВЫЙ максимум
    просадки/прибыли за всё окно отслеживания, без порядка событий: нельзя
    было понять, что случилось РАНЬШЕ — просадка, которая выбила бы стоп,
    или прибыль, которая дала бы тейк. Теперь при каждом проходе трек-лупа
    записывается чекпоинт (значение follow_pct — та же система знаков, что
    у max_follow_pct: положительное = цена продолжила в направлении
    исходного алерта, отрицательное = развернулась) на фиксированных
    отметках времени после сигнала (TRACK_CHECKPOINTS_MIN). Не весь путь
    целиком (было бы слишком много данных на запись), но достаточно точек,
    чтобы задним числом смоделировать разные комбинации стоп/тейк и
    понять реальный порядок событий, а не только крайние значения."""
    elapsed_min = (now - rec["ts"]) / 60.0
    checkpoints = rec.setdefault("checkpoints", {})
    for cp_min in TRACK_CHECKPOINTS_MIN:
        key = str(cp_min)
        if elapsed_min >= cp_min and key not in checkpoints:
            checkpoints[key] = round(follow_pct, 2)


_history_file_locks_master = threading.Lock()
_history_file_locks = {}   # file_path -> threading.Lock()

def _get_history_file_lock(file_path):
    """v0.29.5: реальный баг — _pump_dump_history_append (дозапись одной
    записи) и track-циклы (_vol_anomaly_track_loop и подобные — читают
    ВЕСЬ файл, обрабатывают, потом перезаписывают файл ЦЕЛИКОМ своей
    версией) работали на одном файле без всякой синхронизации между
    потоками. Если дозапись алерта попадала МЕЖДУ чтением и перезаписью
    трек-цикла — трек-цикл в конце перезаписывал файл своей УСТАРЕВШЕЙ
    версией (без только что добавленной записи), и она терялась целиком:
    алерт в Telegram ушёл, а в истории/реестре сработавших его нет —
    отсюда и "сигнал есть, а в списке нет", И повторное срабатывание того
    же всплеска (реестр _vol_peak_get_fired_keys читает ту же историю и
    не видел потерянную запись). Один замок на файл — оба типа операций
    (дозапись и полная перезапись) теперь взаимно исключают друг друга."""
    with _history_file_locks_master:
        if file_path not in _history_file_locks:
            _history_file_locks[file_path] = threading.Lock()
        return _history_file_locks[file_path]

_pump_price_history = {}   # symbol -> [(ts, price), ...] по возрастанию времени
_pump_detect_state  = {}   # symbol -> {"last_alert_ts": float, "last_pct": float}


def _pump_fetch_tickers_snapshot():
    """Один запрос ко всем тикерам Gate.io Futures USDT — даёт last price,
    24h-объём И bid/ask (для фильтра ликвидности) разом по всем контрактам.
    v0.30.3: реальная находка на вопрос "а не часто ли 60 секунд" — раньше
    (с v0.30.1) _pump_detect_loop ДОПОЛНИТЕЛЬНО вызывал _fetch_all_symbols(),
    который делает СВОЙ ЖЕ отдельный запрос на тот же самый /tickers —
    получалось два сетевых похода за цикл вместо одного, при нестабильном
    интернете лишний риск на пустом месте. Теперь bid/ask тоже здесь, и
    фильтр ликвидности считается по этому ЖЕ снапшоту (см.
    _filter_liquid_symbols_from_snapshot) — снова ровно один запрос за
    цикл, как и было изначально задумано."""
    try:
        r = requests.get(f"{GATE_API}/futures/usdt/tickers", timeout=20)
        if r.status_code != 200:
            return {}
        data = r.json()
        if not isinstance(data, list):
            return {}
        out = {}
        for t in data:
            if not isinstance(t, dict):
                continue
            contract = t.get("contract", "")
            if "_USDT" not in contract:
                continue
            if contract in GATE_XSTOCKS_SYMBOLS:
                continue
            try:
                px = float(t.get("last") or t.get("mark_price") or 0)
                vol = float(t.get("volume_24h_usd") or t.get("volume_24h_quote")
                             or t.get("volume_24h") or 0)
                bid = float(t.get("highest_bid") or 0)
                ask = float(t.get("lowest_ask") or 0)
            except (TypeError, ValueError):
                continue
            if px <= 0:
                continue
            out[contract] = {"price": px, "vol24": vol, "bid": bid, "ask": ask}
        return out
    except Exception as e:
        olog(f"[pump_detect] ошибка запроса tickers: {_explain_error(e)}")
        return {}


def _filter_liquid_symbols_from_snapshot(snapshot):
    """Тот же фильтр по ликвидности (объём/спред), что в _fetch_all_symbols,
    но работает на уже готовом снапшоте — не делает свой отдельный сетевой
    запрос. Используется _pump_detect_loop, чтобы не дублировать один и
    тот же /tickers дважды за цикл."""
    min_volume_usd, max_spread_pct = _get_liquidity_thresholds()
    qualifying = []
    for contract, t in snapshot.items():
        if t.get("vol24", 0) < min_volume_usd:
            continue
        bid, ask = t.get("bid", 0), t.get("ask", 0)
        if bid <= 0 or ask <= 0 or ask < bid:
            continue
        spread_pct = (ask - bid) / ((ask + bid) / 2) * 100
        if spread_pct <= max_spread_pct:
            qualifying.append(contract)
    return qualifying


def _pump_update_history(snapshot, top_symbols):
    """Дописывает текущую цену в скользящую историю каждой отслеживаемой
    монеты и обрезает всё, что старше PUMP_CHART_WINDOW_MIN (+запас).
    Монеты, выпавшие из топа по объёму, из истории удаляются — иначе
    словарь рос бы неограниченно по мере ротации топа за дни/недели."""
    now = time.time()
    cutoff = now - (PUMP_CHART_WINDOW_MIN + 15) * 60
    top_set = set(top_symbols)
    with _pump_detect_lock:
        for sym in top_symbols:
            info = snapshot.get(sym)
            if not info:
                continue
            hist = _pump_price_history.setdefault(sym, [])
            hist.append((now, info["price"]))
            while hist and hist[0][0] < cutoff:
                hist.pop(0)
        stale = [s for s in _pump_price_history if s not in top_set]
        for s in stale:
            del _pump_price_history[s]


# v0.20.0: по прямому запросу с реальными скринами — старое ОДНО окно
# (20 минут) ловило только резкие пампы. Медленный разгон (час-полтора,
# как на обоих присланных примерах) не давал 5%+ ни в одном 20-минутном
# сегменте, и алерт либо опаздывал к самому пику, либо не срабатывал
# вообще, пока движение не ускорялось резко под конец — а лучшая точка
# входа в шорт была РАНЬШЕ, там, где цена только-только пробила локальный
# уровень. Теперь несколько окон разной длины проверяются ОДНОВРЕМЕННО, у
# длинных — порог выше (иначе будут ловить обычную дневную волатильность,
# не памп). Проверяем от САМОГО ДЛИННОГО к короткому и берём первое
# сработавшее — так медленный разгон ловится максимально рано, как только
# его накопленное движение впервые пересекает порог для своего окна, а не
# только когда он уже разогнался и попал в короткое 20-минутное окно.
PUMP_MOVE_WINDOWS_MIN = [120, 60, 20]                  # от длинного к короткому
PUMP_THRESHOLD_PCT_BY_WINDOW = {120: 8.0, 60: 6.0, 20: 5.0}  # длиннее окно -> выше порог


def _pump_detect_check(symbol):
    """Возвращает {"base_price":..,"last_price":..,"pct":..,"window_min":..}
    если хотя бы ОДНО из PUMP_MOVE_WINDOWS_MIN окон показывает рост от
    своего минимума на PUMP_THRESHOLD_PCT_BY_WINDOW[окно]% и больше —
    иначе None. Проверка от длинного окна к короткому: медленный
    многочасовой разгон ловится максимально рано (как только пересёк ПОРОГ
    ДЛЯ СВОЕГО окна), а не только тогда, когда уже попал в короткое
    20-минутное окно ближе к пику."""
    now = time.time()
    with _pump_detect_lock:
        hist = list(_pump_price_history.get(symbol, ()))
    if len(hist) < 3:
        return None
    last_t, last_p = hist[-1]
    if last_p <= 0:
        return None
    for window_min in PUMP_MOVE_WINDOWS_MIN:
        move_cutoff = now - window_min * 60
        window = [(t, p) for t, p in hist if t >= move_cutoff]
        if len(window) < 3:
            continue
        base_t, base_p = min(window, key=lambda tp: tp[1])
        if base_p <= 0:
            continue
        pct = (last_p - base_p) / base_p * 100.0
        threshold = PUMP_THRESHOLD_PCT_BY_WINDOW[window_min]
        if pct >= threshold:
            return {"base_price": base_p, "last_price": last_p, "pct": round(pct, 2),
                    "window_min": window_min}
    return None


# v0.13.0: по прямому запросу — "эти пампы и есть триггер сигнала". Зеркало
# Pump Detector для ПАДЕНИЙ: та же скользящая история цен (_pump_price_history,
# общая — не тянем тикеры второй раз), просто ищем движение от МАКСИМУМА
# вниз, а не от минимума вверх. Даёт LONG-сторону (дамп в поддержку) так же,
# как памп даёт SHORT-сторону (памп в сопротивление).
DUMP_MOVE_WINDOWS_MIN = PUMP_MOVE_WINDOWS_MIN
DUMP_THRESHOLD_PCT_BY_WINDOW = PUMP_THRESHOLD_PCT_BY_WINDOW
DUMP_DETECT_COOLDOWN_SEC  = PUMP_DETECT_COOLDOWN_SEC
DUMP_DETECT_HISTORY_FILE  = os.path.expanduser("~/pumpradar_dump_history.json")
_dump_detect_state = {}   # symbol -> {"last_alert_ts": float, "last_pct": float}


def _dump_detect_check(symbol):
    """Зеркало _pump_detect_check (см. его докстринг про несколько окон) —
    только от МАКСИМУМА окна вниз, а не от минимума вверх."""
    now = time.time()
    with _pump_detect_lock:
        hist = list(_pump_price_history.get(symbol, ()))
    if len(hist) < 3:
        return None
    last_t, last_p = hist[-1]
    if last_p <= 0:
        return None
    for window_min in DUMP_MOVE_WINDOWS_MIN:
        move_cutoff = now - window_min * 60
        window = [(t, p) for t, p in hist if t >= move_cutoff]
        if len(window) < 3:
            continue
        base_t, base_p = max(window, key=lambda tp: tp[1])
        if base_p <= 0:
            continue
        pct = (base_p - last_p) / base_p * 100.0
        threshold = DUMP_THRESHOLD_PCT_BY_WINDOW[window_min]
        if pct >= threshold:
            return {"base_price": base_p, "last_price": last_p, "pct": round(pct, 2),
                    "window_min": window_min}
    return None


def _pump_or_dump_maybe_trigger_ema_signal(symbol, expected_bias, confidence_note=None, triggered_by=None):
    """После срабатывания пампа/дампа проверяет ОБА ТФ (неделя/день) на
    касание EMA7/14/28 — если памп/дамп долетел до уровня, это и есть
    полноценный сигнал (не просто уведомление о движении). expected_bias
    ("short" для пампа, "long" для дампа) — используется как sanity-check:
    сторона подхода естественно должна совпасть с направлением самого
    движения (only fire, если реально совпало — иначе это редкий
    пограничный случай рассинхрона между внутридневным движением и
    закрытой историей, лучше промолчать, чем прислать нелогичное
    сочетание направлений). confidence_note — необязательная пометка,
    добавляется в текст алерта как есть (см. вызов из _pump_fire_alert —
    v0.23.0: 120-минутное окно эмпирически даёт заметно худшие исходы для
    шорта, чем 20/60-минутное — см. changelog). triggered_by — v0.29.7:
    необязательный идентификатор источника (например, ключ записи Volume
    Peak Watcher) — сохраняется в итоговую запись EMA-сигнала, чтобы потом
    можно было честно связать "какой именно всплеск объёма привёл к
    какому именно полноценному сигналу со входом/стопом/тейком", а не
    только видеть, куда пошла цена в целом."""
    for tf in EMA_TOUCH_TIMEFRAMES:
        try:
            touches = _ema_touch_check_symbol(symbol, tf)
        except Exception as e:
            olog(f"[ema_trigger] {symbol} {tf}: ошибка проверки: {_explain_error(e)}")
            continue
        for touch in touches:
            if touch["classic_bias"] != expected_bias:
                continue
            key = f"{symbol}|{tf}|{touch['period']}"
            now = time.time()
            cooldown = WEEKLY_EMA_TOUCH_COOLDOWN_SEC if tf == "1w" else DAILY_EMA_TOUCH_COOLDOWN_SEC
            with _weekly_ema_touch_lock:
                last = _weekly_ema_touch_state.get(key, 0)
            if now - last < cooldown:
                continue
            _weekly_ema_fire_alert(touch, confidence_note=confidence_note, triggered_by=triggered_by)
            with _weekly_ema_touch_lock:
                _weekly_ema_touch_state[key] = now


PUMP_RENDER_PARAMS_FILE = os.path.expanduser("~/pumpradar_render_params.json")
# v0.2.0: параметры стиля отрисовки гистограммы теперь настраиваются, а не
# зашиты намертво — подбираются через /pump_match (см. секцию Pump Match
# ниже) и применяются здесь же, к РЕАЛЬНЫМ алертам, без перезапуска процесса.
# v0.2.1: режим "raw" убран — в нём зелёная и красная заливка строились из
# ОДНИХ И ТЕХ ЖЕ значений объёма и рисовались друг на друге, из-за чего
# верхний (зелёный) слой визуально полностью перекрывал красный — на живом
# скрине из /pump_match красного не было видно вообще, хотя score его
# почему-то оценивал прилично (сам критерий сравнивал профиль ПОСЛЕ
# рендера, где красный уже был перекрыт — вводил в заблуждение). Заменено
# на честную "dual_period": зелёная и красная линии — это ДВЕ РАЗНЫЕ EMA
# одного и того же сырого объёма (быстрая/медленная), они не совпадают по
# форме и обе видны.
# v0.2.2: "это EMA" была одна непроверенная подсказка (от третьей стороны),
# не факт. Добавлено поле smoothing_method — /pump_match теперь перебирает
# НЕСКОЛЬКО типов сглаживания (не только EMA), см. _pm_smooth ниже и
# PUMP_MATCH_GRID_SMOOTHING_METHODS.
_PUMP_PARAM_FIELDS = ("volume_mode", "source", "source_slow", "smoothing_method",
                      "ema_period", "ema_period_slow", "floor_frac")
_pump_render_params = {"volume_mode": "directional", "source": "volume", "source_slow": None,
                        "smoothing_method": "ema", "ema_period": 3, "ema_period_slow": None,
                        "floor_frac": 0.15}

def _load_pump_render_params():
    global _pump_render_params
    try:
        with open(PUMP_RENDER_PARAMS_FILE) as f:
            saved = json.load(f)
        _pump_render_params.update({k: saved[k] for k in _PUMP_PARAM_FIELDS if k in saved})
        olog(f"[pump_match] загружены сохранённые параметры рендера: {_pump_render_params}")
    except Exception:
        pass  # первый запуск — файла ещё нет, живём с дефолтами выше
    # v0.22.0: автор стороннего бота подтвердил напрямую (не по пикселям) —
    # красное = объём на продажу (red-свечи), зелёное по симметрии = объём
    # на покупку (green-свечи). Это ровно volume_mode="directional" +
    # source="volume", уже бывший дефолтом в коде — но если когда-то
    # запускался /pump_match (визуальный грид-серч по слепым гипотезам) и
    # он сохранил ДРУГОЙ режим (dual_period/dual_source/другой source), тот
    # файл имел приоритет и мог всё это время рисовать неверную гипотезу.
    # Форсируем подтверждённые поля здесь — применяется при каждом
    # перезапуске, не зависит от того, что лежит в сохранённом файле, не
    # потеряется при следующем обновлении с GitHub.
    if _pump_render_params.get("volume_mode") != "directional" or _pump_render_params.get("source") != "volume":
        olog(f"[pump_match] сохранённый режим рендера ({_pump_render_params.get('volume_mode')}/"
             f"{_pump_render_params.get('source')}) не совпадает с подтверждённым — "
             f"принудительно переключаем на directional/volume")
    _pump_render_params["volume_mode"] = "directional"
    _pump_render_params["source"] = "volume"
    _pump_render_params["source_slow"] = None

def _save_pump_render_params(params):
    global _pump_render_params
    _pump_render_params.update({k: params[k] for k in _PUMP_PARAM_FIELDS if k in params})
    # тот же форс, что и в _load_pump_render_params — подтверждённые
    # напрямую полу режим/источник не должны сбрасываться будущим
    # прогоном /pump_match (тот всё ещё слепо перебирает и это измерение)
    _pump_render_params["volume_mode"] = "directional"
    _pump_render_params["source"] = "volume"
    _pump_render_params["source_slow"] = None
    try:
        with open(PUMP_RENDER_PARAMS_FILE, "w") as f:
            json.dump(_pump_render_params, f)
        olog(f"[pump_match] новые параметры рендера сохранены и применены: {_pump_render_params}")
    except Exception as e:
        olog(f"[pump_match] ⚠ не смог сохранить параметры рендера: {_explain_error(e)}")


def _pm_smooth(arr, period, method="ema"):
    """Единая точка входа для всех проверяемых гипотез сглаживания —
    ВСЕ причинные (используют только прошлые и текущий бар, без
    заглядывания вперёд), проверено на синтетическом спайке отдельно.
      "none" — без сглаживания, сырые значения как есть.
      "sma"  — простое скользящее среднее (окно period баров).
      "ema"  — экспоненциальное (как и было раньше, alpha=2/(period+1)).
      "wma"  — линейно взвешенное (свежие бары весят больше, старые меньше).
      "rma"  — сглаживание Уайлдера (как в RSI/ATR, alpha=1/period) —
               медленнее EMA, "память" длиннее при том же period.
    Неизвестный method — fallback на "ema" (не должно случаться, т.к.
    вызывается только из сетки перебора с известными значениями)."""
    if not arr:
        return []
    if method == "none" or period <= 1:
        return list(arr)
    if method == "sma":
        out = []
        for i in range(len(arr)):
            lo = max(0, i - period + 1)
            window = arr[lo:i + 1]
            out.append(sum(window) / len(window))
        return out
    if method == "wma":
        out = []
        for i in range(len(arr)):
            lo = max(0, i - period + 1)
            window = arr[lo:i + 1]
            weights = list(range(1, len(window) + 1))
            out.append(sum(w * v for w, v in zip(weights, window)) / sum(weights))
        return out
    if method == "rma":
        alpha = 1.0 / period
        out = [arr[0]]
        for v in arr[1:]:
            out.append(out[-1] + alpha * (v - out[-1]))
        return out
    # "ema" (дефолт/fallback)
    k = 2.0 / (period + 1)
    out = [arr[0]]
    for v in arr[1:]:
        out.append(v * k + out[-1] * (1 - k))
    return out


# v0.4.0: мы НЕ знаем, что реально стоит за красной/зелёной волной на
# референсе — объём был лишь одной из догадок. Вместо того чтобы упираться
# в одну идею (объём, цена, RSI-подобное...), даём перебору НЕСКОЛЬКО
# независимых сырых рядов, которые вообще можно посчитать по OHLCV-свече, и
# пусть сравнение с эталоном само покажет, какой ближе — было именно это,
# посчитанное как-то, или что-то совсем другое, а мы так и не найдём.
PUMP_MATCH_GRID_SOURCES = ["volume", "price_change", "range", "upper_wick", "lower_wick", "body"]

def _pm_source_series(candles, source):
    """Сырой (несглаженный) ряд по каждой свече — строительный материал
    и для "directional" (разбивка по направлению свечи), и для
    "dual_period" (одно и то же, две скорости сглаживания), и для
    "dual_source" (зелёная и красная волна вообще из РАЗНЫХ рядов)."""
    if source == "volume":
        return [c.get("vol", 0) or 0 for c in candles]
    if source == "price_change":
        closes = [c["close"] for c in candles]
        return [abs(closes[i] - closes[i - 1]) if i > 0 else 0.0 for i in range(len(candles))]
    if source == "range":
        return [c["high"] - c["low"] for c in candles]
    if source == "upper_wick":
        return [c["high"] - max(c["open"], c["close"]) for c in candles]
    if source == "lower_wick":
        return [min(c["open"], c["close"]) - c["low"] for c in candles]
    if source == "body":
        return [abs(c["close"] - c["open"]) for c in candles]
    return [c.get("vol", 0) or 0 for c in candles]  # fallback


def _render_pump_chart_variant(candles, base_price, volume_mode="directional",
                                source="volume", source_slow=None,
                                smoothing_method="ema", ema_period=3,
                                ema_period_slow=None, floor_frac=0.15):
    """Параметризованная версия рендера — используется и живыми алертами
    (через _render_pump_chart_png ниже, с сохранёнными параметрами), и
    перебором /pump_match (с разными параметрами на кандидата). Требует
    Pillow — если не установлен, возвращает None.
      volume_mode: "directional" — один источник (source) разбит на
                   зелёный/красный по направлению свечи (bull/bear);
                   "dual_period" — обе линии из ОДНОГО источника, но с
                   РАЗНЫМИ периодами сглаживания (ema_period — быстрая/
                   зелёная, ema_period_slow — медленная/красная);
                   "dual_source" — зелёная и красная линии вообще из ДВУХ
                   РАЗНЫХ источников (source — зелёная, source_slow —
                   красная — то же поле переиспользовано, чтобы не плодить
                   параметры), с одним и тем же сглаживанием у обеих.
      source, source_slow: см. PUMP_MATCH_GRID_SOURCES / _pm_source_series —
                   "volume" (объём), "price_change" (величина изменения
                   цены закрытия), "range" (диапазон бара high-low),
                   "upper_wick"/"lower_wick" (тени свечи), "body" (тело).
                   Мы НЕ знаем, что из этого использует референс — поэтому
                   не привязываемся к одному, перебор проверяет все.
      smoothing_method: "none"/"sma"/"ema"/"wma"/"rma" — см. _pm_smooth.
      ema_period:  период сглаживания (быстрой линии для dual_*).
      ema_period_slow: период медленной линии для "dual_period"; для
                   "dual_source" не используется (обе линии — один период).
      floor_frac:  ось объёма НЕ от нуля — начинается с floor_frac*vmax,
                   поэтому заливка не пропадает полностью в затишье
                   (0 = ось от нуля, как в самой первой версии).
    """
    try:
        from PIL import Image, ImageDraw, ImageFont
    except ImportError:
        return None
    if not candles or len(candles) < 3:
        return None

    W, H = 960, 540
    pad_l, pad_r, pad_t, pad_b = 60, 60, 24, 40
    bg           = (255, 255, 255)
    grid_color   = (70, 70, 150)
    line_color   = (15, 15, 15)
    green        = (46, 160, 90)
    red          = (214, 58, 58)
    baseline_col = (206, 28, 28)
    text_color   = (50, 50, 50)

    closes = [c["close"] for c in candles]
    opens  = [c["open"] for c in candles]
    times  = [c["t"] for c in candles]
    n = len(candles)

    pmin = min(closes + [base_price])
    pmax = max(closes + [base_price])
    if pmax <= pmin:
        pmax = pmin + max(abs(pmin) * 0.001, 1e-9)

    if volume_mode == "dual_source":
        src_a = _pm_source_series(candles, source)
        src_b = _pm_source_series(candles, source_slow or source)
        green_s = _pm_smooth(src_a, ema_period, smoothing_method)
        red_s   = _pm_smooth(src_b, ema_period, smoothing_method)
    elif volume_mode == "dual_period":
        base_series = _pm_source_series(candles, source)
        slow_period = ema_period_slow if ema_period_slow else max(2, ema_period * 3)
        green_s = _pm_smooth(base_series, ema_period, smoothing_method)       # быстрая
        red_s   = _pm_smooth(base_series, slow_period, smoothing_method)      # медленная
    else:  # "directional"
        base_series = _pm_source_series(candles, source)
        green_raw = [base_series[i] if closes[i] >= opens[i] else 0.0 for i in range(n)]
        red_raw   = [base_series[i] if closes[i] <  opens[i] else 0.0 for i in range(n)]
        green_s = _pm_smooth(green_raw, ema_period, smoothing_method)
        red_s   = _pm_smooth(red_raw, ema_period, smoothing_method)
    vmax = max(max(green_s, default=1.0), max(red_s, default=1.0)) or 1.0
    axis_min = vmax * max(0.0, min(0.9, floor_frac))

    img = Image.new("RGB", (W, H), bg)
    try:
        font = ImageFont.load_default()
    except Exception:
        font = None

    plot_w = W - pad_l - pad_r
    plot_h = H - pad_t - pad_b

    def x_of(i):
        return pad_l + (i / (n - 1) * plot_w if n > 1 else 0)

    def y_price(v):
        return pad_t + (pmax - v) / (pmax - pmin) * plot_h

    def y_vol(v):
        vv = max(v, axis_min)
        denom = (vmax - axis_min) or 1.0
        return pad_t + (1 - (vv - axis_min) / denom) * plot_h

    floor_y = pad_t + plot_h

    d = ImageDraw.Draw(img)
    for k in range(5):
        v = pmin + (pmax - pmin) * k / 4.0
        yy = y_price(v)
        xx = pad_l
        while xx < W - pad_r:
            d.line([(xx, yy), (min(xx + 6, W - pad_r), yy)], fill=grid_color, width=1)
            xx += 10
        d.text((4, yy - 6), f"{v:.6g}", fill=text_color, font=font)

    # правая ось — шкала объёма (только подписи, без своей сетки поверх
    # левой — референс показывает именно такую пару осей: цена слева,
    # объём справа, обе цифрами, но сетка одна общая)
    for k in range(5):
        vv = axis_min + (vmax - axis_min) * k / 4.0
        yy = y_vol(vv)
        d.text((W - pad_r + 4, yy - 6), f"{vv:,.0f}".replace(",", " "), fill=text_color, font=font)

    overlay = Image.new("RGBA", (W, H), (0, 0, 0, 0))
    od = ImageDraw.Draw(overlay)
    for series, color in ((red_s, red), (green_s, green)):
        pts = [(x_of(i), y_vol(series[i])) for i in range(n)]
        poly = [(x_of(0), floor_y)] + pts + [(x_of(n - 1), floor_y)]
        od.polygon(poly, fill=color + (95,))
        od.line(pts, fill=color + (230,), width=2)
    img = Image.alpha_composite(img.convert("RGBA"), overlay).convert("RGB")
    d = ImageDraw.Draw(img)

    by = y_price(base_price)
    d.line([(pad_l, by), (W - pad_r, by)], fill=baseline_col, width=2)
    d.text((W - pad_r + 4, by - 6), f"{base_price:.6g}", fill=baseline_col, font=font)

    pts = [(x_of(i), y_price(closes[i])) for i in range(n)]
    d.line(pts, fill=line_color, width=2)
    for (xx, yy) in pts:
        d.ellipse([xx - 2, yy - 2, xx + 2, yy + 2], fill=line_color)

    step = max(1, n // 10)
    for i in range(0, n, step):
        xx = x_of(i)
        lbl = time.strftime("%H:%M", time.localtime(times[i]))
        d.text((xx - 14, H - pad_b + 6), lbl, fill=text_color, font=font)

    buf = io.BytesIO()
    img.save(buf, format="PNG")
    return buf.getvalue()


def _render_vol_anomaly_chart_png(candles, base_price, anomaly_t=None, signal_price=None):
    """Параллельный _render_pump_chart_variant специально под алерты
    Sell-Volume Anomaly Watcher — по прямому запросу "визуально отличать"
    от обычных памп/дамп картинок. Та же структура (белый фон, чёрная
    линия цены, синие пунктирные уровни, красная базовая линия), но:
      - заливка — это РЕАЛЬНЫЙ ряд объёма именно на red-свечах (то, что
        детектор реально меряет для z-score), не directional split
        зелёное/красное, как у пампа/дампа;
      - заливка ФИОЛЕТОВАЯ, не зелёно-красная — сразу видно другой тип
        алерта в списке уведомлений Telegram;
      - v0.29.3: по прямому запросу — САМ СТОЛБИК объёма всплеска, на
        который ориентируется детектор (anomaly_t), теперь закрашен
        ОТДЕЛЬНЫМ ЯРКИМ ЦВЕТОМ (алый), не сливается с обычными фиолетовыми
        столбиками — на графике с десятками похожих пиков раньше было
        невозможно на глаз понять, какой из них реально используется;
      - и ТОЧКА НА ЛИНИИ ЦЕНЫ, где сработал сигнал, теперь крупный зелёный
        маркер с чёрной обводкой, а не такая же мелкая точка, как все
        остальные — сразу видно, где именно на графике сигнал, без
        сравнения текста с осью.
      - v0.29.5: реальное несовпадение — маркер рисовался по ПОСЛЕДНЕЙ
        СВЕЧЕ из отдельного, чуть более позднего запроса свечей, а не по
        ТОЧНОЙ цене, что реально вызвала алерт (между проверкой цены и
        фетчем свечей для картинки — небольшой зазор по времени). Теперь
        signal_price передаётся явно из места принятия решения и
        используется и для маркера, и для масштаба цены — картинка
        показывает ровно ту цену, что в тексте алерта.
    """
    try:
        from PIL import Image, ImageDraw, ImageFont
    except ImportError:
        return None
    if not candles or len(candles) < 3:
        return None

    W, H = 960, 540
    pad_l, pad_r, pad_t, pad_b = 60, 60, 24, 40
    bg           = (255, 255, 255)
    grid_color   = (70, 70, 150)
    line_color   = (15, 15, 15)
    fill_color   = (142, 68, 201)     # фиолетовый — не зелёный/красный памп-детектора
    spike_color  = (220, 20, 20)      # алый — именно тот столбик-всплеск, на который ориентируемся
    signal_color = (30, 190, 90)      # зелёный — точка, где реально сработал сигнал
    baseline_col = (206, 28, 28)
    text_color   = (50, 50, 50)

    closes = [c["close"] for c in candles]
    opens  = [c["open"] for c in candles]
    times  = [c["t"] for c in candles]
    n = len(candles)

    price_extremes = closes + [base_price] + ([signal_price] if signal_price else [])
    pmin = min(price_extremes)
    pmax = max(price_extremes)
    if pmax <= pmin:
        pmax = pmin + max(abs(pmin) * 0.001, 1e-9)

    # ряд объёма именно на red-свечах (close < open), 0 на зелёных — ровно
    # то, что _detect_sell_volume_anomaly реально считает
    red_vol_raw = [(c.get("vol", 0) or 0) if closes[i] < opens[i] else 0.0
                   for i, c in enumerate(candles)]
    vmax = max(red_vol_raw, default=1.0) or 1.0

    anomaly_idx = None
    if anomaly_t is not None:
        anomaly_idx = min(range(n), key=lambda i: abs(times[i] - anomaly_t))

    img = Image.new("RGB", (W, H), bg)
    try:
        font = ImageFont.load_default()
    except Exception:
        font = None

    plot_w = W - pad_l - pad_r
    plot_h = H - pad_t - pad_b

    def x_of(i):
        return pad_l + (i / (n - 1) * plot_w if n > 1 else 0)

    def y_price(v):
        return pad_t + (pmax - v) / (pmax - pmin) * plot_h

    # v0.30.10 сделала отдельную суб-панель для объёма (нижние 35%), чтобы
    # устранить визуальное совпадение высот цены и объёма. v0.30.12: по
    # прямому запросу "сделай как у него" — сверил три свежих скрина
    # референса, у него цена и объём делят ОДНУ высоту графика (та же
    # независимая двойная шкала, что была у нас до v0.30.10). Возвращено
    # обратно для привычности при сравнении — совпадение по-прежнему
    # чисто визуальное (математическое следствие независимого
    # масштабирования обеих шкал 0->max в одной высоте), не значит
    # никакой реальной связи, просто теперь выглядит как у него.
    def y_vol(v):
        return pad_t + (1 - v / vmax) * plot_h

    floor_y = pad_t + plot_h

    d = ImageDraw.Draw(img)
    for k in range(5):
        v = pmin + (pmax - pmin) * k / 4.0
        yy = y_price(v)
        xx = pad_l
        while xx < W - pad_r:
            d.line([(xx, yy), (min(xx + 6, W - pad_r), yy)], fill=grid_color, width=1)
            xx += 10
        d.text((4, yy - 6), f"{v:.6g}", fill=text_color, font=font)

    for k in range(5):
        vv = vmax * k / 4.0
        yy = y_vol(vv)
        d.text((W - pad_r + 4, yy - 6), f"{vv:,.0f}".replace(",", " "), fill=text_color, font=font)

    overlay = Image.new("RGBA", (W, H), (0, 0, 0, 0))
    od = ImageDraw.Draw(overlay)
    pts = [(x_of(i), y_vol(red_vol_raw[i])) for i in range(n)]
    poly = [(x_of(0), floor_y)] + pts + [(x_of(n - 1), floor_y)]
    od.polygon(poly, fill=fill_color + (110,))
    od.line(pts, fill=fill_color + (230,), width=2)
    # v0.29.6: реальная жалоба — широкий непрозрачный прямоугольник от
    # высоты столбика (а она у крупных всплесков близка к максимуму окна,
    # то есть почти во весь график, раз оси цены/объёма делят одну и ту же
    # высоту) до самого низа выглядел как будто перекрывает весь график,
    # непонятно что за ним. Теперь — узкая (фиксированная ширина, не
    # растущая от того, сколько свечей в окне), полупрозрачная линия,
    # видно и её, и что под ней.
    if anomaly_idx is not None and red_vol_raw[anomaly_idx] > 0:
        bar_half_w = 3
        ax = x_of(anomaly_idx)
        ay = y_vol(red_vol_raw[anomaly_idx])
        od.rectangle([ax - bar_half_w, ay, ax + bar_half_w, floor_y], fill=spike_color + (140,))
        od.line([(ax, ay), (ax, floor_y)], fill=spike_color + (255,), width=1)
    img = Image.alpha_composite(img.convert("RGBA"), overlay).convert("RGB")
    d = ImageDraw.Draw(img)

    by = y_price(base_price)
    d.line([(pad_l, by), (W - pad_r, by)], fill=baseline_col, width=2)
    d.text((W - pad_r + 4, by - 6), f"{base_price:.6g}", fill=baseline_col, font=font)

    pts_price = [(x_of(i), y_price(closes[i])) for i in range(n)]
    d.line(pts_price, fill=line_color, width=2)
    for (xx, yy) in pts_price:
        d.ellipse([xx - 2, yy - 2, xx + 2, yy + 2], fill=line_color)

    # подпись под алым столбиком -- явно "ВСПЛЕСК", не полагаемся на цвет
    # в одиночку (на маленьком экране телефона оттенки бывает не различить)
    if anomaly_idx is not None:
        ax, ay = x_of(anomaly_idx), y_price(closes[anomaly_idx])
        d.text((ax - 18, min(ay - 22, H - pad_b - 30)), "ВСПЛЕСК", fill=spike_color, font=font)

    # точка, где реально сработал сигнал -- по ТОЧНОЙ цене из момента
    # принятия решения (signal_price), если она передана, а не по
    # последней свече из отдельного, чуть более позднего запроса свечей
    # (могли разойтись на пару тиков) — крупный зелёный маркер с тёмной
    # обводкой, чтобы не сравнивать текст алерта с осью на глаз.
    # v0.30.19: реальная путаница — из-за этого расхождения на пару тиков
    # между сигнальной ценой и последней свечой график иногда показывал
    # видимый разрыв между концом чёрной линии и зелёной точкой (выглядело
    # как будто точка "оторвана", хотя её позиция верна). Дорисовываем
    # соединительный отрезок, чтобы линия визуально доходила до маркера.
    sx = pts_price[-1][0]
    sy = y_price(signal_price) if signal_price else pts_price[-1][1]
    if signal_price and (sx, sy) != pts_price[-1]:
        d.line([pts_price[-1], (sx, sy)], fill=line_color, width=2)
    d.ellipse([sx - 4, sy - 4, sx + 4, sy + 4], fill=signal_color, outline=(20, 60, 30), width=2)
    label = "СИГНАЛ"
    label_x = min(sx - 20, W - pad_r - 55)
    d.text((label_x, sy - 24 if sy > pad_t + 20 else sy + 12), label, fill=signal_color, font=font)

    step = max(1, n // 10)
    for i in range(0, n, step):
        xx = x_of(i)
        lbl = time.strftime("%H:%M", time.localtime(times[i]))
        d.text((xx - 14, H - pad_b + 6), lbl, fill=text_color, font=font)

    buf = io.BytesIO()
    img.save(buf, format="PNG")
    return buf.getvalue()


def _render_pump_chart_png(candles, base_price):
    """Обёртка для живых алертов — рисует текущими сохранёнными параметрами
    (см. _pump_render_params / /pump_match). Сигнатура не меняется, чтобы
    не трогать _pump_fire_alert."""
    p = _pump_render_params
    return _render_pump_chart_variant(candles, base_price,
                                       volume_mode=p.get("volume_mode", "directional"),
                                       source=p.get("source", "volume"),
                                       source_slow=p.get("source_slow"),
                                       smoothing_method=p.get("smoothing_method", "ema"),
                                       ema_period=p.get("ema_period", 3),
                                       ema_period_slow=p.get("ema_period_slow"),
                                       floor_frac=p.get("floor_frac", 0.15))


PUMP_TRACK_WINDOW_SEC = 4 * 3600   # сколько времени после сигнала следим за ценой (по запросу — унифицировано с VOL_PEAK на 4ч)
PUMP_TRACK_POLL_SEC   = 60         # раз в минуту проверяем ещё не закрытые записи


def _pump_dump_history_append(file_path, rec):
    """Общая запись в историю пампов/дампов — атомарно (tmp + os.replace),
    не растим файл бесконечно (последние 200 записей). v0.29.5: обёрнуто
    в замок по файлу — см. _get_history_file_lock, защита от гонки с
    track-циклами, которые читают/перезаписывают тот же файл целиком."""
    lock = _get_history_file_lock(file_path)
    with lock:
        try:
            hist = []
            if os.path.exists(file_path):
                with open(file_path) as f:
                    hist = json.load(f)
            hist.append(rec)
            hist = hist[-200:]
            tmp_path = file_path + f".tmp{os.getpid()}"
            with open(tmp_path, "w") as f:
                json.dump(hist, f)
            os.replace(tmp_path, file_path)
        except Exception as e:
            olog(f"[pump_dump_track] ⚠ не смог записать {file_path}: {_explain_error(e)}")


def _pump_dump_history_save_all(file_path, hist):
    try:
        tmp_path = file_path + f".tmp{os.getpid()}"
        with open(tmp_path, "w") as f:
            json.dump(hist, f)
        os.replace(tmp_path, file_path)
    except Exception as e:
        olog(f"[pump_dump_track] ⚠ не смог сохранить {file_path}: {_explain_error(e)}")


def _pump_dump_track_loop():
    """Фоновый цикл: раз в PUMP_TRACK_POLL_SEC смотрит все ещё не
    "закрытые" (track_done=False) записи в истории пампов/дампов и
    обновляет максимальное движение цены В ТУ ЖЕ сторону, что и сигнал
    (max_follow_pct — "разогналось дальше") и В ОБРАТНУЮ (max_reverse_pct —
    "откатило назад") за PUMP_TRACK_WINDOW_SEC после срабатывания. У
    памп/дамп алертов нет явных стоп/тейк уровней (в отличие от EMA-
    сигналов) — тут просто трекинг движения цены, не гонка take-vs-stop."""
    time.sleep(40)
    while True:
        try:
            for file_path, kind in ((PUMP_DETECT_HISTORY_FILE, "pump"),
                                     (DUMP_DETECT_HISTORY_FILE, "dump")):
                lock = _get_history_file_lock(file_path)
                with lock:
                    try:
                        if not os.path.exists(file_path):
                            continue
                        with open(file_path) as f:
                            hist = json.load(f)
                    except Exception as e:
                        olog(f"[pump_dump_track] {file_path}: не смог прочитать: {_explain_error(e)}")
                        continue
                    now = time.time()
                    price_cache = {}
                    changed = False
                    for rec in hist:
                        if rec.get("track_done", True):   # старые записи без поля тоже не трогаем
                            continue
                        try:
                            sym = rec["symbol"]
                            if sym not in price_cache:
                                try:
                                    price_cache[sym] = _gate_get_price(sym)
                                except Exception as e:
                                    olog(f"[pump_dump_track] {sym}: ошибка получения цены: {_explain_error(e)}")
                                    price_cache[sym] = None
                            price = price_cache[sym]
                            if price and rec.get("last_price"):
                                pct = (price - rec["last_price"]) / rec["last_price"] * 100.0
                                if kind == "pump":
                                    follow, reverse = pct, -pct
                                else:
                                    follow, reverse = -pct, pct
                                rec["max_follow_pct"] = round(max(rec.get("max_follow_pct", 0.0), follow), 2)
                                rec["max_reverse_pct"] = round(max(rec.get("max_reverse_pct", 0.0), reverse), 2)
                                rec["last_tracked_price"] = price
                                rec["last_tracked_ts"] = int(now)
                                _track_record_checkpoint(rec, now, follow)
                                _maybe_fire_ema28w_reversal_alert(rec, sym, price, kind)
                                changed = True
                            if now >= rec.get("track_until", 0):
                                rec["track_done"] = True
                                changed = True
                        except Exception as e:
                            olog(f"[pump_dump_track] ⚠ пропущена битая запись "
                                 f"({rec.get('symbol','?')}): {_explain_error(e)}")
                            continue
                    if changed:
                        _pump_dump_history_save_all(file_path, hist)
        except Exception as e:
            olog(f"[pump_dump_track] ошибка цикла: {_explain_error(e)}")
        time.sleep(PUMP_TRACK_POLL_SEC)


def _pump_fire_alert(symbol, res):
    """Тянет 1m-свечи за последнее PUMP_CHART_WINDOW_MIN окно, рисует
    картинку и шлёт в Telegram/ntfy (через уже существующий
    _send_alert_photo — если Pillow недоступен/рендер не удался, тот сам
    откатится на обычный текстовый _send_alert, ничего не потеряется).
    После обычного алерта — проверяет, не долетел ли этот памп до уровня
    EMA (см. _pump_or_dump_maybe_trigger_ema_signal); если да, следом
    уходит ещё и полноценный сигнал со входом/стопом/тейком. Плюс ставит
    запись на трекинг движения цены после сигнала (см. _pump_dump_track_loop)."""
    candles = []
    try:
        days = max(1, math.ceil(PUMP_CHART_WINDOW_MIN * 60 / 86400) + 1)
        raw = _fetch_candles(symbol, PUMP_DETECT_TF, days)
        cutoff = time.time() - PUMP_CHART_WINDOW_MIN * 60
        candles = [c for c in raw if c["t"] >= cutoff]
    except Exception as e:
        olog(f"[pump_detect] {symbol}: не смог получить свечи для графика: {_explain_error(e)}")

    png = None
    try:
        png = _render_pump_chart_png(candles, res["base_price"])
    except Exception as e:
        olog(f"[pump_detect] {symbol}: ошибка рендера графика: {_explain_error(e)}")

    caption = (f"<b>{symbol}</b>\n"
               f"Pump: {res['pct']}% (окно {res.get('window_min','?')}м)\n"
               f"{_fmt_px(res['base_price'])} -> {_fmt_px(res['last_price'])}")
    if PUMP_DUMP_ALERTS_ENABLED:
        _send_alert_photo(png, caption)
        olog(f"[pump_detect] 🔥 {symbol}: памп {res['pct']}% "
             f"({res['base_price']:.6g} → {res['last_price']:.6g}) — алерт отправлен")
    else:
        olog(f"[pump_detect] 🔥 {symbol}: памп {res['pct']}% "
             f"({res['base_price']:.6g} → {res['last_price']:.6g}) — уведомление отключено "
             f"(PUMP_DUMP_ALERTS_ENABLED=False), детект и EMA-проверка продолжаются")

    now = time.time()
    rec = {"ts": int(now), "symbol": symbol, "pct": res["pct"],
           "base_price": res["base_price"], "last_price": res["last_price"],
           "window_min": res.get("window_min"),
           "kind": "pump", "track_until": now + PUMP_TRACK_WINDOW_SEC,
           "track_done": False, "max_follow_pct": 0.0, "max_reverse_pct": 0.0,
           "last_tracked_price": res["last_price"], "last_tracked_ts": int(now)}
    _pump_dump_history_append(PUMP_DETECT_HISTORY_FILE, rec)

    try:
        # v0.23.0: по данным реальной статистики — пампы, пойманные
        # 120-минутным окном, откатывают вниз (выигрышно для SHORT) только
        # в 44% случаев против 85% у 20/60-минутных (n=13-16 на группу,
        # проверено на реальных пампах). Помечаем такие сигналы прямо в
        # алерте, не отключаем — данных пока не железобетонно много, но
        # предупредить лучше, чем молчать.
        confidence_note = None
        if res.get("window_min") == 120:
            confidence_note = ("памп пойман медленным (120-мин) окном — по накопленной статистике "
                                "такие чаще продолжают расти, чем откатывают (только ~44% случаев в "
                                "пользу шорта против ~85% у резких пампов) — ниже уверенность")
        _pump_or_dump_maybe_trigger_ema_signal(symbol, expected_bias="short", confidence_note=confidence_note)
    except Exception as e:
        olog(f"[pump_detect] {symbol}: ошибка проверки EMA-триггера: {_explain_error(e)}")


def _dump_fire_alert(symbol, res):
    """Зеркало _pump_fire_alert для падений — тот же рендер графика
    (цвета/направление там уже определяются по самим свечам, не по тому,
    памп это или дамп, так что переиспользуем как есть), тот же принцип
    записи истории (с трекингом движения цены после сигнала), и та же
    проверка EMA-триггера, но с ожиданием LONG-стороны (дамп в поддержку,
    а не памп в сопротивление)."""
    candles = []
    try:
        days = max(1, math.ceil(PUMP_CHART_WINDOW_MIN * 60 / 86400) + 1)
        raw = _fetch_candles(symbol, PUMP_DETECT_TF, days)
        cutoff = time.time() - PUMP_CHART_WINDOW_MIN * 60
        candles = [c for c in raw if c["t"] >= cutoff]
    except Exception as e:
        olog(f"[dump_detect] {symbol}: не смог получить свечи для графика: {_explain_error(e)}")

    png = None
    try:
        png = _render_pump_chart_png(candles, res["base_price"])
    except Exception as e:
        olog(f"[dump_detect] {symbol}: ошибка рендера графика: {_explain_error(e)}")

    caption = (f"<b>{symbol}</b>\n"
               f"Dump: -{res['pct']}% (окно {res.get('window_min','?')}м)\n"
               f"{_fmt_px(res['base_price'])} -> {_fmt_px(res['last_price'])}")
    if PUMP_DUMP_ALERTS_ENABLED:
        _send_alert_photo(png, caption)
        olog(f"[dump_detect] 💧 {symbol}: дамп -{res['pct']}% "
             f"({res['base_price']:.6g} → {res['last_price']:.6g}) — алерт отправлен")
    else:
        olog(f"[dump_detect] 💧 {symbol}: дамп -{res['pct']}% "
             f"({res['base_price']:.6g} → {res['last_price']:.6g}) — уведомление отключено "
             f"(PUMP_DUMP_ALERTS_ENABLED=False), детект и EMA-проверка продолжаются")

    now = time.time()
    rec = {"ts": int(now), "symbol": symbol, "pct": res["pct"],
           "base_price": res["base_price"], "last_price": res["last_price"],
           "window_min": res.get("window_min"),
           "kind": "dump", "track_until": now + PUMP_TRACK_WINDOW_SEC,
           "track_done": False, "max_follow_pct": 0.0, "max_reverse_pct": 0.0,
           "last_tracked_price": res["last_price"], "last_tracked_ts": int(now)}
    _pump_dump_history_append(DUMP_DETECT_HISTORY_FILE, rec)

    try:
        _pump_or_dump_maybe_trigger_ema_signal(symbol, expected_bias="long")
    except Exception as e:
        olog(f"[dump_detect] {symbol}: ошибка проверки EMA-триггера: {_explain_error(e)}")


def _pump_detect_loop():
    """Фоновый цикл: раз в PUMP_DETECT_POLL_SEC — один запрос /tickers,
    обновление скользящей истории цен по топ-N монетам, проверка каждой
    И на памп, И на дамп (общая история цен, общий снапшот — не дублируем
    сетевой запрос), алерт с картинкой при первом срабатывании и затем не
    чаще, чем раз в *_DETECT_COOLDOWN_SEC по той же монете.
    v0.13.0: раньше проверялся только памп; EMA-касания сканировались
    ОТДЕЛЬНЫМ постоянным опросом топ-100 монет (см. историю в доке файла).
    По прямому запросу — "эти пампы и есть триггер сигнала" — отдельный
    опрос убран ПОЛНОСТЬЮ: теперь EMA-сигнал проверяется ТОЛЬКО в момент,
    когда реально сработал памп или дамп (см. _pump_fire_alert/
    _dump_fire_alert → _pump_or_dump_maybe_trigger_ema_signal), а не всё
    время у всех монет сразу."""
    time.sleep(20)
    while True:
        try:
            snapshot = _pump_fetch_tickers_snapshot()
            if snapshot:
                # v0.30.3: реальная находка — v0.30.1 добавила здесь вызов
                # _fetch_all_symbols(), который делает СВОЙ отдельный запрос
                # на тот же /tickers — получалось два сетевых похода за
                # цикл вместо одного (замечено на вопрос "а не часто ли 60
                # секунд" — при нестабильном интернете лишний риск на
                # пустом месте). Теперь фильтр ликвидности считается на ТОМ
                # ЖЕ снапшоте, что уже получили — снова один запрос за цикл.
                liquid = set(_filter_liquid_symbols_from_snapshot(snapshot))
                qualifying = [s for s in snapshot.keys() if s in liquid and _has_sufficient_ema_history(s)]
                top_symbols = sorted(qualifying, key=lambda s: snapshot[s]["vol24"], reverse=True)[:_get_scan_top_n()]
                _pump_update_history(snapshot, top_symbols)
                now = time.time()
                for sym in top_symbols:
                    try:
                        # v0.29.14: по запросу — нестабильный интернет,
                        # частые обрывы. Раньше на каждую монету не было
                        # своей защиты: если сеть оборвётся именно во время
                        # _pump_fire_alert/_dump_fire_alert (свечи для
                        # картинки + отправка в Telegram — оба сетевые), это
                        # исключение улетало во ВНЕШНИЙ try/except и обрывало
                        # ВЕСЬ оставшийся проход — монеты дальше по списку в
                        # этом цикле просто пропускались, а не только та,
                        # что вызвала ошибку. Теперь одна монета — своя
                        # защита, соседи по списку не страдают из-за неё.
                        pump_res = _pump_detect_check(sym)
                        if pump_res:
                            with _pump_detect_lock:
                                last_alert = _pump_detect_state.get(sym, {}).get("last_alert_ts", 0)
                            if now - last_alert >= PUMP_DETECT_COOLDOWN_SEC:
                                _pump_fire_alert(sym, pump_res)
                                with _pump_detect_lock:
                                    _pump_detect_state[sym] = {"last_alert_ts": now, "last_pct": pump_res["pct"]}
                        dump_res = _dump_detect_check(sym)
                        if dump_res:
                            with _pump_detect_lock:
                                last_alert = _dump_detect_state.get(sym, {}).get("last_alert_ts", 0)
                            if now - last_alert >= DUMP_DETECT_COOLDOWN_SEC:
                                _dump_fire_alert(sym, dump_res)
                                with _pump_detect_lock:
                                    _dump_detect_state[sym] = {"last_alert_ts": now, "last_pct": dump_res["pct"]}
                    except Exception as e:
                        olog(f"[pump_detect] {sym}: ошибка обработки, пропускаем монету, продолжаем со следующей: {_explain_error(e)}")
                        continue
        except Exception as e:
            olog(f"[pump_detect] ошибка цикла: {_explain_error(e)}")
        time.sleep(PUMP_DETECT_POLL_SEC)
# ─── конец Live Pump/Dump Detector ──────────────────────────────────────────

# ─── Volume Peak Level Watcher ───────────────────────────────────────────────
# v0.27.0: УПРОЩЕНО по прямому подтверждению — оказалось, референсные
# скрины это просто наш ЖЕ формат обычного Pump Detector ("Pump: X%"),
# просто монета до этого отметилась ДОМИНИРУЮЩИМ всплеском объёма.
# Никакого отдельного отслеживания "пика цены" с ожиданием отката не
# нужно — оба предыдущих подхода (v0.25: ждать возврата к цене всплеска;
# v0.26: ждать подтверждённого отката от бегущего максимума) были неверны.
# Логика теперь: (1) находим ДОМИНИРУЮЩИЙ всплеск red-объёма (z-score
# порог поднят до 4.0 — только явно выделяющиеся пики, не мелкая рябь) —
# начинаем следить, запоминаем цену В МОМЕНТ всплеска; (2) как только живая
# цена выросла от неё на VOL_PEAK_CONFIRM_PCT — СРАЗУ, без ожидания
# отката, шлём алерт (частый 30-секундный опрос — не пропустить момент
# пересечения порога, как было с прошлой версией).
VOL_PEAK_SCAN_POLL_SEC       = 300     # раз в 5 минут — фетч реальных свечей дороже тикеров (только поиск новых всплесков)
VOL_PEAK_WATCH_POLL_SEC      = 30      # раз в 30 секунд — только живая цена (дёшево), не пропустить момент пересечения порога
VOL_PEAK_LOOKBACK_MIN        = 240     # ищем САМ всплеск (кандидата) в пределах последних 4 часов — свежий, релевантный сейчас
VOL_PEAK_BASELINE_LOOKBACK_MIN = 24 * 60   # v0.30.6: но среднее/std для z-score считаем по более длинному окну (24ч) — надёжнее, чем по тому же короткому окну поиска
VOL_PEAK_ZSCORE_THRESHOLD    = 4.0     # доминирующие всплески ("в самом верху графика"), не мелкая рябь — это подтверждено верно
VOL_PEAK_EXPIRY_SEC          = 6 * 3600     # расширено обратно с 90 минут — по прямому запросу: не каждый памп успевает подтвердиться за 90 минут, а нормальную защиту от протухания теперь даёт _vol_peak_verify_at_peak (v0.29.2, проверяет ВЕСЬ путь цены с момента всплеска, не полагается на таймер) + постоянный реестр сработавших (v0.28.3) + один активный на монету (v0.28.6)
VOL_PEAK_CONFIRM_PCT         = 5.0     # насколько цена должна вырасти от цены всплеска, чтобы сразу сработал алерт (без ожидания отката) — подтверждено верно на реальном примере ESP0RTS
VOL_ANOMALY_SCHEMA_VERSION   = 1       # v0.29.7: версия логики срабатывания (не формата файла) — поднимать при изменении порога/выбора пика/подтверждения, чтобы будущий анализ мог явно отсеять записи от старой логики, а не вычислять это вручную по возрасту/дублям
EMA28W_TOUCH_HISTORY_FILE = os.path.expanduser("~/pumpradar_ema28w_touch_history.json")
EMA28W_TOUCH_TRACK_WINDOW_SEC = 4 * 3600   # v0.30.14: то же окно, что у пампа/дампа/аномалии — для сравнимости
VOL_PEAK_MAX_TOLERANCE_PCT   = 0.3     # алерт шлётся, только если текущая цена в пределах этого % от бегущего максимума — не после отката от него
VOL_PEAK_TRACK_WINDOW_SEC    = 4 * 3600    # трекинг исхода после срабатывания — по запросу расширено с 2ч до 4ч
VOL_ANOMALY_HISTORY_FILE     = os.path.expanduser("~/pumpradar_vol_anomaly_history.json")  # тот же файл, что и раньше
VOL_PEAK_WATCHLIST_FILE      = os.path.expanduser("~/pumpradar_vol_peak_watchlist.json")

_vol_peak_watchlist = {}   # symbol -> [{"spike_ts","spike_vol","z","mean","std","peak_price","peak_ts","expires_at"}, ...]
_vol_peak_lock = threading.Lock()


MANUAL_SCAN_HISTORY_FILE = os.path.expanduser("~/pumpradar_manual_scans.json")
MANUAL_SCAN_CHRONOLOGY_FILE = os.path.expanduser("~/pumpradar_manual_scan_chronology.json")
MANUAL_SCAN_TIMEFRAMES = ["1h", "4h"]
MANUAL_SCAN_LOOKBACK_DAYS = {"1h": 14, "4h": 30}   # разумный охват на каждый ТФ


def _manual_scan_find_anomalies(symbol, tf, lookback_days, at_ts=None):
    """v0.30.21/23: по запросу — ручное исследовательское сканирование
    монеты (не живой алертинг). Ищет ВСЕ доминирующие всплески объёма в
    окне (не только самый крупный, как в живом _find_volume_peaks) — чтобы
    видеть полную картину при сравнении с внешним референс-ботом. С at_ts
    — окно смещается в прошлое (offset_days), обрезка только свечами до
    указанного момента, для занесения данных задним числом."""
    try:
        if at_ts is None:
            candles = _fetch_candles(symbol, tf, lookback_days)
        else:
            offset_days = max(0.0, (time.time() - at_ts) / 86400)
            candles = _fetch_candles(symbol, tf, lookback_days, offset_days=offset_days)
            candles = [c for c in candles if c["t"] <= at_ts]
    except Exception as e:
        return {"error": _explain_error(e)}
    red_candles = [c for c in candles if c["close"] < c["open"]]
    if len(red_candles) < 10:
        return []
    red_vols = [c.get("vol", 0) or 0 for c in red_candles]
    mean_v = sum(red_vols) / len(red_vols)
    var = sum((v - mean_v) ** 2 for v in red_vols) / len(red_vols)
    std_v = var ** 0.5
    if std_v <= 0:
        return []
    anomalies = []
    for c in red_candles:
        vol = c.get("vol", 0) or 0
        z = (vol - mean_v) / std_v
        if z >= VOL_PEAK_ZSCORE_THRESHOLD:
            anomalies.append({"t": int(c["t"]), "price": c["close"], "vol": vol, "z": round(z, 2)})
    anomalies.sort(key=lambda a: -a["z"])
    return anomalies[:10]   # топ-10 самых заметных, не заваливать лог


def _manual_scan_compute_from_candles(candles):
    """v0.30.23: общая вычислительная часть (EMA/SMA/ATR/лесенка/моментум/
    хай-лоу), вынесена отдельно от способа получения свечей — используется
    и для "сейчас" (через кэшированный ctx), и для "на дату/время" (через
    отдельный исторический фетч), чтобы не дублировать логику."""
    min_bars = max(WEEKLY_EMA_PERIODS) + 5
    if len(candles) < min_bars:
        return {"insufficient_history": True}
    closes = [c["close"] for c in candles]
    i = len(candles) - 1
    price = closes[i]
    atr_arr = _atr(candles, 14)
    atr_v = atr_arr[i]
    if not atr_v:
        return {"insufficient_history": True}
    ema_vals = {p: _ema(closes, p)[i] for p in (7, 14, 28)}
    sma_vals = {p: _sma(closes, p)[i] for p in (7, 14, 28)}
    if any(v is None for v in ema_vals.values()):
        return {"insufficient_history": True}
    atr_pctl = _atr_percentile(atr_arr, i)
    ladder = _ladder_order([ema_vals[7], ema_vals[14], ema_vals[28]])
    lookback = min(30, i)
    recent = closes[max(0, i - lookback):i + 1]
    high_30, low_30 = (max(recent), min(recent)) if recent else (None, None)

    def chg(n):
        if i - n < 0 or not closes[i - n]:
            return None
        return round((price - closes[i - n]) / closes[i - n] * 100, 2)

    return {
        "price": price, "atr": atr_v, "atr_percentile": atr_pctl, "ladder": ladder,
        "ema7": ema_vals[7], "ema14": ema_vals[14], "ema28": ema_vals[28],
        "sma7": sma_vals[7], "sma14": sma_vals[14], "sma28": sma_vals[28],
        "dist_atr_7": round(abs(price - ema_vals[7]) / atr_v, 3) if atr_v else None,
        "dist_atr_14": round(abs(price - ema_vals[14]) / atr_v, 3) if atr_v else None,
        "dist_atr_28": round(abs(price - ema_vals[28]) / atr_v, 3) if atr_v else None,
        "chg_1bar_pct": chg(1), "chg_3bar_pct": chg(min(3, i)), "chg_7bar_pct": chg(min(7, i)),
        "dist_from_30bar_high_pct": round((price - high_30) / high_30 * 100, 2) if high_30 else None,
        "dist_from_30bar_low_pct": round((price - low_30) / low_30 * 100, 2) if low_30 else None,
        "bar_close_ts": int(candles[i]["t"]),
    }


def _manual_scan_tf_snapshot(symbol, tf, at_ts=None):
    """v0.30.22/23: по запросу — единый, богатый снимок по одному ТФ (день
    или неделя). Без at_ts — "сейчас", через уже кэширующий
    _ema_touch_get_closed_ctx (доп. запросов не добавляет). С at_ts (по
    прямому запросу — "если сигнал был ночью, а тикет загружаю в обед") —
    отдельный ИСТОРИЧЕСКИЙ фетч со сдвигом в прошлое (offset_days), обрезка
    по факту только свечами, закрывшимися ДО указанного момента — так
    можно занести данные задним числом, не только для монет, увиденных
    прямо сейчас."""
    if at_ts is None:
        try:
            ctx = _ema_touch_get_closed_ctx(symbol, tf)
        except Exception as e:
            return {"error": _explain_error(e)}
        if not ctx:
            return {"insufficient_history": True}
        return _manual_scan_compute_from_candles(ctx["candles"][:ctx["i"] + 1])
    try:
        fetch_days = WEEKLY_EMA_FETCH_DAYS if tf == "1w" else DAILY_EMA_FETCH_DAYS
        offset_days = max(0.0, (time.time() - at_ts) / 86400)
        raw = _fetch_candles(symbol, "1d", fetch_days, offset_days=offset_days)
    except Exception as e:
        return {"error": _explain_error(e)}
    candles = _resample_to_weekly(raw) if tf == "1w" else raw
    candles = [c for c in candles if c["t"] <= at_ts]
    return _manual_scan_compute_from_candles(candles)


def _manual_scan_historical_volume_24h(symbol, at_ts):
    """v0.30.24: по прямому замечанию — тикер Gate.io всегда даёт ТЕКУЩЕЕ
    скользящее 24ч окно объёма, не окно, заканчивающееся в указанный
    at_ts (сигнал в 3 ночи, тикер ввели в 9 утра — "живое" окно захватит
    9 утра вчера -> 9 утра сегодня, а не 3 ночи вчера -> 3 ночи сегодня,
    первые 6 часов после сигнала останутся за кадром теми, что реально
    нужны). Считаем честно по 1h-свечам за 24 часа ДО at_ts — сумма по
    свечам может чуть отличаться от истинного $-объёма (формат "vol" в
    свечах не всегда 1-в-1 с settle-валютой тикера), но куда точнее, чем
    брать текущий тикер для прошлой даты."""
    try:
        offset_days = max(0.0, (time.time() - at_ts) / 86400)
        candles = _fetch_candles(symbol, "1h", 2, offset_days=offset_days)
    except Exception as e:
        return {"error": _explain_error(e)}
    window = [c for c in candles if at_ts - 24 * 3600 <= c["t"] <= at_ts]
    if not window:
        return None
    return round(sum(c.get("vol", 0) or 0 for c in window), 2)


def _manual_scan_get_contract_extras(symbol):
    """v0.30.22: прямой, не долго кэшируемый запрос — funding rate и
    объём за 24ч (оба часто меняются, отдельно от статичных
    quanto_multiplier/order_price_round в _gate_get_contract_info).
    v0.30.26: реальный баг — раньше стучался в /futures/usdt/contracts/,
    там funding_rate есть, а полей объёма НЕТ ВООБЩЕ (не в названии дело —
    у самого эндпоинта нет такого поля, проверено по официальной схеме
    Gate.io). volume_24h_* существуют только в /futures/usdt/tickers —
    туда и переключено, заодно там же есть и funding_rate, двух запросов
    не нужно."""
    try:
        contract = symbol.replace("/", "_").upper()
        r = requests.get(f"{GATE_API}/futures/usdt/tickers",
                          params={"contract": contract}, timeout=8)
        if r.status_code != 200:
            return {}
        data = r.json()
        if isinstance(data, list):
            data = next((t for t in data if t.get("contract") == contract), {})
        if not isinstance(data, dict):
            return {}
        vol24 = (data.get("volume_24h_usd") or data.get("volume_24h_settle")
                 or data.get("volume_24h_quote") or data.get("volume_24h_base") or data.get("volume_24h"))
        return {
            "funding_rate": float(data["funding_rate"]) if data.get("funding_rate") is not None else None,
            "volume_24h_usd": float(vol24) if vol24 is not None else None,
        }
    except Exception as e:
        return {"error": _explain_error(e)}


def _manual_symbol_scan(symbol, at_ts=None, side=None, ref_entry=None, ref_stop=None, ref_take=None):
    """v0.30.21/22/23/26: по прямому запросу — вводишь тикер (и, по
    желанию, дату/время сигнала — "если сигнал был ночью, а тикет
    загружаю в обед") — бот качает свечи НА ТОТ МОМЕНТ (не всегда
    "сейчас") и собирает широкий набор данных (не только EMA — на всякий
    случай, пока не знаем, что именно важно для сравнения с внешним
    референс-ботом): EMA/SMA/ATR/лесенка/моментум по дню И неделе, объём
    (24ч + всплески на 1h/4h), funding rate. at_ts=None — обычное
    "сейчас". Не даёт сигнал сам по себе, просто копит данные. Не влияет
    на живые алерты вообще. v0.30.27: side/ref_entry/ref_stop/ref_take —
    необязательные, из скрина ИХ бота, чтобы потом можно было проверить,
    коррелирует ли направление ИХ сигнала с нашими данными (ладдер,
    дистанция, момент), а не только "какая монета была близко к EMA"."""
    now = time.time()
    # v0.30.25: по прямому запросу — простой, независимый лог "тикер +
    # время сигнала", отдельно от полного расчёта ниже. Пишется ПЕРВЫМ,
    # до любых вычислений — даже если детальный скан ниже упадёт по
    # какой-то причине, хронология сигналов всё равно сохранится. Смысл —
    # даже если за эту неделю логику отбора не разгадаем, останется чистый
    # список "какая монета, когда был сигнал", по которому потом можно
    # будет прогнать уже УЛУЧШЕННЫЕ индикаторы задним числом, не теряя сами
    # наблюдения.
    try:
        _pump_dump_history_append(MANUAL_SCAN_CHRONOLOGY_FILE, {
            "symbol": symbol, "signal_ts": int(at_ts) if at_ts else None,
            "recorded_at": int(now), "side": side,
            "ref_entry": ref_entry, "ref_stop": ref_stop, "ref_take": ref_take,
        })
    except Exception as e:
        olog(f"[manual_scan] ⚠ не смог записать хронологию для {symbol}: {_explain_error(e)}")
    result = {"ts": int(now), "symbol": symbol, "at_ts": int(at_ts) if at_ts else None,
               "side": side, "ref_entry": ref_entry, "ref_stop": ref_stop, "ref_take": ref_take,
               "daily": None, "weekly": None, "contract": None, "anomalies": {}}
    result["daily"] = _manual_scan_tf_snapshot(symbol, "1d", at_ts=at_ts)
    result["weekly"] = _manual_scan_tf_snapshot(symbol, "1w", at_ts=at_ts)
    result["contract"] = _manual_scan_get_contract_extras(symbol)
    if at_ts:
        result["contract"]["live_only"] = True   # volume_24h_usd/funding_rate тут всегда ТЕКУЩИЕ, не на at_ts
        result["contract"]["volume_24h_historical"] = _manual_scan_historical_volume_24h(symbol, at_ts)
    for tf in MANUAL_SCAN_TIMEFRAMES:
        try:
            result["anomalies"][tf] = _manual_scan_find_anomalies(
                symbol, tf, MANUAL_SCAN_LOOKBACK_DAYS.get(tf, 14), at_ts=at_ts)
        except Exception as e:
            result["anomalies"][tf] = {"error": _explain_error(e)}
    # v0.30.46: реальная находка — "weekly"/"daily" выше считают dist_atr
    # от ЦЕНЫ ПОСЛЕДНЕЙ ЗАКРЫТОЙ свечи (bar_close_ts, может быть на дни
    # старее момента скана), а живой движок (_ema_touch_check_symbol,
    # ровно то, что реально решает — слать алерт или нет) сравнивает с
    # ЖИВОЙ ценой прямо сейчас. Из-за этого расхождения ручной скан мог
    # показывать "должно было сработать" (dist_atr по старой цене мал),
    # хотя на живой цене монета уже отошла от уровня и движок закономенно
    # молчал. Теперь скан прогоняет ВСЕ те же гейты, что и реальный
    # slow_impulse (живая дистанция, cooldown, гейт по аномалии объёма,
    # потолок/пол объёма) и даёт единый вердикт would_fire_now — без
    # этого поля "почему не сработало" было только гадать по кускам.
    if at_ts:
        result["live_engine_check"] = {"note": "не считается для исторического скана (at_ts задан)"}
    else:
        try:
            live_touches = _ema_touch_check_symbol(symbol, SLOW_IMPULSE_TF)
            live_out = []
            max_z = None
            for tf_a in MANUAL_SCAN_TIMEFRAMES:
                an = result["anomalies"].get(tf_a)
                if isinstance(an, list):
                    for a in an:
                        if max_z is None or a["z"] > max_z:
                            max_z = a["z"]
            anomaly_ok = (not SLOW_IMPULSE_REQUIRE_SELL_ANOMALY) or \
                         (max_z is not None and max_z >= VOL_PEAK_ZSCORE_THRESHOLD)
            vol24 = (result["contract"] or {}).get("volume_24h_usd")
            volume_ok = vol24 is not None and SLOW_IMPULSE_MIN_VOLUME_USD <= vol24 <= SLOW_IMPULSE_MAX_VOLUME_USD
            for t in live_touches:
                k = f"{symbol}|{SLOW_IMPULSE_TF}|{t['period']}"
                with _weekly_ema_touch_lock:
                    last_fire = _weekly_ema_touch_state.get(k, 0)
                cooldown_left = max(0, round(SLOW_IMPULSE_TOUCH_COOLDOWN_SEC - (time.time() - last_fire)))
                live_out.append({"period": t["period"], "dist_atr": t["dist_atr"],
                                  "side": t["side"], "classic_bias": t["classic_bias"],
                                  "cooldown_remaining_sec": cooldown_left})
            would_fire = bool(live_out) and volume_ok and anomaly_ok and \
                         any(x["cooldown_remaining_sec"] == 0 for x in live_out)
            result["live_engine_check"] = {
                "touches": live_out, "max_anomaly_z": max_z, "anomaly_ok": anomaly_ok,
                "volume_24h_usd": vol24, "volume_ok": volume_ok, "would_fire_now": would_fire,
            }
        except Exception as e:
            result["live_engine_check"] = {"error": _explain_error(e)}
    _pump_dump_history_append(MANUAL_SCAN_HISTORY_FILE, result)
    when_note = f" на момент {time.strftime('%Y-%m-%d %H:%M', time.localtime(at_ts))}" if at_ts else ""
    olog(f"[manual_scan] {symbol}: скан завершён{when_note}, записан в {MANUAL_SCAN_HISTORY_FILE}")
    return result


def _find_volume_peaks(symbol):
    """Сканирует ВСЁ окно VOL_PEAK_LOOKBACK_MIN (не только последнюю
    свечу, как раньше) и находит red-свечу с САМЫМ БОЛЬШИМ объёмом во всём
    окне — если её z-score проходит порог. По прямому уточнению — ориентир
    только на САМЫЙ крупный всплеск в отображаемом окне, не на любой, что
    просто перевалил порог.
    v0.30.6: по запросу — "более длинный горизонт сравнения для z-score".
    Раньше среднее/std считались по тому же короткому окну
    (VOL_PEAK_LOOKBACK_MIN=4ч), где искали и сам всплеск — если эти 4 часа
    сами по себе оказывались необычно тихими, z-score мог получиться
    завышенным на совершенно рядовом по факту объёме. Теперь среднее/std
    считаются по ОТДЕЛЬНОМУ, более длинному окну (VOL_PEAK_BASELINE_LOOKBACK_MIN,
    24 часа) — "нормальный" объём для этой монеты, а кандидат на "самый
    крупный" по-прежнему ищется только в недавнем окне (свежий, релевантный
    сейчас всплеск), просто оценивается против более надёжного ориентира.
    v0.28.1: реальный баг — монета часто попадает в топ-N по объёму ИЗ-ЗА
    того же пампа, который мы пытаемся поймать; когда мы её обнаруживаем,
    сам всплеск может быть давним, а цена уже успела дойти до истинного
    пика И ОТКАТИТЬ ОБРАТНО почти к порогу — проверка одной только ТЕКУЩЕЙ
    цены на этот случай не ловит (текущая цена может быть "только-только"
    у порога, хотя реальный максимум давно пройден и упущен). Теперь
    проверяем МАКСИМУМ цены по всем свечам ПОСЛЕ всплеска — если он уже
    заметно (более чем в 1.5 раза от порога) превышен, движение уже
    состоялось и отыграно назад до того, как мы его обнаружили — не
    свежий сигнал, отбрасываем."""
    try:
        days = max(1, math.ceil(VOL_PEAK_BASELINE_LOOKBACK_MIN * 60 / 86400) + 1)
        all_candles = _fetch_candles(symbol, "1m", days)
    except Exception:
        return []
    now = time.time()
    baseline_cutoff = now - VOL_PEAK_BASELINE_LOOKBACK_MIN * 60
    baseline_candles = [c for c in all_candles if c["t"] >= baseline_cutoff]
    baseline_red = [c for c in baseline_candles if c["close"] < c["open"]]
    if len(baseline_red) < 10:
        return []
    red_vols = [c.get("vol", 0) or 0 for c in baseline_red]
    mean_v = sum(red_vols) / len(red_vols)
    var = sum((v - mean_v) ** 2 for v in red_vols) / len(red_vols)
    std_v = var ** 0.5
    if std_v <= 0:
        return []
    search_cutoff = now - VOL_PEAK_LOOKBACK_MIN * 60
    red_candles = [c for c in baseline_red if c["t"] >= search_cutoff]
    if not red_candles:
        return []
    biggest = max(red_candles, key=lambda c: c.get("vol", 0) or 0)
    vol = biggest.get("vol", 0) or 0
    z = (vol - mean_v) / std_v
    if z < VOL_PEAK_ZSCORE_THRESHOLD:
        return []
    spike_price = biggest["close"]
    # v0.29.13: раньше здесь была проверка "обнаружено задним числом"
    # (полностью отбрасывала кандидата, если цена уже ушла на 7.5%+ с
    # момента всплеска). Побочный эффект: более значимый (реально
    # самый крупный) всплеск, случившийся ПОЗЖЕ уже отслеживаемого, мог
    # никогда не попасть даже на сравнение "не крупнее ли он текущего" в
    # _vol_peak_find_loop — если цена после него уже успела заметно
    # сдвинуться (а у по-настоящему значимых всплесков так обычно и
    # бывает), кандидат отбрасывался ЗДЕСЬ, до того как вообще мог начать
    # конкурировать за замену. Убрано — своя, более надёжная защита от
    # "протухшей" цены есть в _vol_peak_verify_at_peak (v0.29.2, проверяет
    # ВЕСЬ путь с момента всплеска прямо перед отправкой), она не мешает
    # replace-логике конкурировать за самый крупный всплеск.
    return [{"spike_price": spike_price, "spike_ts": biggest["t"], "spike_vol": vol,
             "z": round(z, 2), "mean": round(mean_v, 2), "std": round(std_v, 2)}]


def _save_vol_peak_watchlist():
    try:
        with _vol_peak_lock:
            snapshot = dict(_vol_peak_watchlist)
        tmp_path = VOL_PEAK_WATCHLIST_FILE + f".tmp{os.getpid()}"
        with open(tmp_path, "w") as f:
            json.dump(snapshot, f)
        os.replace(tmp_path, VOL_PEAK_WATCHLIST_FILE)
    except Exception as e:
        olog(f"[vol_peak] ⚠ не смог сохранить watchlist: {_explain_error(e)}")


def _load_vol_peak_watchlist():
    """v0.29.10: реальный баг, подтверждённый на актуальной версии —
    expires_at считается и сохраняется на диск ОДИН раз при добавлении, с
    ТЕМ значением VOL_PEAK_EXPIRY_SEC, что было в коде на тот момент. Мы
    за это время несколько раз меняли константу (12ч -> 90мин -> 6ч) — но
    уже сохранённые на диск записи продолжают жить со СТАРЫМ сроком,
    потому что при подхвате с диска после перезапуска (что происходит
    при каждом обновлении бота) мы просто доверяли сохранённому значению,
    не пересчитывали его под ТЕКУЩУЮ константу (реальный случай:
    TAC_USDT, всплеск 29-часовой давности пережил уже два обновления
    лимита). Теперь при загрузке КАЖДАЯ запись пересчитывается заново:
    expires_at = spike_ts + ТЕКУЩИЙ VOL_PEAK_EXPIRY_SEC — если по новым
    правилам она уже должна была истечь, отбрасываем сразу, не ждём
    следующего скана."""
    global _vol_peak_watchlist
    try:
        if os.path.exists(VOL_PEAK_WATCHLIST_FILE):
            with open(VOL_PEAK_WATCHLIST_FILE) as f:
                loaded = json.load(f)
            if isinstance(loaded, dict):
                now = time.time()
                revalidated = {}
                dropped = 0
                consolidated = 0
                for symbol, items in loaded.items():
                    kept = []
                    for item in items:
                        item["expires_at"] = item.get("spike_ts", 0) + VOL_PEAK_EXPIRY_SEC
                        if item["expires_at"] > now:
                            kept.append(item)
                        else:
                            dropped += 1
                    if len(kept) > 1:
                        # v0.29.11: доп. защита — "один активный на монету"
                        # (v0.28.6/v0.29.6) проверяется только при
                        # добавлении НОВОГО всплеска, не чистит то, что уже
                        # оказалось в файле по любой другой причине (старые
                        # баги, ручное редактирование, будущие пока не
                        # найденные пути). При каждой загрузке принудительно
                        # схлопываем до одной — самой крупной по объёму —
                        # записи на монету, независимо от того, как там
                        # оказалось несколько.
                        consolidated += len(kept) - 1
                        kept = [max(kept, key=lambda p: p.get("spike_vol", 0))]
                    if kept:
                        revalidated[symbol] = kept
                with _vol_peak_lock:
                    _vol_peak_watchlist = revalidated
                total = sum(len(v) for v in revalidated.values())
                extra = []
                if dropped:
                    extra.append(f"{dropped} отброшено — устарели по текущему лимиту {VOL_PEAK_EXPIRY_SEC//60}мин")
                if consolidated:
                    extra.append(f"{consolidated} схлопнуто — было несколько записей на одну монету")
                olog(f"[vol_peak] watchlist подхвачен с диска: {total} пиков по {len(revalidated)} монетам"
                     + (f" ({'; '.join(extra)})" if extra else ""))
    except Exception as e:
        olog(f"[vol_peak] ⚠ не смог подхватить watchlist с диска: {_explain_error(e)}")


def _vol_peak_fire_alert(symbol, watch_item, current_price, pct_move):
    """По прямому подтверждению — "сразу слать, как только цена обновила
    максимум... всё как на примерах" — референсные скрины оказались
    просто нашим ЖЕ форматом обычного Pump Detector ("Pump: X%"), просто
    монета до этого отметилась всплеском объёма. Никакого ожидания
    отката — шлём СРАЗУ, как только цена выросла от цены всплеска на
    VOL_PEAK_CONFIRM_PCT. Картинка — тот же _render_vol_anomaly_chart_png
    (фиолетовая заливка red-объёма + подсветка на месте всплеска)."""
    candles = []
    try:
        chart_lookback_min = max(VOL_PEAK_LOOKBACK_MIN,
                                  int((time.time() - watch_item["spike_ts"]) / 60) + 30)
        days = max(1, math.ceil(chart_lookback_min * 60 / 86400) + 1)
        raw = _fetch_candles(symbol, "1m", days)
        cutoff = watch_item["spike_ts"] - 20 * 60
        candles = [c for c in raw if c["t"] >= cutoff]
    except Exception as e:
        olog(f"[vol_peak] {symbol}: не смог получить свечи для графика: {_explain_error(e)}")

    png = None
    try:
        png = _render_vol_anomaly_chart_png(candles, watch_item["spike_price"],
                                             anomaly_t=watch_item["spike_ts"],
                                             signal_price=current_price)
    except Exception as e:
        olog(f"[vol_peak] {symbol}: ошибка рендера графика: {_explain_error(e)}")

    spike_ago_min = round((time.time() - watch_item["spike_ts"]) / 60)
    # v0.30.6: по запросу — та же логика, что подтвердилась данными на
    # обычных пампах (20/60-минутное окно даёт 85% в пользу шорта,
    # 120-минутное — только 44-57%): быстрое движение цены от всплеска до
    # подтверждения надёжнее, чем медленный, долгий отжим. Помечаем так же.
    speed_note = None
    if spike_ago_min > 60:
        speed_note = (f"движение от всплеска до подтверждения заняло {spike_ago_min} мин "
                      f"(>60) — на обычных пампах такой медленный характер движения давал "
                      f"заметно более слабый результат для шорта, чем быстрое (~44-57% против ~85%)")
    # v0.30.6 (пункт 4) — ТОЛЬКО диагностика, не влияет на отправку.
    # Классический признак климакса/распределения: объём затухает, пока
    # цена продолжает расти (последний рывок уже "на измене", без
    # поддержки объёмом). Считаем по свечам ПОСЛЕ всплеска, что уже есть
    # в candles (для картинки), без доп. запроса.
    divergence = None
    try:
        post_spike = [c for c in candles if c["t"] >= watch_item["spike_ts"]]
        if len(post_spike) >= 6:
            mid = len(post_spike) // 2
            first_half, second_half = post_spike[:mid], post_spike[mid:]
            vol_first = sum(c.get("vol", 0) or 0 for c in first_half) / len(first_half)
            vol_second = sum(c.get("vol", 0) or 0 for c in second_half) / len(second_half)
            price_first = sum(c["close"] for c in first_half) / len(first_half)
            price_second = sum(c["close"] for c in second_half) / len(second_half)
            divergence = {
                "volume_decelerating": vol_second < vol_first,
                "price_still_rising": price_second > price_first,
                "vol_first_half": round(vol_first, 1), "vol_second_half": round(vol_second, 1),
            }
    except Exception as e:
        olog(f"[vol_peak] {symbol}: не смог посчитать дивергенцию (диагностика, не критично): {_explain_error(e)}")

    caption = (
        f"📐 <b>{symbol}</b> — Pump: {pct_move}% после аномального объёма продаж\n"
        f"Всплеск объёма был {spike_ago_min} мин назад: z-score {watch_item['z']} "
        f"(объём {watch_item['spike_vol']:.0f} против среднего {watch_item['mean']:.0f} ± {watch_item['std']:.0f})\n"
        f"{_fmt_px(watch_item['spike_price'])} -> {_fmt_px(current_price)}"
        + (f"\n⚠️ {speed_note}" if speed_note else "")
    )
    if PUMP_DUMP_ALERTS_ENABLED:
        _send_alert_photo(png, caption)
        olog(f"[vol_peak] {symbol}: цена выросла на {pct_move}% с момента всплеска "
             f"({watch_item['spike_price']:.6g} -> {current_price:.6g}, z={watch_item['z']}) — алерт отправлен")
    else:
        olog(f"[vol_peak] {symbol}: цена выросла на {pct_move}% с момента всплеска "
             f"({watch_item['spike_price']:.6g} -> {current_price:.6g}, z={watch_item['z']}) — уведомление "
             f"отключено (PUMP_DUMP_ALERTS_ENABLED=False, приоритет — только сигналы у EMA, как у референс-бота), "
             f"детект и EMA-проверка продолжаются")

    now = time.time()
    source_id = f"vol_anomaly|{symbol}|{watch_item['spike_ts']}"
    rec = {"ts": int(now), "symbol": symbol, "z": watch_item["z"], "vol": watch_item["spike_vol"],
           "mean": watch_item["mean"], "std": watch_item["std"], "price": current_price,
           "peak_price": watch_item["spike_price"], "peak_ts": watch_item["spike_ts"],
           "peak_age_min": spike_ago_min, "pct_move": pct_move, "slow_move": spike_ago_min > 60,
           "divergence": divergence,
           "schema_version": VOL_ANOMALY_SCHEMA_VERSION, "source_id": source_id,
           "track_until": now + VOL_PEAK_TRACK_WINDOW_SEC, "track_done": False,
           "max_follow_pct": 0.0, "max_reverse_pct": 0.0,
           "last_tracked_price": current_price, "last_tracked_ts": int(now),
           # v0.29.8: по запросу — весь путь цены по времени, не только
           # крайние точки. max_follow_pct/max_reverse_pct сами по себе не
           # говорят, В КАКОМ ПОРЯДКЕ случились просадка и прибыль — а это
           # критично для честной симуляции "стоп -X% / тейк +Y%" (стоп
           # мог сработать раньше, чем случился разворот в прибыль).
           # path — {"ts","price"} на каждой проверке, первая точка это
           # вход (цена самого алерта).
           "path": [{"ts": int(now), "price": current_price}]}
    _pump_dump_history_append(VOL_ANOMALY_HISTORY_FILE, rec)

    try:
        _pump_or_dump_maybe_trigger_ema_signal(symbol, expected_bias="short",
                                                confidence_note=speed_note, triggered_by=source_id)
    except Exception as e:
        olog(f"[vol_peak] {symbol}: ошибка проверки EMA-триггера: {_explain_error(e)}")


def _vol_peak_get_fired_keys():
    """Постоянный реестр УЖЕ СРАБОТАВШИХ всплесков — независимый от
    текущего watchlist (тот чистится при срабатывании, а этот — нет).
    Без этого один и тот же исторически крупнейший в окне всплеск
    находится заново на каждом скане после того, как алерт по нему уже
    ушёл и запись удалилась из watchlist — получается бесконечный цикл
    переоткрытия одного и того же события (реальный случай: SOXL_USDT,
    один и тот же z-score/объём, "182 мин назад" -> потом "423 мин назад",
    два отдельных алерта по факту на одно и то же)."""
    try:
        if not os.path.exists(VOL_ANOMALY_HISTORY_FILE):
            return set()
        with open(VOL_ANOMALY_HISTORY_FILE) as f:
            hist = json.load(f)
        return {(r["symbol"], r["peak_ts"]) for r in hist if "peak_ts" in r}
    except Exception:
        return set()


def _vol_peak_find_loop():
    """Раз в VOL_PEAK_SCAN_POLL_SEC (5 минут — фетч реальных свечей на
    монету, дорого) ищет НОВЫЕ всплески объёма по топ-N монетам и
    начинает следить (кладёт в watchlist со spike_price — точкой отсчёта
    для % движения). Только поиск — проверка цены вынесена в отдельный
    частый _vol_peak_watch_loop (см. ниже). Проверка на "обнаружено задним
    числом" — внутри самой _find_volume_peaks (смотрит на весь путь цены
    после всплеска, не только на живую цену в момент обнаружения). Плюс
    проверка на "уже срабатывал раньше" по постоянному реестру
    _vol_peak_get_fired_keys() — не даёт одному и тому же событию
    переоткрываться циклически после каждого срабатывания."""
    time.sleep(80)
    _load_vol_peak_watchlist()
    while True:
        try:
            symbols = _get_tradeable_symbols()
            now = time.time()
            changed = False
            fired_keys = _vol_peak_get_fired_keys()
            for symbol in symbols:
                try:
                    new_spikes = _find_volume_peaks(symbol)
                except Exception as e:
                    olog(f"[vol_peak] {symbol}: ошибка поиска всплесков: {_explain_error(e)}")
                    new_spikes = []
                with _vol_peak_lock:
                    existing = _vol_peak_watchlist.get(symbol, [])
                    existing_spike_ts = {p["spike_ts"] for p in existing}
                    added = 0
                    for spike in new_spikes:
                        if spike["spike_ts"] in existing_spike_ts:
                            continue
                        if (symbol, spike["spike_ts"]) in fired_keys:
                            continue
                        if existing:
                            # v0.29.6: реальная жалоба — раньше ЛЮБОЙ новый
                            # всплеск на уже отслеживаемой монете просто
                            # игнорировался, даже если он оказывался
                            # объективно КРУПНЕЕ того, что уже отслеживаем
                            # (реальный случай: слежение началось с
                            # умеренного всплеска, а через 30 минут пришёл
                            # гораздо более крупный — его проигнорировали,
                            # хотя именно он "более явный высокий пик").
                            # Теперь — если новый всплеск БОЛЬШЕ текущего
                            # по объёму, ЗАМЕНЯЕМ (не добавляем вторым —
                            # это по-прежнему один активный на монету, как в
                            # v0.28.6, просто "активный" теперь значит
                            # "самый крупный из известных", а не "первый
                            # найденный").
                            current_best = max(existing, key=lambda p: p.get("spike_vol", 0))
                            if spike["spike_vol"] > current_best.get("spike_vol", 0):
                                olog(f"[vol_peak] {symbol}: новый всплеск ({spike['spike_vol']:.0f}) "
                                     f"крупнее уже отслеживаемого ({current_best.get('spike_vol',0):.0f}) "
                                     f"— заменяем")
                                existing = []
                            else:
                                olog(f"[vol_peak] {symbol}: новый всплеск найден, но монета "
                                     f"уже отслеживается более крупным — пропускаем")
                                continue
                        item = dict(spike)
                        # v0.28.4: срок жизни считаем от ВРЕМЕНИ САМОГО
                        # ВСПЛЕСКА (spike_ts), не от момента, когда МЫ его
                        # обнаружили — иначе долго "тихо просидевший" в
                        # 4-часовом окне всплеск (найденный только когда
                        # стал самым крупным) получал ещё 90 минут ПОВЕРХ
                        # уже прошедшего времени, реальный возраст к
                        # моменту срабатывания мог заметно превышать 90 мин.
                        item["expires_at"] = spike["spike_ts"] + VOL_PEAK_EXPIRY_SEC
                        existing.append(item)
                        added += 1
                    before = len(existing)
                    existing = [p for p in existing if p["expires_at"] > now]
                    if added or len(existing) != before:
                        changed = True
                    _vol_peak_watchlist[symbol] = existing
            if changed:
                _save_vol_peak_watchlist()
        except Exception as e:
            olog(f"[vol_peak] ошибка цикла поиска всплесков: {_explain_error(e)}")
        time.sleep(VOL_PEAK_SCAN_POLL_SEC)


def _vol_peak_verify_at_peak(symbol, price, spike_ts, lookback_min=None):
    """v0.28.5: не полагаться на накопленное за много опросов состояние —
    самостоятельная свежая проверка ПРЯМО ПЕРЕД отправкой.
    v0.29.2: реальный баг — фиксированное окно 20 минут не видит пик,
    случившийся ЧАСЫ назад (реальный случай: истинный пик всего дня был в
    15:55, алерт попытался уйти на цене из ~19:00, намного ниже — 20-минутное
    окно этого просто не замечает, потому что не смотрит так далеко назад).
    Теперь окно — ВЕСЬ период с момента самого всплеска (spike_ts), не
    фиксированные 20 минут: сколько бы времени ни прошло с начала слежения,
    проверяем полный путь цены за это время, а не последний обрывок."""
    if lookback_min is None:
        lookback_min = max(20, (time.time() - spike_ts) / 60 + 5)
    try:
        days = max(1, math.ceil(lookback_min * 60 / 86400) + 1)
        candles = _fetch_candles(symbol, "1m", days)
    except Exception as e:
        olog(f"[vol_peak_watch] {symbol}: не смог проверить пик свежими свечами: {_explain_error(e)}")
        return True
    cutoff = max(spike_ts, time.time() - lookback_min * 60)
    recent = [c["close"] for c in candles if c["t"] >= cutoff]
    if not recent:
        return True
    recent_max = max(recent + [price])
    if recent_max <= 0:
        return True
    return price >= recent_max * (1 - VOL_PEAK_MAX_TOLERANCE_PCT / 100)


def _vol_peak_watch_loop():
    """Раз в VOL_PEAK_WATCH_POLL_SEC (30 секунд — дёшево, только живая
    цена) проверяет цену каждой отслеживаемой монеты.
    v0.28.2: реальный баг, подтверждённый пользователем на свежих (не
    просроченных при обнаружении) всплесках — простое пересечение порога
    VOL_PEAK_CONFIRM_PCT не гарантирует, что мы поймали цену ИМЕННО У ПИКА:
    между двумя опросами (раз в 30с) цена вполне может успеть залететь
    ЕЩЁ ВЫШЕ и начать откатывать назад, а мы обнаружим уже откатившуюся
    цену — порог формально пройден, но настоящий пик уже упущен (график
    в алерте потом показывает лишний откат в хвосте — прямой диагностический
    признак от пользователя: "у него график всегда обрывается на
    максимальной цене, у меня идёт откат"). Теперь у каждой записи
    watchlist отдельно отслеживается running_max — бегущий максимум живой
    цены с момента начала слежения (обновляется на КАЖДОМ опросе, без
    доп. стоимости — цена и так уже фетчится). Алерт шлётся, только если
    ОДНОВРЕМЕННО: (1) цена выросла от spike_price на VOL_PEAK_CONFIRM_PCT
    И (2) текущая цена — это и есть (в пределах небольшого допуска)
    бегущий максимум, а не уже откатившееся от него значение."""
    time.sleep(90)
    while True:
        try:
            with _vol_peak_lock:
                watched_symbols = [s for s, items in _vol_peak_watchlist.items() if items]
            changed = False
            for symbol in watched_symbols:
                with _vol_peak_lock:
                    watch_now = list(_vol_peak_watchlist.get(symbol, []))
                if not watch_now:
                    continue
                try:
                    price = _gate_get_price(symbol)
                except Exception as e:
                    olog(f"[vol_peak_watch] {symbol}: ошибка получения цены: {_explain_error(e)}")
                    price = None
                if not price:
                    continue
                fired_ts = set()
                for item in watch_now:
                    if item["spike_price"] <= 0:
                        continue
                    running_max = max(item.get("running_max", item["spike_price"]), price)
                    if running_max != item.get("running_max"):
                        with _vol_peak_lock:
                            for p in _vol_peak_watchlist.get(symbol, []):
                                if p["spike_ts"] == item["spike_ts"]:
                                    p["running_max"] = running_max
                        changed = True
                    pct_move = round((price - item["spike_price"]) / item["spike_price"] * 100, 2)
                    at_running_max = price >= running_max * (1 - VOL_PEAK_MAX_TOLERANCE_PCT / 100)
                    if pct_move >= VOL_PEAK_CONFIRM_PCT and at_running_max:
                        if _vol_peak_verify_at_peak(symbol, price, item["spike_ts"]):
                            _vol_peak_fire_alert(symbol, item, price, pct_move)
                            fired_ts.add(item["spike_ts"])
                            changed = True
                        else:
                            olog(f"[vol_peak_watch] {symbol}: порог формально пройден "
                                 f"({pct_move}%), но свежая проверка свечей показала — "
                                 f"цена уже НЕ на максимуме последних 20 минут, ждём дальше")
                if fired_ts:
                    with _vol_peak_lock:
                        _vol_peak_watchlist[symbol] = [
                            p for p in _vol_peak_watchlist.get(symbol, [])
                            if p["spike_ts"] not in fired_ts
                        ]
            if changed:
                _save_vol_peak_watchlist()
        except Exception as e:
            olog(f"[vol_peak_watch] ошибка цикла: {_explain_error(e)}")
        time.sleep(VOL_PEAK_WATCH_POLL_SEC)


def _vol_anomaly_track_loop():
    """Зеркало _pump_dump_track_loop для истории аномалий объёма —
    max_follow_pct/max_reverse_pct для быстрой сводки, ПЛЮС (v0.29.8)
    весь путь цены по времени в поле "path" — по запросу, чтобы можно
    было честно смоделировать любой стоп/тейк с учётом порядка событий
    (крайние точки сами по себе не говорят, что случилось раньше — просадка
    или разворот в прибыль)."""
    time.sleep(100)
    while True:
        try:
            lock = _get_history_file_lock(VOL_ANOMALY_HISTORY_FILE)
            with lock:
                if not os.path.exists(VOL_ANOMALY_HISTORY_FILE):
                    time.sleep(VOL_PEAK_SCAN_POLL_SEC)
                    continue
                try:
                    with open(VOL_ANOMALY_HISTORY_FILE) as f:
                        hist = json.load(f)
                except Exception as e:
                    olog(f"[vol_anomaly_track] не смог прочитать историю: {_explain_error(e)}")
                    time.sleep(VOL_PEAK_SCAN_POLL_SEC)
                    continue
                now = time.time()
                price_cache = {}
                changed = False
                for rec in hist:
                    if rec.get("track_done", True):
                        continue
                    try:
                        sym = rec["symbol"]
                        if sym not in price_cache:
                            try:
                                price_cache[sym] = _gate_get_price(sym)
                            except Exception as e:
                                olog(f"[vol_anomaly_track] {sym}: ошибка получения цены: {_explain_error(e)}")
                                price_cache[sym] = None
                        price = price_cache[sym]
                        if price and rec.get("price"):
                            pct = (price - rec["price"]) / rec["price"] * 100.0
                            # гипотеза автора — сигнал ПРЕДШЕСТВУЕТ ПАМПУ (рост
                            # цены), поэтому "в пользу" здесь = рост, "против" = падение
                            follow, reverse = pct, -pct
                            rec["max_follow_pct"] = round(max(rec.get("max_follow_pct", 0.0), follow), 2)
                            rec["max_reverse_pct"] = round(max(rec.get("max_reverse_pct", 0.0), reverse), 2)
                            rec["last_tracked_price"] = price
                            rec["last_tracked_ts"] = int(now)
                            _track_record_checkpoint(rec, now, follow)
                            # v0.29.8: дописываем путь по времени — на
                            # VOL_PEAK_TRACK_WINDOW_SEC=2ч и опросе раз в
                            # VOL_PEAK_SCAN_POLL_SEC=5мин это ~24 точки на
                            # сделку, не растёт бесконечно (трекинг сам
                            # заканчивается через track_until)
                            path = rec.get("path", [])
                            path.append({"ts": int(now), "price": price})
                            rec["path"] = path
                            _maybe_fire_ema28w_reversal_alert(rec, sym, price, "vol_anomaly")
                            changed = True
                        if now >= rec.get("track_until", 0):
                            rec["track_done"] = True
                            changed = True
                    except Exception as e:
                        olog(f"[vol_anomaly_track] ⚠ пропущена битая запись "
                             f"({rec.get('symbol','?')}): {_explain_error(e)}")
                        continue
                if changed:
                    _pump_dump_history_save_all(VOL_ANOMALY_HISTORY_FILE, hist)
        except Exception as e:
            olog(f"[vol_anomaly_track] ошибка цикла: {_explain_error(e)}")
        time.sleep(VOL_PEAK_SCAN_POLL_SEC)
# ─── конец Volume Peak Level Watcher ─────────────────────────────────────────


def _ema28w_touch_track_loop():
    """v0.30.14: следит за исходом ПОСЛЕ срабатывания
    _maybe_fire_ema28w_reversal_alert — у каждой записи уже есть source
    (pump/dump/vol_anomaly), так что как накопится достаточно данных,
    можно будет сравнить: реально ли отскок вниз после аномалии объёма
    надёжнее, чем после голого пампа/дампа, а не гадать. Тот же принцип
    знаков, что у аномалии объёма — follow = цена продолжила расти
    (плохо для ожидаемого отскока вниз), reverse = цена пошла вниз
    (собственно тот самый отскок, которого ждём)."""
    time.sleep(110)
    while True:
        try:
            lock = _get_history_file_lock(EMA28W_TOUCH_HISTORY_FILE)
            with lock:
                if not os.path.exists(EMA28W_TOUCH_HISTORY_FILE):
                    time.sleep(VOL_PEAK_SCAN_POLL_SEC)
                    continue
                try:
                    with open(EMA28W_TOUCH_HISTORY_FILE) as f:
                        hist = json.load(f)
                except Exception as e:
                    olog(f"[ema28w_track] не смог прочитать историю: {_explain_error(e)}")
                    time.sleep(VOL_PEAK_SCAN_POLL_SEC)
                    continue
                now = time.time()
                price_cache = {}
                changed = False
                for rec in hist:
                    if rec.get("track_done", True):
                        continue
                    try:
                        sym = rec["symbol"]
                        if sym not in price_cache:
                            try:
                                price_cache[sym] = _gate_get_price(sym)
                            except Exception as e:
                                olog(f"[ema28w_track] {sym}: ошибка получения цены: {_explain_error(e)}")
                                price_cache[sym] = None
                        price = price_cache[sym]
                        if price and rec.get("price"):
                            pct = (price - rec["price"]) / rec["price"] * 100.0
                            follow, reverse = pct, -pct
                            rec["max_follow_pct"] = round(max(rec.get("max_follow_pct", 0.0), follow), 2)
                            rec["max_reverse_pct"] = round(max(rec.get("max_reverse_pct", 0.0), reverse), 2)
                            rec["last_tracked_price"] = price
                            rec["last_tracked_ts"] = int(now)
                            _track_record_checkpoint(rec, now, follow)
                            changed = True
                        if now >= rec.get("track_until", 0):
                            rec["track_done"] = True
                            changed = True
                    except Exception as e:
                        olog(f"[ema28w_track] ⚠ пропущена битая запись "
                             f"({rec.get('symbol','?')}): {_explain_error(e)}")
                        continue
                if changed:
                    _pump_dump_history_save_all(EMA28W_TOUCH_HISTORY_FILE, hist)
        except Exception as e:
            olog(f"[ema28w_track] ошибка цикла: {_explain_error(e)}")
        time.sleep(VOL_PEAK_SCAN_POLL_SEC)


# ─── Weekly/Daily EMA Touch Watcher ─────────────────────────────────────────
# Отдельный, независимый ни от Pump Detector, ни от (остановленного) EMA-
# инверт движка сканер: раз в WEEKLY_EMA_SCAN_POLL_SEC проверяет топ-N монет
# по объёму на касание ценой EMA7/14/28 — теперь на ДВУХ таймфреймах разом
# (недельный и дневной, по прямому запросу), не только на недельном — и
# шлёт текстовый алерт в Telegram. Чисто информационно — не сигнал на вход,
# не завязан на автоторговлю.
WEEKLY_EMA_PERIODS = [7, 14, 28]
WEEKLY_EMA_TOUCH_ATR = 0.25       # тот же допуск касания на обоих ТФ (это доля ATR ЭТОГО же ТФ, масштаб уже учтён)
# Вход/Стоп/Тейк по ATR — подсмотрено у стороннего "AI EMA SIGNALS" (реальный
# пример APE: риск 0.0077/прибыль 0.0115 ≈ RR 1.5). Свои множители, не их
# точные цифры — мы не знаем их формулу, ATR-based даёт масштаб под
# конкретную волатильность монеты вместо фиксированного % для всех.
EMA_TOUCH_STOP_ATR_MULT = 1.5     # УСТАРЕЛО с v0.30.49, оставлено только как исторический дефолт для старых записей — см. EMA_TOUCH_STOP_FIXED_PCT
EMA_TOUCH_STOP_FIXED_PCT = 1.5     # v0.30.50: пересчитано под ЧАСОВОЙ масштаб (см. SLOW_IMPULSE_TF ниже) — калибровано по своей диагностике: avg_mae на 1h — 1.41-1.64% по трём периодам (median 0.64-0.76%, использую avg, не median — важно среднее ожидание, не типичная сделка). Старое значение 3.0% было откалибровано под НЕДЕЛЬНЫЙ масштаб (v0.30.49) — на часовом это было бы неоправданно широко
EMA_TOUCH_RR = 1.5                # тейк = стоп-дистанция × RR в прибыльную сторону — v0.30.39: больше не используется для расчёта тейка (см. EMA_TOUCH_TAKE_FIXED_PCT), оставлен только как исторический дефолт для старых записей без rr
EMA_TOUCH_TAKE_FIXED_PCT = 1.8    # v0.30.50: пересчитано под часовой масштаб — avg_mfe на 1h 1.76-1.84% по трём периодам. RR = 1.8/1.5 = 1.2. Старое значение 4.5% было калибровано под недельный тейк референс-бота (v0.30.39) — больше не актуально, переходим на свою, часовую стратегию
SLOW_IMPULSE_TF = "1h"            # v0.30.50: реальная находка по своей диагностике (ema_diagnostics, 8180 касаний) — недельный ТФ (все 3 периода) статистически ХУДШИЙ, avg_mae>avg_mfe везде; часовой — ЕДИНСТВЕННЫЙ ТФ, где во всех 3 периодах avg_mfe>avg_mae (1h p7: 1.84>1.41, p14: 1.78>1.43, p28: 1.76>1.64, bounce_rate 40-44%, n=604-855 — приличная выборка). Больше не гонимся исключительно за копированием референс-бота (он был на 1w) — переходим на то, что показывает реальный edge в своих же данных
SLOW_IMPULSE_TOUCH_COOLDOWN_SEC = 4 * 3600   # v0.30.50: старый WEEKLY_EMA_TOUCH_COOLDOWN_SEC=24ч был откалиброван под недельные бары — на часовых это неоправданно редко (пропустим много валидных повторных касаний). Диагностика не даёт готового ответа на "какой кулдаун оптимален" — 4ч осторожный компромисс (не спамить на каждый час, но не пропускать сутки), можно потом пересмотреть по реальным исходам
VOL_RATIO_CONFIDENCE_THRESHOLD = 1.2   # v0.30.8: порог из диагностики (5000 записей) — выше него отскок заметно надёжнее
# v0.11.0: раньше опрос был раз в 30 минут — по прямому фидбеку со скрина
# ("сигнал приходит не тогда, когда надо, цена уже упала") это было СЛИШКОМ
# редко: между проверками цена может уйти далеко от EMA. Причина 30 минут
# была не произвольной — на каждой проверке заново перекачивалась вся
# дневная история (дорого). Теперь дорогая часть (EMA/ATR по закрытым
# свечам) кэшируется и пересчитывается только когда должен закрыться новый
# бар (см. _ema_touch_get_closed_ctx, тот же приём, что _ema_get_closed_ctx
# в исходном движке) — а живая цена (дешёвый тикер-запрос) опрашивается
# намного чаще.
WEEKLY_EMA_SCAN_POLL_SEC = 60     # раз в минуту — дорогая часть закэширована, дёшево
WEEKLY_EMA_SCAN_TOP_N = 100
WEEKLY_EMA_FETCH_DAYS = 400       # с запасом под EMA28 на неделях (~28 недель=196 дней) + история ATR
DAILY_EMA_FETCH_DAYS = 120        # с запасом под EMA28 на днях (28 дней) + история ATR — намного короче
SLOW_IMPULSE_HOURLY_FETCH_DAYS = 30   # v0.30.50: с запасом под EMA28 на часах (28ч=~1.2дня) + стабильный ATR(14) — 30 дней = 720 часовых баров, разумный компромисс между качеством EMA и объёмом фетча (Gate.io отдаёт до 999 баров за запрос)
WEEKLY_EMA_TOUCH_COOLDOWN_SEC = 24 * 3600   # недельный ТФ: не спамить чаще раза в сутки по той же связке
DAILY_EMA_TOUCH_COOLDOWN_SEC = 8 * 3600     # дневной ТФ движется быстрее — кулдаун короче
EMA_TOUCH_TIMEFRAMES = ["1w", "1d"]         # какие ТФ сканирует live-вотчер
# "работаем только в шорт" — в LIVE-алертах фильтруем на только те касания,
# где классический bias == short (подход снизу вверх, тест сопротивления).
# В бэктесте long НЕ фильтруется (см. by_bias) — там обе стороны видны для
# сравнения, полезно проверить эмпирически, действительно ли short лучше.
EMA_TOUCH_LIVE_ONLY_SHORT = True

_weekly_ema_touch_lock = threading.Lock()
_weekly_ema_touch_state = {}   # "symbol|tf|period" -> last_alert_ts
_weekly_ema_recent = []        # последние сработавшие касания — для /weekly_ema_status
_weekly_ema_skipped = []       # v0.30.45: ОТДЕЛЬНЫЙ буфер для срезанных кандидатов
                                # (skipped_cooldown/skipped_no_anomaly) — НЕ персистится,
                                # НЕ используется резолвером исхода. Раньше (v0.30.44) эти
                                # записи шли в ТОТ ЖЕ _weekly_ema_recent — реальная находка:
                                # за один цикл сканирования (10 мин) это ~190 разных монет
                                # разом, буфер на 200 записей забивался целиком ОДНИМ циклом,
                                # и реально открытые сделки ("open"), которые ждут резолва
                                # тейка/стопа, вымывались из истории быстрее, чем успевали
                                # разрешиться — резолвер терял их из виду. На экспорте
                                # (/weekly_ema_export) оба буфера склеиваются в один файл —
                                # для пользователя выглядит так же, как раньше, а по факту
                                # реальные сделки больше не страдают от диагностического шума.
WEEKLY_EMA_HISTORY_FILE = os.path.expanduser("~/pumpradar_weekly_ema_history.json")


def _save_weekly_ema_history():
    """Атомарная запись (tmp-файл + os.replace) — если процесс убьют
    посреди записи (разряд телефона, OOM-killer, падение Termux), файл на
    диске не может остаться битым наполовину: либо целиком старая версия,
    либо целиком новая, третьего не дано."""
    try:
        tmp_path = WEEKLY_EMA_HISTORY_FILE + f".tmp{os.getpid()}"
        with open(tmp_path, "w") as f:
            json.dump(_weekly_ema_recent, f)
        os.replace(tmp_path, WEEKLY_EMA_HISTORY_FILE)
    except Exception as e:
        olog(f"[weekly_ema] ⚠ не смог сохранить историю: {_explain_error(e)}")


def _load_weekly_ema_history():
    """Подхватывает историю сигналов с прошлого запуска. ВАЖНО: раньше это
    делалось только внутри _weekly_ema_touch_loop — когда в v0.13.0 этот
    цикл остановили (заменили на триггер от пампа/дампа), подгрузка истории
    при старте молча перестала происходить вообще, и после любого
    перезапуска процесса (падение Termux, потеря интернета с разрывом
    сессии, OOM) все ОТКРЫТЫЕ сигналы теряли отслеживание — на диске
    оставались, но _weekly_ema_resolve_loop их больше не видел. Теперь
    вызывается прямо в main(), не зависит от того, какие циклы включены."""
    global _weekly_ema_recent
    try:
        if os.path.exists(WEEKLY_EMA_HISTORY_FILE):
            with open(WEEKLY_EMA_HISTORY_FILE) as f:
                loaded = json.load(f)
            if isinstance(loaded, list):
                _weekly_ema_recent = loaded
                still_open = sum(1 for r in loaded if r.get("outcome") == "open")
                olog(f"[weekly_ema] история подхвачена с диска: {len(loaded)} записей, "
                     f"из них ещё открыто {still_open}")
    except Exception as e:
        olog(f"[weekly_ema] ⚠ не смог подхватить историю с диска: {_explain_error(e)} "
             f"(файл мог повредиться при аварийном завершении — начинаем с пустой истории)")


def _migrate_open_signals_to_fixed_take():
    """v0.30.39: по прямому запросу — "надо учесть уже открытые сделки в
    ожидании". Формула тейка поменялась (ATR-based -> фиксированный %,
    см. EMA_TOUCH_TAKE_FIXED_PCT, подтверждено на 23 наблюдениях внешнего
    референс-бота) — без этой миграции уже открытые (ещё не решённые)
    сигналы остались бы зависшими на старой, ATR-based цели. Пересчитывает
    take/rr для ВСЕХ ещё открытых записей по НОВОЙ формуле, используя уже
    сохранённые entry/stop/classic_bias — стоп не трогает (по нему данных
    от референс-бота нет вообще). Запускается один раз при старте,
    сохраняет результат на диск.
    v0.30.54: реальный баг — эта миграция продолжала запускаться на
    КАЖДЫЙ рестарт и слепо перетирала take любой ещё открытой записи под
    ТЕКУЩЕЕ значение константы, даже когда та изменилась по СОВСЕМ другой
    причине (переход на часовой ТФ, v0.30.50) — древним зависшим сделкам
    с tf="1w" (открытым ещё на недельной ATR-based логике, стоп доходил
    до 971% от входа!) тихо подрезало тейк до нового часового 1.8%,
    создавая мешанину "новая цель + древний риск", которая давала
    случайные "победы", не имеющие отношения к реальной стратегии. Теперь
    трогает только записи с tf == SLOW_IMPULSE_TF (актуальная схема) —
    более старые (другой ТФ, другое поколение стратегии) вместо этого
    закрываются как "stale_retired" (см. ниже), не микшируются дальше."""
    global _weekly_ema_recent
    changed = 0
    retired = 0
    now = time.time()
    with _weekly_ema_touch_lock:
        for rec in _weekly_ema_recent:
            if rec.get("outcome") != "open":
                continue
            if rec.get("tf") != SLOW_IMPULSE_TF:
                rec["outcome"] = "stale_retired"
                rec["resolved_at"] = int(now)
                rec["resolved_price"] = None
                retired += 1
                continue
            entry, stop = rec.get("entry"), rec.get("stop")
            classic_bias = rec.get("classic_bias")
            if not entry or not stop or classic_bias not in ("short", "long"):
                continue
            stop_dist = abs(stop - entry)
            if classic_bias == "short":
                new_take = entry * (1 - EMA_TOUCH_TAKE_FIXED_PCT / 100)
            else:
                new_take = entry * (1 + EMA_TOUCH_TAKE_FIXED_PCT / 100)
            new_take_r = _round_price(new_take)
            if rec.get("take") == new_take_r:
                continue
            rec["take"] = new_take_r
            rec["rr"] = round(abs(new_take - entry) / stop_dist, 3) if stop_dist else None
            changed += 1
        if changed or retired:
            _save_weekly_ema_history()
    if changed:
        olog(f"[weekly_ema] пересчитан тейк на фиксированные {EMA_TOUCH_TAKE_FIXED_PCT}% "
             f"от входа для {changed} открытых сигналов текущей схемы ({SLOW_IMPULSE_TF})")
    if retired:
        olog(f"[weekly_ema] {retired} древних открытых сигналов другого поколения стратегии "
             f"(tf != {SLOW_IMPULSE_TF}) закрыты как stale_retired — не резолвятся дальше")
    return changed


_ema_touch_ctx_cache = {}   # "symbol|tf" -> {candles, closes, atr_v, i, ema_arrays, next_close_t} ИЛИ {"insufficient": True, next_close_t}
_ema_touch_ctx_lock = threading.Lock()
# монета без достаточной истории не "дорастает" до неё за минуты — не
# смысла дёргать полный фетч (до 400 дней для недельного ТФ) на каждую
# проверку после каждого пампа/дампа. Перепроверяем раз в несколько дней.
EMA_TOUCH_INSUFFICIENT_HISTORY_RECHECK_SEC = 3 * 86400


def _ema_touch_get_closed_ctx(symbol, tf):
    """Дорогая часть (фетч дневной истории + EMA/ATR по ЗАКРЫТЫМ барам) —
    кэшируется и пересчитывается только когда по времени должен был
    закрыться новый бар этого ТФ (без похода в API, чистая арифметика от
    времени последнего известного закрытия), а не на каждый опрос.
    Отдельно кэширует НЕДОСТАТОЧНОСТЬ истории (новые листинги) — иначе
    каждый памп/дамп на свежей монете зря дёргал бы полный фетч и получал
    один и тот же отрицательный результат снова и снова."""
    key = f"{symbol}|{tf}"
    now = time.time()
    with _ema_touch_ctx_lock:
        cached = _ema_touch_ctx_cache.get(key)
    if cached and now < cached["next_close_t"]:
        return None if cached.get("insufficient") else cached
    fetch_days = WEEKLY_EMA_FETCH_DAYS if tf == "1w" else (SLOW_IMPULSE_HOURLY_FETCH_DAYS if tf == "1h" else DAILY_EMA_FETCH_DAYS)
    # v0.30.50: реальный баг — тут ВСЕГДА тянулись ДНЕВНЫЕ свечи
    # (_fetch_candles(symbol, "1d", ...)), а для любого tf, кроме "1w"
    # (ресемпл), просто использовались как есть — то есть при переходе
    # slow_impulse на "1h" код бы молча посчитал EMA/ATR по ДНЕВНЫМ барам,
    # выдавая их за часовые. Gate.io API и так поддерживает нативный
    # интервал "1h" (используется в других местах, напр. поиск аномалий
    # объёма) — просто нужно было фетчить ЕГО, а не подменять.
    if tf == "1w":
        raw = _fetch_candles(symbol, "1d", fetch_days)
        candles = _resample_to_weekly(raw)
    elif tf == "1d":
        candles = _fetch_candles(symbol, "1d", fetch_days)
    else:
        candles = _fetch_candles(symbol, tf, fetch_days)   # нативный фетч этого интервала (1h и т.п.)
    min_bars = max(WEEKLY_EMA_PERIODS) + 5
    if len(candles) < min_bars:
        with _ema_touch_ctx_lock:
            _ema_touch_ctx_cache[key] = {
                "insufficient": True,
                "next_close_t": now + EMA_TOUCH_INSUFFICIENT_HISTORY_RECHECK_SEC,
            }
        olog(f"[weekly_ema] {symbol} {tf}: недостаточно истории для EMA28 "
             f"({len(candles)} баров из {min_bars} нужных, похоже на новый листинг) — "
             f"пропускаем, перепроверим через "
             f"{EMA_TOUCH_INSUFFICIENT_HISTORY_RECHECK_SEC // 86400} дн.")
        return None
    closes = [c["close"] for c in candles]
    atr_arr = _atr(candles, 14)
    i = len(candles) - 1
    atr_v = atr_arr[i]
    if not atr_v:
        return None
    ema_arrays = {p: _ema(closes, p) for p in WEEKLY_EMA_PERIODS}
    if any(arr[i] is None for arr in ema_arrays.values()):
        return None
    interval_sec = 86400 * 7 if tf == "1w" else TF_SECONDS.get(tf, 86400)   # v0.30.50: было 86400 (сутки) для ВСЕГО не-1w, включая часовой — контекст обновлялся бы раз в сутки вместо раза в час
    ctx = {"candles": candles, "closes": closes, "atr_v": atr_v, "i": i,
           "ema_arrays": ema_arrays, "next_close_t": candles[i]["t"] + interval_sec + 5}
    with _ema_touch_ctx_lock:
        _ema_touch_ctx_cache[key] = ctx
    return ctx


def _check_ema28w_proximity(symbol, price):
    """v0.30.13: по запросу — отдельно от исходного сигнала. Подтверждено
    на диагностике (3800+ наблюдений): 1w EMA28 — самый заметный уровень
    из всех проверенных (60.5% отскок для шорта на n=76, против 25-40% у
    большинства других связок). Пока мы продолжаем отслеживать УЖЕ
    сработавший сигнал (памп/дамп/аномалия объёма), эта функция проверяет
    — не подошла ли цена ПОЗЖЕ (не обязательно в момент исходного
    сигнала) близко к этому конкретному уровню. Переиспользует тот же
    кэшированный контекст и тот же допуск (WEEKLY_EMA_TOUCH_ATR), что и
    обычная проверка касания — ничего нового не считает, просто смотрит
    отдельно."""
    try:
        ctx = _ema_touch_get_closed_ctx(symbol, "1w")
        if not ctx:
            return None
        i = ctx["i"]
        atr_v = ctx["atr_v"]
        ema28_arr = ctx["ema_arrays"].get(28)
        if not atr_v or not ema28_arr or ema28_arr[i] is None:
            return None
        ema_value = ema28_arr[i]
        dist_atr = abs(price - ema_value) / atr_v
        if dist_atr <= WEEKLY_EMA_TOUCH_ATR:
            return {"ema_value": ema_value, "dist_atr": round(dist_atr, 3)}
        return None
    except Exception:
        return None


def _maybe_fire_ema28w_reversal_alert(rec, symbol, price, source_label):
    """Общий помощник для трёх трек-лупов (памп/дамп/аномалия объёма) —
    если запись ещё не помечена (ema28w_alerted), проверяет близость к
    EMA28(W) и, если найдена, шлёт ОТДЕЛЬНОЕ уведомление и помечает
    запись, чтобы не спамить повторно на каждом опросе. Возвращает True,
    если запись изменилась (нужно сохранить).
    v0.30.14: по прямому вопросу — "есть ли разница памп/дамп/аномалия
    объёма, может после аномалии шанс на отскок вниз ещё выше" — сейчас
    ответить нечем, потому что событие просто слалось и забывалось, без
    отслеживания дальнейшей цены и источника. Теперь при каждом
    срабатывании создаётся ОТДЕЛЬНАЯ отслеживаемая запись (со своим
    source — pump/dump/vol_anomaly) в EMA28W_TOUCH_HISTORY_FILE — свой
    трек-луп (_ema28w_touch_track_loop) следит за ней ещё
    EMA28W_TOUCH_TRACK_WINDOW_SEC часов, считая max_follow/max_reverse. Как
    накопится достаточно — можно будет честно сравнить винрейт по
    источнику, не гадая."""
    if rec.get("ema28w_alerted"):
        return False
    prox = _check_ema28w_proximity(symbol, price)
    if not prox:
        return False
    rec["ema28w_alerted"] = True
    source_display = {"pump": "памп", "dump": "дамп", "vol_anomaly": "аномалия объёма"}.get(source_label, source_label)
    msg = (f"📍 <b>{symbol}</b> — цена после сигнала ({source_display}) подошла к EMA28(W)\n"
           f"Уровень: {_fmt_px(prox['ema_value'])} | текущая цена: {_fmt_px(price)} | "
           f"расстояние: {prox['dist_atr']} ATR\n"
           f"По диагностике — самый заметный уровень из проверенных (60.5% отскок для шорта на n=76)")
    _send_alert(msg)
    olog(f"[ema28w] {symbol}: цена подошла к EMA28(W) во время отслеживания {source_display} "
         f"({price:.6g} рядом с {prox['ema_value']:.6g}, {prox['dist_atr']} ATR)")
    now = time.time()
    touch_rec = {
        "ts": int(now), "symbol": symbol, "source": source_label,
        "price": price, "ema_value": prox["ema_value"], "dist_atr": prox["dist_atr"],
        "track_until": now + EMA28W_TOUCH_TRACK_WINDOW_SEC, "track_done": False,
        "max_follow_pct": 0.0, "max_reverse_pct": 0.0,
        "last_tracked_price": price, "last_tracked_ts": int(now),
    }
    _pump_dump_history_append(EMA28W_TOUCH_HISTORY_FILE, touch_rec)
    return True


def _ema_touch_check_symbol(symbol, tf="1w"):
    """Живая (дешёвая) часть — берёт закэшированный контекст (см.
    _ema_touch_get_closed_ctx) и проверяет касание ТЕКУЩЕЙ живой ценой
    любого из трёх уровней (цена экстраполирует EMA на текущий момент
    через _ema_live_value — та же логика, что в исходном движке, просто
    переиспользована здесь как чистая функция). Для каждого касания также
    определяет СТОРОНУ подхода (по предыдущей закрытой свече — была она
    выше или ниже уровня) и классическую bounce-трактовку (снизу вверх =
    тест сопротивления = short-ожидание отскока; сверху вниз = тест
    поддержки = long-ожидание отскока), плюс конкретные Вход/Стоп/Тейк по
    ATR (RR по умолчанию ~1.5, подсмотрено у стороннего "AI EMA SIGNALS" —
    на их примере APE вход/стоп/тейк дали риск 0.0077/прибыль 0.0115 ≈
    RR 1.5) — это ИНФОРМАЦИЯ о контексте, не готовый сигнал на автоторговлю
    (её тут нет). Возвращает список touch-записей (может быть больше
    одной, если цена одновременно у нескольких уровней) или пустой
    список. В каждой записи также live_ema_values — экстраполированные
    "сейчас" значения ВСЕХ трёх EMA (не только сработавшей) — нужно, чтобы
    график к алерту рисовал линии до текущего момента, а не обрывался на
    вчерашнем закрытии (именно этот разрыв и создавал впечатление
    "сигнал после нужного момента" на скрине)."""
    ctx = _ema_touch_get_closed_ctx(symbol, tf)
    if ctx is None:
        return []
    candles, closes, atr_v, i, ema_arrays = (ctx["candles"], ctx["closes"], ctx["atr_v"],
                                              ctx["i"], ctx["ema_arrays"])
    live_price = _gate_get_price(symbol)
    price = live_price if live_price else closes[i]
    live_ema_values = {p: (_ema_live_value(ema_arrays[p][i], price, p) if live_price else ema_arrays[p][i])
                        for p in WEEKLY_EMA_PERIODS}
    ladder = _ladder_order([price] + [live_ema_values[p] for p in WEEKLY_EMA_PERIODS])
    touches = []
    for period in WEEKLY_EMA_PERIODS:
        ema_arr = ema_arrays[period]
        ema_closed = ema_arr[i]
        ema_now = live_ema_values[period]
        dist = abs(price - ema_now)
        tol = WEEKLY_EMA_TOUCH_ATR * atr_v
        if dist <= tol:
            prev_close = closes[i - 1] if i > 0 else closes[i]
            prev_ema = ema_arr[i - 1] if (i > 0 and ema_arr[i - 1] is not None) else ema_closed
            side = "from_below" if prev_close < prev_ema else "from_above"
            classic_bias = "short" if side == "from_below" else "long"
            stop_dist = level * (EMA_TOUCH_STOP_FIXED_PCT / 100)
            # v0.12.0: Вход = САМ УРОВЕНЬ (лимитка "ждём откат к EMA"), а не
            # текущая живая цена — подсмотрено на реальных скринах стороннего
            # бота (там entry систематически ОТЛИЧАЕТСЯ от текущей цены,
            # ближе к конкретному EMA-уровню; их RR тоже ровно 1.5, но
            # посчитан именно от этой лимитки, не от цены). Раньше entry=price
            # означало "вход по рынку прямо сейчас" — что и создавало путаницу
            # с моментом входа, раз цена к моменту алерта уже могла отойти.
            level = ema_now
            if classic_bias == "short":
                entry, stop = level, level + stop_dist
                take = level * (1 - EMA_TOUCH_TAKE_FIXED_PCT / 100)
            else:
                entry, stop = level, level - stop_dist
                take = level * (1 + EMA_TOUCH_TAKE_FIXED_PCT / 100)
            # v0.30.39: rr теперь не константа — тейк фиксированный % от
            # входа, стоп по-прежнему ATR-based, так что фактическое
            # соотношение прибыль/риск теперь МЕНЯЕТСЯ от сигнала к
            # сигналу (у волатильных монет стоп шире при том же тейке —
            # RR ниже, и наоборот). Считаем фактическое значение, не
            # берём константу.
            actual_rr = round(abs(take - entry) / stop_dist, 3) if stop_dist else None
            # v0.16.2: для очень волатильных дешёвых монет ATR может быть
            # настолько большим относительно самой цены (реально видели
            # >45% от цены), что stop_dist*RR превышает саму цену — тейк
            # (или, реже, стоп) уходит в ОТРИЦАТЕЛЬНУЮ цену, что бессмысленно
            # физически. Лучше молча пропустить такое касание, чем прислать
            # сигнал с ценой ниже нуля.
            if stop <= 0 or take <= 0 or entry <= 0:
                olog(f"[weekly_ema] {symbol} {tf} EMA{period}: пропуск — ATR слишком "
                     f"большой относительно цены (entry={entry:.6g} stop={stop:.6g} "
                     f"take={take:.6g}), стоп/тейк ушли бы в отрицательную цену")
                continue
            # v0.23.0: side/classic_bias считается по ПРЕДЫДУЩЕМУ закрытому
            # бару (вчера), а entry=level сравнивается с ЖИВОЙ ценой
            # (сейчас) — если цена внутри текущего (ещё не закрытого) бара
            # уже прошла уровень НАСКВОЗЬ, эти два получаются рассогласованы:
            # для SHORT вход обязан быть ВЫШЕ текущей цены (продаём дороже,
            # ждём отскока ВНИЗ от уровня) — если уровень уже НИЖЕ цены,
            # значит цена его уже пробила, а не тестирует свежо снизу, как
            # предполагает bias. Зеркально для LONG (вход обязан быть
            # НИЖЕ цены). Сверено на реальных примерах — у всех рабочих
            # его сигналов эта связь строго соблюдается (SHORT: вход выше
            # цены, LONG: вход ниже). Если не соблюдается — сторона и
            # уровень уже рассогласованы внутри дня, тихо пропускаем.
            if (classic_bias == "short" and entry < price) or (classic_bias == "long" and entry > price):
                olog(f"[weekly_ema] {symbol} {tf} EMA{period}: пропуск — вход ({entry:.6g}) "
                     f"с неверной стороны от живой цены ({price:.6g}) для {classic_bias} "
                     f"— уровень уже пробит внутри дня, side/bias устарели")
                continue
            levels = []
            for lvl_period in WEEKLY_EMA_PERIODS:
                lvl_val = live_ema_values[lvl_period]
                role = "поддержка снизу" if price > lvl_val else "сопротивление сверху"
                levels.append({"period": lvl_period, "value": _round_price(lvl_val), "role": role})
            touches.append({
                "symbol": symbol, "tf": tf, "period": period, "price": price,
                "ema_value": ema_now, "dist_atr": round(dist / atr_v, 3),
                "ladder": ladder, "side": side, "classic_bias": classic_bias,
                "entry": _round_price(entry), "stop": _round_price(stop),
                "take": _round_price(take), "rr": actual_rr, "levels": levels,
                "week_t": candles[i]["t"], "vol_ratio": _vol_ratio(candles, i),
                "candles": candles, "ema_arrays": ema_arrays, "bar_i": i,
                "live_ema_values": live_ema_values, "live_t": int(time.time()),
            })
    return touches


def _render_ema_touch_chart_png(touch, lookback=50):
    """Свечи + 3 линии EMA (7/14/28) поверх + пунктирные уровни Вход/Стоп/
    Тейк + белая точка на текущей цене — в стиле стороннего "AI EMA
    SIGNALS" (тёмная тема, не белый фон как у Pump-чартов). Требует
    Pillow — если недоступен, возвращает None (тогда _send_alert_photo
    сама откатится на текстовый алерт)."""
    try:
        from PIL import Image, ImageDraw, ImageFont
    except ImportError:
        return None
    candles = touch.get("candles")
    ema_arrays = touch.get("ema_arrays")
    bar_i = touch.get("bar_i")
    if not candles or not ema_arrays or bar_i is None:
        return None

    W, H = 960, 620
    pad_l, pad_r, pad_t, pad_b = 60, 60, 50, 130
    bg = (13, 13, 18)
    grid_color = (40, 40, 48)
    text_color = (200, 200, 205)
    green = (46, 189, 133)
    red = (240, 90, 90)
    ema_colors = {7: (66, 133, 244), 14: (156, 120, 230), 28: (230, 168, 60)}
    entry_col = (80, 210, 120)
    stop_col = (230, 90, 90)
    take_col = (100, 220, 140)

    entry, stop, take = touch["entry"], touch["stop"], touch["take"]
    live_ema_values = touch.get("live_ema_values") or {}
    live_price = touch["price"]
    lo = max(0, bar_i - lookback + 1)
    window = candles[lo:bar_i + 1]
    n_hist = len(window)
    if n_hist < 2:
        return None
    # v0.11.0: раньше график обрывался на последней ЗАКРЫТОЙ свече (вчера),
    # а белая точка стояла НА НЕЙ — по факту это создавало видимый разрыв
    # с реальной живой ценой, по которой сработал алерт (жалоба со скрина:
    # "цена уже упала, сигнал должен быть на линии EMA" — на деле линия
    # просто не дорисована до текущего момента). Добавляем ОДНУ виртуальную
    # точку "сейчас" — не настоящую закрытую свечу, а маркер живой цены и
    # экстраполированных EMA, чтобы линии и точка визуально доходили туда,
    # где касание реально проверялось.
    n = n_hist + 1
    ema_window = {p: list(arr[lo:bar_i + 1]) + [live_ema_values.get(p)] for p, arr in ema_arrays.items()}

    highs = [c["high"] for c in window]
    lows = [c["low"] for c in window]
    all_vals = highs + lows + [entry, stop, take, live_price]
    for arr in ema_window.values():
        all_vals += [v for v in arr if v is not None]
    pmin, pmax = min(all_vals), max(all_vals)
    if pmax <= pmin:
        pmax = pmin + max(abs(pmin) * 0.001, 1e-9)
    pad_range = (pmax - pmin) * 0.08
    pmin -= pad_range
    pmax += pad_range

    img = Image.new("RGB", (W, H), bg)
    d = ImageDraw.Draw(img)
    try:
        font = ImageFont.load_default()
    except Exception:
        font = None

    plot_w = W - pad_l - pad_r
    plot_h = H - pad_t - pad_b

    def x_of(i):
        return pad_l + (i / (n - 1) * plot_w if n > 1 else plot_w / 2)

    def y_of(v):
        return pad_t + (pmax - v) / (pmax - pmin) * plot_h

    for k in range(5):
        v = pmin + (pmax - pmin) * k / 4.0
        yy = y_of(v)
        d.line([(pad_l, yy), (W - pad_r, yy)], fill=grid_color, width=1)
        d.text((4, yy - 5), f"{v:.6g}", fill=text_color, font=font)

    body_w = max(2, plot_w / n_hist * 0.55)
    for i, c in enumerate(window):
        xc = x_of(i)
        bullish = c["close"] >= c["open"]
        color = green if bullish else red
        d.line([(xc, y_of(c["high"])), (xc, y_of(c["low"]))], fill=color, width=1)
        y_o, y_c = y_of(c["open"]), y_of(c["close"])
        ytop, ybot = min(y_o, y_c), max(y_o, y_c)
        if ybot - ytop < 1.5:
            ybot = ytop + 1.5
        d.rectangle([xc - body_w / 2, ytop, xc + body_w / 2, ybot], fill=color)

    # тонкая линия от последней ЗАКРЫТОЙ свечи к живой точке — визуально
    # показывает, что дальше это ещё не закрытый бар, а текущая живая цена
    d.line([(x_of(n_hist - 1), y_of(window[-1]["close"])), (x_of(n_hist), y_of(live_price))],
           fill=(120, 120, 130), width=1)

    for p in (7, 14, 28):
        pts = [(x_of(i), y_of(v)) for i, v in enumerate(ema_window.get(p, [])) if v is not None]
        if len(pts) >= 2:
            d.line(pts, fill=ema_colors[p], width=2)

    def dashed_hline(v, color, label):
        yy = y_of(v)
        xx = pad_l
        while xx < W - pad_r:
            d.line([(xx, yy), (min(xx + 7, W - pad_r), yy)], fill=color, width=2)
            xx += 12
        d.text((W - pad_r + 4, yy - 5), label, fill=color, font=font)
    dashed_hline(entry, entry_col, "ВХОД")
    dashed_hline(stop, stop_col, "СТОП")
    dashed_hline(take, take_col, "ТЕЙК")

    # белая точка — ЖИВАЯ цена (по которой реально сработало касание), а не
    # последняя закрытая свеча (та стояла бы на день/неделю "в прошлом" —
    # именно этот разрыв и создавал впечатление "сигнал после нужного
    # момента" на реальном скрине).
    live_x, live_y = x_of(n - 1), y_of(live_price)
    d.ellipse([live_x - 4, live_y - 4, live_x + 4, live_y + 4], fill=(255, 255, 255))

    tf_label = "W" if touch["tf"] == "1w" else "D"
    title = f"{touch['symbol']} | EMA{touch['period']}({tf_label}) | {touch['classic_bias'].upper()}"
    d.text((pad_l, 10), title, fill=(255, 255, 255), font=font)

    lx = pad_l
    for p in (7, 14, 28):
        d.line([(lx, 30), (lx + 16, 30)], fill=ema_colors[p], width=2)
        d.text((lx + 20, 25), f"EMA{p}", fill=text_color, font=font)
        lx += 70

    step = max(1, n_hist // 8)
    for i in range(0, n_hist, step):
        xx = x_of(i)
        lbl = time.strftime("%m-%d", time.localtime(window[i]["t"]))
        d.text((xx - 16, H - pad_b + 8), lbl, fill=text_color, font=font)
    d.text((x_of(n - 1) - 16, H - pad_b + 8), "сейчас", fill=(255, 255, 255), font=font)

    ty = H - pad_b + 30
    d.text((pad_l, ty), f"Цена: {touch['price']:.6g}", fill=text_color, font=font)
    d.text((pad_l, ty + 14), f"Вход: {entry:.6g}", fill=entry_col, font=font)
    d.text((pad_l, ty + 28), f"Стоп: {stop:.6g}  (RR {touch.get('rr', EMA_TOUCH_RR)})", fill=stop_col, font=font)
    d.text((pad_l, ty + 42), f"Тейк: {take:.6g}", fill=take_col, font=font)

    buf = io.BytesIO()
    img.save(buf, format="PNG")
    return buf.getvalue()


VOL_PEAK_WINRATE_MIN_SAMPLE = 10   # v0.30.6: не показываем статистику по связке (ТФ/период), пока сделок меньше этого — мала выборка, введёт в заблуждение


def _vol_peak_historical_winrate(tf, period):
    """v0.30.6: по запросу — обратная связь по накопленной статистике.
    Смотрит на УЖЕ РЕШЁННЫЕ (take/stop) сигналы, пришедшие именно от
    Volume Peak Watcher (triggered_by начинается с "vol_anomaly|"), для
    ТОЙ ЖЕ связки таймфрейм+период EMA, и считает реальный винрейт.
    Пока сделок меньше VOL_PEAK_WINRATE_MIN_SAMPLE — возвращает None,
    чтобы не сбивать с толку статистикой на 2-3 наблюдениях."""
    with _weekly_ema_touch_lock:
        snapshot = list(_weekly_ema_recent)
    resolved = [r for r in snapshot
                if r.get("tf") == tf and r.get("period") == period
                and (r.get("triggered_by") or "").startswith("vol_anomaly|")
                and r.get("outcome") in ("take", "stop")]
    if len(resolved) < VOL_PEAK_WINRATE_MIN_SAMPLE:
        return None
    wins = sum(1 for r in resolved if r["outcome"] == "take")
    return wins, len(resolved)


def _weekly_ema_fire_alert(touch, confidence_note=None, triggered_by=None, anomaly_z=None, anomaly_count=None):
    # v0.30.8: по запросу — фильтр по объёму в момент касания, на
    # реальных данных диагностики (5000 записей): при vol_ratio > 1.2
    # отскок случается заметно чаще (SHORT 45.5% против 27.7% на обычном
    # объёме, n=995/1893; LONG 43.6% против 24.7%, n=599/1454) — эффект
    # держится симметрично в обе стороны. Не блокируем сигнал полностью
    # (даже на обычном объёме отскок не редкость, ~27%), но чётко
    # помечаем, когда объём в моменте не подтверждён.
    if touch.get("vol_ratio") is not None and touch["vol_ratio"] <= VOL_RATIO_CONFIDENCE_THRESHOLD:
        vol_note = (f"объём в момент касания не повышен (vol_ratio={touch['vol_ratio']}) — "
                    f"на диагностике касания с обычным объёмом отскакивали заметно реже "
                    f"(~27% против ~45% при повышенном объёме)")
        confidence_note = f"{confidence_note}; {vol_note}" if confidence_note else vol_note
    # v0.30.6: если сигнал пришёл от Volume Peak Watcher, добавляем
    # реальную статистику по этой же связке (ТФ/период), если данных
    # накопилось достаточно — иначе молчим, не гадаем на 2-3 сделках.
    if triggered_by and triggered_by.startswith("vol_anomaly|"):
        stats = _vol_peak_historical_winrate(touch["tf"], touch["period"])
        if stats:
            wins, total = stats
            stats_note = (f"по накопленной статистике эта связка (EMA{touch['period']}, "
                          f"{'неделя' if touch['tf']=='1w' else 'день'}) от аномалии объёма "
                          f"дала {wins}/{total} = {wins/total*100:.0f}% в тейк")
            confidence_note = f"{confidence_note}; {stats_note}" if confidence_note else stats_note
    ladder_note = {"up": "лесенка вверх (здоровый up-тренд)",
                   "down": "лесенка вниз (здоровый down-тренд)",
                   None: "лесенка не выстроена (боковик/переход)"}[touch["ladder"]]
    side_note = ("снизу вверх, тест сопротивления" if touch["side"] == "from_below"
                 else "сверху вниз, тест поддержки")
    bias_note = "классический bounce-контекст: SHORT" if touch["classic_bias"] == "short" \
                else "классический bounce-контекст: LONG"
    levels_lines = "\n".join(
        f"• EMA{lv['period']}({'W' if touch['tf']=='1w' else 'D'}): {_fmt_px(lv['value'])} — {lv['role']}"
        for lv in touch.get("levels", [])
    )
    confidence_line = f"\n⚠️ {confidence_note}" if confidence_note else ""
    msg = (f"📐 <b>{touch['symbol']}</b> | EMA{touch['period']}({'W' if touch['tf']=='1w' else 'D'}) | "
           f"{touch['classic_bias'].upper()}\n"
           f"Цена: {_fmt_px(touch['price'])}\n"
           f"Вход: {_fmt_px(touch['entry'])} (лимитка, ждём откат к уровню)\n"
           f"Стоп: {_fmt_px(touch['stop'])} | Тейк: {_fmt_px(touch['take'])} (RR {touch['rr']})\n\n"
           f"Уровни:\n{levels_lines}\n\n"
           f"Расстояние: {touch['dist_atr']} ATR | подход {side_note}\n"
           f"{bias_note} (если сыграет отскок, а не пробой) | {ladder_note}"
           f"{confidence_line}")
    png = None
    try:
        png = _render_ema_touch_chart_png(touch)
    except Exception as e:
        olog(f"[weekly_ema] {touch['symbol']}: ошибка рендера графика: {_explain_error(e)}")
    _send_alert_photo(png, msg)
    olog(f"[weekly_ema] {touch['symbol']}: касание EMA{touch['period']} на {touch['tf']} "
         f"(dist={touch['dist_atr']} ATR, side={touch['side']}) — алерт отправлен"
         + (f" [{confidence_note}]" if confidence_note else ""))
    rec = {"ts": int(time.time()), "symbol": touch["symbol"], "tf": touch["tf"],
           "period": touch["period"],
           "price": touch["price"], "ema_value": touch["ema_value"],
           "dist_atr": touch["dist_atr"], "ladder": touch["ladder"],
           "side": touch["side"], "classic_bias": touch["classic_bias"],
           "entry": touch["entry"], "stop": touch["stop"], "take": touch["take"],
           "rr": touch["rr"], "confidence_note": confidence_note, "triggered_by": triggered_by,
           "vol_ratio": touch.get("vol_ratio"),
           "anomaly_z": anomaly_z, "anomaly_count": anomaly_count,
           "outcome": "open", "resolved_at": None, "resolved_price": None}
    global _weekly_ema_recent
    # v0.29.12: аудит — единственное место, где этот список менялся БЕЗ
    # _weekly_ema_touch_lock, хотя чтение/очистка/сохранение отовсюду его
    # используют. Функция может вызываться параллельно из пампа, дампа и
    # аномалии объёма одновременно — реальный риск гонки на структурной
    # мутации общего списка.
    with _weekly_ema_touch_lock:
        _weekly_ema_recent.append(rec)
        _weekly_ema_recent = _weekly_ema_recent[-200:]
        _save_weekly_ema_history()


WEEKLY_EMA_RESOLVE_POLL_SEC = 120   # раз в 2 минуты — недельные/дневные уровни не убегают быстро
WEEKLY_EMA_ENTRY_FILL_TIMEOUT_SEC = 3 * 86400   # v0.30.41: если цена так и не дошла до точного уровня входа за 3 дня — считаем, что сделки не было (лимитка не исполнилась), не тейк и не стоп


SLOW_IMPULSE_MIN_WEEKLY_CHG_PCT = 15.0   # v0.30.28: по прямому сравнению с внешним референс-ботом — 6 из 8 их сигналов имели |изменение за 7 недельных баров| в диапазоне 18-109%, порог поставлен консервативно ниже этого диапазона
SLOW_IMPULSE_SCAN_POLL_SEC = 600         # 10 минут — менее срочно, чем быстрые памп/дамп-детекторы, само накопленное движение никуда не убежит за 10 минут
SLOW_IMPULSE_ENABLED = True              # УСТАРЕЛО с v0.30.53 — реальный вкл/выкл теперь через _get_slow_impulse_enabled() (UI-переключатель, персистится в scan_settings), эта константа больше не читается
SLOW_IMPULSE_MIN_VOLUME_USD = 5_000    # v0.30.38: реальная находка — порог $150к (v0.30.29) СНОВА резал реальный сигнал (BANANA_USDT, объём $21,643) — уже третий раз находим их сигнал на объёме ниже нашего же "низкого" порога. Опущено до почти нулевого уровня (отсеивает только полностью мёртвые листинги) — реальную защиту от проскальзывания даёт проверка СПРЕДА (max_spread_pct, тот же общий порог), это более прямой показатель, чем сырой объём
SLOW_IMPULSE_MAX_VOLUME_USD = 20_000_000   # v0.30.43: по разбору 20 его сигналов — максимум среди них $17.5М (B_USDT), у 19 из 20 сильно ниже; после гейта по аномалии объёма (v0.30.42) slow_impulse начал стрелять по BTC/ETH/SOL/DOGE (объём — миллиарды), которых референс-бот не даёт НИ РАЗУ — крупные монеты тоже иногда ловят z>=4.0 на общерыночных распродажах, но это другая механика, не то, что он торгует. Потолок с запасом (~15% выше $17.5М) отсекает именно мажоры/крупные альты, не трогая его реальный диапазон
SLOW_IMPULSE_REQUIRE_SELL_ANOMALY = True   # v0.30.42: по разбору 18 ручных сканов его сигналов (manual_scans) — ВСЕ 18 (кроме ROAM, недостаточно истории) имели доминирующую распродажную (red-candle) аномалию объёма (z>=VOL_PEAK_ZSCORE_THRESHOLD=4.0) на 1h ИЛИ 4h где-то в истории (мин. найденный max_z=5.0, у остальных 6.3-11.4) — этого фильтра у slow_impulse не было вообще, отсюда и разрыв в количестве (мы стреляли по касанию EMA само по себе, без этого условия, почти по всему универсуму). Использует ТУ ЖЕ _manual_scan_find_anomalies, что и ручной скан (тот же порог, те же lookback), просто ТЕПЕРЬ как блокирующий гейт, а не только для диагностики


def _weekly_chg_7bar_pct(ctx):
    """v0.30.28: изменение цены за последние 7 ЗАКРЫТЫХ баров ЭТОГО ТФ
    (для 1w — 7 недель) — грубая мера НАКОПЛЕННОГО движения, в отличие от
    резкого рывка за минуты, который ловят памп/дамп-детекторы."""
    closes = ctx["closes"]
    i = ctx["i"]
    if i - 7 < 0 or not closes[i - 7]:
        return None
    return round((closes[i] - closes[i - 7]) / closes[i - 7] * 100, 2)


def _slow_impulse_maybe_trigger_ema_signal(symbol):
    """v0.30.28: по прямому запросу — сравнение с внешним референс-ботом
    показало, что 6 из 8 их сигналов НЕ имели резкого пампа/дампа (наш
    пампdetector физически не мог их поймать — движение растянуто на
    недели, не минуты), а крупное НАКОПЛЕННОЕ движение за несколько
    недель, из-за которого цена вернулась к недельной EMA. Отдельный,
    независимый от пампа/дампа/аномалии объёма источник сигналов — та же
    логика касания, что и везде (_ema_touch_check_symbol, включая
    "вход=сам уровень EMA" — уже совпадала с их ботом ДО этого сравнения).
    Использует ТОТ ЖЕ _weekly_ema_touch_state/cooldown, что и остальные
    источники — не задублирует алерт, если по этой же связке (символ/
    период) уже сработал памп/дамп/аномалия объёма недавно. Только 1w —
    по прямому уточнению, "точки для отскока только ема 7 14 и 28 на
    неделе".
    v0.30.35: реальная находка на РАСШИРЕННОЙ выборке (19 сигналов
    суммарно за две партии) — порог по накопленному движению
    (SLOW_IMPULSE_MIN_WEEKLY_CHG_PCT=15%) резал БОЛЬШЕ реальных сигналов
    референс-бота, чем помогал: 6 из 9 их сигналов, прошедших по
    расстоянию до EMA, НЕ набирали 15% по моментуму (ACE +0.9%, PROVE
    -3.9%, EPIC -5.8% — далеко ниже, не просто "чуть не дотянули").
    Расстояние до EMA само по себе оказалось надёжным критерием (9 из 11
    прошли), момент — нет. Убран как БЛОКИРУЮЩИЙ фильтр, оставлен только
    как информационная пометка в тексте алерта, когда он есть."""
    try:
        ctx = _ema_touch_get_closed_ctx(symbol, SLOW_IMPULSE_TF)
    except Exception as e:
        olog(f"[slow_impulse] {symbol}: ошибка получения контекста: {_explain_error(e)}")
        return
    if not ctx:
        return
    weekly_chg = _weekly_chg_7bar_pct(ctx)
    try:
        touches = _ema_touch_check_symbol(symbol, SLOW_IMPULSE_TF)
    except Exception as e:
        olog(f"[slow_impulse] {symbol}: ошибка проверки касания: {_explain_error(e)}")
        return
    now = time.time()
    if not touches:
        return
    # v0.30.37: реальная находка — мы слали ОТДЕЛЬНЫЙ алерт на КАЖДЫЙ
    # период EMA, который подходил по расстоянию одновременно (пример из
    # логов: TLM сработал на EMA7+14+28 разом, WLD и NEAR — по 2 раза
    # каждая) — раздувало число "лишних" алертов относительно
    # референс-бота, который шлёт один сигнал на монету, по ближайшему
    # уровню. Теперь берём только САМОЕ БЛИЗКОЕ касание (наименьший
    # dist_atr), не все подряд.
    touch = min(touches, key=lambda t: t.get("dist_atr", 999))
    key = f"{symbol}|{touch['tf']}|{touch['period']}"
    with _weekly_ema_touch_lock:
        last = _weekly_ema_touch_state.get(key, 0)
    if now - last < SLOW_IMPULSE_TOUCH_COOLDOWN_SEC:
        _slow_impulse_append_diag(touch, outcome="skipped_cooldown",
                                   cooldown_remaining_sec=round(SLOW_IMPULSE_TOUCH_COOLDOWN_SEC - (now - last)),
                                   anomaly_z=None, anomaly_count=None, weekly_chg=weekly_chg)
        return
    max_anomaly_z = None
    anomaly_count = 0
    if SLOW_IMPULSE_REQUIRE_SELL_ANOMALY:
        for tf_a in MANUAL_SCAN_TIMEFRAMES:
            anomalies = _manual_scan_find_anomalies(symbol, tf_a, MANUAL_SCAN_LOOKBACK_DAYS[tf_a])
            if isinstance(anomalies, list):
                anomaly_count += len(anomalies)
                for a in anomalies:
                    if max_anomaly_z is None or a["z"] > max_anomaly_z:
                        max_anomaly_z = a["z"]
        if max_anomaly_z is None or max_anomaly_z < VOL_PEAK_ZSCORE_THRESHOLD:
            olog(f"[slow_impulse] {symbol}: касание EMA{touch['period']} есть, но нет "
                 f"распродажной аномалии объёма (z>={VOL_PEAK_ZSCORE_THRESHOLD}) в истории "
                 f"1h/{MANUAL_SCAN_LOOKBACK_DAYS['1h']}д или 4h/{MANUAL_SCAN_LOOKBACK_DAYS['4h']}д "
                 f"— пропуск (max_z={max_anomaly_z})")
            _slow_impulse_append_diag(touch, outcome="skipped_no_anomaly",
                                       cooldown_remaining_sec=0,
                                       anomaly_z=max_anomaly_z, anomaly_count=anomaly_count,
                                       weekly_chg=weekly_chg)
            return
    note_parts = []
    if weekly_chg is not None:
        note_parts.append(f"накопленное движение за 7 баров ({SLOW_IMPULSE_TF}): {weekly_chg:+.1f}%")
    if max_anomaly_z is not None:
        note_parts.append(f"распродажная аномалия объёма z={max_anomaly_z}")
    note = "; ".join(note_parts) if note_parts else None
    source_id = f"slow_impulse|{symbol}|{int(now)}"
    _weekly_ema_fire_alert(touch, confidence_note=note, triggered_by=source_id,
                            anomaly_z=max_anomaly_z, anomaly_count=anomaly_count)
    with _weekly_ema_touch_lock:
        _weekly_ema_touch_state[key] = now
    chg_txt = f"{weekly_chg:+.1f}%" if weekly_chg is not None else "н/д"
    olog(f"[slow_impulse] {symbol}: {SLOW_IMPULSE_TF} EMA{touch['period']} (ближайшая, накопленное "
         f"движение {chg_txt}) — сигнал отправлен")


def _slow_impulse_append_diag(touch, outcome, cooldown_remaining_sec, anomaly_z, anomaly_count, weekly_chg):
    """v0.30.44: по прямому запросу — не отдельный лог/файл, а те же
    записи, что и обычные сигналы, просто с outcome="skipped_cooldown"/
    "skipped_no_anomaly" вместо "open" — попадают в тот же
    /weekly_ema_export (weekly_ema_signals файл), который уже скачивают.
    Резолвер исхода (открыт/тейк/стоп) фильтрует строго по outcome=="open"
    — эти записи он не тронет. Позволяет напрямую видеть в уже знакомом
    файле: монета дошла до касания EMA, но была отсеяна — и почему
    именно (cooldown ещё активен на столько-то секунд, или аномалии не
    нашлось вообще / какой z был максимальный)."""
    rec = {"ts": int(time.time()), "symbol": touch["symbol"], "tf": touch["tf"],
           "period": touch["period"],
           "price": touch["price"], "ema_value": touch["ema_value"],
           "dist_atr": touch["dist_atr"], "ladder": touch["ladder"],
           "side": touch["side"], "classic_bias": touch["classic_bias"],
           "entry": touch["entry"], "stop": touch["stop"], "take": touch["take"],
           "rr": touch["rr"], "confidence_note": None, "triggered_by": f"slow_impulse|{touch['symbol']}",
           "vol_ratio": touch.get("vol_ratio"),
           "weekly_chg_7bar_pct": weekly_chg,
           "anomaly_z": anomaly_z, "anomaly_count": anomaly_count,
           "cooldown_remaining_sec": cooldown_remaining_sec,
           "outcome": outcome, "resolved_at": None, "resolved_price": None}
    global _weekly_ema_skipped
    with _weekly_ema_touch_lock:
        _weekly_ema_skipped.append(rec)
        _weekly_ema_skipped = _weekly_ema_skipped[-200:]


def _slow_impulse_scan_loop():
    time.sleep(90)
    while True:
        try:
            if _get_slow_impulse_enabled():
                # v0.30.29: не _get_tradeable_symbols() (общий строгий порог
                # ликвидности $3млн, для быстрых детекторов) — свой, более
                # низкий порог, см. SLOW_IMPULSE_MIN_VOLUME_USD
                symbols = [s for s in _fetch_all_symbols(min_volume_usd_override=SLOW_IMPULSE_MIN_VOLUME_USD,
                                                          max_volume_usd_override=SLOW_IMPULSE_MAX_VOLUME_USD)
                           if _has_sufficient_ema_history(s)]
                for symbol in symbols:
                    try:
                        _slow_impulse_maybe_trigger_ema_signal(symbol)
                    except Exception as e:
                        olog(f"[slow_impulse] {symbol}: ошибка обработки, пропускаем: {_explain_error(e)}")
                        continue
        except Exception as e:
            olog(f"[slow_impulse] ошибка цикла: {_explain_error(e)}")
        time.sleep(SLOW_IMPULSE_SCAN_POLL_SEC)


def _weekly_ema_source_stats():
    """v0.30.36: по прямому вопросу — "отслеживаем ли статистику сигналов
    этих [источников], и где она". Данные УЖЕ были (каждая запись
    _weekly_ema_recent хранит triggered_by и итог take/stop/open), просто
    не было готовой сводки по источнику. Разбивает по: памп/дамп напрямую
    (triggered_by пусто), аномалия объёма, накопленный импульс
    (slow_impulse) — с винрейтом среди уже решённых (take/stop).
    v0.30.41: добавлен no_fill — вход (лимитка на EMA-уровне) так и не
    исполнился за WEEKLY_EMA_ENTRY_FILL_TIMEOUT_SEC, сделки по-настоящему
    не было. Не участвует в винрейте, но виден отдельно для прозрачности —
    "сколько сигналов вообще не дошли до входа"."""
    with _weekly_ema_touch_lock:
        snapshot = list(_weekly_ema_recent)

    def classify(triggered_by):
        if not triggered_by:
            return "памп/дамп напрямую"
        if triggered_by.startswith("vol_anomaly|"):
            return "аномалия объёма"
        if triggered_by.startswith("slow_impulse|"):
            return "накопленный импульс"
        return "прочее"

    stats = {}
    for r in snapshot:
        cat = classify(r.get("triggered_by"))
        s = stats.setdefault(cat, {"total": 0, "take": 0, "stop": 0, "open": 0, "no_fill": 0})
        s["total"] += 1
        outcome = r.get("outcome", "open")
        if outcome in s:
            s[outcome] += 1
    result = []
    for cat, s in stats.items():
        resolved = s["take"] + s["stop"]
        win_rate = round(s["take"] / resolved * 100, 1) if resolved else None
        result.append({"source": cat, "total": s["total"], "take": s["take"],
                        "stop": s["stop"], "open": s["open"], "no_fill": s["no_fill"],
                        "resolved": resolved, "win_rate_pct": win_rate})
    result.sort(key=lambda x: -x["total"])
    return result


def _weekly_ema_resolve_loop():
    """Фоновый цикл: раз в WEEKLY_EMA_RESOLVE_POLL_SEC проверяет все ещё
    ОТКРЫТЫЕ сигналы из истории на предмет тейка/стопа.
    v0.30.40: реальный найденный баг — раньше проверялась ТОЛЬКО живая
    цена в момент опроса (раз в 2 минуты), не весь путь цены МЕЖДУ
    опросами. Если цена сначала пробивала стоп, а потом отскакивала выше
    тейка ДО следующего опроса — это оставалось незамеченным, записывался
    тейк, хотя по-настоящему сделка закрылась бы в минус раньше. Системная
    погрешность в пользу тейка. Теперь — свечи (1m, high/low) с момента
    ПОСЛЕДНЕЙ проверки, в хронологическом порядке: первая свеча, где
    пробит хоть один уровень, и определяет исход. Если свеча накрывает
    ОБА уровня разом (для очень волатильных монет) — консервативно
    считаем стоп (не можем знать точный порядок внутри одной свечи без
    тиковых данных). Группирует по монете, чтобы не дёргать свечи больше
    одного раза за проход, даже если у монеты несколько открытых сигналов
    сразу.
    v0.30.41: ещё один реальный найденный баг — вход (лимитка на уровне
    EMA) вообще не проверялся, просто ПРЕДПОЛАГАЛОСЬ, что раз алерт
    сработал (касание в пределах допуска), значит лимитка исполнилась.
    Но допуск — до WEEKLY_EMA_TOUCH_ATR от уровня, не точно НА уровне —
    цена вполне могла развернуться, так и не дойдя до точной цены входа,
    и "сделки" по-настоящему не было бы вообще. Теперь сначала ищем
    первую свечу, где цена реально дошла до entry (high/low, не close), и
    ТОЛЬКО С ЭТОГО МОМЕНТА начинаем считать стоп/тейк — до подтверждения
    входа стоп/тейк не проверяются вообще. Если вход не подтверждается за
    WEEKLY_EMA_ENTRY_FILL_TIMEOUT_SEC — запись помечается "no_fill" и не
    участвует в винрейте (сделки не было)."""
    time.sleep(30)
    while True:
        try:
            with _weekly_ema_touch_lock:
                open_recs = [r for r in _weekly_ema_recent if r.get("outcome") == "open"]
            if open_recs:
                candles_cache = {}
                changed = False
                now = time.time()
                for rec in open_recs:
                    try:
                        sym = rec["symbol"]
                        since_ts = rec.get("last_checked_ts") or rec["ts"]
                        if since_ts >= now:
                            continue
                        if sym not in candles_cache:
                            try:
                                span_days = max(1, math.ceil((now - since_ts) / 86400) + 1)
                                offset_days = 0.0   # нужны свечи ДО текущего момента включительно
                                candles_cache[sym] = _fetch_candles(sym, "1m", span_days, offset_days=offset_days)
                            except Exception as e:
                                olog(f"[weekly_ema_resolve] {sym}: ошибка получения свечей: {_explain_error(e)}")
                                candles_cache[sym] = []
                        relevant = sorted(
                            [c for c in candles_cache[sym] if c["t"] >= since_ts and c["t"] <= now],
                            key=lambda c: c["t"])
                        if not relevant:
                            continue
                        outcome = None
                        resolved_price = None
                        entry_filled_at = rec.get("entry_filled_at")
                        for c in relevant:
                            high = c.get("high", c["close"])
                            low = c.get("low", c["close"])
                            if entry_filled_at is None:
                                # ждём, пока цена реально дойдёт до точного
                                # уровня входа — до этого стоп/тейк не считаем
                                filled = (high >= rec["entry"]) if rec["classic_bias"] == "short" \
                                         else (low <= rec["entry"])
                                if not filled:
                                    continue
                                entry_filled_at = c["t"]
                                rec["entry_filled_at"] = int(entry_filled_at)
                            if rec["classic_bias"] == "long":
                                hit_take = high >= rec["take"]
                                hit_stop = low <= rec["stop"]
                            else:
                                hit_take = low <= rec["take"]
                                hit_stop = high >= rec["stop"]
                            if hit_stop:
                                outcome, resolved_price = "stop", rec["stop"]
                                break
                            if hit_take:
                                outcome, resolved_price = "take", rec["take"]
                                break
                        rec["last_checked_ts"] = int(relevant[-1]["t"]) + 60
                        if outcome:
                            rec["outcome"] = outcome
                            rec["resolved_at"] = int(time.time())
                            rec["resolved_price"] = resolved_price
                            changed = True
                            emoji = "✅" if outcome == "take" else "❌"
                            _send_alert(
                                f"{emoji} <b>{rec['symbol']}</b> | EMA{rec['period']}"
                                f"({'W' if rec['tf']=='1w' else 'D'}) — "
                                f"{'ТЕЙК' if outcome=='take' else 'СТОП'} по сигналу от "
                                f"{time.strftime('%d.%m %H:%M', time.localtime(rec['ts']))}\n"
                                f"Вход: {_fmt_px(rec['entry'])} → {_fmt_px(resolved_price)}"
                            )
                            olog(f"[weekly_ema_resolve] {rec['symbol']}: сигнал от "
                                 f"{time.strftime('%H:%M', time.localtime(rec['ts']))} закрылся по "
                                 f"{'тейку' if outcome=='take' else 'стопу'} ({resolved_price}), "
                                 f"проверено по свечам с {time.strftime('%H:%M', time.localtime(since_ts))}")
                        elif entry_filled_at is None and (now - rec["ts"]) > WEEKLY_EMA_ENTRY_FILL_TIMEOUT_SEC:
                            rec["outcome"] = "no_fill"
                            rec["resolved_at"] = int(time.time())
                            changed = True
                            olog(f"[weekly_ema_resolve] {rec['symbol']}: сигнал от "
                                 f"{time.strftime('%H:%M', time.localtime(rec['ts']))} — вход так и не "
                                 f"исполнился за {WEEKLY_EMA_ENTRY_FILL_TIMEOUT_SEC//3600}ч, "
                                 f"сделки не было, в винрейт не считается")
                        else:
                            changed = True   # last_checked_ts продвинулся, стоит сохранить
                    except Exception as e:
                        # одна повреждённая/неполная запись не должна срывать
                        # проверку ОСТАЛЬНЫХ открытых сигналов в этом же проходе
                        olog(f"[weekly_ema_resolve] ⚠ пропущена битая запись "
                             f"({rec.get('symbol','?')}): {_explain_error(e)}")
                        continue
                if changed:
                    with _weekly_ema_touch_lock:
                        _save_weekly_ema_history()
        except Exception as e:
            olog(f"[weekly_ema_resolve] ошибка цикла: {_explain_error(e)}")
        time.sleep(WEEKLY_EMA_RESOLVE_POLL_SEC)


def _weekly_ema_touch_loop():
    time.sleep(25)
    # история теперь подхватывается централизованно в main() (см.
    # _load_weekly_ema_history) — этот вызов оставлен на случай, если цикл
    # запустят отдельно от main() при отладке, лишним не будет.
    _load_weekly_ema_history()
    while True:
        try:
            symbols = _fetch_all_symbols()  # v0.30.4: потолок уже применён внутри, повторная обрезка была лишней
            now = time.time()
            for symbol in symbols:
                for tf in EMA_TOUCH_TIMEFRAMES:
                    try:
                        touches = _ema_touch_check_symbol(symbol, tf)
                    except Exception as e:
                        olog(f"[weekly_ema] {symbol} {tf}: ошибка проверки: {_explain_error(e)}")
                        continue
                    cooldown = WEEKLY_EMA_TOUCH_COOLDOWN_SEC if tf == "1w" else DAILY_EMA_TOUCH_COOLDOWN_SEC
                    for touch in touches:
                        if EMA_TOUCH_LIVE_ONLY_SHORT and touch["classic_bias"] != "short":
                            continue   # "работаем только в шорт" — long-касания не алертим
                        key = f"{symbol}|{tf}|{touch['period']}"
                        with _weekly_ema_touch_lock:
                            last = _weekly_ema_touch_state.get(key, 0)
                        if now - last < cooldown:
                            continue
                        _weekly_ema_fire_alert(touch)
                        with _weekly_ema_touch_lock:
                            _weekly_ema_touch_state[key] = now
        except Exception as e:
            olog(f"[weekly_ema] ошибка цикла: {_explain_error(e)}")
        time.sleep(WEEKLY_EMA_SCAN_POLL_SEC)
# ─── конец Weekly/Daily EMA Touch Watcher ───────────────────────────────────

# ─── Weekly EMA Backtest ─────────────────────────────────────────────────────
# По запросу: "исторически прогнать все возможные сигналы и посмотреть куда
# и на сколько шла цена, составить рейтинг угадывания направления по каждой
# монете". ВАЖНАЯ поправка от пользователя: сигнал — это касание на
# НЕДЕЛЬНОМ графике, но сама сделка — это НЕ удержание позиции неделями, а
# ставка на МГНОВЕННУЮ реакцию (3-4%, иногда 6-10%) в первые минуты/часы
# после касания, сделки на минуты-час максимум. Поэтому детект сигнала
# остаётся на недельных барах (та же логика, что в live-вотчере), а ОЦЕНКА
# исхода — по 1-минутным свечам сразу после момента касания, не по недельным
# закрытиям через недели (первая версия этого бэктеста мерила не то, что
# нужно — недельные горизонты, что было прямой ошибкой относительно того,
# как реально торгуется этот сигнал).
# ОГРАНИЧЕНИЕ: у Gate.io 1m-свечи почти наверняка не хранятся годами (в
# отличие от дневных/недельных) — поэтому реакцию физически можно проверить
# только на ОТНОСИТЕЛЬНО НЕДАВНИХ касаниях (WEEKLY_EMA_REACTION_MAX_AGE_DAYS).
# Для более старых сигналов total_signals всё равно посчитает сам факт
# касания (это не требует минутных данных), но в винрейт по реакции они не
# попадут — "evaluable" в статистике явно меньше "total_signals", это не
# баг, а честное отражение того, что для старых сигналов данных физически
# нет у биржи.
WEEKLY_EMA_BACKTEST_DEDUP_WEEKS = 2   # не считаем сигнал каждую неделю подряд, пока цена сидит у уровня
WEEKLY_EMA_BACKTEST_DAYS = 1500        # глубина недельной истории — для самого детекта касаний
WEEKLY_EMA_BACKTEST_TOP_N = 100
WEEKLY_EMA_REACTION_WINDOW_MIN = 180       # сколько минут после касания проверяем реакцию (3ч, с запасом над "час максимум")
WEEKLY_EMA_REACTION_THRESHOLDS = [3, 4, 6, 10]   # % цели, запрошенные явно
WEEKLY_EMA_REACTION_MAX_AGE_DAYS = 90      # старше — не пытаемся тянуть 1m (почти наверняка их уже нет у биржи)
WEEKLY_EMA_BACKTEST_FILE_TEMPLATE = os.path.expanduser("~/pumpradar_ema_backtest_{tf}.json")

def _weekly_ema_backtest_file(tf):
    return WEEKLY_EMA_BACKTEST_FILE_TEMPLATE.format(tf=tf.replace("/", "_"))


def _weekly_ema_detect_signals(candles):
    """Проходит свечи (недельные ИЛИ дневные — общая логика для обоих ТФ)
    ОДИН РАЗ (без заглядывания вперёд) и находит все touch-события.
    Возвращает (signals, closes)."""
    closes = [c["close"] for c in candles]
    atr_arr = _atr(candles, 14)
    ema_arrays = {p: _ema(closes, p) for p in WEEKLY_EMA_PERIODS}
    signals = []
    n = len(candles)
    start = max(WEEKLY_EMA_PERIODS) + 1
    i = start
    while i < n:
        atr_v = atr_arr[i]
        if not atr_v:
            i += 1
            continue
        fired_here = False
        for period in WEEKLY_EMA_PERIODS:
            ema_arr = ema_arrays[period]
            ema_v = ema_arr[i]
            prev_ema = ema_arr[i - 1]
            if ema_v is None or prev_ema is None:
                continue
            dist = abs(closes[i] - ema_v)
            tol = WEEKLY_EMA_TOUCH_ATR * atr_v
            if dist <= tol:
                side = "from_below" if closes[i - 1] < prev_ema else "from_above"
                bias = "short" if side == "from_below" else "long"
                signals.append({"i": i, "period": period, "side": side, "bias": bias,
                                 "entry": closes[i], "week_t": candles[i]["t"]})
                fired_here = True
        i += WEEKLY_EMA_BACKTEST_DEDUP_WEEKS if fired_here else 1
    return signals, closes


def _weekly_ema_evaluate_intraday(signal, symbol, window_min=WEEKLY_EMA_REACTION_WINDOW_MIN,
                                   thresholds=WEEKLY_EMA_REACTION_THRESHOLDS):
    """Тянет 1m-свечи за window_min минут ПОСЛЕ момента касания (переиспо-
    льзует _pm_fetch_candles_window из Pump Match — тот же принцип: явное
    окно [start_ts, end_ts], а не 'от now назад'). Считает максимальное
    благоприятное (MFE) и неблагоприятное (MAE) движение от цены входа в
    сторону classic_bias, и для каждого порога из thresholds — достигнут ли
    он и через сколько минут. Возвращает None, если свечей не нашлось
    (сигнал слишком старый — у биржи почти наверняка уже нет 1m-истории)."""
    end_ts = signal["week_t"] + window_min * 60
    candles = _pm_fetch_candles_window(symbol, signal["week_t"], end_ts, interval="1m")
    if not candles:
        return None
    entry_price = signal["entry"]
    bias = signal["bias"]
    if not entry_price:
        return None
    out = {"mfe_pct": 0.0, "mfe_time_min": None, "mae_pct": 0.0, "mae_time_min": None,
           "hits": {t: {"hit": False, "time_min": None} for t in thresholds}}
    t0 = candles[0]["t"]
    for c in candles:
        minute = int((c["t"] - t0) / 60)
        if bias == "long":
            fav = (c["high"] - entry_price) / entry_price * 100.0
            adv = (c["low"] - entry_price) / entry_price * 100.0
        else:
            fav = (entry_price - c["low"]) / entry_price * 100.0
            adv = (entry_price - c["high"]) / entry_price * 100.0
        if fav > out["mfe_pct"]:
            out["mfe_pct"] = round(fav, 2)
            out["mfe_time_min"] = minute
        if adv < out["mae_pct"]:
            out["mae_pct"] = round(adv, 2)
            out["mae_time_min"] = minute
        for t in thresholds:
            if not out["hits"][t]["hit"] and fav >= t:
                out["hits"][t] = {"hit": True, "time_min": minute}
    return out


def _weekly_ema_backtest_symbol(symbol, tf="1w", days=WEEKLY_EMA_BACKTEST_DAYS,
                                 window_min=WEEKLY_EMA_REACTION_WINDOW_MIN,
                                 thresholds=WEEKLY_EMA_REACTION_THRESHOLDS,
                                 max_age_days=WEEKLY_EMA_REACTION_MAX_AGE_DAYS):
    fetch_days = days if tf == "1w" else min(days, DAILY_EMA_FETCH_DAYS * 3)
    raw = _fetch_candles(symbol, "1d", fetch_days)
    candles = _resample_to_weekly(raw) if tf == "1w" else raw
    if len(candles) < max(WEEKLY_EMA_PERIODS) + 5:
        return []
    signals, closes = _weekly_ema_detect_signals(candles)
    cutoff_ts = time.time() - max_age_days * 86400
    for s in signals:
        if s["week_t"] < cutoff_ts:
            # слишком старый сигнал — 1m-историю у биржи почти наверняка уже
            # не достать, не тратим запрос впустую
            s["intraday"] = None
            continue
        try:
            s["intraday"] = _weekly_ema_evaluate_intraday(s, symbol, window_min, thresholds)
        except Exception as e:
            olog(f"[weekly_ema_backtest] {symbol}: intraday оценка упала: {_explain_error(e)}")
            s["intraday"] = None
    return signals


def _weekly_ema_aggregate_symbol(signals, thresholds=WEEKLY_EMA_REACTION_THRESHOLDS):
    """Сворачивает список сигналов одной монеты в статистику по порогам
    реакции (+ отдельно по direction long/short — у монеты с трендом одна
    сторона обычно работает заметно лучше другой)."""
    def _agg(subset):
        evaluable = [s for s in subset if s.get("intraday") is not None]
        by_threshold = {}
        for t in thresholds:
            hits = [s for s in evaluable if s["intraday"]["hits"][t]["hit"]]
            times = [s["intraday"]["hits"][t]["time_min"] for s in hits]
            by_threshold[t] = {
                "total": len(evaluable), "hits": len(hits),
                "hit_rate": round(len(hits) / len(evaluable) * 100, 1) if evaluable else None,
                "avg_time_min": round(sum(times) / len(times), 1) if times else None,
            }
        avg_mfe = round(sum(s["intraday"]["mfe_pct"] for s in evaluable) / len(evaluable), 2) if evaluable else None
        avg_mae = round(sum(s["intraday"]["mae_pct"] for s in evaluable) / len(evaluable), 2) if evaluable else None
        return {"total_signals": len(subset), "evaluable": len(evaluable),
                "by_threshold": by_threshold, "avg_mfe_pct": avg_mfe, "avg_mae_pct": avg_mae}

    result = _agg(signals)
    result["by_bias"] = {bias: _agg([s for s in signals if s["bias"] == bias])
                          for bias in ("long", "short")}
    return result


def _weekly_ema_backtest_run(symbols, tf="1w", days=WEEKLY_EMA_BACKTEST_DAYS,
                              window_min=WEEKLY_EMA_REACTION_WINDOW_MIN,
                              thresholds=WEEKLY_EMA_REACTION_THRESHOLDS,
                              max_age_days=WEEKLY_EMA_REACTION_MAX_AGE_DAYS,
                              progress_cb=None):
    """Прогоняет бэктест по списку монет на указанном ТФ ("1w" или "1d"),
    возвращает {symbol: агрегат}. progress_cb(done, total), если передан,
    вызывается после каждой монеты."""
    results = {}
    total = len(symbols)
    for idx, symbol in enumerate(symbols):
        try:
            signals = _weekly_ema_backtest_symbol(symbol, tf=tf, days=days, window_min=window_min,
                                                   thresholds=thresholds, max_age_days=max_age_days)
            if signals:
                results[symbol] = _weekly_ema_aggregate_symbol(signals, thresholds)
        except Exception as e:
            olog(f"[weekly_ema_backtest] {symbol}: ошибка: {_explain_error(e)}")
        if progress_cb:
            progress_cb(idx + 1, total)
    return results


_weekly_ema_backtest_jobs_lock = threading.Lock()
_weekly_ema_backtest_jobs = {}   # job_id -> {"status","done","total","result","created_at"}
_WEEKLY_EMA_BACKTEST_JOB_TTL_SEC = 3600


def _weekly_ema_backtest_new_job():
    """~100 монет × годы дневных свечей — минуты работы (в основном сетевой
    фетч), один блокирующий запрос выглядел бы зависшим. Тот же паттерн,
    что и у /pump_match: POST сразу отдаёт job_id, прогресс — через GET."""
    job_id = uuid.uuid4().hex[:12]
    with _weekly_ema_backtest_jobs_lock:
        _weekly_ema_backtest_jobs[job_id] = {"status": "running", "done": 0, "total": 0,
                                              "result": None, "created_at": time.time()}
        stale = [k for k, v in _weekly_ema_backtest_jobs.items()
                 if time.time() - v.get("created_at", 0) > _WEEKLY_EMA_BACKTEST_JOB_TTL_SEC]
        for k in stale:
            del _weekly_ema_backtest_jobs[k]
    return job_id


def _weekly_ema_backtest_run_background(job_id, symbols, tf, days, window_min, thresholds, max_age_days):
    def progress_cb(done, total):
        with _weekly_ema_backtest_jobs_lock:
            if job_id in _weekly_ema_backtest_jobs:
                _weekly_ema_backtest_jobs[job_id]["done"] = done
                _weekly_ema_backtest_jobs[job_id]["total"] = total
    try:
        results = _weekly_ema_backtest_run(symbols, tf=tf, days=days, window_min=window_min,
                                            thresholds=thresholds, max_age_days=max_age_days,
                                            progress_cb=progress_cb)
        payload = {"ok": True, "tf": tf, "thresholds": thresholds, "window_min": window_min,
                   "max_age_days": max_age_days, "results": results,
                   "generated_at": int(time.time())}
        try:
            with open(_weekly_ema_backtest_file(tf), "w") as f:
                json.dump(payload, f)
        except Exception as e:
            olog(f"[weekly_ema_backtest] ⚠ не смог сохранить результат: {_explain_error(e)}")
        with _weekly_ema_backtest_jobs_lock:
            if job_id in _weekly_ema_backtest_jobs:
                _weekly_ema_backtest_jobs[job_id]["status"] = "done"
                _weekly_ema_backtest_jobs[job_id]["result"] = payload
    except Exception as e:
        olog(f"[weekly_ema_backtest] задача {job_id} упала: {_explain_error(e)}")
        with _weekly_ema_backtest_jobs_lock:
            if job_id in _weekly_ema_backtest_jobs:
                _weekly_ema_backtest_jobs[job_id]["status"] = "error"
                _weekly_ema_backtest_jobs[job_id]["result"] = {"ok": False, "msg": f"Внутренняя ошибка: {_explain_error(e)}"}
# ─── конец Weekly EMA Backtest ──────────────────────────────────────────────

# ─── EMA Diagnostics: система сбора данных для анализа алгоритма ───────────
# По прямому запросу — "собирать все, все возможные ема сигналы, куда пошла
# цена сразу, на какое расстояние, был ли откат, где через час-два,
# профили объёма, упор на ема — откуда отскакивали чаще всего, на разных
# фреймах". В отличие от живого вотчера (WEEKLY_EMA_TOUCH_ATR=0.25, узкий
# порог именно под алерты) — тут порог НАМНОГО шире (DIAG_TOUCH_ATR=1.0) и
# таймфреймов больше (1h/4h добавлены к 1d/1w) — это отдельный, более
# широкий сбор данных ДЛЯ АНАЛИЗА, не для принятия решения о сигнале.
# Карта ликвидации: честно — настоящей нет и не будет без платного
# источника (Coinglass и т.п.), Gate.io публично такое не отдаёт. Вместо
# неё — funding rate (реально доступный, хоть и более грубый прокси
# перекошенности позиций лонг/шорт).
# Версия СХЕМЫ диагностики — отдельно от APP_VERSION (тот растёт на каждый
# релиз вообще). Эту поднимаем ТОЛЬКО когда меняется что-то, что делает
# старые записи несопоставимыми с новыми: формула детекта касания/side,
# сетка чекпоинтов (DIAG_CHECKPOINTS_MIN), ширина допуска DIAG_TOUCH_ATR,
# способ счёта MFE/MAE. НЕ поднимаем из-за: порога DIAG_BOUNCE_THRESHOLD_PCT
# (он применяется при АНАЛИЗЕ поверх сырых mfe/mae, а не при записи — старые
# записи одинаково корректно пересчитываются под новый порог), добавления
# нового поля (старые записи просто не будут его иметь, это не конфликт).
DIAG_SCHEMA_VERSION = 1
DIAG_TIMEFRAMES     = ["1h", "4h", "1d", "1w"]
DIAG_TOUCH_ATR      = 1.0        # значительно шире алертового порога — собираем данные, а не только то, что алертим
DIAG_FETCH_DAYS     = {"1h": 30, "4h": 60, "1d": 120, "1w": 400}
DIAG_SCAN_POLL_SEC  = 900        # раз в 15 минут — новый скан по всем ТФ/монетам
DIAG_CHECKPOINTS_MIN = [15, 30, 60, 120, 240, 360, 480, 600, 720]   # v0.30.15: расширено с 240 до 720 мин (12ч) по запросу — меньше сделок останется "нерешёнными" при симуляции стоп/тейк
DIAG_TRACK_WINDOW_SEC = max(DIAG_CHECKPOINTS_MIN) * 60 + 600   # с запасом сверх последнего чекпоинта
DIAG_TRACK_POLL_SEC = 120        # раз в 2 минуты проверяем ещё не закрытые записи
DIAG_FILE           = os.path.expanduser("~/pumpradar_ema_diagnostics.json")
DIAG_MAX_RECORDS    = 5000       # не растим файл бесконечно

_diag_lock = threading.Lock()
_diag_records = []   # общий список всех диагностических записей (открытых и закрытых)
_diag_ctx_cache = {}       # "symbol|tf" -> closed-context (тот же приём, что _ema_touch_ctx_cache, но своя копия — разные пороги/ТФ)
_diag_ctx_cache_lock = threading.Lock()


def _diag_get_closed_ctx(symbol, tf):
    """Аналог _ema_touch_get_closed_ctx, но с собственным кэшем (разные
    таймфреймы/глубина фетча) и собственным негативным кэшем недостаточной
    истории — не мешаем друг другу с live-вотчером."""
    key = f"{symbol}|{tf}"
    now = time.time()
    with _diag_ctx_cache_lock:
        cached = _diag_ctx_cache.get(key)
    if cached and now < cached["next_close_t"]:
        return None if cached.get("insufficient") else cached
    fetch_days = DIAG_FETCH_DAYS.get(tf, 60)
    if tf == "1w":
        raw = _fetch_candles(symbol, "1d", fetch_days)
        candles = _resample_to_weekly(raw)
    else:
        candles = _fetch_candles(symbol, tf, fetch_days)
    min_bars = max(WEEKLY_EMA_PERIODS) + 5
    if len(candles) < min_bars:
        with _diag_ctx_cache_lock:
            _diag_ctx_cache[key] = {"insufficient": True,
                                     "next_close_t": now + EMA_TOUCH_INSUFFICIENT_HISTORY_RECHECK_SEC}
        return None
    closes = [c["close"] for c in candles]
    atr_arr = _atr(candles, 14)
    i = len(candles) - 1
    atr_v = atr_arr[i]
    if not atr_v:
        return None
    ema_arrays = {p: _ema(closes, p) for p in WEEKLY_EMA_PERIODS}
    if any(arr[i] is None for arr in ema_arrays.values()):
        return None
    interval_sec = 86400 * 7 if tf == "1w" else TF_SECONDS.get(tf, 86400)
    ctx = {"candles": candles, "closes": closes, "atr_v": atr_v, "i": i,
           "ema_arrays": ema_arrays, "next_close_t": candles[i]["t"] + interval_sec + 5}
    with _diag_ctx_cache_lock:
        _diag_ctx_cache[key] = ctx
    return ctx


def _diag_volume_profile(candles, lookback=50, bins=10):
    """Простой Volume Profile по последним lookback барам: делит ценовой
    диапазон на bins корзин, суммирует объём каждой свечи в корзину её
    средней цены, возвращает POC (цена корзины с максимальным объёмом) и
    какую долю общего объёма она забирает (poc_vol_share — насколько
    выражен пик, а не размазан объём равномерно)."""
    window = candles[-lookback:]
    if not window:
        return None
    lo = min(c["low"] for c in window)
    hi = max(c["high"] for c in window)
    if hi <= lo:
        return None
    bin_size = (hi - lo) / bins
    vol_by_bin = [0.0] * bins
    for c in window:
        mid = (c["high"] + c["low"]) / 2
        idx = min(bins - 1, max(0, int((mid - lo) / bin_size)))
        vol_by_bin[idx] += c.get("vol", 0) or 0
    total_vol = sum(vol_by_bin)
    if total_vol <= 0:
        return None
    poc_idx = max(range(bins), key=lambda k: vol_by_bin[k])
    poc_price = lo + (poc_idx + 0.5) * bin_size
    return {
        "poc_price": _round_price(poc_price),
        "range_low": _round_price(lo), "range_high": _round_price(hi),
        "poc_vol_share": round(vol_by_bin[poc_idx] / total_vol, 3),
    }


def _diag_get_funding_rate(symbol):
    """НЕ карта ликвидации — честный, но куда более грубый прокси:
    funding rate показывает, платят ли лонги шортам или наоборот (сильный
    перекос = много позиций в одну сторону = потенциальная зона
    ликвидаций при движении против них, но БЕЗ конкретных уровней цены,
    в отличие от настоящей карты ликвидаций типа Coinglass). Отдельный
    прямой запрос, НЕ через _gate_get_contract_info — тот кэш бессрочный и
    используется для торговой логики (плечо/размер ордера), а funding
    rate меняется каждые несколько часов, кэшировать вместе с ним нельзя."""
    try:
        contract = symbol.replace("/", "_").upper()
        r = requests.get(f"{GATE_API}/futures/usdt/contracts/{contract}", timeout=5)
        if r.status_code != 200:
            return None
        data = r.json()
        fr = data.get("funding_rate")
        return float(fr) if fr is not None else None
    except Exception:
        return None


def _diag_check_symbol(symbol, tf):
    """Широкий (DIAG_TOUCH_ATR=1.0, не алертовый 0.25) детект приближения
    к EMA7/14/28 на заданном ТФ — для сбора ДАННЫХ, не для решения слать
    ли сигнал. Возвращает список диагностических записей (без исхода —
    его дозаполняет _diag_track_loop)."""
    ctx = _diag_get_closed_ctx(symbol, tf)
    if ctx is None:
        return []
    candles, closes, atr_v, i, ema_arrays = (ctx["candles"], ctx["closes"], ctx["atr_v"],
                                              ctx["i"], ctx["ema_arrays"])
    live_price = _gate_get_price(symbol)
    price = live_price if live_price else closes[i]
    vol_ratio = _vol_ratio(candles, i)
    vol_profile = _diag_volume_profile(candles)
    funding_rate = _diag_get_funding_rate(symbol)
    ladder = _ladder_order([price] + [
        (_ema_live_value(ema_arrays[p][i], price, p) if live_price else ema_arrays[p][i])
        for p in WEEKLY_EMA_PERIODS
    ])
    records = []
    now = time.time()
    for period in WEEKLY_EMA_PERIODS:
        ema_arr = ema_arrays[period]
        ema_closed = ema_arr[i]
        if ema_closed is None:
            continue
        ema_now = _ema_live_value(ema_closed, price, period) if live_price else ema_closed
        dist = abs(price - ema_now)
        dist_atr = dist / atr_v
        if dist_atr > DIAG_TOUCH_ATR:
            continue
        prev_close = closes[i - 1] if i > 0 else closes[i]
        prev_ema = ema_arr[i - 1] if (i > 0 and ema_arr[i - 1] is not None) else ema_closed
        side = "from_below" if prev_close < prev_ema else "from_above"
        classic_bias = "short" if side == "from_below" else "long"
        records.append({
            "id": f"{symbol}|{tf}|{period}|{int(now)}",
            "diag_version": DIAG_SCHEMA_VERSION,
            "ts": int(now), "symbol": symbol, "tf": tf, "period": period,
            "price": price, "ema_value": _round_price(ema_now),
            "dist_atr": round(dist_atr, 3), "side": side, "classic_bias": classic_bias,
            "ladder": ladder, "vol_ratio": vol_ratio, "vol_profile": vol_profile,
            "funding_rate": funding_rate,
            "checkpoints": {str(m): None for m in DIAG_CHECKPOINTS_MIN},
            "mfe_pct": 0.0, "mae_pct": 0.0,
            "retrace_before_favorable": False,   # был ли момент MAE>0 ПОСЛЕ уже начавшегося MFE>0 (просадка после начала движения по тренду)
            "track_until": now + DIAG_TRACK_WINDOW_SEC, "track_done": False,
        })
    return records


def _diag_save():
    """Атомарная запись (tmp + os.replace), капается на DIAG_MAX_RECORDS."""
    try:
        with _diag_lock:
            trimmed = _diag_records[-DIAG_MAX_RECORDS:]
        tmp_path = DIAG_FILE + f".tmp{os.getpid()}"
        with open(tmp_path, "w") as f:
            json.dump(trimmed, f)
        os.replace(tmp_path, DIAG_FILE)
    except Exception as e:
        olog(f"[ema_diag] ⚠ не смог сохранить {DIAG_FILE}: {_explain_error(e)}")


def _diag_load():
    """Подхватывает диагностику с прошлого запуска (тот же принцип, что
    _load_weekly_ema_history — вызывается прямо в main(), не зависит от
    того, какие циклы включены)."""
    global _diag_records
    try:
        if os.path.exists(DIAG_FILE):
            with open(DIAG_FILE) as f:
                loaded = json.load(f)
            if isinstance(loaded, list):
                with _diag_lock:
                    _diag_records = loaded
                still_open = sum(1 for r in loaded if not r.get("track_done", True))
                olog(f"[ema_diag] история подхвачена с диска: {len(loaded)} записей, "
                     f"из них ещё отслеживается {still_open}")
    except Exception as e:
        olog(f"[ema_diag] ⚠ не смог подхватить историю с диска: {_explain_error(e)}")


def _diag_scan_loop():
    """Раз в DIAG_SCAN_POLL_SEC проходит топ-N монет (та же настраиваемая
    _get_scan_top_n(), что и у пампа/дампа) по всем DIAG_TIMEFRAMES и
    добавляет в _diag_records новые широкие касания. Дедуп: не добавляем
    новую запись на ту же связку символ+ТФ+период, если уже есть открытая
    (ещё отслеживаемая) — иначе одно и то же затяжное касание плодило бы
    записи каждые 15 минут."""
    time.sleep(60)
    while True:
        try:
            symbols = _fetch_all_symbols()  # v0.30.4: потолок уже применён внутри, повторная обрезка была лишней
            added = 0
            for symbol in symbols:
                for tf in DIAG_TIMEFRAMES:
                    try:
                        new_records = _diag_check_symbol(symbol, tf)
                    except Exception as e:
                        olog(f"[ema_diag] {symbol} {tf}: ошибка проверки: {_explain_error(e)}")
                        continue
                    if not new_records:
                        continue
                    with _diag_lock:
                        open_keys = {f"{r['symbol']}|{r['tf']}|{r['period']}"
                                     for r in _diag_records if not r.get("track_done", True)}
                        for rec in new_records:
                            rkey = f"{rec['symbol']}|{rec['tf']}|{rec['period']}"
                            if rkey in open_keys:
                                continue
                            _diag_records.append(rec)
                            open_keys.add(rkey)
                            added += 1
            if added:
                _diag_save()
                olog(f"[ema_diag] скан завершён: +{added} новых записей "
                     f"(всего в истории {len(_diag_records)})")
        except Exception as e:
            olog(f"[ema_diag] ошибка цикла сканирования: {_explain_error(e)}")
        time.sleep(DIAG_SCAN_POLL_SEC)


def _diag_backfill_extended_checkpoints():
    """v0.30.16: по прямому вопросу — "можно ли для старых записей
    досчитать по новым 12-часовым ожиданиям". Обычный трек-луп СТАРЫЕ,
    уже завершённые (track_done=True) записи больше не трогает —
    отслеживание закончилось на их СТАРОМ окне (было 4ч), и просто
    "продолжить" с текущей живой ценой нельзя: пропущенный кусок времени
    (от старого track_until до сейчас) навсегда потерян для live-цены.
    Но у нас есть исторические свечи — можно честно восстановить, что
    РЕАЛЬНО было в момент каждой новой отметки (360/480/600/720 мин),
    не что происходит сейчас. Ограничение — глубина истории на бирже
    (обычно порядка пары недель для 1m); для совсем старых записей данных
    может не быть, это НЕ ошибка, просто пропускаем."""
    now = time.time()
    with _diag_lock:
        records_snapshot = list(_diag_records)
    updated = 0
    skipped_no_gap = 0
    skipped_no_data = 0
    for rec in records_snapshot:
        if not rec.get("track_done"):
            continue
        cps = rec.get("checkpoints", {})
        missing = sorted(m for m in DIAG_CHECKPOINTS_MIN if m > 240 and cps.get(str(m)) is None)
        missing = [m for m in missing if rec["ts"] + m * 60 <= now]   # ещё не наступило -- не считаем вообще
        if not missing:
            skipped_no_gap += 1
            continue
        try:
            needed_until = rec["ts"] + max(missing) * 60
            span_days = max(1, math.ceil((needed_until - rec["ts"]) / 86400) + 1)
            offset_days = max(0.0, (now - needed_until) / 86400)
            candles = _fetch_candles(rec["symbol"], "1m", span_days, offset_days=offset_days)
        except Exception as e:
            olog(f"[ema_diag] backfill {rec['symbol']}: {_explain_error(e)}")
            skipped_no_data += 1
            continue
        if not candles:
            skipped_no_data += 1
            continue
        entry_price = rec["price"]
        changed_this = False
        for m in missing:
            target_t = rec["ts"] + m * 60
            closest = min(candles, key=lambda c: abs(c["t"] - target_t))
            if abs(closest["t"] - target_t) > 300:   # ближайшая свеча слишком далеко -- нет данных на этот момент
                continue
            pct = (closest["close"] - entry_price) / entry_price * 100.0
            rec["checkpoints"][str(m)] = {"price": closest["close"], "pct": round(pct, 2)}
            changed_this = True
        # заодно честно продлеваем mfe/mae на весь восстановленный исторический диапазон,
        # не только на отдельные точки-чекпоинты
        if changed_this:
            for c in candles:
                if c["t"] < rec["ts"] or c["t"] > needed_until:
                    continue
                pct = (c["close"] - entry_price) / entry_price * 100.0
                if rec["classic_bias"] == "long":
                    fav, adv = pct, -pct
                else:
                    fav, adv = -pct, pct
                if fav > rec["mfe_pct"]:
                    rec["mfe_pct"] = round(fav, 2)
                if adv > rec["mae_pct"]:
                    rec["mae_pct"] = round(adv, 2)
            rec["backfilled_extended"] = True
            updated += 1
    if updated:
        with _diag_lock:
            pass  # rec — те же объекты, что в _diag_records (список общий), мутация уже применилась
        _diag_save()
    olog(f"[ema_diag] backfill 12ч чекпоинтов: обновлено {updated}, "
         f"без нового окна (уже было всё) {skipped_no_gap}, без данных на бирже {skipped_no_data}")
    return {"updated": updated, "skipped_no_gap": skipped_no_gap, "skipped_no_data": skipped_no_data}


def _diag_track_loop():
    """Раз в DIAG_TRACK_POLL_SEC проверяет живую цену всех ещё
    отслеживаемых записей: обновляет MFE/MAE (максимум за и против
    сигнала), заполняет ближайший ПРОШЕДШИЙ чекпоинт (15/30/60/120/240
    минут — на первом опросе ПОСЛЕ того, как порог времени пройден, не
    ровно в момент — точность ограничена частотой опроса), и отмечает
    retrace_before_favorable, если после уже начавшегося благоприятного
    движения (MFE>0) была просадка (MAE стал больше нуля уже ПОСЛЕ этого) —
    отдельно от простого MAE, который может быть и ДО того, как пошло по
    тренду (это нормальный шум входа, не "просадка после начала движения")."""
    time.sleep(90)
    while True:
        try:
            with _diag_lock:
                open_recs = [r for r in _diag_records if not r.get("track_done", True)]
            if open_recs:
                price_cache = {}
                changed = False
                now = time.time()
                for rec in open_recs:
                    try:
                        sym = rec["symbol"]
                        if sym not in price_cache:
                            try:
                                price_cache[sym] = _gate_get_price(sym)
                            except Exception as e:
                                olog(f"[ema_diag_track] {sym}: ошибка получения цены: {_explain_error(e)}")
                                price_cache[sym] = None
                        price = price_cache[sym]
                        if not price or not rec.get("price"):
                            continue
                        pct = (price - rec["price"]) / rec["price"] * 100.0
                        if rec["classic_bias"] == "long":
                            fav, adv = pct, -pct
                        else:
                            fav, adv = -pct, pct
                        had_started_moving = rec["mfe_pct"] > 0.1   # уже пошло по тренду хоть немного
                        if fav > rec["mfe_pct"]:
                            rec["mfe_pct"] = round(fav, 2)
                        if adv > rec["mae_pct"]:
                            rec["mae_pct"] = round(adv, 2)
                            if had_started_moving:
                                rec["retrace_before_favorable"] = True
                        elapsed_min = (now - rec["ts"]) / 60.0
                        for cp in DIAG_CHECKPOINTS_MIN:
                            key = str(cp)
                            if rec["checkpoints"].get(key) is None and elapsed_min >= cp:
                                rec["checkpoints"][key] = {"price": price, "pct": round(pct, 2)}
                        changed = True
                        if now >= rec.get("track_until", 0):
                            rec["track_done"] = True
                    except Exception as e:
                        olog(f"[ema_diag_track] ⚠ пропущена битая запись "
                             f"({rec.get('symbol','?')}): {_explain_error(e)}")
                        continue
                if changed:
                    _diag_save()
        except Exception as e:
            olog(f"[ema_diag_track] ошибка цикла: {_explain_error(e)}")
        time.sleep(DIAG_TRACK_POLL_SEC)
DIAG_BOUNCE_THRESHOLD_PCT = 1.0   # от какого MFE считаем, что реально "отскочило", а не шум


def _diag_summary(min_touches=3):
    """Сводка по каждой связке (период EMA × таймфрейм): сколько касаний,
    доля реально отскочивших (MFE >= порога) против пробитых (MAE >= порога
    без соответствующего MFE), средний И МЕДИАННЫЙ MFE/MAE, доля с
    просадкой перед движением по тренду. Отдельно то же самое разбито по
    классической стороне (short/long) — они часто ведут себя по-разному.
    v0.19.3: добавлена медиана — на реальных данных выяснилось, что среднее
    по крипте может быть полностью искажено 1-2 экстремальными выбросами на
    тонких монетах (конкретный случай: один шиткоин дал +250% за 15 минут и
    утащил среднюю по всей связке 1w-SHORT с +0.04% до -3.7%, хотя медиана
    всё время показывала честные +0.27%). Сырые данные не отсекаем (это
    реальный исход, не ошибка) — просто считаем медиану РЯДОМ со средним,
    чтобы расхождение между ними сразу было видно как сигнал "тут выброс"."""
    with _diag_lock:
        records = list(_diag_records)
    groups = {}
    for r in records:
        key = (r["tf"], r["period"])
        groups.setdefault(key, []).append(r)

    def _agg(items):
        total = len(items)
        evaluated = [r for r in items if r.get("track_done") or r.get("mfe_pct", 0) > 0 or r.get("mae_pct", 0) > 0]
        bounced = sum(1 for r in evaluated if r["mfe_pct"] >= DIAG_BOUNCE_THRESHOLD_PCT)
        broke = sum(1 for r in evaluated if r["mae_pct"] >= DIAG_BOUNCE_THRESHOLD_PCT and r["mfe_pct"] < DIAG_BOUNCE_THRESHOLD_PCT)
        retraced = sum(1 for r in evaluated if r.get("retrace_before_favorable"))
        mfe_vals = [r["mfe_pct"] for r in evaluated]
        mae_vals = [r["mae_pct"] for r in evaluated]
        avg_mfe = round(sum(mfe_vals) / len(mfe_vals), 2) if mfe_vals else None
        avg_mae = round(sum(mae_vals) / len(mae_vals), 2) if mae_vals else None
        med_mfe = round(statistics.median(mfe_vals), 2) if mfe_vals else None
        med_mae = round(statistics.median(mae_vals), 2) if mae_vals else None
        # v0.19.4: 0 = "легаси" (запись создана до появления этого поля) —
        # если в группе смешались разные версии схемы, это значит логика
        # детекта/чекпоинтов менялась в середине выборки, и сравнивать
        # такую группу как единое целое некорректно (см. changelog).
        versions = sorted({r.get("diag_version", 0) for r in evaluated})
        return {
            "total": total, "evaluated": len(evaluated), "bounced": bounced, "broke": broke,
            "bounce_rate": round(bounced / len(evaluated) * 100, 1) if evaluated else None,
            "avg_mfe_pct": avg_mfe, "avg_mae_pct": avg_mae,
            "median_mfe_pct": med_mfe, "median_mae_pct": med_mae,
            "retrace_rate": round(retraced / len(evaluated) * 100, 1) if evaluated else None,
            "diag_versions": versions, "mixed_versions": len(versions) > 1,
        }

    result = []
    for (tf, period), items in groups.items():
        if len(items) < min_touches:
            continue
        entry = {"tf": tf, "period": period, "overall": _agg(items)}
        for bias in ("long", "short"):
            subset = [r for r in items if r["classic_bias"] == bias]
            if subset:
                entry[bias] = _agg(subset)
        result.append(entry)
    result.sort(key=lambda e: (e["overall"]["bounce_rate"] or 0), reverse=True)
    return result


# ─── конец EMA Diagnostics ───────────────────────────────────────────────────

# ─── EMA Stretch/Retrace Diagnostics ────────────────────────────────────────
# По прямому запросу — "цена уходит на % от EMA5 после закрытия свечи, часто
# хочет откат". В отличие от EMA Diagnostics выше (триггер — БЛИЗОСТЬ к EMA,
# касание), здесь триггер — ОТРЫВ от короткой EMA на X%, а дальше следим,
# сколько происходит отката — ЛИБО до полного возврата (касания той же EMA
# обратно), ЛИБО до таймаута. Периоды короче обычных (5/10/20, не 7/14/28) —
# по прямому уточнению, отрыв смотрим именно от короткой EMA. ТФ от 15 минут.
STRETCH_DIAG_PERIODS = [5, 10, 20]
STRETCH_DIAG_TIMEFRAMES = ["15m", "30m", "1h", "4h"]

# v0.30.95: по прямому запросу — скан "Отрыва от EMA" можно ограничить
# выбранными параметрами ЕМА+ТФ (галочки на странице /ema_stretch), чтобы
# не гонять все 3×4=12 связок, если интересны только некоторые. По
# умолчанию (файла конфига ещё нет, или списки в нём пустые) — выбрано
# всё, поведение как раньше. Пустой список с любой из сторон трактуется
# как "выбрано всё" — если пользователь случайно снял все галочки,
# безопаснее вернуться к старому поведению (сканировать всё), чем молча
# остановить весь скан.
STRETCH_SCAN_CFG_FILE = os.path.expanduser("~/pumpradar_stretch_scan_cfg.json")
_stretch_scan_cfg_lock = threading.Lock()
_stretch_scan_cfg_state = {
    "periods": list(STRETCH_DIAG_PERIODS),
    "timeframes": list(STRETCH_DIAG_TIMEFRAMES),
}


def _load_stretch_scan_cfg():
    try:
        with open(STRETCH_SCAN_CFG_FILE) as f:
            saved = json.load(f)
        with _stretch_scan_cfg_lock:
            for k in ("periods", "timeframes"):
                if k in saved and isinstance(saved[k], list):
                    _stretch_scan_cfg_state[k] = saved[k]
    except Exception:
        pass


def _save_stretch_scan_cfg():
    try:
        with _stretch_scan_cfg_lock:
            snapshot = dict(_stretch_scan_cfg_state)
        with open(STRETCH_SCAN_CFG_FILE, "w") as f:
            json.dump(snapshot, f)
    except Exception as e:
        olog(f"[ema_stretch] ⚠ не смог сохранить настройки скана: {_explain_error(e)}")


def _stretch_scan_active_timeframes():
    with _stretch_scan_cfg_lock:
        tfs = [tf for tf in STRETCH_DIAG_TIMEFRAMES if tf in _stretch_scan_cfg_state["timeframes"]]
    return tfs or list(STRETCH_DIAG_TIMEFRAMES)


def _stretch_scan_active_periods():
    with _stretch_scan_cfg_lock:
        periods = [p for p in STRETCH_DIAG_PERIODS if p in _stretch_scan_cfg_state["periods"]]
    return periods or list(STRETCH_DIAG_PERIODS)


STRETCH_DIAG_FETCH_DAYS = {"15m": 10, "30m": 15, "1h": 30, "4h": 60}
STRETCH_DIAG_MIN_PCT = 1.0          # минимальный % отрыва ВВЕРХ от EMA, чтобы зафиксировать событие (v0.30.52: только вверх, см. _stretch_check_symbol) — иначе шум почти на каждой свече
STRETCH_TREND_LOOKBACK_BARS = 20    # v0.30.90: по прямому запросу — "затишье vs тренд" — сколько баров ДО триггерной свечи смотрим, чтобы измерить фон, на котором произошёл отрыв
STRETCH_DIAG_TOUCH_TOL_PCT = 0.1    # ближе этого к EMA (или пересечение на другую сторону) считается "коснулась снова"
STRETCH_DIAG_SCAN_POLL_SEC = 45      # v0.30.63: по прямому запросу — "сигналы приходят позже, чем надо, следующий прогон может быть уже поздно". Раньше 180с (3 мин) было разумно при ПОСЛЕДОВАТЕЛЬНОМ обходе (сам скан и так уже мог занимать сравнимое время), теперь скан параллельный (v0.30.62) и завершается на порядок быстрее — можно опрашивать намного чаще, не перегружая API
STRETCH_DIAG_TRACK_POLL_SEC = 60
STRETCH_DIAG_TRACK_WINDOW_SEC = {"15m": 6 * 3600, "30m": 12 * 3600, "1h": 24 * 3600, "4h": 4 * 86400}   # с запасом под масштаб ТФ
STRETCH_DIAG_FILE = os.path.expanduser("~/pumpradar_ema_stretch_diag.json")
STRETCH_DIAG_MAX_RECORDS = 5000
STRETCH_DIAG_BUCKETS = [(1, 3, "1-3%"), (3, 6, "3-6%"), (6, 999, "6%+")]   # для сводки — какой % отрыва даёт какой средний откат
STRETCH_TREND_BUCKETS = [(0, 3, "затишье"), (3, 8, "умеренный тренд"), (8, 999, "сильный тренд")]   # v0.30.90: по прямому запросу — разбивка по |trend_pct| (сила движения ДО отрыва), чтобы сравнить винрейт фейда на затишье vs на тренде
STRETCH_VOL_BUCKETS = [(0, 1, "низкий объём"), (1, 2, "нормальный объём"), (2, 999, "высокий объём")]   # v0.30.91: по прямому запросу "что добавить для лонга" — разбивка по trigger_vol_ratio (объём триггерной свечи к фону), тезис: настоящий пробой идёт с ростом объёма

_stretch_diag_lock = threading.Lock()
_stretch_diag_records = []
_stretch_diag_ctx_cache = {}
_stretch_diag_ctx_cache_lock = threading.Lock()


STRETCH_LIQUIDITY_FLAT_BARS = 8         # v0.30.69: по прямому запросу — если цена не двигалась вообще последние N закрытых баров, это явный признак отсутствия реальной торговли (даже если формальный объём не нулевой) — не настоящий "отрыв", а мёртвая котировка
STRETCH_LIQUIDITY_FLAT_TOL_PCT = 0.08   # v0.30.87: ужесточено с 0.02 — три живых промаха подряд (RLS/FUTUON/MEGA/MUFG) показали, что старый допуск был слишком узким, реальные "мёртвые" хвосты двигаются чуть сильнее чистого нуля
STRETCH_LIQUIDITY_GAP_CHECK_BARS = 30   # сколько последних баров смотрим на предмет пропусков по времени
STRETCH_LIQUIDITY_GAP_MULT = 1.5        # разрыв между соседними барами больше interval×это — считаем дырой (биржа не смогла закрыть свечу, сделок не было вообще)
STRETCH_LIQUIDITY_NOTRADE_WINDOW = 20   # v0.30.70: реальная находка на живом примере (RLS_USDT) — первые две проверки не ловят паттерн "вперемешку мёртвые бары без объёма + редкий одиночный скачок" (не все последние 8 плоские, скачок сам по себе не создаёт временной разрыв). Считаем долю баров БЕЗ объёма в окне
STRETCH_LIQUIDITY_NOTRADE_MAX_FRAC = 0.25   # v0.30.87: ужесточено с 0.35 — та же серия живых промахов, порог был слишком снисходителен (нужно было слишком много мёртвых баров, чтобы сработать)
STRETCH_LIQUIDITY_NOTRADE_REL_THRESHOLD = 0.05   # v0.30.87: ужесточено с 0.02 (2%→5% от пика) по прямому запросу "ужесточаем" — четвёртый живой промах подряд (MUFG_USDT после RLS/FUTUON/MEGA) показал, что даже относительный порог 2% всё ещё пропускал тонкие хвосты


def _stretch_liquidity_issue(candles, tf):
    """Возвращает текст причины, если монета выглядит неликвидной, иначе
    None. Три независимые проверки: (1) цена стоит на месте несколько
    баров подряд — почти наверняка мёртвый рынок, а не реальный отрыв;
    (2) большие временные разрывы между соседними барами — тонкий рынок,
    дыры в истории торгов; (3) много баров с объёмом, ничтожным
    относительно пика в этом же окне (не обязательно строго нулевым) —
    вперемешку с редкими реальными сделками — не ловится (1)/(2), но
    именно такой паттерн дал ложный отрыв на живых примерах (RLS_USDT,
    FUTUON_USDT): одиночный скачок среди почти-мёртвых баров выглядит как
    настоящий отрыв, хотя это просто редкая сделка на тонком рынке."""
    if len(candles) < STRETCH_LIQUIDITY_FLAT_BARS:
        return None
    tail = candles[-STRETCH_LIQUIDITY_FLAT_BARS:]
    closes_tail = [c["close"] for c in tail]
    lo, hi = min(closes_tail), max(closes_tail)
    if lo > 0 and (hi - lo) / lo * 100 < STRETCH_LIQUIDITY_FLAT_TOL_PCT:
        return f"цена стоит на месте последние {STRETCH_LIQUIDITY_FLAT_BARS} баров (диапазон {(hi - lo) / lo * 100:.4f}%)"
    interval_sec = TF_SECONDS.get(tf, 900)
    gap_window = candles[-STRETCH_LIQUIDITY_GAP_CHECK_BARS:]
    for a, b in zip(gap_window, gap_window[1:]):
        gap = b["t"] - a["t"]
        if gap > interval_sec * STRETCH_LIQUIDITY_GAP_MULT:
            return f"разрыв {gap / 60:.0f} мин между свечами (ожидалось {interval_sec / 60:.0f} мин)"
    notrade_window = candles[-STRETCH_LIQUIDITY_NOTRADE_WINDOW:]
    if len(notrade_window) >= STRETCH_LIQUIDITY_NOTRADE_WINDOW:
        vols = [c.get("vol") or 0 for c in notrade_window]
        peak_vol = max(vols)
        notrade_n = sum(1 for v in vols if peak_vol > 0 and v < peak_vol * STRETCH_LIQUIDITY_NOTRADE_REL_THRESHOLD)
        frac = notrade_n / len(notrade_window)
        if frac > STRETCH_LIQUIDITY_NOTRADE_MAX_FRAC:
            return (f"{notrade_n}/{len(notrade_window)} последних баров с объёмом <2% от пика окна "
                    f"({frac * 100:.0f}%) — тонкий рынок с редкими одиночными сделками")
    return None


STRETCH_RATE_LIMIT_PER_SEC = 16   # v0.30.73: реальная находка по новой разбивке задержки (v0.30.72) — медиана ожидания в самом рейт-лимите всего 0.4с, максимум времени запроса 13.2с (не 25с+, как было бы при повторных попытках после 429) — то есть до реального лимита Gate.io мы пока не достаём, узкое место оказалось в другом: 24 потока × ~3.1с медианный запрос = максимум ~7.7 запросов/сек пропускной способности, что почти совпадает со старым лимитом в 8/сек — воркеры упирались в СВОЮ ЖЕ мощность раньше, чем в лимит скорости. Подняли лимит и число потоков вместе (см. STRETCH_DIAG_SCAN_WORKERS), продолжаем следить за debug_gate_wait_sec — если начнёт расти, это будет сигналом, что настоящий потолок API всё-таки где-то рядом
_stretch_rate_lock = threading.Lock()
_stretch_rate_window = []   # временные метки недавних запросов (скользящее окно 1 секунда)


def _stretch_rate_gate():
    """Блокирует вызывающий поток, пока не освободится место в лимите
    STRETCH_RATE_LIMIT_PER_SEC запросов/сек — общий для всех потоков
    пула, не даёт всплеску одновременных задач превратиться в залп по
    API разом. Возвращает, сколько секунд реально прождал (для честной
    разбивки задержки по стадиям, v0.30.72)."""
    wait_start = time.time()
    while True:
        with _stretch_rate_lock:
            now = time.time()
            while _stretch_rate_window and _stretch_rate_window[0] < now - 1.0:
                _stretch_rate_window.pop(0)
            if len(_stretch_rate_window) < STRETCH_RATE_LIMIT_PER_SEC:
                _stretch_rate_window.append(now)
                return round(now - wait_start, 3)
        time.sleep(0.05)


def _stretch_get_closed_ctx(symbol, tf):
    """Аналог _diag_get_closed_ctx, но свои периоды (5/10/20) и свой кэш —
    не пересчитывает EMA7/14/28, которые тут не нужны.
    v0.30.72: по прямому запросу — "нужно точно понять, откуда задержка"
    — теперь замеряет и сохраняет в ctx время ожидания в рейт-лимите и
    время самого сетевого запроса, чтобы честно разложить задержку по
    стадиям, а не гадать."""
    key = f"{symbol}|{tf}"
    now = time.time()
    with _stretch_diag_ctx_cache_lock:
        cached = _stretch_diag_ctx_cache.get(key)
    if cached and now < cached["next_close_t"]:
        return None if cached.get("insufficient") else cached
    gate_wait_sec = _stretch_rate_gate()
    fetch_days = STRETCH_DIAG_FETCH_DAYS.get(tf, 15)
    fetch_t0 = time.time()
    candles = _fetch_candles(symbol, tf, fetch_days)
    fetch_sec = round(time.time() - fetch_t0, 3)
    min_bars = max(STRETCH_DIAG_PERIODS) + 5
    if len(candles) < min_bars:
        with _stretch_diag_ctx_cache_lock:
            _stretch_diag_ctx_cache[key] = {"insufficient": True,
                                             "next_close_t": now + EMA_TOUCH_INSUFFICIENT_HISTORY_RECHECK_SEC}
        return None
    liquidity_issue = _stretch_liquidity_issue(candles, tf)
    if liquidity_issue:
        with _stretch_diag_ctx_cache_lock:
            _stretch_diag_ctx_cache[key] = {"insufficient": True,
                                             "next_close_t": now + EMA_TOUCH_INSUFFICIENT_HISTORY_RECHECK_SEC}
        return None
    closes = [c["close"] for c in candles]
    i = len(candles) - 1
    ema_arrays = {p: _ema(closes, p) for p in STRETCH_DIAG_PERIODS}
    if any(arr[i] is None for arr in ema_arrays.values()):
        return None
    interval_sec = TF_SECONDS.get(tf, 900)
    ctx = {"candles": candles, "closes": closes, "i": i, "ema_arrays": ema_arrays,
           "next_close_t": candles[i]["t"] + interval_sec + 5,
           "_debug_gate_wait_sec": gate_wait_sec, "_debug_fetch_sec": fetch_sec}
    with _stretch_diag_ctx_cache_lock:
        _stretch_diag_ctx_cache[key] = ctx
    return ctx


def _stretch_check_symbol(symbol, tf, submitted_ts=None):
    """Возвращает список кандидатов на новую запись (отрыв ≥
    STRETCH_DIAG_MIN_PCT от любой из STRETCH_DIAG_PERIODS) — без дедупа,
    им занимается вызывающий цикл.
    v0.30.55: реальная находка по прямому уточнению — раньше триггер
    считался от ЖИВОЙ цены на каждом опросе (раз в 3 минуты), а не строго
    на ЗАКРЫТИИ свечи — мог поймать внутрисвечной всплеск, который к
    закрытию сам же откатился (то есть событие, которого "по свече" не
    было). Теперь используется цена и EMA ИМЕННО последней ЗАКРЫТОЙ свечи
    (ctx закэширован до следующего закрытия — при повторных опросах
    внутри одной и той же ещё не закрытой свечи вернёт то же самое, так
    что переоценки не будет, только настоящий новый бар даёт новую
    проверку). Живая цена (_gate_get_price) осталась ТОЛЬКО в трек-лупе —
    там она и должна быть, чтобы поймать момент возврата к EMA, не
    дожидаясь следующего закрытия.
    v0.30.72: submitted_ts (когда задача попала в пул) — для честной
    разбивки задержки на "ждала своей очереди у потоков" отдельно от
    "ждала в рейт-лимите" и "сам сетевой запрос"."""
    picked_ts = time.time()
    queue_wait_sec = round(picked_ts - submitted_ts, 3) if submitted_ts else None
    ctx = _stretch_get_closed_ctx(symbol, tf)
    if ctx is None:
        return []
    now = time.time()
    closed_price = ctx["closes"][ctx["i"]]
    # v0.30.90: по прямому запросу — "если всплеск на затишье, шанс на
    # откат выше, чем в трендовом рынке". Меряем силу тренда ДО отрыва —
    # изменение цены за STRETCH_TREND_LOOKBACK_BARS баров, предшествующих
    # закрытой свече триггера (не включая саму эту свечу — иначе сам
    # отрыв исказил бы измерение). Данные уже есть в ctx (не нужен
    # дополнительный сетевой запрос)
    trend_i = ctx["i"] - STRETCH_TREND_LOOKBACK_BARS
    trend_pct = None
    if trend_i >= 0 and ctx["closes"][trend_i]:
        base = ctx["closes"][trend_i]
        ref_price = ctx["closes"][ctx["i"] - 1] if ctx["i"] >= 1 else closed_price   # v0.30.90: тренд ДО текущего бара, сам триггерный бар не включаем
        trend_pct = round((ref_price - base) / base * 100, 3)
    # v0.30.91: по прямому запросу — "что можно добавить для лонга". Настоящий
    # пробой обычно идёт с ростом объёма на самой триггерной свече, а
    # случайный шпиль на тонком объёме скорее выдохнется — особенно
    # значимо для continuation (тезис "это реальный импульс"). Объём
    # триггерной свечи относительно среднего за предыдущие
    # STRETCH_TREND_LOOKBACK_BARS баров (не включая саму свечу)
    trigger_vol_ratio = None
    if trend_i >= 0:
        prev_vols = [c.get("vol") or 0 for c in ctx["candles"][trend_i:ctx["i"]]]
        avg_vol = sum(prev_vols) / len(prev_vols) if prev_vols else 0
        trigger_vol = ctx["candles"][ctx["i"]].get("vol") or 0
        if avg_vol > 0:
            trigger_vol_ratio = round(trigger_vol / avg_vol, 2)
    out = []
    for period in _stretch_scan_active_periods():   # v0.30.95: по прямому запросу — скан только по выбранным ЕМА (галочки в настройках), ema_arrays выше всё равно посчитаны для всех STRETCH_DIAG_PERIODS (дёшево, кэш общий), фильтр только на генерацию сигналов
        ema_closed = ctx["ema_arrays"][period][ctx["i"]]
        if ema_closed is None:
            continue
        stretch_pct = (closed_price - ema_closed) / ema_closed * 100.0
        if stretch_pct < STRETCH_DIAG_MIN_PCT:   # v0.30.52: по прямому запросу — только отрыв ВВЕРХ (цена выше EMA), ожидаем падение к EMA обратно. Отрыв вниз (price < ema) больше не фиксируем — раньше ловили оба направления симметрично
            continue
        bar_close_ts = ctx["candles"][ctx["i"]]["t"] + TF_SECONDS.get(tf, 0)
        entry = closed_price
        stop = _round_price(entry * (1 + STRETCH_REF_STOP_PCT / 100))   # v0.30.67: по прямому запросу — реальный сигнал с тейком/стопом. Шорт на возврат к EMA: стоп ВЫШЕ входа (цена ушла ещё дальше), тейк — сам уровень EMA
        take = _round_price(ema_closed)
        out.append({
            "id": f"{symbol}|{tf}|{period}|{int(now)}",
            "ts": int(now), "symbol": symbol, "tf": tf, "period": period,
            "bar_close_ts": bar_close_ts,   # v0.30.63: по прямому запросу — прозрачность задержки: реальный момент закрытия свечи, не момент обнаружения
            "debug_queue_wait_sec": queue_wait_sec,     # v0.30.72: сколько ждала своей очереди у 24 потоков
            "debug_submitted_ts": submitted_ts,   # v0.30.79: момент постановки в очередь — нужен, чтобы отличить "долго обрабатывали" от "долго ждала своей очереди после закрытия свечи" (типично после рестарта)
            "debug_gate_wait_sec": ctx.get("_debug_gate_wait_sec"),   # сколько ждала в ограничителе скорости (8 запросов/сек)
            "debug_fetch_sec": ctx.get("_debug_fetch_sec"),           # сколько занял сам сетевой запрос к Gate.io
            "trigger_price": closed_price, "trigger_ema": _round_price(ema_closed),
            "entry": entry, "stop": stop, "take": take,
            "strategy": "fade", "direction": "short",   # v0.30.83: явное поле — раньше стратегия была одна (фейд), теперь есть вторая (продолжение), нужно различать в сводке/ранжировании
            "trend_pct": trend_pct,   # v0.30.90: сила тренда ДО отрыва — по прямому запросу, для сравнения винрейта "затишье vs тренд"
            "trigger_vol_ratio": trigger_vol_ratio,   # v0.30.91: объём триггерной свечи относительно среднего фона — подтверждение реального пробоя, не тонкого шпиля
            "stretch_pct": round(stretch_pct, 3), "side": "above" if stretch_pct > 0 else "below",
            "touched": False, "touched_at": None, "time_to_touch_sec": None,
            "closest_stretch_pct": round(stretch_pct, 3),
            "max_retrace_pct_of_stretch": 0.0,
            "max_adverse_stretch_pct": round(stretch_pct, 3),
            "signal_outcome": "open",   # v0.30.67: "open" | "take" | "stop" | "timeout" — реальный исход С УЧЁТОМ порядка событий (что случилось раньше), не задним числом по пиковым значениям
            "last_checked_ts": int(now),
            "track_until": now + STRETCH_DIAG_TRACK_WINDOW_SEC.get(tf, 6 * 3600), "track_done": False,
        })
        if stretch_pct > STRETCH_CONTINUATION_MIN_PCT:
            # v0.30.83: по прямому запросу — "силу отрыва больше 2% можно
            # использовать как открытие сделки в другую сторону". Данные
            # (979 решённых фейд-сигналов) показали: чем сильнее отрыв,
            # тем реже цена откатывается к EMA до фиксированного стопа —
            # сильные отрывы почти всегда оказываются реальным трендом.
            # Это разворачивает тезис на 180° — не фейд (шорт на возврат),
            # а продолжение (лонг на импульс). ВАЖНО: это НЕ проверено
            # эмпирически — своих исторических данных по продолжению нет,
            # цифры стоп/тейк ниже НАЧАЛЬНАЯ прикидка (RR 2:1 по аналогии
            # с фейдом), не откалиброванный результат. Копим статистику
            # с нуля, как и с фейдом в своё время.
            c_entry = closed_price
            c_stop = _round_price(c_entry * (1 - STRETCH_CONTINUATION_STOP_PCT / 100))
            c_take = _round_price(c_entry * (1 + STRETCH_CONTINUATION_TAKE_PCT / 100))
            out.append({
                "id": f"{symbol}|{tf}|{period}|cont|{int(now)}",
                "ts": int(now), "symbol": symbol, "tf": tf, "period": period,
                "bar_close_ts": bar_close_ts,
                "debug_queue_wait_sec": queue_wait_sec, "debug_submitted_ts": submitted_ts,
                "debug_gate_wait_sec": ctx.get("_debug_gate_wait_sec"), "debug_fetch_sec": ctx.get("_debug_fetch_sec"),
                "trigger_price": closed_price, "trigger_ema": _round_price(ema_closed),
                "entry": c_entry, "stop": c_stop, "take": c_take,
                "strategy": "continuation", "direction": "long",
                "trend_pct": trend_pct,
                "trigger_vol_ratio": trigger_vol_ratio,
                "stretch_pct": round(stretch_pct, 3), "side": "above",
                "touched": False, "touched_at": None, "time_to_touch_sec": None,
                "closest_stretch_pct": round(stretch_pct, 3),
                "max_retrace_pct_of_stretch": 0.0,
                "max_adverse_stretch_pct": round(stretch_pct, 3),
                "signal_outcome": "open",
                "last_checked_ts": int(now),
                "track_until": now + STRETCH_DIAG_TRACK_WINDOW_SEC.get(tf, 6 * 3600), "track_done": False,
            })
    return out


def _stretch_diag_save():
    try:
        with _stretch_diag_lock:
            trimmed = _stretch_diag_records[-STRETCH_DIAG_MAX_RECORDS:]
        tmp_path = STRETCH_DIAG_FILE + f".tmp{os.getpid()}"
        with open(tmp_path, "w") as f:
            json.dump(trimmed, f)
        os.replace(tmp_path, STRETCH_DIAG_FILE)
    except Exception as e:
        olog(f"[ema_stretch] ⚠ не смог сохранить {STRETCH_DIAG_FILE}: {_explain_error(e)}")


def _stretch_diag_load():
    global _stretch_diag_records
    try:
        if os.path.exists(STRETCH_DIAG_FILE):
            with open(STRETCH_DIAG_FILE) as f:
                loaded = json.load(f)
            if isinstance(loaded, list):
                with _stretch_diag_lock:
                    _stretch_diag_records = loaded
                still_open = sum(1 for r in loaded if not r.get("track_done", True))
                olog(f"[ema_stretch] история подхвачена с диска: {len(loaded)} записей, "
                     f"из них ещё отслеживается {still_open}")
    except Exception as e:
        olog(f"[ema_stretch] ⚠ не смог подхватить историю с диска: {_explain_error(e)}")


STRETCH_DIAG_SCAN_WORKERS = 48   # v0.30.73: реальная находка — 24 потока × ~3.1с медианный запрос давали максимум ~7.7 запросов/сек пропускной способности, то есть воркеры сами были узким местом (не лимит скорости, не rate limit API). Подняли до 48 (вместе с STRETCH_RATE_LIMIT_PER_SEC=16) — должно примерно вдвое ускорить разгребание всплеска в момент синхронного закрытия свечей


_stretch_diag_last_cycle_sec = None   # v0.30.66: по прямому вопросу "сколько времени уходит на цикл" — раньше никак не измерялось, только гадали по косвенным признакам (лагу). Теперь честный замер.


def _stretch_diag_scan_loop():
    global _stretch_diag_last_cycle_sec
    time.sleep(60)
    while True:
        cycle_start = time.time()
        try:
            symbols = _fetch_all_symbols()
            added = 0
            now0 = time.time()
            # v0.30.68: реальная находка по прямому вопросу "почему задержка
            # всё ещё большая" — раньше на КАЖДОМ цикле в очередь уходили
            # ВСЕ ~700 монет × 4 ТФ, даже те, чей кэш свечи ещё не истёк
            # (99% проверок впустую — кэш просто возвращает то же самое).
            # Эти бесполезные проверки всё равно занимали место в очереди
            # у ограниченного числа потоков, оттягивая внимание от
            # немногих, кому СЕЙЧАС реально нужен свежий фетч. Теперь
            # заранее фильтруем — в пул уходят только те (symbol, tf), у
            # которых кэш просрочен или ещё не создавался вообще.
            active_tfs = _stretch_scan_active_timeframes()   # v0.30.95: по прямому запросу — скан только по выбранным ТФ (галочки в настройках)
            with _stretch_diag_ctx_cache_lock:
                due_tasks = []
                for symbol in symbols:
                    for tf in active_tfs:
                        cached = _stretch_diag_ctx_cache.get(f"{symbol}|{tf}")
                        if not cached or now0 >= cached["next_close_t"]:
                            due_tasks.append((symbol, tf))
            tasks = due_tasks
            submitted_ts = time.time()
            with ThreadPoolExecutor(max_workers=STRETCH_DIAG_SCAN_WORKERS) as pool:
                futures = {pool.submit(_stretch_check_symbol, symbol, tf, submitted_ts): (symbol, tf) for symbol, tf in tasks}
                for fut in _as_completed(futures):
                    symbol, tf = futures[fut]
                    try:
                        candidates = fut.result()
                    except Exception as e:
                        olog(f"[ema_stretch] {symbol} {tf}: ошибка проверки: {_explain_error(e)}")
                        continue
                    if not candidates:
                        continue
                    with _stretch_diag_lock:
                        # v0.30.96: реальный и серьёзный баг, найден по
                        # прямому вопросу "стоп по bill и magma, что скажешь
                        # по винрейту" — BILL_USDT дал 483 continuation-записи,
                        # но ТОЛЬКО 34 уникальные комбинации (tf,период,вход,
                        # стоп,тейк). Причина: дедуп блокировал новую запись
                        # ТОЛЬКО пока предыдущая ещё открыта (track_done=False).
                        # У continuation узкий стоп (1%) — резолвится быстро, и
                        # как только резолвится, ключ освобождается. Но сама
                        # закрытая свеча (bar_close_ts) на 4h ещё НЕ сменилась
                        # (следующая только через часы) — следующий скан-цикл
                        # видит ТОТ ЖЕ триггер и создаёт НОВУЮ запись с
                        # ИДЕНТИЧНЫМИ entry/stop/take, та тоже резолвится быстро
                        # (то же самое ценовое окружение) — и так по кругу
                        # каждый скан-цикл, пока свеча реально не закроется.
                        # Это не только портит статистику (одно событие
                        # считается десятками раз), но и РИСК ДЛЯ АВТОТОРГОВЛИ
                        # — мог открывать реальные сделки по кругу на одном и
                        # том же сетапе. Теперь ключ дедупа включает
                        # bar_close_ts и проверяется по ВСЕМ записям (не только
                        # открытым) — одна и та же свеча даёт сигнал РОВНО один
                        # раз, резолвилась она уже или нет.
                        seen_keys = {f"{r['symbol']}|{r['tf']}|{r['period']}|{r.get('strategy', 'fade')}|{r.get('bar_close_ts')}"
                                     for r in _stretch_diag_records}
                        fresh = []
                        for rec in candidates:
                            rkey = f"{rec['symbol']}|{rec['tf']}|{rec['period']}|{rec.get('strategy', 'fade')}|{rec.get('bar_close_ts')}"
                            if rkey in seen_keys:   # уже был сигнал по этой же свече (закрыта она или ещё торгуется) — не дублируем
                                continue
                            _stretch_diag_records.append(rec)
                            seen_keys.add(rkey)
                            fresh.append(rec)
                            added += 1
                    if fresh:
                        # v0.30.58: реальный баг — раньше алерт проверялся ОДИН
                        # раз, после того как проход по ВСЕМ символам×ТФ
                        # (может занимать десятки минут реального сетевого
                        # времени) полностью завершался — события копились
                        # весь проход, а алерты по ним улетали разом пачкой
                        # в конце, с задержкой в десятки минут от реального
                        # момента и с "тишиной" на весь следующий проход.
                        # Теперь алерт проверяется СРАЗУ по каждой найденной
                        # записи, внутри цикла — без накопления и без
                        # привязки к длительности всего прохода.
                        _stretch_diag_save()
                        _stretch_diag_maybe_alert_top_combo(fresh)
                        _stretch_autotrade_process_fresh(fresh)   # v0.30.95: по прямому запросу — автоторговля по выбранным конфигам, не привязана к топ-связке алертов
            cycle_sec = round(time.time() - cycle_start, 1)
            _stretch_diag_last_cycle_sec = cycle_sec
            olog(f"[ema_stretch] скан завершён за {cycle_sec}с: +{added} новых отрывов "
                 f"(задач {len(tasks)} из {len(symbols) * len(active_tfs)} возможных, "
                 f"всего в истории {len(_stretch_diag_records)})")
        except Exception as e:
            olog(f"[ema_stretch] ошибка цикла сканирования: {_explain_error(e)}")
        time.sleep(STRETCH_DIAG_SCAN_POLL_SEC)


STRETCH_TOP_ALERT_MIN_EVENTS = 10   # v0.30.53: не объявляем "топовую связку" по горстке случайных событий — ждём минимум столько оценённых, прежде чем на неё начать алертить
STRETCH_ALERT_MAX_STALENESS_SEC = 300   # v0.30.79: по прямому запросу — "задержка по факту позднего запуска, сигнал неактуален, зачем слать". 5 минут "мёртвого" времени ДО постановки в очередь (не путать с самой обработкой — та обычно секунды) — типичный признак пропущенного цикла/рестарта, не нормальная рабочая задержка
STRETCH_SIGNAL_MAX_PCT = 2.0   # v0.30.83: по прямому запросу вернули обратно на 2.0 (было временно занижено до 1.5 в v0.30.82) — отрывы больше этого порога теперь не отбрасываются, а идут в отдельную стратегию продолжения (см. STRETCH_CONTINUATION_*)
STRETCH_CONTINUATION_MIN_PCT = 5.0   # v0.30.88: реальная находка на 1491 решённых continuation-сигналах — у continuation ОБРАТНАЯ зависимость от фейда: чем сильнее отрыв, тем ЛУЧШЕ (2-4% давали винрейт 25-27%/PnL от -0.18% до -0.26%, а 5%+ дали 40%/+0.21%). Зона 2-5% — "ничья земля", не работает ни как фейд (уже отсекается STRETCH_SIGNAL_MAX_PCT), ни как продолжение. Раньше continuation триггерился сразу с >2.0 (тем же порогом, что и потолок фейда) — теперь свой, отдельный, более высокий порог, дальше от границы
STRETCH_CONTINUATION_STOP_PCT = 1.0   # v0.30.84: по прямому предложению — раз у фейда была логика "стоп 2% / тейк ~1%" (широкий стоп, узкая цель — ждём слабый откат), для продолжения тренда логично ЗЕРКАЛЬНО: узкий стоп (настоящий тренд не должен откатываться против нас сильно) и широкая цель (ловим сам импульс). Всё ещё НЕ откалибровано по данным — стартовая прикидка, копим статистику с нуля
STRETCH_CONTINUATION_TAKE_PCT = 2.0   # v0.30.84: см. выше — RR теперь 2:1 при СУЖЕННОМ стопе (было 4%/2%=2:1 при широком стопе — тот же RR, но абсолютные числа меньше и логика соответствует самой идее тренда, а не скопирована с фейда бездумно


def _stretch_render_alert_chart(rec):
    """v0.30.59: по прямому запросу — "к оповещению добавлять кусочек
    графика, метку где сработал сигнал и куда планируется движение".
    Переиспользует _render_signal_chart_png (был в файле, но нигде не
    подключён) — рисует последние свечи, entry = цена триггера (текущая,
    это последняя свеча в окне), tp = уровень EMA (это и есть "куда
    планируется движение" — цель возврата), sl не показываем (тут не
    сделка, стопа нет).
    v0.30.61: реальная находка по прямому вопросу "нет графика" — раньше
    любая причина провала (PIL не установлен, мало свечей, что угодно)
    тихо схлопывалась в один и тот же None, видимый только в логах
    консоли, до которых на телефоне неудобно добираться. Теперь функция
    возвращает (png, причина_отказа) — причина явно попадает в текст
    самого алерта в Telegram, не нужно лезть в логи, чтобы понять, что
    произошло."""
    try:
        from PIL import Image  # noqa: F401 — только для проверки доступности
        have_pil = True
    except ImportError:
        have_pil = False
    try:
        ctx = _stretch_get_closed_ctx(rec["symbol"], rec["tf"])
        if not ctx:
            return None, "не удалось получить контекст свечей для графика (ctx пуст)"
        dir_label = (f"LONG — продолжение импульса" if rec.get("strategy") == "continuation"
                     else f"SHORT — цель EMA{rec['period']}")
        sig = {"entry": rec.get("entry", rec["trigger_price"]), "tp": rec.get("take", rec["trigger_ema"]),
               "sl": rec.get("stop"), "dir": dir_label, "entry_i": ctx["i"]}
        if have_pil:
            png = _render_signal_chart_png(ctx["candles"], sig, rec["symbol"], rec["tf"])
            if png:
                return png, None
            # PIL есть, но рендер всё равно не удался — пробуем stdlib-версию
        png = _render_signal_chart_png_stdlib(ctx["candles"], sig, rec["symbol"], rec["tf"])
        if not png:
            return None, "оба рендерера графика (PIL и stdlib) вернули пусто — недостаточно свечей в окне или внутренняя ошибка отрисовки"
        return png, None
    except Exception as e:
        return None, _explain_error(e)


def _stretch_rec_is_stale(rec):
    """v0.30.95: вынесено из _fire (было только для алерта) — событие
    считается устаревшим, если задержка ДО постановки в очередь
    (submitted_ts - bar_close_ts) больше STRETCH_ALERT_MAX_STALENESS_SEC
    — типичный след пропущенного цикла/рестарта, не нормальная рабочая
    задержка. Теперь используется и независимо в автоторговле по
    выбранным конфигам (_stretch_autotrade_process_fresh), чтобы не
    открывать позицию по сигналу, который уже неактуален к моменту
    обработки, даже если по нему не отправлялся Telegram-алерт (не
    топ-связка). Возвращает (is_stale: bool, reason: str|None)."""
    submitted_ts = rec.get("debug_submitted_ts")
    if submitted_ts:
        staleness_before_queue = submitted_ts - rec.get("bar_close_ts", submitted_ts)
        if staleness_before_queue > STRETCH_ALERT_MAX_STALENESS_SEC:
            return True, (f"событие устарело ещё до постановки в очередь "
                           f"({round(staleness_before_queue/60,1)} мин, типичный след рестарта)")
    return False, None


def _stretch_diag_maybe_alert_top_combo(new_recs):
    """По прямому запросу — "алертить топовую связку в тг": каждый скан
    заново смотрит текущий рейтинг (_stretch_diag_summary, тот же, что
    видно на странице) и, если СРЕДИ НОВЫХ записей этого прохода есть
    хотя бы одна с (tf, period), совпадающим с #1 в рейтинге — шлёт
    алерт. Топ пересчитывается каждый раз заново (не зафиксирован
    навсегда) — по мере накопления данных лидер может смениться, и
    алерты будут идти уже по новому лидеру, без ручного вмешательства.
    v0.30.83: по прямому запросу — сильные отрывы (>STRETCH_SIGNAL_MAX_PCT)
    теперь не просто отбрасываются, а идут отдельной стратегией
    "продолжение" (LONG на импульс, а не SHORT на возврат). У неё свой
    топ, независимый от фейда — обе стратегии могут алертить параллельно."""
    if not _get_stretch_top_alert_enabled():
        return
    try:
        summary = _stretch_diag_summary(min_events=STRETCH_TOP_ALERT_MIN_EVENTS)
    except Exception as e:
        olog(f"[ema_stretch] ⚠ не смог посчитать топ связку: {_explain_error(e)}")
        return
    if not summary:
        return   # ещё не набралось достаточно данных ни по одной связке

    def _fire(rec, top, label):
        top_tf, top_period = top["tf"], top["period"]
        o = top["overall"]
        lag_sec = max(0, int(time.time() - rec.get("bar_close_ts", rec["ts"])))
        # v0.30.79: по прямому запросу — "задержка просто по факту запуска
        # позднего, такие сигналы неактуальны, зачем их слать". Раньше
        # уже пробовали резать по общей задержке — не годилось, тогда
        # задержка была большой почти у всех сигналов. Теперь режем точнее:
        # смотрим не на ИТОГОВУЮ задержку, а на то, сколько времени прошло
        # ДО постановки в очередь (submitted_ts - bar_close_ts) — если
        # событие уже было старым, когда его только поставили на
        # обработку, это типичный след пропущенного цикла/рестарта, не
        # нормальная рабочая задержка. Запись всё равно остаётся в истории
        # для статистики — режем только отправку алерта.
        # v0.30.95: проверка вынесена в _stretch_rec_is_stale — теперь
        # используется и здесь, и независимо в автоторговле по выбранным
        # конфигам (та больше не идёт следом за этим алертом).
        stale, stale_reason = _stretch_rec_is_stale(rec)
        if stale:
            olog(f"[ema_stretch] {rec['symbol']} {top_tf} EMA{top_period} ({label}): "
                 f"алерт пропущен — {stale_reason}")
            return
        lag_txt = f"{lag_sec}с" if lag_sec < 60 else f"{lag_sec // 60}м{lag_sec % 60}с"
        qw, gw, fs = rec.get("debug_queue_wait_sec"), rec.get("debug_gate_wait_sec"), rec.get("debug_fetch_sec")
        debug_txt = (f" (очередь {qw}с, рейт-лимит {gw}с, запрос {fs}с)"
                     if qw is not None or gw is not None or fs is not None else "")
        if rec.get("strategy") == "continuation":
            msg = (f"📈 <b>{rec['symbol']}</b> — LONG на продолжение импульса от EMA{top_period} ({top_tf}), "
                   f"топ связка по винрейту тейк/стоп (⚠️ экспериментально, стоп/тейк не откалиброваны по истории)\n"
                   f"Вход: {_fmt_px(rec['entry'])} | Стоп: {_fmt_px(rec['stop'])} ({STRETCH_CONTINUATION_STOP_PCT}%) | "
                   f"Тейк: {_fmt_px(rec['take'])} ({STRETCH_CONTINUATION_TAKE_PCT}%, фикс. цель, не EMA)\n"
                   f"Отрыв на входе: {rec['stretch_pct']:+.2f}%\n"
                   f"Задержка от закрытия свечи: {lag_txt}{debug_txt}\n"
                   f"По статистике этой связки (n={o['evaluated']}): "
                   f"винрейт тейк/стоп (n={o['signal_resolved_n']}) — {o['signal_win_rate']}%")
        else:
            msg = (f"📉 <b>{rec['symbol']}</b> — SHORT на возврат к EMA{top_period} ({top_tf}), "
                   f"топ связка по винрейту тейк/стоп\n"
                   f"Вход: {_fmt_px(rec['entry'])} | Стоп: {_fmt_px(rec['stop'])} ({STRETCH_REF_STOP_PCT}%) | "
                   f"Тейк: {_fmt_px(rec['take'])} (уровень EMA{top_period})\n"
                   f"Отрыв: {rec['stretch_pct']:+.2f}%\n"
                   f"Задержка от закрытия свечи: {lag_txt}{debug_txt}\n"
                   f"По статистике этой связки (n={o['evaluated']}, риск-выборка n={o['stop_survival_n']}): "
                   f"выживает при стопе {STRETCH_REF_STOP_PCT}% — {o['stop_survival_rate']}%, "
                   f"винрейт тейк/стоп (n={o['signal_resolved_n']}) — {o['signal_win_rate']}%")
        png, err = _stretch_render_alert_chart(rec)
        if png:
            _send_alert_photo(png, msg)
        else:
            msg += f"\n⚠️ график не приложен: {err}"
            olog(f"[ema_stretch] ⚠ график для {rec['symbol']} не отрисован: {err}")
            _send_alert(msg)
        olog(f"[ema_stretch] {rec['symbol']} {top_tf} EMA{top_period} ({label}): "
             f"алерт по топ-связке отправлен (отрыв {rec['stretch_pct']:+.2f}%)")
        # v0.30.95: автоторговля больше не вызывается отсюда — раньше
        # была жёстко привязана к топ-связке (только для сигналов,
        # совпавших с текущим лидером по винрейту), теперь она
        # независимый путь по явно выбранным пользователем конфигам, см.
        # _stretch_autotrade_process_fresh (вызывается из скан-лупа для
        # каждого прохода, а не отсюда).

    for strategy, label in (("fade", "фейд"), ("continuation", "продолжение")):
        candidates = [e for e in summary if e.get("strategy", "fade") == strategy]
        if not candidates:
            continue
        top = candidates[0]
        top_tf, top_period = top["tf"], top["period"]
        for rec in new_recs:
            if rec.get("strategy", "fade") != strategy:
                continue
            if rec["tf"] != top_tf or rec["period"] != top_period:
                continue
            _fire(rec, top, label)


STRETCH_TRACK_WORKERS = 32   # v0.30.74: реальный баг, найден по прямому сообщению "стоп был, а в логах не нашёл" — трек-луп был ОДНОПОТОЧНЫМ: шёл по всем открытым записям ПО ОДНОЙ, с сетевым запросом на каждую. Когда открытых записей накопилось за тысячу (100% из них!), last_checked_ts НИ РАЗУ не обновлялся после создания — однопоточный проход просто не успевал закончиться до следующего цикла, а список тем временем только рос. Ровно та же болезнь, что была у скана монет (v0.30.62), только тут пропустил. Теперь параллельно, пулом потоков — как и скан


_stretch_1m_cache = {}
_stretch_1m_cache_lock = threading.Lock()


def _stretch_recent_1m_high_low(symbol):
    """v0.30.77: реальная находка по прямому вопросу "стоп словил, а в
    логах нет" (EPIC_USDT) — _gate_get_price() берёт ОДНУ точку цены раз в
    60 секунд (STRETCH_DIAG_TRACK_POLL_SEC). Если цена дёрнулась выше
    стопа и вернулась обратно быстрее минуты между двумя опросами — эта
    точечная выборка её просто не видит, хотя на бирже стоп сработал бы
    по факту касания. Возвращает (макс_high, мин_low) по последним ~15
    минутам 1-минутных свечей — покрывает разрыв между опросами с
    запасом. Свой короткий кэш (30 сек) — несколько записей с одним и тем
    же symbol, но разными period/tf, не дублируют сетевой запрос."""
    key = symbol
    now = time.time()
    with _stretch_1m_cache_lock:
        cached = _stretch_1m_cache.get(key)
        if cached and now - cached[0] < 30:
            return cached[1]
    try:
        candles = _fetch_candles(symbol, "1m", 0.015)
        result = (max(c["high"] for c in candles), min(c["low"] for c in candles)) if candles else None
    except Exception:
        result = None
    with _stretch_1m_cache_lock:
        _stretch_1m_cache[key] = (now, result)
    return result


STRETCH_AUTOTRADE_CFG_FILE = os.path.expanduser("~/pumpradar_stretch_autotrade_cfg.json")
_stretch_autotrade_lock = threading.Lock()
_stretch_autotrade_state = {
    "enabled": False,
    "mode": "off",          # v0.30.92: по прямому запросу — "Long или Short на выбор". "off" | "long" | "short"
    "position_pct": 3.0,    # % депозита (маржи) на одну сделку
    "leverage": 10,         # v0.30.92: по памяти проекта — план торговать с фикс. 10х плечом; leverage передаётся не напрямую в _gate_open_position (та выводит плечо из risk_pct/sl_pct%), а через risk_pct = leverage × sl_pct, вычисляемый на лету под фактический стоп сигнала — так плечо получается ровно заданным независимо от того, у фейда стоп 0.5% или у продолжения 1%
    "max_concurrent": None,
    "configs": [],   # v0.30.95: по прямому запросу — "по какому конфигу осуществлять автоторговлю, а не по топ 1". Список строк "tf|period|strategy" (напр. "15m|5|fade") — явно выбранные пользователем связки, галочки на странице. Пусто = автоторговля не откроет ни одной позиции, даже если enabled=True (раньше по умолчанию торговала по меняющейся топ-связке — теперь нужен явный выбор)
}


def _load_stretch_autotrade_cfg():
    try:
        with open(STRETCH_AUTOTRADE_CFG_FILE) as f:
            saved = json.load(f)
        with _stretch_autotrade_lock:
            for k in ("enabled", "mode", "position_pct", "leverage", "max_concurrent", "configs"):
                if k in saved:
                    _stretch_autotrade_state[k] = saved[k]
    except Exception:
        pass


def _save_stretch_autotrade_cfg():
    try:
        with _stretch_autotrade_lock:
            snapshot = dict(_stretch_autotrade_state)
        with open(STRETCH_AUTOTRADE_CFG_FILE, "w") as f:
            json.dump(snapshot, f)
    except Exception as e:
        olog(f"[stretch_autotrade] ошибка сохранения настроек: {_explain_error(e)}")


_stretch_autotrade_open_lock = threading.Lock()   # v0.30.93: реальная находка при финальной проверке безопасности — между проверкой "нет ли уже открытой позиции" и самим открытием ордера проходит сетевое время, в параллельном скане (48 потоков) теоретически два потока могли пройти проверку одновременно и открыть ДВЕ позиции по одной монете. Явная блокировка сериализует весь check-then-open, устраняя гонку полностью, не только для этого конкретного сценария


def _stretch_maybe_open_live_trade(rec):
    """v0.30.92: по прямому запросу — "автоторговля для Long или Short на
    выбор, тейки и стоп из сигнала". Переиспользует уже готовый
    _gate_open_position (расчёт плеча/размера позиции, ретраи, TP/SL,
    алерты на провал — весь этот код уже был в файле от старой системы
    EMA INVERT, просто не был подключён к отрывам от EMA).
    v0.30.95: раньше вызывалась ТОЛЬКО из _stretch_diag_maybe_alert_top_combo
    (то есть только для сигналов текущей топ-связки, чей алерт реально
    ушёл). Теперь вызывается из _stretch_autotrade_process_fresh — по
    явно выбранным пользователем конфигам, независимо от топ-связки.
    Проверки enabled/mode здесь оставлены как есть (защита от гонки —
    настройки могли поменяться между отбором в вызывающей функции и
    этим вызовом)."""
    with _stretch_autotrade_lock:
        if not _stretch_autotrade_state["enabled"]:
            return
        mode = _stretch_autotrade_state["mode"]
        if mode == "off" or rec.get("direction") != mode:
            return
        position_pct = _stretch_autotrade_state["position_pct"]
        leverage = _stretch_autotrade_state["leverage"]
        max_c = _stretch_autotrade_state["max_concurrent"]
    if not (GATE_KEY and GATE_SECRET):
        olog(f"[stretch_autotrade] {rec['symbol']}: включено, но ключи Gate.io не настроены (/gate_cfg) — пропуск")
        return
    symbol = rec["symbol"]
    contract = symbol.replace("/", "_").upper()
    with _stretch_autotrade_open_lock:   # v0.30.93: сериализует ВЕСЬ check-then-open, не только чтение списка — см. докстринг лока выше
        with _stretch_diag_lock:
            live_open = [r for r in _stretch_diag_records if r.get("live") and not r.get("live_closed")]
        if any(r["symbol"] == symbol for r in live_open):
            olog(f"[stretch_autotrade] {symbol}: уже есть открытая live-позиция по этой монете — пропуск")
            return
        if max_c and len(live_open) >= max_c:
            olog(f"[stretch_autotrade] лимит {max_c} одновр. live-позиций достигнут — пропуск {symbol}")
            return
        try:
            existing = _gate_get_position(contract)
        except Exception as e:
            olog(f"[stretch_autotrade] {symbol}: не смог проверить биржу перед входом ({_explain_error(e)}) — пропуск на этот раз")
            return
        if existing:
            olog(f"[stretch_autotrade] {symbol}: на бирже уже есть позиция вне нашего учёта — пропуск")
            return
        entry, stop, take = rec["entry"], rec["stop"], rec["take"]
        sl_pct = abs(entry - stop) / entry * 100.0
        risk_pct = leverage * sl_pct   # см. докстринг _stretch_autotrade_state["leverage"] — так выводится ровно заданное плечо
        strat_label = "фейд" if rec.get("strategy") == "fade" else "продолжение"
        pos_info = _gate_open_position(
            symbol, rec["direction"], entry, stop, take, risk_pct,
            position_pct=position_pct, label=f"STRETCH {strat_label}", text_prefix="stretch",
        )
        if not pos_info:
            return
        with _stretch_diag_lock:
            rec["live"] = True
            rec["live_size"] = pos_info.get("size")
            rec["live_leverage"] = pos_info.get("leverage")
            rec["live_notional"] = pos_info.get("notional")
            rec["live_opened_at"] = int(time.time())
        _stretch_diag_save()


def _stretch_autotrade_process_fresh(fresh):
    """v0.30.95: по прямому запросу — "по какому конфигу осуществлять
    автоторговлю, а не по топ 1". Раньше автоторговля была жёстко
    привязана к топ-связке по винрейту (вызывалась только из _fire,
    только для сигналов, совпавших с текущим лидером) — список конфигов
    для торговли менялся сам вслед за лидером, без участия пользователя.
    Теперь независимый путь: для каждой свежей записи скана проверяем,
    совпадает ли её (tf, period, strategy) с одним из явно выбранных
    пользователем конфигов (галочки в настройках автоторговли,
    _stretch_autotrade_state['configs']). Полностью отвязано от того,
    ушёл ли по этой связке алерт в Telegram — торговый и алертовый пути
    независимы. Проверка на устаревание (_stretch_rec_is_stale)
    сохранена — не торгуем по сигналу, который уже неактуален к моменту
    обработки, даже если он не идёт в Telegram."""
    with _stretch_autotrade_lock:
        if not _stretch_autotrade_state["enabled"]:
            return
        mode = _stretch_autotrade_state["mode"]
        configs = set(_stretch_autotrade_state.get("configs") or [])
    if mode == "off" or not configs:
        return
    for rec in fresh:
        if rec.get("direction") != mode:
            continue
        cfg_key = f"{rec['tf']}|{rec['period']}|{rec.get('strategy', 'fade')}"
        if cfg_key not in configs:
            continue
        stale, reason = _stretch_rec_is_stale(rec)
        if stale:
            olog(f"[stretch_autotrade] {rec['symbol']} {rec['tf']} EMA{rec['period']}: "
                 f"сигнал пропущен для автоторговли — {reason}")
            continue
        _stretch_maybe_open_live_trade(rec)


def _stretch_autotrade_watch_loop():
    """Раз в минуту проверяет реально открытые live-позиции по отрывам от
    EMA — если позиция на бирже закрылась (TP/SL сработал или закрыта
    вручную), шлёт алерт с реальным PnL (переиспользует
    _check_position_closed_and_alert, тот же код, что и у старой системы
    EMA INVERT) и помечает запись как live_closed, чтобы не проверять
    повторно."""
    time.sleep(120)
    while True:
        try:
            with _stretch_diag_lock:
                live_open = [r for r in _stretch_diag_records if r.get("live") and not r.get("live_closed")]
            for rec in live_open:
                closed = _check_position_closed_and_alert(
                    rec["symbol"], {"dir": rec["direction"], "entry": rec["entry"]})
                if closed:
                    with _stretch_diag_lock:
                        rec["live_closed"] = True
                    _stretch_diag_save()
        except Exception as e:
            olog(f"[stretch_autotrade] ошибка цикла проверки live-позиций: {_explain_error(e)}")
        time.sleep(60)


def _stretch_diag_track_loop():
    """Раз в STRETCH_DIAG_TRACK_POLL_SEC пересчитывает живую EMA и цену для
    каждой ещё открытой записи, обновляет ближайший подход к EMA
    (closest_stretch_pct, знак может смениться — тогда это уже
    "проскочила через EMA", тоже считаем как touched) и % отката от
    исходного отрыва. Останавливает слежение либо по факту касания, либо
    по таймауту (свой на каждый ТФ, шире для медленных)."""
    time.sleep(90)
    while True:
        try:
            with _stretch_diag_lock:
                open_recs = [r for r in _stretch_diag_records if not r.get("track_done", True)]
            if open_recs:
                now = time.time()

                def _process(rec):
                    try:
                        if rec.get("strategy") == "continuation":
                            # v0.30.83: по прямому запросу — сигналы на
                            # продолжение (LONG) не связаны с EMA вообще,
                            # это простая фикс. цель/стоп от цены входа —
                            # своя, более простая ветка слежения
                            live_price = _gate_get_price(rec["symbol"])
                            if not live_price:
                                if now >= rec.get("track_until", 0):
                                    rec["track_done"] = True
                                    return True
                                return False
                            recent_hl = _stretch_recent_1m_high_low(rec["symbol"])
                            stop, take = rec.get("stop"), rec.get("take")
                            stop_hit = bool(stop and live_price <= stop)
                            if not stop_hit and stop and recent_hl and recent_hl[1] <= stop:
                                stop_hit = True   # v0.30.77-стиль: диапазон за 15 мин, не одна точка
                            take_hit = bool(take and live_price >= take)
                            if not take_hit and take and recent_hl and recent_hl[0] >= take:
                                take_hit = True
                            rec["last_checked_ts"] = int(now)
                            if stop_hit and take_hit:
                                # оба задело в одном окне опроса — порядок
                                # внутри окна неизвестен, берём консервативное
                                # предположение (стоп), не занижаем риск
                                rec["signal_outcome"] = "stop"
                                rec["track_done"] = True
                            elif stop_hit:
                                rec["signal_outcome"] = "stop"
                                rec["track_done"] = True
                            elif take_hit:
                                rec["touched"] = True
                                rec["touched_at"] = int(now)
                                rec["time_to_touch_sec"] = int(now - rec["ts"])
                                rec["signal_outcome"] = "take"
                                rec["track_done"] = True
                            elif now >= rec.get("track_until", 0):
                                rec["signal_outcome"] = "timeout"
                                rec["track_done"] = True
                            return True
                        ctx = _stretch_get_closed_ctx(rec["symbol"], rec["tf"])
                        live_price = _gate_get_price(rec["symbol"])
                        if not ctx or not live_price:
                            if now >= rec.get("track_until", 0):
                                rec["track_done"] = True
                                return True
                            return False
                        ema_closed = ctx["ema_arrays"][rec["period"]][ctx["i"]]
                        ema_now = _ema_live_value(ema_closed, live_price, rec["period"]) if ema_closed else None
                        if not ema_now:
                            return False
                        current_stretch = (live_price - ema_now) / ema_now * 100.0
                        crossed = (current_stretch > 0) != (rec["stretch_pct"] > 0)
                        if abs(current_stretch) < abs(rec["closest_stretch_pct"]):
                            rec["closest_stretch_pct"] = round(current_stretch, 3)
                        recent_hl = _stretch_recent_1m_high_low(rec["symbol"])   # v0.30.77: диапазон за ~15 мин, не одна точка
                        worst_stretch = current_stretch
                        if recent_hl and rec["stretch_pct"] > 0:
                            worst_stretch = max(worst_stretch, (recent_hl[0] - ema_now) / ema_now * 100.0)
                        if not crossed and abs(worst_stretch) > abs(rec.get("max_adverse_stretch_pct", rec["stretch_pct"])):
                            rec["max_adverse_stretch_pct"] = round(worst_stretch, 3)   # v0.30.56: по прямому запросу — сколько ДАЛЬШЕ от EMA ушло, прежде чем откатиться (или так и не откатилось): видим не только шанс на возврат, но и худший случай на пути
                        retrace = 100.0 * (1 - abs(current_stretch) / abs(rec["stretch_pct"])) if rec["stretch_pct"] else 0.0
                        if retrace > rec["max_retrace_pct_of_stretch"]:
                            rec["max_retrace_pct_of_stretch"] = round(retrace, 1)
                        rec["last_checked_ts"] = int(now)
                        # v0.30.67: по прямому запросу — реальный сигнал с
                        # тейком/стопом. СТОП проверяется ПЕРВЫМ (в порядке
                        # реальных событий) — если цена уже ушла дальше
                        # условного стопа, сделка была бы закрыта по стопу,
                        # даже если ПОЗЖЕ цена всё-таки дошла бы до EMA —
                        # в реальной торговле это уже не считалось бы, потому
                        # что позиции бы уже не было. Раньше (v0.30.56) мы
                        # просто фиксировали "рекорд" худшего захода без
                        # выхода из отслеживания — годилось для наблюдения,
                        # но не для честного сигнала с реальным исходом.
                        stop = rec.get("stop")
                        stop_hit = bool(stop and rec["stretch_pct"] > 0 and live_price >= stop)
                        if not stop_hit and stop and rec["stretch_pct"] > 0 and recent_hl:
                            if recent_hl[0] >= stop:
                                stop_hit = True   # v0.30.77: словили спайком по 1m-свечам, который точечный опрос пропустил бы
                        if stop_hit:
                            rec["signal_outcome"] = "stop"
                            rec["touched_at"] = None
                            rec["track_done"] = True
                        elif crossed or abs(current_stretch) <= STRETCH_DIAG_TOUCH_TOL_PCT:
                            rec["touched"] = True
                            rec["touched_at"] = int(now)
                            rec["time_to_touch_sec"] = int(now - rec["ts"])
                            rec["max_retrace_pct_of_stretch"] = 100.0
                            rec["signal_outcome"] = "take"
                            rec["track_done"] = True
                        elif now >= rec.get("track_until", 0):
                            rec["signal_outcome"] = "timeout"
                            rec["track_done"] = True
                        return True
                    except Exception as e:
                        olog(f"[ema_stretch_track] ⚠ пропущена битая запись "
                             f"({rec.get('symbol', '?')}): {_explain_error(e)}")
                        return False

                with ThreadPoolExecutor(max_workers=STRETCH_TRACK_WORKERS) as pool:
                    t0 = time.time()
                    results = list(pool.map(_process, open_recs))
                    olog(f"[ema_stretch_track] проверено {len(open_recs)} открытых записей за {round(time.time()-t0,1)}с")
                if any(results):
                    _stretch_diag_save()
        except Exception as e:
            olog(f"[ema_stretch_track] ошибка цикла: {_explain_error(e)}")
        time.sleep(STRETCH_DIAG_TRACK_POLL_SEC)


STRETCH_REF_STOP_PCT = 0.5   # v0.30.85: реальная находка по прямому вопросу "RR 1 к 2 при 60% это убыток, а для шорта?" — старый стоп 2.0% при обычно МЕНЬШЕМ тейке (уровень EMA, часто 1-2%) означал риск больше награды, ровно убыточную конфигурацию. Симуляция на 2969 решённых сигналах: внутри лучшего сегмента (отрыв 1-2%, n=1458) — стоп 2.0%: винрейт 66%, средний PnL всего +0.19%/сделку; стоп 0.5%: винрейт 55%, средний PnL +0.48%/сделку (почти в 2.5 раза лучше матожидание, хотя винрейт ниже). Ушли ещё уже (0.1-0.3%) давали чуть лучше в бэктесте, но это уже опасно близко к обычному спреду/проскальзыванию на бирже — 0.5% разумный компромисс между эджем и устойчивостью к шуму реального исполнения. Старое значение 2.0 было калибровано под 10х плечо как линейка сравнения (v0.30.60), не под реальную прибыльность — теперь и то, и другое: разумная линейка И прибыльный стоп. НЕ равно ликвидации — та на 10х ближе к ~9-10%


def _stretch_diag_summary(min_events=3):
    """Сводка по каждой связке (ТФ × период), плюс разбивка по величине
    исходного отрыва (STRETCH_DIAG_BUCKETS) — цель ровно та, что просили:
    "какой фрейм и период EMA лучше всего по проценту отката".
    v0.30.60: реальная находка по прямому уточнению про 10х плечо — чистый
    % отката ничего не говорит о том, насколько БОЛЬНО было по пути. У
    4h/EMA20 (лидер по старому критерию) при стопе 2% сносило бы ~24%
    случаев ДО разворота — а у 15m/EMA5 почти никогда. Ранжирование
    теперь по stop_survival_rate (доля случаев, где цена НЕ прошла
    STRETCH_REF_STOP_PCT против входа прежде чем начать откат) — прямая
    оценка риска именно под плечо, а не абстрактный "% отката когда-то"."""
    with _stretch_diag_lock:
        records = list(_stretch_diag_records)
    groups = {}
    for r in records:
        groups.setdefault((r["tf"], r["period"], r.get("strategy", "fade")), []).append(r)

    def _agg(items):
        total = len(items)
        evaluated = [r for r in items if r.get("track_done")]
        touched = sum(1 for r in evaluated if r.get("touched"))
        retrace_vals = [r["max_retrace_pct_of_stretch"] for r in evaluated]
        touch_times = [r["time_to_touch_sec"] for r in evaluated if r.get("touched") and r.get("time_to_touch_sec")]
        # v0.30.56: по прямому запросу — не только "вернулась ли", но и
        # "насколько хуже стало по пути" — excess = на сколько % ДАЛЬШЕ от
        # исходного отрыва ушла цена, прежде чем откатиться (0, если сразу
        # пошла в откат без дальнейшего ухудшения). Считаем по ВСЕМ
        # записям (не только оценённым) — это живой риск-профиль, важен
        # даже для ещё не решённых
        adverse_items = [r for r in items if "max_adverse_stretch_pct" in r]
        excess_vals = [max(0.0, abs(r["max_adverse_stretch_pct"]) - abs(r["stretch_pct"]))
                       for r in adverse_items]
        # v0.30.60: движение ПРОТИВ ВХОДА (не против исходного отрыва) —
        # именно это режет P&L при реальной позиции с фиксированным стопом
        survived = sum(1 for r in adverse_items
                       if abs(r["max_adverse_stretch_pct"] - r["stretch_pct"]) < STRETCH_REF_STOP_PCT)
        # v0.30.67: реальный сигнал с тейком/стопом — исход по факту
        # порядка событий (не задним числом), считается в трек-лупе.
        # win_rate — среди РЕШЁННЫХ по стопу/тейку (таймауты — отдельно,
        # это "ни то ни другое", не считаем как проигрыш явно)
        resolved_sig = [r for r in items if r.get("signal_outcome") in ("take", "stop")]
        wins = sum(1 for r in resolved_sig if r["signal_outcome"] == "take")
        timeouts = sum(1 for r in items if r.get("signal_outcome") == "timeout")
        return {
            "total": total, "evaluated": len(evaluated), "touched": touched,
            "touch_rate": round(touched / len(evaluated) * 100, 1) if evaluated else None,
            "avg_retrace_pct_of_stretch": round(sum(retrace_vals) / len(retrace_vals), 1) if retrace_vals else None,
            "median_retrace_pct_of_stretch": round(statistics.median(retrace_vals), 1) if retrace_vals else None,
            "avg_time_to_touch_min": round(sum(touch_times) / len(touch_times) / 60, 1) if touch_times else None,
            "avg_extra_adverse_pct": round(sum(excess_vals) / len(excess_vals), 2) if excess_vals else None,
            "max_extra_adverse_pct": round(max(excess_vals), 2) if excess_vals else None,
            "stop_survival_n": len(adverse_items),
            "stop_survival_rate": round(survived / len(adverse_items) * 100, 1) if adverse_items else None,
            "signal_resolved_n": len(resolved_sig),
            "signal_win_rate": round(wins / len(resolved_sig) * 100, 1) if resolved_sig else None,
            "signal_timeouts": timeouts,
        }

    result = []
    for (tf, period, strategy), items in groups.items():
        if len(items) < min_events:
            continue
        entry = {"tf": tf, "period": period, "strategy": strategy, "overall": _agg(items)}
        for lo, hi, label in STRETCH_DIAG_BUCKETS:
            subset = [r for r in items if lo <= abs(r["stretch_pct"]) < hi]
            if subset:
                entry[f"stretch_{label}"] = _agg(subset)
        for lo, hi, label in STRETCH_TREND_BUCKETS:
            subset = [r for r in items if r.get("trend_pct") is not None and lo <= abs(r["trend_pct"]) < hi]
            if subset:
                entry[f"trend_{label}"] = _agg(subset)
        for lo, hi, label in STRETCH_VOL_BUCKETS:
            subset = [r for r in items if r.get("trigger_vol_ratio") is not None and lo <= r["trigger_vol_ratio"] < hi]
            if subset:
                entry[f"vol_{label}"] = _agg(subset)
        result.append(entry)
    # v0.30.78: по прямому запросу — приоритет отдан винрейту тейк/стоп
    # (signal_win_rate), а не stop_survival_rate, как было с v0.30.60.
    # stop_survival_rate остался как второй критерий (тай-брейк) — он
    # всё ещё полезен как более широкая, менее шумная выборка риска,
    # раз выше сами же обсуждали разницу между ними. Отсутствие данных
    # (None) по-прежнему считаем худшим (0), не даём связке без
    # решённых сигналов случайно всплыть в топ.
    # v0.30.89: реальная находка по прямому вопросу "топ-1 продолжение,
    # а сигнал шортом" — раньше сортировка была ЕДИНАЯ по всем строкам
    # разом (фейд и continuation вперемешку), из-за чего выглядело так,
    # будто есть только ОДИН общий лидер — хотя на самом деле у каждой
    # стратегии свой топ-1, и алерты идут по нему независимо. Теперь
    # сначала группируем по стратегии (фейд первым, continuation вторым),
    # ВНУТРИ каждой группы сортируем как раньше — топ каждой стратегии
    # сразу виден как первая строка своего блока, не теряется в общем списке
    strategy_order = {"fade": 0, "continuation": 1}
    result.sort(key=lambda e: (
        strategy_order.get(e.get("strategy", "fade"), 2),
        -(e["overall"]["signal_win_rate"] if e["overall"]["signal_win_rate"] is not None else 0),
        -(e["overall"]["stop_survival_rate"] if e["overall"]["stop_survival_rate"] is not None else 0),
        -(e["overall"]["avg_retrace_pct_of_stretch"] or 0),
    ))
    return result

# ─── конец EMA Stretch/Retrace Diagnostics ──────────────────────────────────



def _load_alert_cfg():
    """Подхватывает сохранённые TG/ntfy настройки из файла (приоритет над env)."""
    global TG_TOKEN, TG_CHAT, NTFY_URL, WATCHDOG_ENABLED, WATCHDOG_TIMEOUT_MIN, HC_URL
    global PUMP_DUMP_ALERTS_ENABLED
    try:
        with open(ALERT_CFG_PATH, "r") as f:
            cfg = json.load(f)
        TG_TOKEN = cfg.get("tg_token", TG_TOKEN) or TG_TOKEN
        TG_CHAT  = cfg.get("tg_chat",  TG_CHAT)  or TG_CHAT
        NTFY_URL = cfg.get("ntfy_url", NTFY_URL) or NTFY_URL
        HC_URL   = cfg.get("hc_url",   HC_URL)   or HC_URL
        if "watchdog_enabled" in cfg:
            WATCHDOG_ENABLED = bool(cfg.get("watchdog_enabled"))
        if "watchdog_timeout_min" in cfg:
            try:
                WATCHDOG_TIMEOUT_MIN = max(5, min(1440, int(cfg.get("watchdog_timeout_min"))))
            except (TypeError, ValueError):
                pass
        if "pump_dump_alerts_enabled" in cfg:
            PUMP_DUMP_ALERTS_ENABLED = bool(cfg.get("pump_dump_alerts_enabled"))
    except FileNotFoundError:
        pass
    except Exception as e:
        olog(f"⚠ Не удалось прочитать {ALERT_CFG_PATH}: {_explain_error(e)}")

def _save_alert_cfg():
    try:
        with open(ALERT_CFG_PATH, "w") as f:
            json.dump({"tg_token": TG_TOKEN, "tg_chat": TG_CHAT, "ntfy_url": NTFY_URL,
                       "hc_url": HC_URL,
                       "watchdog_enabled": WATCHDOG_ENABLED,
                       "watchdog_timeout_min": WATCHDOG_TIMEOUT_MIN,
                       "pump_dump_alerts_enabled": PUMP_DUMP_ALERTS_ENABLED}, f)
    except Exception as e:
        olog(f"⚠ Не удалось сохранить {ALERT_CFG_PATH}: {_explain_error(e)}")

def _test_alert():
    """Шлёт тестовое уведомление и честно проверяет, дошло ли оно."""
    if not ((TG_TOKEN and TG_CHAT) or NTFY_URL):
        return False, "Не заданы TG_TOKEN+TG_CHAT или NTFY_URL"
    msg = "✅ Pump Radar: тестовое уведомление. Если ты это видишь — алерты настроены верно."
    ok_any, errs = False, []
    if TG_TOKEN and TG_CHAT:
        try:
            r = requests.post(f"https://api.telegram.org/bot{TG_TOKEN}/sendMessage",
                json={"chat_id":TG_CHAT,"text":msg,"parse_mode":"HTML"}, timeout=8)
            j = r.json()
            if j.get("ok"):
                ok_any = True
            else:
                errs.append("Telegram: " + j.get("description","неизвестная ошибка"))
        except Exception as e:
            errs.append(f"Telegram: {_explain_error(e)}")
    if NTFY_URL:
        try:
            r = requests.post(NTFY_URL, data=msg.encode(), timeout=8)
            if r.status_code < 300:
                ok_any = True
            else:
                errs.append(f"ntfy: HTTP {r.status_code}")
        except Exception as e:
            errs.append(f"ntfy: {_explain_error(e)}")
    return ok_any, ("; ".join(errs) if errs else None)

# ─── Watchdog пропажи интернета ──────────────────────────────────────────────
_watchdog_lock       = threading.Lock()
_watchdog_down_since = None
_watchdog_alerted    = False
_watchdog_last_alert = None
WATCHDOG_CHECK_SEC   = 60

def _check_connectivity():
    try:
        r = requests.get(f"{GATE_API}/futures/usdt/contracts/BTC_USDT", timeout=6)
        return r.ok
    except Exception:
        return False

def _watchdog_loop():
    global _watchdog_down_since, _watchdog_alerted, _watchdog_last_alert
    while True:
        time.sleep(WATCHDOG_CHECK_SEC)
        try:
            if not WATCHDOG_ENABLED:
                continue
            online = _check_connectivity()
            now = time.time()
            with _watchdog_lock:
                if online:
                    if _watchdog_alerted and _watchdog_down_since:
                        down_min = max(1, int((now - _watchdog_down_since) / 60))
                        _send_alert(f"🟢 Связь восстановлена. Не было интернета/Gate.io ~{down_min} мин.")
                        olog(f"🟢 watchdog: связь восстановлена, простой ~{down_min} мин")
                    _watchdog_down_since = None
                    _watchdog_alerted    = False
                    _watchdog_last_alert = None
                else:
                    if _watchdog_down_since is None:
                        _watchdog_down_since = now
                        olog("⚠ watchdog: нет связи с интернетом/Gate.io — таймер запущен")
                        continue
                    elapsed_min = (now - _watchdog_down_since) / 60
                    if elapsed_min < WATCHDOG_TIMEOUT_MIN:
                        continue
                    if (not _watchdog_alerted) or \
                       (now - (_watchdog_last_alert or 0) >= WATCHDOG_TIMEOUT_MIN * 60):
                        _send_alert(f"🔴 Нет связи с интернетом/Gate.io уже {int(elapsed_min)} мин.")
                        _watchdog_alerted    = True
                        _watchdog_last_alert = now
        except Exception as e:
            # v0.29.12: аудит — единственный из всех фоновых циклов был БЕЗ
            # этой защиты. Необработанное исключение здесь тихо убивало бы
            # поток watchdog навсегда, без единой строчки в лог — мониторинг
            # связи с интернетом просто переставал бы работать до
            # следующего перезапуска, и никто бы не узнал почему.
            olog(f"[watchdog] ошибка цикла: {_explain_error(e)}")

# ─── Heartbeat healthchecks.io ────────────────────────────────────────────────
HEARTBEAT_INTERVAL_SEC = 300

def _heartbeat_loop():
    while True:
        time.sleep(HEARTBEAT_INTERVAL_SEC)
        if not HC_URL:
            continue
        try:
            requests.get(HC_URL, timeout=10)
        except Exception:
            pass


def _fmt_px(v):
    if v is None: return "—"
    if v >= 100:  return f"{v:.2f}"
    if v >= 1:    return f"{v:.4f}"
    return f"{v:.6f}".rstrip("0").rstrip(".")

def _rss_mb():
    """Текущий RSS процесса в МБ."""
    try:
        import resource
        return round(resource.getrusage(resource.RUSAGE_SELF).ru_maxrss / 1024, 1)
    except Exception:
        return None

def _shutdown_pool_safely(pool):
    """Явный terminate() всех ещё живых дочерних процессов после shutdown,
    плюс gc.collect() (только для ProcessPool)."""
    try:
        pool.shutdown(wait=True, cancel_futures=True)
    except Exception as e:
        olog(f"⚠ pool.shutdown: {_explain_error(e)}")
    if _POOL_TYPE == "process":
        try:
            for proc in (getattr(pool, "_processes", {}) or {}).values():
                if proc.is_alive():
                    olog(f"⚠ pool respawn: воркер pid={proc.pid} не завершился штатно — terminate()")
                    proc.terminate()
                    proc.join(timeout=3)
        except Exception as e:
            olog(f"⚠ pool cleanup: {_explain_error(e)}")
    import gc
    gc.collect()

# ─── Pump Match: авто-подбор параметров рендера по скрину-эталону ─────────
# Веб-страница /pump_match: загружаешь скрин чужого бота + монету/дату/окно
# времени → тянем РЕАЛЬНЫЕ свечи с Gate.io за это окно → перебираем сетку
# параметров рендера (_render_pump_chart_variant) → каждый вариант сверяем
# со скрином по цветовому профилю (HSV, устойчиво к сжатию JPEG и блёклым
# полупрозрачным заливкам) → лучший вариант СРАЗУ сохраняется в
# PUMP_RENDER_PARAMS_FILE и с этого момента используется в реальных алертах
# (см. _render_pump_chart_png выше — она читает эти же сохранённые параметры).

PUMP_MATCH_GRID_VOLUME_MODES = ["directional", "dual_period", "dual_source"]
# v0.3.0: по запросу — "используй всё что есть, пока не совпадёт". Не
# привязываемся ни к одной идее (ни к EMA, ни к объёму, ни к чему-то
# конкретному вроде RSI) — перебираем несколько независимых сырых рядов
# (PUMP_MATCH_GRID_SOURCES/_pm_source_series) и несколько способов их
# скомбинировать в красную/зелёную волну (PUMP_MATCH_GRID_VOLUME_MODES).
PUMP_MATCH_GRID_SMOOTHING_METHODS = ["none", "sma", "ema", "wma", "rma"]
PUMP_MATCH_GRID_EMA_PERIODS  = [2, 3, 4, 5, 6, 8, 10, 13]
PUMP_MATCH_GRID_DUAL_PAIRS   = [(2, 6), (2, 8), (2, 10), (3, 9), (3, 12),
                                 (4, 12), (4, 16), (5, 15)]  # (быстрая, медленная) для dual_period
# dual_source: зелёная и красная волна из ДВУХ РАЗНЫХ рядов — самая
# "агностичная" гипотеза на случай, если это вообще не один и тот же
# показатель, разложенный двумя способами, а два независимых.
PUMP_MATCH_GRID_SOURCE_PAIRS = [
    ("volume", "price_change"), ("volume", "range"), ("volume", "body"),
    ("upper_wick", "lower_wick"), ("range", "body"), ("price_change", "body"),
]
PUMP_MATCH_GRID_FLOOR_FRACS  = [0.0, 0.05, 0.10, 0.15, 0.20, 0.25, 0.35, 0.50]
# автопоиск временного окна по форме ценовой линии (см. _pm_find_best_window)
PUMP_MATCH_WINDOW_COARSE_STEP_MIN = 5
PUMP_MATCH_WINDOW_REFINE_RADIUS_MIN = 10
PUMP_MATCH_WINDOW_TOP_CANDIDATES = 3
# указанная пользователем длительность — ориентир "на глаз", а не точная
# цифра (жалоба с реального теста: конец графика обрезан короче референса)
# — пробуем и её, и несколько соседних вариантов длины окна.
PUMP_MATCH_DURATION_MULTIPLIERS = [0.7, 1.0, 1.3, 1.6, 2.0]
# сетка стиля отрисовки (теперь ОЧЕНЬ большая, см. выше) перебирается
# полностью только для топ-N окон по времени — позиция во времени и так
# надёжно находится сама (0.98-0.99 на тестах), весь бюджет времени
# выполнения переброшен в глубину перебора стиля, а не в ширину по времени.
PUMP_MATCH_STYLE_SEARCH_WINDOWS = 1


def _pm_parse_multipart(body_bytes, content_type_header):
    """Разбирает multipart/form-data через email-парсер стандартной
    библиотеки (оборачиваем тело в псевдо-письмо с тем же Content-Type) —
    надёжнее, чем ручной разбор boundary руками, и не тянет отдельных
    зависимостей. Возвращает {field_name: (raw_bytes, filename_or_None)}."""
    import email
    from email import policy as _email_policy
    from email.parser import BytesParser
    header = f"Content-Type: {content_type_header}\r\nMIME-Version: 1.0\r\n\r\n".encode()
    msg = BytesParser(policy=_email_policy.default).parsebytes(header + body_bytes)
    fields = {}
    if not msg.is_multipart():
        return fields
    for part in msg.iter_parts():
        cd = part.get("Content-Disposition", "") or ""
        name = None
        filename = None
        for piece in cd.split(";"):
            piece = piece.strip()
            if piece.startswith("name="):
                name = piece.split("=", 1)[1].strip('"')
            elif piece.startswith("filename="):
                filename = piece.split("=", 1)[1].strip('"')
        if name is None:
            continue
        payload = part.get_payload(decode=True)
        fields[name] = (payload, filename)
    return fields


def _pm_autocrop_chart(img, bright_thresh=200, row_frac_thresh=0.5, col_frac_thresh=0.5,
                        inset_frac=0.015):
    """Находит ограничивающий прямоугольник самой большой яркой (белой)
    прямоугольной области на скрине — это и есть карточка с графиком на
    тёмном фоне Telegram. Возвращает None, если ничего похожего не нашлось
    (тогда вызывающий код сравнивает по всему присланному изображению).
    inset_frac — небольшой отступ внутрь после обрезки: скруглённые углы
    карточки и их тень иначе иногда попадают в кадр и портят извлечение
    ценовой линии (тёмный пиксель угла путает с чёрной линией цены)."""
    img = img.convert("RGB")
    W, H = img.size
    px = img.load()
    step = max(1, min(W, H) // 300)

    bright_rows = []
    for y in range(0, H, step):
        cnt = 0
        tot = 0
        for x in range(0, W, step):
            r, g, b = px[x, y]
            if (r + g + b) / 3 >= bright_thresh:
                cnt += 1
            tot += 1
        if tot and cnt / tot >= row_frac_thresh:
            bright_rows.append(y)
    if not bright_rows:
        return None
    top, bottom = min(bright_rows), max(bright_rows)

    bright_cols = []
    for x in range(0, W, step):
        cnt = 0
        tot = 0
        for y in range(top, bottom + 1, step):
            r, g, b = px[x, y]
            if (r + g + b) / 3 >= bright_thresh:
                cnt += 1
            tot += 1
        if tot and cnt / tot >= col_frac_thresh:
            bright_cols.append(x)
    if not bright_cols:
        return None
    left, right = min(bright_cols), max(bright_cols)

    if (right - left) * (bottom - top) / (W * H) < 0.05:
        return None
    iw = int((right - left) * inset_frac)
    ih = int((bottom - top) * inset_frac)
    return img.crop((left + iw, top + ih, right - iw + 1, bottom - ih + 1))


def _pm_price_line_profile(img, w=200, h=100):
    """Извлекает нормализованную (0..1, где 1 = самая высокая цена в
    кадре) позицию чёрной линии цены по каждому X-столбцу — используется
    ДЛЯ ПОИСКА подходящего временного окна (сравниваем форму графика цены,
    а не абсолютные значения — они у эталона и у наших данных в разных
    единицах). Пропуски (столбцы без явного тёмного пикселя, между точками
    графика) интерполируются линейно. Возвращает None, если чёрной линии
    почти не нашлось (совсем не похоже на такой график)."""
    img = img.convert("RGB").resize((w, h))
    px = img.load()
    profile = [None] * w
    for x in range(w):
        best_y = None
        best_dark = 999
        for y in range(h):
            r, g, b = px[x, y]
            dark = r + g + b
            # чёрная линия — все каналы низкие и близкие друг к другу
            # (не цветной пиксель гридлайна/заливки)
            if dark < 220 and abs(r - g) < 30 and abs(g - b) < 30 and abs(r - b) < 30:
                if dark < best_dark:
                    best_dark = dark
                    best_y = y
        profile[x] = best_y
    known = [(x, y) for x, y in enumerate(profile) if y is not None]
    if len(known) < w * 0.3:
        return None
    out = []
    for x in range(w):
        if profile[x] is not None:
            out.append(profile[x])
            continue
        left = [k for k in known if k[0] <= x]
        right = [k for k in known if k[0] >= x]
        if left and right:
            x0, y0 = left[-1]
            x1, y1 = right[0]
            out.append(y0 if x1 == x0 else y0 + (y1 - y0) * (x - x0) / (x1 - x0))
        elif left:
            out.append(left[-1][1])
        else:
            out.append(right[0][1])
    ymin, ymax = min(out), max(out)
    if ymax <= ymin:
        return [0.5] * w
    return [1 - (y - ymin) / (ymax - ymin) for y in out]


def _pm_candle_price_profile(closes, w=200):
    """То же самое (нормализованная форма 0..1), но из массива реальных
    close-цен свечей — ресемплится линейной интерполяцией до w точек,
    чтобы длина совпадала с профилем из картинки для корреляции."""
    n = len(closes)
    if n < 2:
        return None
    out = []
    for i in range(w):
        idx = i / (w - 1) * (n - 1) if w > 1 else 0
        lo = int(idx)
        hi = min(lo + 1, n - 1)
        frac = idx - lo
        out.append(closes[lo] * (1 - frac) + closes[hi] * frac)
    cmin, cmax = min(out), max(out)
    if cmax <= cmin:
        return [0.5] * w
    return [(v - cmin) / (cmax - cmin) for v in out]


def _pm_find_best_window(day_candles, ref_price_profile, duration_bars,
                          coarse_step=PUMP_MATCH_WINDOW_COARSE_STEP_MIN,
                          refine_radius=PUMP_MATCH_WINDOW_REFINE_RADIUS_MIN,
                          top_k=PUMP_MATCH_WINDOW_TOP_CANDIDATES):
    """Скользит окном длиной duration_bars (в 1m-барах) по всему
    day_candles, сравнивая ФОРМУ ценовой линии (не абсолютные цены) с
    ref_price_profile через корреляцию Пирсона. Сначала грубый проход
    (шаг coarse_step минут) по всему дню — дёшево, затем точное уточнение
    (шаг 1 минута) в окрестности refine_radius вокруг top_k лучших грубых
    позиций. Возвращает список [(score, start_index), ...] по убыванию,
    без дублей слишком близких друг к другу позиций."""
    n = len(day_candles)
    if n < duration_bars + 1:
        return []
    closes_all = [c["close"] for c in day_candles]

    coarse = []
    i = 0
    while i + duration_bars <= n:
        prof = _pm_candle_price_profile(closes_all[i:i + duration_bars])
        score = _pm_pearson(ref_price_profile, prof)
        coarse.append((score, i))
        i += max(1, coarse_step)
    coarse.sort(key=lambda t: t[0], reverse=True)

    refined = {}
    for _, start in coarse[:top_k]:
        lo = max(0, start - refine_radius)
        hi = min(n - duration_bars, start + refine_radius)
        for j in range(lo, hi + 1):
            if j in refined:
                continue
            prof = _pm_candle_price_profile(closes_all[j:j + duration_bars])
            refined[j] = _pm_pearson(ref_price_profile, prof)

    results = sorted(refined.items(), key=lambda kv: kv[1], reverse=True)
    # дедуп близких позиций (в пределах половины радиуса уточнения)
    out = []
    for j, score in results:
        if any(abs(j - oj) < refine_radius for oj, _ in out):
            continue
        out.append((j, score))
        if len(out) >= top_k:
            break
    return [(score, j) for j, score in out]


def _pm_color_profile(img, w=200, h=100, sat_thresh=0.05):
    """Для каждого столбца изображения (после ресайза до w×h) считает долю
    зеленоватых и красноватых пикселей по HSV (оттенок+насыщенность, а не
    сырые RGB-пороги) — устойчиво к бледным полупрозрачным заливкам и
    JPEG-артефактам реальных скринов (проверено на живом скрине: чистые
    RGB-пороги давали почти нулевой сигнал на бледной зелёной заливке,
    HSV с низким порогом насыщенности — нет)."""
    import colorsys
    img = img.convert("RGB").resize((w, h))
    px = img.load()
    green = [0.0] * w
    red = [0.0] * w
    for x in range(w):
        gc = 0
        rc = 0
        for y in range(h):
            r, g, b = px[x, y]
            hh, ss, vv = colorsys.rgb_to_hsv(r / 255, g / 255, b / 255)
            if ss < sat_thresh or vv < 0.20:
                continue
            deg = hh * 360
            if 60 <= deg <= 170:
                gc += 1
            elif deg <= 30 or deg >= 335:
                rc += 1
        green[x] = gc / h
        red[x] = rc / h
    return green, red


def _pm_pearson(a, b):
    n = len(a)
    if n == 0 or n != len(b):
        return 0.0
    ma = sum(a) / n
    mb = sum(b) / n
    cov = sum((a[i] - ma) * (b[i] - mb) for i in range(n))
    va = sum((x - ma) ** 2 for x in a)
    vb = sum((x - mb) ** 2 for x in b)
    if va <= 0 or vb <= 0:
        return 0.0
    return cov / (va * vb) ** 0.5


def _pm_fetch_candles_window(symbol, start_ts, end_ts, interval="1m"):
    """Тянет свечи Gate.io Futures в явном окне [start_ts, end_ts] (unix-
    секунды) — отдельная от боевой _fetch_candles функция (та заточена под
    'от now назад на days дней', тут нужно произвольное окно в прошлом),
    чтобы не рисковать стабильностью живого сканера/автотрейда."""
    interval_sec = TF_SECONDS.get(interval, 60)
    all_candles = []
    current_from = int(start_ts)
    end_ts = int(end_ts)
    fail_count = 0
    while current_from < end_ts:
        try:
            r = requests.get(
                f"{GATE_API}/futures/usdt/candlesticks",
                params={"contract": symbol, "interval": interval,
                        "from": current_from, "limit": 999},
                timeout=20,
            )
            if r.status_code != 200:
                fail_count += 1
                if fail_count >= 5:
                    break
                time.sleep(1)
                continue
            fail_count = 0
            raw = r.json()
            if not raw:
                break
            batch = []
            for c in raw:
                t = int(c.get("t", 0))
                if t > end_ts:
                    continue
                batch.append({
                    "t": t, "open": float(c.get("o", 0)), "high": float(c.get("h", 0)),
                    "low": float(c.get("l", 0)), "close": float(c.get("c", 0)),
                    "vol": float(c.get("v", 0)),
                })
            if not batch:
                break
            all_candles.extend(batch)
            last_t = batch[-1]["t"]
            if last_t >= end_ts - interval_sec:
                break
            current_from = last_t + interval_sec
            time.sleep(0.1)
        except Exception as e:
            fail_count += 1
            olog(f"[pump_match] fetch ошибка: {_explain_error(e)}")
            if fail_count >= 5:
                break
            time.sleep(1)
    seen = set()
    out = []
    for c in sorted(all_candles, key=lambda x: x["t"]):
        if c["t"] not in seen:
            seen.add(c["t"])
            out.append(c)
    return out


def _pm_png_to_b64(png_bytes):
    return "data:image/png;base64," + base64.b64encode(png_bytes).decode("ascii")


def _pil_to_png_bytes(img):
    buf = io.BytesIO()
    img.convert("RGB").save(buf, format="PNG")
    return buf.getvalue()


def _pm_color_balance_ratio(g_profile, r_profile):
    """Доля красного в общей 'массе' цвета (сумма по всем столбцам) —
    0 = совсем нет красного, 1 = совсем нет зелёного, 0.5 = поровну.
    Используется отдельно от позиционной корреляции: Пирсон по столбцам
    может дать приличный score, даже если один цвет в кандидате на самом
    деле в разы тоньше/слабее эталона — он ловит СОВПАДЕНИЕ ПОЗИЦИЙ, а не
    относительную СИЛУ цвета. Проверено на живом случае: dual_period давал
    красный как тонкую подложку под зелёным (мат. так и должно быть — обе
    линии из одного объёма, медленная EMA/RMA физически не может быть
    выше быстрой на самом пике) и получал неоправданно высокий score."""
    tg = sum(g_profile)
    tr = sum(r_profile)
    if tg + tr <= 0:
        return 0.5
    return tr / (tg + tr)


def _pm_grid_total_combos():
    """Сколько вариантов реально перебирается — для прогресс-бара и оценки
    времени. Держать формулу синхронной с циклом в _pm_style_grid_search.
    v0.22.0: режим/источник подтверждены напрямую (directional/volume) —
    перебираются только способ и период сглаживания, не десятки гипотез
    про источник данных, как раньше."""
    per_ff = 1  # directional "none"
    per_ff += (len(PUMP_MATCH_GRID_SMOOTHING_METHODS) - 1) * len(PUMP_MATCH_GRID_EMA_PERIODS)
    return per_ff * len(PUMP_MATCH_GRID_FLOOR_FRACS)


def _pm_style_grid_search(candles, base_price, ref_g, ref_r, progress_cb=None):
    """Перебирает сетку параметров ОТРИСОВКИ (не времени) на уже
    зафиксированном наборе свечей и оценивает каждый вариант по цветовому
    профилю. Возвращает список результатов, отсортированный по score.
    progress_cb(done, total), если передан, вызывается периодически.
    v0.22.0: автор стороннего бота подтвердил напрямую — красное это объём
    на продажу (red-свечи), зелёное по симметрии объём на покупку
    (green-свечи). Это ровно volume_mode="directional" + source="volume" —
    та гипотеза, что мы и так уже держали дефолтом. Раньше здесь вслепую
    перебирались ещё dual_period/dual_source и 6 разных source (тысячи
    комбинаций) — больше нет смысла тратить на них время, раз известно,
    что это неверные гипотезы. Перебираем только то, что ДЕЙСТВИТЕЛЬНО
    неизвестно — способ и период сглаживания поверх подтверждённых
    directional/volume."""
    ref_balance = _pm_color_balance_ratio(ref_g, ref_r)
    total = _pm_grid_total_combos()
    done = 0
    results = []
    src = "volume"
    for ff in PUMP_MATCH_GRID_FLOOR_FRACS:
        # "none" один раз (период не важен), остальные методы — по сетке периодов
        results.extend(_pm_try_variant(candles, base_price, ref_g, ref_r, ref_balance,
                                        "directional", src, None, "none", 1, None, ff))
        done += 1
        for method in PUMP_MATCH_GRID_SMOOTHING_METHODS:
            if method == "none":
                continue
            for ep in PUMP_MATCH_GRID_EMA_PERIODS:
                results.extend(_pm_try_variant(candles, base_price, ref_g, ref_r, ref_balance,
                                                "directional", src, None, method, ep, None, ff))
                done += 1
                if progress_cb and done % 20 == 0:
                    progress_cb(done, total)
    if progress_cb:
        progress_cb(total, total)
    results.sort(key=lambda r: r["score"], reverse=True)
    return results


def _pm_try_variant(candles, base_price, ref_g, ref_r, ref_balance, vm, src, src_slow,
                     method, ep, ep_slow, ff):
    from PIL import Image
    try:
        png = _render_pump_chart_variant(candles, base_price, volume_mode=vm,
                                          source=src, source_slow=src_slow,
                                          smoothing_method=method, ema_period=ep,
                                          ema_period_slow=ep_slow, floor_frac=ff)
        if not png:
            return []
        cand_img = Image.open(io.BytesIO(png))
        cg, cr = _pm_color_profile(cand_img)
        sg = _pm_pearson(ref_g, cg)
        sr = _pm_pearson(ref_r, cr)
        cand_balance = _pm_color_balance_ratio(cg, cr)
        # штраф за дисбаланс: 1.0 если доля красного у кандидата совпадает
        # с эталоном, тем ниже, чем сильнее расходится (мат. разница до 1.0)
        balance_score = 1.0 - abs(ref_balance - cand_balance)
    except Exception as e:
        olog(f"[pump_match] вариант vm={vm} src={src}/{src_slow} method={method} "
             f"ep={ep}/{ep_slow} ff={ff} упал: {_explain_error(e)}")
        return []
    position_score = (sg + sr) / 2
    # v0.3.0: приоритет явно сдвинут на форму волны (по прямому запросу) —
    # 75% позиционное совпадение красной/зелёной волны, 25% баланс (баланс
    # остаётся только грубым фильтром структурно неверных гипотез, не
    # решающим фактором).
    final_score = position_score * 0.75 + balance_score * 0.25
    return [{"score": final_score, "score_g": sg, "score_r": sr,
              "balance_score": balance_score, "ref_balance": round(ref_balance, 3),
              "cand_balance": round(cand_balance, 3),
              "volume_mode": vm, "source": src, "source_slow": src_slow,
              "smoothing_method": method, "ema_period": ep,
              "ema_period_slow": ep_slow, "floor_frac": ff, "png": png}]


def _pump_match_run(image_bytes, symbol, search_start_ts, search_end_ts,
                     duration_min=120, base_price_override=None, progress_cb=None):
    """Основной пайплайн подбора:
      1) обрезать эталон, извлечь и цветовой профиль (для стиля отрисовки),
         и профиль формы ценовой линии (для поиска времени);
      2) стянуть РЕАЛЬНЫЕ свечи одним широким запросом на весь диапазон
         поиска (а не заранее известное окно — пользователь его не знает);
      3) найти несколько кандидатов на позицию И длину окна, сравнивая
         форму цены (см. _pm_find_best_window) — сама duration_min от
         пользователя лишь ОРИЕНТИР (на глаз по подписям оси), поэтому
         пробуем и её саму, и несколько соседних вариантов длины;
      4) для лучших позиций окна перебрать сетку параметров ОТРИСОВКИ и
         оценить по цветовому профилю (включая штраф за дисбаланс
         красный/зелёный — см. _pm_try_variant);
      5) сохранить и вернуть общий лучший результат (время + длина + стиль).
    progress_cb(done, total), если передан, транслируется в перебор стиля —
    сетка сейчас большая (тысячи комбинаций), без прогресса запрос выглядел
    бы зависшим."""
    from PIL import Image
    try:
        ref_img = Image.open(io.BytesIO(image_bytes))
    except Exception as e:
        return {"ok": False, "msg": f"Не смог открыть картинку: {_explain_error(e)}"}

    cropped = _pm_autocrop_chart(ref_img) or ref_img.convert("RGB")
    ref_g, ref_r = _pm_color_profile(cropped)
    ref_price_profile = _pm_price_line_profile(cropped)

    duration_base = max(3, int(duration_min))
    day_candles = _pm_fetch_candles_window(symbol, search_start_ts, search_end_ts)
    if not day_candles or len(day_candles) < duration_base + 1:
        return {"ok": False,
                "msg": f"Не удалось получить достаточно свечей для '{symbol}' в диапазоне "
                       f"поиска (получено {len(day_candles) if day_candles else 0}, нужно "
                       f"минимум {duration_base+1}). Контракт может называться иначе на "
                       f"Gate.io (для монет с крошечной ценой часто есть множитель в имени, "
                       f"напр. 1000{symbol}), либо диапазон поиска слишком узкий."}

    window_candidates = []  # список (score, start_idx, duration_bars)
    if ref_price_profile is not None:
        for mult in PUMP_MATCH_DURATION_MULTIPLIERS:
            dur = max(3, int(duration_base * mult))
            if dur + 1 > len(day_candles):
                continue
            for score, start_idx in _pm_find_best_window(day_candles, ref_price_profile, dur):
                window_candidates.append((score, start_idx, dur))
        window_candidates.sort(key=lambda t: t[0], reverse=True)
        # дедуп: одинаковая позиция+длина (в пределах половины шага уточнения) не повторяем
        deduped = []
        for score, start_idx, dur in window_candidates:
            if any(abs(start_idx - s) < PUMP_MATCH_WINDOW_REFINE_RADIUS_MIN and dur == d
                   for _, s, d in deduped):
                continue
            deduped.append((score, start_idx, dur))
            if len(deduped) >= PUMP_MATCH_WINDOW_TOP_CANDIDATES:
                break
        window_candidates = deduped
    if not window_candidates:
        # не смогли распознать ценовую линию на скрине (или совпадений нет) —
        # fail-open: просто берём последнее окно базовой длины в диапазоне
        olog(f"[pump_match] {symbol}: не смог распознать/сопоставить ценовую линию — "
             f"беру последнее окно диапазона поиска как есть")
        window_candidates = [(0.0, len(day_candles) - duration_base, duration_base)]

    all_style_results = []
    for win_score, start_idx, dur in window_candidates[:PUMP_MATCH_STYLE_SEARCH_WINDOWS]:
        window = day_candles[start_idx:start_idx + dur]
        base_price = base_price_override if base_price_override else min(c["close"] for c in window)
        style_results = _pm_style_grid_search(window, base_price, ref_g, ref_r, progress_cb=progress_cb)
        for r in style_results:
            r["window_start_idx"] = start_idx
            r["window_score"] = win_score
            r["combined_score"] = 0.2 * win_score + 0.8 * r["score"] if ref_price_profile is not None else r["score"]
            r["base_price"] = base_price
            r["window"] = window
        all_style_results.extend(style_results)

    if not all_style_results:
        return {"ok": False, "msg": "Ни один вариант рендера не удалось построить (нет Pillow?)"}

    all_style_results.sort(key=lambda r: r["combined_score"], reverse=True)
    top = all_style_results[:6]
    best = top[0]
    _save_pump_render_params({"volume_mode": best["volume_mode"],
                               "source": best["source"], "source_slow": best["source_slow"],
                               "smoothing_method": best["smoothing_method"],
                               "ema_period": best["ema_period"],
                               "ema_period_slow": best["ema_period_slow"],
                               "floor_frac": best["floor_frac"]})

    best_window = best["window"]
    best_start_t = best_window[0]["t"]
    best_end_t = best_window[-1]["t"]
    last_price = best_window[-1]["close"]
    pct = ((last_price - best["base_price"]) / best["base_price"] * 100.0
           if best["base_price"] else 0.0)

    olog(f"[pump_match] {symbol}: лучший вариант combined={best['combined_score']:.3f} "
         f"(время={best['window_score']:.3f} стиль={best['score']:.3f}) "
         f"окно={time.strftime('%H:%M', time.localtime(best_start_t))}-"
         f"{time.strftime('%H:%M', time.localtime(best_end_t))} "
         f"vm={best['volume_mode']} src={best['source']}/{best['source_slow']} "
         f"method={best['smoothing_method']} "
         f"ema={best['ema_period']}/{best['ema_period_slow']} "
         f"floor={best['floor_frac']} — сохранён как рабочий")

    return {
        "ok": True,
        "candles_scanned": len(day_candles),
        "window_start": time.strftime("%Y-%m-%d %H:%M", time.localtime(best_start_t)),
        "window_end": time.strftime("%H:%M", time.localtime(best_end_t)),
        "time_match_score": round(best["window_score"], 3),
        "base_price": best["base_price"], "last_price": last_price, "pct": round(pct, 2),
        "cropped_preview_b64": _pm_png_to_b64(_pil_to_png_bytes(cropped)),
        "top": [{
            "score": round(r["score"], 3), "score_g": round(r["score_g"], 3),
            "score_r": round(r["score_r"], 3), "combined_score": round(r["combined_score"], 3),
            "volume_mode": r["volume_mode"], "source": r["source"], "source_slow": r["source_slow"],
            "smoothing_method": r["smoothing_method"],
            "ema_period": r["ema_period"],
            "ema_period_slow": r["ema_period_slow"], "floor_frac": r["floor_frac"],
            "balance_score": round(r["balance_score"], 3), "ref_balance": r["ref_balance"],
            "cand_balance": r["cand_balance"],
            "window_start": time.strftime("%H:%M", time.localtime(r["window"][0]["t"])),
            "window_end": time.strftime("%H:%M", time.localtime(r["window"][-1]["t"])),
            "png_b64": _pm_png_to_b64(r["png"]),
        } for r in top],
    }


_pump_match_jobs_lock = threading.Lock()
_pump_match_jobs = {}  # job_id -> {"status","done","total","result","created_at"}
_PUMP_MATCH_JOB_TTL_SEC = 3600  # старые завершённые задачи не копим бесконечно

def _pump_match_new_job():
    """Сетка перебора теперь тысячи комбинаций — один блокирующий HTTP-запрос
    на несколько минут выглядел бы как зависание. Вместо этого: POST сразу
    возвращает job_id, сама работа идёт в фоновом потоке, прогресс — через
    отдельный GET /pump_match_status?job_id=..."""
    job_id = uuid.uuid4().hex[:12]
    with _pump_match_jobs_lock:
        _pump_match_jobs[job_id] = {"status": "running", "done": 0, "total": 0,
                                     "result": None, "created_at": time.time()}
        stale = [k for k, v in _pump_match_jobs.items()
                 if time.time() - v.get("created_at", 0) > _PUMP_MATCH_JOB_TTL_SEC]
        for k in stale:
            del _pump_match_jobs[k]
    return job_id

def _pump_match_run_background(job_id, image_bytes, symbol, search_start_ts, search_end_ts,
                                duration_min, base_price_override):
    def progress_cb(done, total):
        with _pump_match_jobs_lock:
            if job_id in _pump_match_jobs:
                _pump_match_jobs[job_id]["done"] = done
                _pump_match_jobs[job_id]["total"] = total
    try:
        result = _pump_match_run(image_bytes, symbol, search_start_ts, search_end_ts,
                                  duration_min=duration_min, base_price_override=base_price_override,
                                  progress_cb=progress_cb)
        with _pump_match_jobs_lock:
            if job_id in _pump_match_jobs:
                _pump_match_jobs[job_id]["status"] = "done"
                _pump_match_jobs[job_id]["result"] = result
    except Exception as e:
        olog(f"[pump_match] задача {job_id} упала: {_explain_error(e)}")
        with _pump_match_jobs_lock:
            if job_id in _pump_match_jobs:
                _pump_match_jobs[job_id]["status"] = "error"
                _pump_match_jobs[job_id]["result"] = {"ok": False, "msg": f"Внутренняя ошибка: {_explain_error(e)}"}


MANUAL_SCAN_HTML_PAGE = """<!DOCTYPE html>
<html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Ручной скан монеты</title>
<style>
body{background:#0d1117;color:#c9d1d9;font-family:-apple-system,sans-serif;margin:0;padding:16px}
.card{background:#161b22;border:1px solid #30363d;border-radius:10px;padding:16px;margin-bottom:16px;max-width:640px}
input{background:#0d1117;color:#c9d1d9;border:1px solid #30363d;border-radius:6px;padding:10px;font-size:16px;width:200px}
button{background:#238636;color:#fff;border:none;border-radius:6px;padding:10px 16px;font-size:14px;margin-left:8px;cursor:pointer}
button.secondary{background:#21262d;border:1px solid #30363d}
pre{background:#0d1117;border:1px solid #30363d;border-radius:6px;padding:10px;overflow-x:auto;font-size:12px;white-space:pre-wrap;word-break:break-all}
table{width:100%;border-collapse:collapse;font-size:13px}
th,td{padding:6px 8px;text-align:left;border-bottom:1px solid #21262d}
</style></head><body>
<div style="background:#161b22;border-bottom:1px solid #30363d;padding:8px 12px;display:flex;gap:6px;flex-wrap:wrap;font-size:12px">
  <a href="/ema" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#127968; Главная</a>
  <a href="/manual_scan" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128269; Ручной скан</a>
  <a href="/ema_diag" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128202; EMA Диагностика</a>
  <a href="/ema_stretch" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128201; Отрыв от EMA</a>
  <a href="/pump_match" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#127919; Pump Match</a>
  <a href="/weekly_ema_backtest" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128200; Бэктест EMA</a>
</div>

<div class="card">
  <h2 style="margin-top:0">Ручной скан монеты (для сравнения с внешним референс-ботом)</h2>
  <p style="font-size:13px;color:#8b949e">Вводишь тикер — качаются свечи по нескольким ТФ, ищутся всплески объёма + смотрится статус недельных EMA7/14/28. Не влияет на живые алерты, только копит данные.</p>
  <div style="margin-bottom:8px"><input id="symbolInput" placeholder="SYMBOL (например FF)"></div>
  <div style="margin-bottom:8px;display:flex;gap:8px;align-items:center;flex-wrap:wrap">
    <label style="font-size:12px;color:#8b949e">Дата/время сигнала (необязательно — если пусто, берётся "сейчас"):</label>
    <input type="date" id="scanDateInput" style="width:150px">
    <input type="time" id="scanTimeInput" style="width:110px">
    <button class="secondary" onclick="clearDateTime()" style="font-size:12px;padding:6px 10px">Очистить (сейчас)</button>
  </div>
  <div style="margin-bottom:8px;display:flex;gap:8px;align-items:center;flex-wrap:wrap">
    <label style="font-size:12px;color:#8b949e">Со скрина их бота (всё необязательно):</label>
    <select id="scanSideInput" style="background:#0d1117;color:#c9d1d9;border:1px solid #30363d;border-radius:6px;padding:10px">
      <option value="">Сторона: -</option>
      <option value="LONG">LONG</option>
      <option value="SHORT">SHORT</option>
    </select>
    <input id="scanEntryInput" placeholder="Вход" style="width:100px">
    <input id="scanStopInput" placeholder="Стоп" style="width:100px">
    <input id="scanTakeInput" placeholder="Тейк" style="width:100px">
  </div>
  <button onclick="doScan()">Сканировать</button>
  <span id="scanMsg" style="font-size:12px;color:#8b949e;margin-left:8px"></span>
  <div id="resultBox" style="margin-top:12px"></div>
</div>
<div class="card">
  <h3 style="margin-top:0">Накопленные сканы <a href="/manual_scan_export" style="float:right;font-size:13px">&#128190; Скачать JSON</a></h3>
  <p style="font-size:12px;color:#8b949e;margin-top:-6px">Отдельный простой лог "тикер + время сигнала" (независимо от расчётов, на случай если логику пересчёта потом улучшим): <a href="/manual_scan_chronology_export" style="font-size:12px">&#128190; Скачать хронологию</a></p>
  <div id="historyBox"><table><thead><tr><th>Монета</th><th>Сторона</th><th>Когда</th><th>EMA7(W)</th><th>EMA14(W)</th><th>EMA28(W)</th><th>Лесенка(W)</th><th>Vol24ч,$</th><th>Funding</th><th>Всплесков 1h/4h</th></tr></thead><tbody id="historyBody"></tbody></table></div>
</div>
<script>
async function doScan(){
  const symbol = document.getElementById('symbolInput').value.trim();
  const msg = document.getElementById('scanMsg');
  if(!symbol){ msg.style.color='#f85149'; msg.innerText='Введи тикер'; return; }
  const dateVal = document.getElementById('scanDateInput').value;
  const timeVal = document.getElementById('scanTimeInput').value;
  let at_ts = null;
  if(dateVal){
    const dt = new Date(dateVal + 'T' + (timeVal || '00:00') + ':00');
    if(isNaN(dt.getTime())){ msg.style.color='#f85149'; msg.innerText='Некорректная дата/время'; return; }
    at_ts = Math.floor(dt.getTime()/1000);
  }
  const side = document.getElementById('scanSideInput').value;
  const refEntry = document.getElementById('scanEntryInput').value.trim();
  const refStop = document.getElementById('scanStopInput').value.trim();
  const refTake = document.getElementById('scanTakeInput').value.trim();
  msg.style.color='#8b949e'; msg.innerText='Сканирую...';
  try{
    const payload = {symbol};
    if(at_ts) payload.at_ts = at_ts;
    if(side) payload.side = side;
    if(refEntry) payload.ref_entry = refEntry;
    if(refStop) payload.ref_stop = refStop;
    if(refTake) payload.ref_take = refTake;
    const r = await fetch('/manual_scan', {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify(payload)});
    const d = await r.json();
    if(!d.ok){ msg.style.color='#f85149'; msg.innerText = d.msg || 'Ошибка'; return; }
    msg.style.color='#3fb950'; msg.innerText = at_ts ? 'Готово (на указанный момент)' : 'Готово (сейчас)';
    document.getElementById('resultBox').innerHTML = '<pre>'+JSON.stringify(d.result, null, 2)+'</pre>';
    document.getElementById('symbolInput').value = '';
    document.getElementById('scanSideInput').value = '';
    document.getElementById('scanEntryInput').value = '';
    document.getElementById('scanStopInput').value = '';
    document.getElementById('scanTakeInput').value = '';
    loadHistory();
  }catch(e){ msg.style.color='#f85149'; msg.innerText='Ошибка запроса'; }
}
function clearDateTime(){
  document.getElementById('scanDateInput').value = '';
  document.getElementById('scanTimeInput').value = '';
}
async function loadHistory(){
  try{
    const r = await fetch('/manual_scan_status'); const d = await r.json();
    const tbody = document.getElementById('historyBody');
    tbody.innerHTML = (d.records||[]).slice().reverse().map(rec=>{
      const we = rec.weekly || {};
      const ct = rec.contract || {};
      const a1 = (rec.anomalies && rec.anomalies['1h'] && rec.anomalies['1h'].length) || 0;
      const a4 = (rec.anomalies && rec.anomalies['4h'] && rec.anomalies['4h'].length) || 0;
      const when = rec.at_ts
        ? new Date(rec.at_ts*1000).toLocaleString() + ' <span style="color:#8b949e">(задним числом)</span>'
        : new Date(rec.ts*1000).toLocaleString();
      const vol = ct.volume_24h_usd ? Number(ct.volume_24h_usd).toLocaleString() : '-';
      const fr = ct.funding_rate!=null ? (ct.funding_rate*100).toFixed(3)+'%' : '-';
      return `<tr><td>${rec.symbol}</td><td>${rec.side||'-'}</td><td>${when}</td><td>${we.ema7?we.ema7.toPrecision(6):'-'}</td><td>${we.ema14?we.ema14.toPrecision(6):'-'}</td><td>${we.ema28?we.ema28.toPrecision(6):'-'}</td><td>${we.ladder||'-'}</td><td>${vol}</td><td>${fr}</td><td>${a1}/${a4}</td></tr>`;
    }).join('');
  }catch(e){}
}
loadHistory();
</script></body></html>"""
EMA_DIAG_HTML_PAGE = """<!DOCTYPE html>
<html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>EMA Diagnostics</title>
<style>
body{background:#0d1117;color:#c9d1d9;font-family:system-ui,sans-serif;margin:0;padding:16px}
h1{font-size:20px}
h3{font-size:15px;margin:0}
button{background:#238636;color:#fff;border:0;padding:8px 14px;border-radius:6px;font-size:13px;margin:4px 6px 4px 0;cursor:pointer}
.btn-danger-sm{background:#3a1414;border:1px solid #f85149;color:#f85149;padding:4px 10px;font-size:12px;border-radius:6px;margin:0}
table{width:100%;border-collapse:collapse;margin-top:10px;font-size:12px}
th,td{padding:5px 7px;text-align:left;border-bottom:1px solid #21262d;white-space:nowrap}
th{color:#8b949e;position:sticky;top:0;background:#161b22}
tbody tr:hover{background:#1c2128}
.section-card{background:#161b22;border:1px solid #30363d;border-radius:10px;padding:14px;margin-top:14px}
.section-head{display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:8px}
.table-scroll{overflow-x:auto}
.wr-hi{color:#3fb950;font-weight:bold}
.wr-lo{color:#f85149;font-weight:bold}
select,input{background:#0d1117;color:#c9d1d9;border:1px solid #30363d;border-radius:6px;padding:6px;font-size:13px}
.summary-line{font-size:13px;color:#8b949e;margin-top:6px}
</style></head><body>
<div style="background:#161b22;border-bottom:1px solid #30363d;padding:8px 12px;display:flex;gap:6px;flex-wrap:wrap;font-size:12px">
  <a href="/ema" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#127968; Главная</a>
  <a href="/manual_scan" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128269; Ручной скан</a>
  <a href="/ema_diag" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128202; EMA Диагностика</a>
  <a href="/ema_stretch" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128201; Отрыв от EMA</a>
  <a href="/pump_match" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#127919; Pump Match</a>
  <a href="/weekly_ema_backtest" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128200; Бэктест EMA</a>
</div>

<h1>&#128300; EMA Diagnostics — сбор данных для анализа</h1>
<p style="color:#8b949e;font-size:13px;max-width:520px">Широкий (не алертовый) скан касаний EMA7/14/28 на 1h/4h/1d/1w — собирает ВСЕ приближения в пределах 1.0 ATR (не только те, что проходят узкий порог для реального сигнала), отслеживает MFE/MAE и срезы через 15/30/60/120/240 минут после касания. Цель — понять, откуда цена реально отскакивает чаще всего, а не только то, что мы уже алертим.</p>
<p style="color:#d29922;font-size:12px;max-width:520px">⚠️ "Карта ликвидации" в исходном запросе — честно, у нас её нет (нужен платный источник типа Coinglass). Funding rate — единственный реально доступный прокси перекошенности позиций, это не то же самое, показан отдельным полем в сырых записях.</p>

<div class="section-card">
  <div class="section-head">
    <h3>&#128202; Сводка: откуда отскакивает чаще всего</h3>
    <button onclick="loadSummary()">&#128260; Обновить</button>
  </div>
  <div id="summaryStatus" class="summary-line">загрузка...</div>
  <div class="table-scroll">
    <table id="summaryTable">
      <thead><tr><th>ТФ</th><th>EMA</th><th>Схема</th><th>Касаний</th><th>Оценено</th><th>Bounce rate</th><th>Avg MFE</th><th>Med MFE</th><th>Avg MAE</th><th>Med MAE</th><th>С просадкой</th><th>LONG bounce%</th><th>SHORT bounce%</th></tr></thead>
      <tbody></tbody>
    </table>
  </div>
</div>

<div class="section-card">
  <div class="section-head">
    <h3>&#128203; Последние записи (сырые)</h3>
    <div>
      <select id="filterTf" onchange="loadRaw()">
        <option value="">Все ТФ</option>
        <option value="1h">1h</option>
        <option value="4h">4h</option>
        <option value="1d">1d</option>
        <option value="1w">1w</option>
      </select>
      <select id="filterPeriod" onchange="loadRaw()">
        <option value="">Все EMA</option>
        <option value="7">EMA7</option>
        <option value="14">EMA14</option>
        <option value="28">EMA28</option>
      </select>
      <select id="filterVersion" onchange="loadRaw()">
        <option value="">Все версии схемы</option>
      </select>
      <a href="/ema_diag_export" style="text-decoration:none"><button>&#128190; Скачать JSON</button></a>
      <button onclick="backfillDiag()" style="background:#21262d;border:1px solid #30363d">Досчитать 12ч для старых</button>
      <span id="backfillMsg" style="font-size:12px;color:#8b949e"></span>
      <button onclick="clearDiag()" class="btn-danger-sm">Очистить всё</button>
    </div>
  </div>
  <div id="rawStatus" class="summary-line"></div>
  <div class="table-scroll">
    <table id="rawTable">
      <thead><tr><th>Монета</th><th>ТФ</th><th>EMA</th><th>Схема</th><th>Подход</th><th>Dist(ATR)</th><th>Vol ratio</th><th>Funding</th><th>MFE</th><th>MAE</th><th>Откат</th><th>15м</th><th>30м</th><th>60м</th><th>120м</th><th>240м</th><th>Статус</th><th>Когда</th></tr></thead>
      <tbody></tbody>
    </table>
  </div>
</div>

<script>
function wrClass(v, hi, lo){
  if(v === null || v === undefined) return '';
  if(v >= hi) return 'wr-hi';
  if(v <= lo) return 'wr-lo';
  return '';
}
function fmtAgo(ts){
  const s = Math.floor(Date.now()/1000) - ts;
  if(s < 3600) return Math.floor(s/60)+'м назад';
  if(s < 86400) return Math.floor(s/3600)+'ч назад';
  return Math.floor(s/86400)+'д назад';
}
function fmtCp(cp){
  if(!cp) return '—';
  return `${cp.pct>=0?'+':''}${cp.pct}%`;
}

async function loadSummary(){
  const statusEl = document.getElementById('summaryStatus');
  statusEl.innerText = 'Загрузка...';
  try{
    const r = await fetch('/ema_diag_summary'); const d = await r.json();
    const tbody = document.querySelector('#summaryTable tbody'); tbody.innerHTML = '';
    for(const e of d.summary){
      const o = e.overall;
      const tr = document.createElement('tr');
      const longBr = e.long ? e.long.bounce_rate : null;
      const shortBr = e.short ? e.short.bounce_rate : null;
      // если среднее сильно разошлось с медианой -- знак, что среднее
      // искажено выбросом (см. случай AKE_USDT: +250% за 15м утащил
      // среднюю связки с +0.04% до -3.7%, хотя медиана была честной)
      const mfeSkewed = o.avg_mfe_pct!=null && o.median_mfe_pct!=null && Math.abs(o.avg_mfe_pct - o.median_mfe_pct) > Math.abs(o.median_mfe_pct)*2 + 1;
      const maeSkewed = o.avg_mae_pct!=null && o.median_mae_pct!=null && Math.abs(o.avg_mae_pct - o.median_mae_pct) > Math.abs(o.median_mae_pct)*2 + 1;
      const verLabel = (o.diag_versions||[]).join(',') || '—';
      const verCell = o.mixed_versions
        ? `<span style="color:#d29922" title="В этой группе смешаны записи из РАЗНЫХ версий схемы (${verLabel}) -- логика детекта/чекпоинтов менялась в середине выборки, сравнение как единого целого некорректно">⚠ ${verLabel}</span>`
        : verLabel;
      tr.innerHTML = `<td>${e.tf}</td><td>EMA${e.period}</td><td>${verCell}</td><td>${o.total}</td><td>${o.evaluated}</td>`
        + `<td class="${wrClass(o.bounce_rate,60,35)}">${o.bounce_rate ?? '—'}%</td>`
        + `<td${mfeSkewed?' style="color:#d29922" title="Среднее сильно разошлось с медианой -- похоже на выброс"':''}>${o.avg_mfe_pct ?? '—'}%${mfeSkewed?' ⚠':''}</td>`
        + `<td>${o.median_mfe_pct ?? '—'}%</td>`
        + `<td${maeSkewed?' style="color:#d29922" title="Среднее сильно разошлось с медианой -- похоже на выброс"':''}>${o.avg_mae_pct ?? '—'}%${maeSkewed?' ⚠':''}</td>`
        + `<td>${o.median_mae_pct ?? '—'}%</td>`
        + `<td>${o.retrace_rate ?? '—'}%</td>`
        + `<td class="${wrClass(longBr,60,35)}">${longBr ?? '—'}${longBr!=null?'%':''}</td>`
        + `<td class="${wrClass(shortBr,60,35)}">${shortBr ?? '—'}${shortBr!=null?'%':''}</td>`;
      tbody.appendChild(tr);
    }
    statusEl.innerText = d.summary.length ? `Связок с достаточным числом касаний: ${d.summary.length}` : 'Пока недостаточно данных (нужно хотя бы 3 касания на связку)';
  }catch(e){ statusEl.innerText = '❌ ошибка загрузки'; }
}

async function loadRaw(){
  const statusEl = document.getElementById('rawStatus');
  const tf = document.getElementById('filterTf').value;
  const period = document.getElementById('filterPeriod').value;
  const version = document.getElementById('filterVersion').value;
  let url = '/ema_diag_status?limit=100';
  if(tf) url += '&tf=' + tf;
  if(period) url += '&period=' + period;
  if(version) url += '&diag_version=' + version;
  try{
    const r = await fetch(url); const d = await r.json();
    const tbody = document.querySelector('#rawTable tbody'); tbody.innerHTML = '';
    const seenVersions = new Set();
    for(const it of d.recent){
      const tr = document.createElement('tr');
      const biasCol = it.classic_bias === 'short' ? '#f85149' : '#3fb950';
      const cps = it.checkpoints || {};
      const ver = it.diag_version ?? 0;
      seenVersions.add(ver);
      tr.innerHTML = `<td>${it.symbol}</td><td>${it.tf}</td><td>EMA${it.period}</td><td>${ver}</td>`
        + `<td style="color:${biasCol}">${it.side==='from_below'?'снизу→SHORT':'сверху→LONG'}</td>`
        + `<td>${it.dist_atr}</td><td>${it.vol_ratio ?? '—'}</td><td>${it.funding_rate ?? '—'}</td>`
        + `<td style="color:#3fb950">+${it.mfe_pct}%</td><td style="color:#f85149">-${it.mae_pct}%</td>`
        + `<td>${it.retrace_before_favorable ? 'да' : 'нет'}</td>`
        + `<td>${fmtCp(cps['15'])}</td><td>${fmtCp(cps['30'])}</td><td>${fmtCp(cps['60'])}</td><td>${fmtCp(cps['120'])}</td><td>${fmtCp(cps['240'])}</td>`
        + `<td>${it.track_done ? 'закрыт' : 'отслеживаем'}</td><td>${fmtAgo(it.ts)}</td>`;
      tbody.appendChild(tr);
    }
    // подсказка списка версий для выпадающего фильтра (наполняем один раз при первой загрузке)
    const verSel = document.getElementById('filterVersion');
    if(verSel.options.length <= 1){
      for(const v of [...seenVersions].sort()){
        const opt = document.createElement('option');
        opt.value = v; opt.text = `Схема v${v}`;
        verSel.appendChild(opt);
      }
    }
    statusEl.innerText = `Показано ${d.recent.length} из ${d.total}`;
  }catch(e){ statusEl.innerText = '❌ ошибка загрузки'; }
}

async function clearDiag(){
  if(!confirm('Точно очистить ВСЮ диагностическую историю? Отменить нельзя.')) return;
  await fetch('/ema_diag_clear', {method:'POST'});
  loadSummary(); loadRaw();
}
async function backfillDiag(){
  const msg = document.getElementById('backfillMsg');
  msg.style.color = '#8b949e';
  msg.innerText = 'Запущено в фоне...';
  try{
    const r = await fetch('/ema_diag_backfill_extended', {method:'POST'});
    const d = await r.json();
    msg.style.color = d.ok ? '#3fb950' : '#f85149';
    msg.innerText = d.msg || (d.ok ? 'Запущено' : 'Ошибка');
  }catch(e){ msg.style.color = '#f85149'; msg.innerText = 'Ошибка запроса'; }
}

loadSummary();
loadRaw();
setInterval(loadRaw, 30000);
</script></body></html>"""


EMA_STRETCH_HTML_PAGE = """<!DOCTYPE html>
<html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>EMA Stretch/Retrace</title>
<style>
body{background:#0d1117;color:#c9d1d9;font-family:system-ui,sans-serif;margin:0;padding:16px}
h1{font-size:20px}
h3{font-size:15px;margin:0}
button{background:#238636;color:#fff;border:0;padding:8px 14px;border-radius:6px;font-size:13px;margin:4px 6px 4px 0;cursor:pointer}
.btn-danger-sm{background:#3a1414;border:1px solid #f85149;color:#f85149;padding:4px 10px;font-size:12px;border-radius:6px;margin:0}
table{width:100%;border-collapse:collapse;margin-top:10px;font-size:12px}
th,td{padding:5px 7px;text-align:left;border-bottom:1px solid #21262d;white-space:nowrap}
th{color:#8b949e;position:sticky;top:0;background:#161b22}
tbody tr:hover{background:#1c2128}
.section-card{background:#161b22;border:1px solid #30363d;border-radius:10px;padding:14px;margin-top:14px}
.section-head{display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:8px}
.table-scroll{overflow-x:auto}
.wr-hi{color:#3fb950;font-weight:bold}
.wr-lo{color:#f85149;font-weight:bold}
select,input{background:#0d1117;color:#c9d1d9;border:1px solid #30363d;border-radius:6px;padding:6px;font-size:13px}
.summary-line{font-size:13px;color:#8b949e;margin-top:6px}
</style></head><body>
<div style="background:#161b22;border-bottom:1px solid #30363d;padding:8px 12px;display:flex;gap:6px;flex-wrap:wrap;font-size:12px">
  <a href="/ema" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#127968; Главная</a>
  <a href="/manual_scan" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128269; Ручной скан</a>
  <a href="/ema_diag" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128202; EMA Диагностика</a>
  <a href="/ema_stretch" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128201; Отрыв от EMA</a>
  <a href="/weekly_ema_backtest" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128200; Бэктест EMA</a>
</div>

<h1>&#128201; Отрыв от EMA — сколько в среднем откатывает</h1>
<p style="color:#8b949e;font-size:13px;max-width:520px">Триггер — отрыв цены ВВЕРХ от короткой EMA (5/10/20) на ЗАКРЫТИИ свечи, ≥1%, на ТФ 15м/30м/1h/4h (только вверх — ожидаем падение к EMA обратно, отрыв вниз не отслеживается). Дальше следим ЛИБО до касания той же EMA обратно, ЛИБО до таймаута (свой на каждый ТФ) — и считаем % отката от исходного отрыва (100% = полностью коснулась). Сводка разбита ещё и по величине самого отрыва (1-3% / 3-6% / 6%+), чтобы видеть, растёт ли шанс отката вместе с силой отрыва.</p>
<p style="color:#d29922;font-size:12px;max-width:520px">С v0.30.78 топ-связка (та, по которой шлются алерты) ранжируется в первую очередь по винрейту тейк/стоп (колонка "Винрейт тейк/стоп"), "выживаемость при стопе" — второй критерий (тай-брейк), не первый. До v0.30.60 было по чистому % отката. С v0.30.85 стоп для фейда — 0.5% (было 2.0%) — узкий стоп при типично небольшом тейке (уровень EMA) даёт заметно лучшее матожидание, даже при более низком винрейте (найдено симуляцией на реальных данных).</p>
<p style="color:#3fb950;font-size:12px;max-width:520px">С v0.30.67 это уже настоящий сигнал: Вход = цена триггера, Стоп = вход+2% (SHORT — цена ушла вверх, ждём падения), Тейк = уровень EMA. Исход (тейк/стоп/таймаут) считается в реальном порядке событий — если стоп задело раньше, чем цена дошла до EMA, это "stop", даже если ПОЗЖЕ цена всё равно дошла бы до цели (реальной позиции бы уже не было). Колонка "Винрейт тейк/стоп" — по этому честному исходу.</p>
<p style="color:#58a6ff;font-size:12px;max-width:520px">С v0.30.83 — вторая, отдельная стратегия: "📈 продолжение" (LONG). Данные показали, что отрывы больше 2% почти никогда не откатываются до фиксированного стопа — это чаще реальный тренд, не перегрев. Для таких случаев теперь отдельный сигнал на продолжение импульса вместо фейда. Стоп 1% / тейк 2% (RR 2:1, узкий стоп — зеркально фейду, настоящий тренд не должен откатываться сильно) — ⚠️ экспериментально, НЕ откалибровано по своим историческим данным, копим статистику с нуля так же, как и с фейдом в своё время.</p>

<div class="section-card">
  <h3>&#9989; Скан только по выбранным ЕМА+ТФ</h3>
  <p style="color:#8b949e;font-size:12px">По умолчанию скан проверяет все связки (ЕМА5/10/20 × 15м/30м/1h/4h). Сними лишние галочки, чтобы сканировать только интересующие параметры — сэкономит запросы к бирже. Если снять все галочки с одной из сторон, скан вернётся к "всё" (защита от случайной остановки).</p>
  <div id="scanFilterBox" style="display:flex;gap:24px;flex-wrap:wrap;margin:8px 0"></div>
  <button onclick="saveScanSettings()">&#128190; Сохранить</button>
  <div id="scanFilterStatus" style="font-size:12px;color:#8b949e;margin-top:6px"></div>
</div>

<div class="section-card" style="border:1px solid #f85149">
  <h3 style="color:#f85149">&#9888;&#65039; Автоторговля (реальные деньги)</h3>
  <p style="color:#8b949e;font-size:12px">Открывает настоящую позицию на Gate.io. С v0.30.95 — СТРОГО по выбранным ниже конфигам (галочки ТФ×ЕМА, отдельно для фейда и продолжения), а не по меняющейся топ-связке. Вход/стоп/тейк — из самого сигнала, плечо — фиксированное (задаётся ниже, по умолчанию 10х). Выключено по умолчанию — включай осознанно.</p>
  <div style="display:flex;gap:8px;flex-wrap:wrap;align-items:center;margin:8px 0">
    <label style="font-size:13px;color:#c9d1d9">Режим:
      <select id="autoTradeMode" style="background:#0d1117;color:#c9d1d9;border:1px solid #30363d;border-radius:6px;padding:6px">
        <option value="off">Выкл</option>
        <option value="long">Long (продолжение)</option>
        <option value="short">Short (фейд)</option>
      </select>
    </label>
    <label style="font-size:13px;color:#c9d1d9">% депозита на сделку:
      <input type="number" id="autoTradePct" step="0.1" min="0.1" max="20" style="width:70px;background:#0d1117;color:#c9d1d9;border:1px solid #30363d;border-radius:6px;padding:6px">
    </label>
    <label style="font-size:13px;color:#c9d1d9">Плечо:
      <input type="number" id="autoTradeLev" step="1" min="1" max="20" style="width:60px;background:#0d1117;color:#c9d1d9;border:1px solid #30363d;border-radius:6px;padding:6px">
    </label>
  </div>
  <div style="font-size:13px;color:#c9d1d9;margin:8px 0 4px">По каким конфигам ведётся автоторговля:</div>
  <div id="autoTradeConfigsBox" style="display:flex;gap:24px;flex-wrap:wrap;margin:8px 0"></div>
  <div style="display:flex;gap:8px;align-items:center;flex-wrap:wrap">
    <label style="display:flex;gap:6px;align-items:center;font-size:13px;color:#c9d1d9;cursor:pointer">
      <input type="checkbox" id="autoTradeEnabled" style="width:16px;height:16px">
      Включить автоторговлю
    </label>
    <button onclick="saveAutoTradeSettings()">&#128190; Сохранить</button>
  </div>
  <div id="autoTradeStatus" style="font-size:12px;color:#8b949e;margin-top:6px"></div>
  <div id="autoTradeActiveConfigs" style="font-size:12px;color:#8b949e;margin-top:6px"></div>
  <div id="autoTradeLive" style="font-size:12px;margin-top:8px"></div>
</div>


<div class="section-card">
  <div class="section-head">
    <h3>&#128202; Сводка: какой ТФ/период откатывает лучше всего</h3>
    <div>
      <button onclick="loadSummary()">&#128260; Обновить</button>
      <button onclick="clearStretchHistory()" style="background:#3a1414;border:1px solid #f85149;color:#f85149">🗑 Очистить историю</button>
    </div>
  </div>
  <div id="summaryStatus" class="summary-line">загрузка...</div>
  <div class="table-scroll">
    <table id="summaryTable">
      <thead><tr><th>Стратегия</th><th>ТФ</th><th>EMA</th><th>Событий</th><th>Оценено</th><th>Винрейт тейк/стоп</th><th>Выжив. при стопе (10х)</th><th>Touch rate</th><th>Avg откат%</th><th>Med откат%</th><th>Avg время до касания</th><th>Avg хуже на%</th><th>Худший случай</th><th>1-3% events</th><th>3-6% events</th><th>6%+ events</th></tr></thead>
      <tbody></tbody>
    </table>
  </div>
</div>

<div class="section-card">
  <div class="section-head">
    <h3>&#128203; Последние отрывы (сырые)</h3>
    <div>
      <select id="filterTf" onchange="loadRaw()">
        <option value="">Все ТФ</option>
        <option value="15m">15m</option>
        <option value="30m">30m</option>
        <option value="1h">1h</option>
        <option value="4h">4h</option>
      </select>
      <select id="filterPeriod" onchange="loadRaw()">
        <option value="">Все EMA</option>
        <option value="5">EMA5</option>
        <option value="10">EMA10</option>
        <option value="20">EMA20</option>
      </select>
      <a href="/ema_stretch_export" style="text-decoration:none"><button>&#128190; Скачать JSON</button></a>
    </div>
  </div>
  <div id="rawStatus" class="summary-line"></div>
  <div class="table-scroll">
    <table id="rawTable">
      <thead><tr><th>Стратегия</th><th>Монета</th><th>ТФ</th><th>EMA</th><th>Вход</th><th>Стоп</th><th>Тейк</th><th>Отрыв%</th><th>Ближайший подход%</th><th>Худший заход%</th><th>Откат%</th><th>Исход</th><th>Время до касания</th><th>Когда</th></tr></thead>
      <tbody></tbody>
    </table>
  </div>
</div>

<script>
function fmtAgo(ts){
  const s = Math.floor(Date.now()/1000) - ts;
  if(s < 3600) return Math.floor(s/60)+'м назад';
  if(s < 86400) return Math.floor(s/3600)+'ч назад';
  return Math.floor(s/86400)+'д назад';
}

async function loadSummary(){
  const statusEl = document.getElementById('summaryStatus');
  statusEl.innerText = 'Загрузка...';
  try{
    const r = await fetch('/ema_stretch_status'); const d = await r.json();
    const tbody = document.querySelector('#summaryTable tbody'); tbody.innerHTML = '';
    statusEl.innerText = `Всего событий в истории: ${d.count}` + (d.last_cycle_sec != null ? ` · последний скан занял ${d.last_cycle_sec}с` : '');
    let lastStrategy = null;
    for(const e of (d.summary || [])){
      const o = e.overall;
      if(e.strategy !== lastStrategy){
        lastStrategy = e.strategy;
        const hdr = document.createElement('tr');
        hdr.innerHTML = `<td colspan="15" style="background:#161b22;color:${e.strategy==='continuation'?'#58a6ff':'#f85149'};font-weight:bold;padding-top:14px">`+
          `${e.strategy === 'continuation' ? '📈 ПРОДОЛЖЕНИЕ — свой отдельный топ, алертит независимо от фейда' : '📉 ФЕЙД — свой отдельный топ, алертит независимо от продолжения'}</td>`;
        tbody.appendChild(hdr);
      }
      const isLeader = tbody.querySelectorAll(`tr[data-strategy="${e.strategy}"]`).length === 0;
      const tr = document.createElement('tr');
      tr.setAttribute('data-strategy', e.strategy);
      if(isLeader) tr.style.background = '#132d1d';
      const b13 = e['stretch_1-3%'], b36 = e['stretch_3-6%'], b6p = e['stretch_6%+'];
      tr.innerHTML = `<td>${isLeader ? '👑 ' : ''}${e.strategy === 'continuation' ? '📈 продолжение' : '📉 фейд'}</td><td>${e.tf}</td><td>EMA${e.period}</td><td>${o.total}</td><td>${o.evaluated}</td>`+
        `<td>${o.signal_win_rate ?? '—'}% (n=${o.signal_resolved_n ?? 0}, таймаутов=${o.signal_timeouts ?? 0})</td>`+
        `<td>${o.stop_survival_rate ?? '—'}% (n=${o.stop_survival_n ?? 0})</td>`+
        `<td>${o.touch_rate ?? '—'}%</td><td>${o.avg_retrace_pct_of_stretch ?? '—'}%</td>`+
        `<td>${o.median_retrace_pct_of_stretch ?? '—'}%</td><td>${o.avg_time_to_touch_min ?? '—'} мин</td>`+
        `<td>${o.avg_extra_adverse_pct ?? '—'}%</td><td>${o.max_extra_adverse_pct ?? '—'}%</td>`+
        `<td>${b13 ? b13.evaluated+' / '+(b13.avg_retrace_pct_of_stretch??'—')+'%' : '—'}</td>`+
        `<td>${b36 ? b36.evaluated+' / '+(b36.avg_retrace_pct_of_stretch??'—')+'%' : '—'}</td>`+
        `<td>${b6p ? b6p.evaluated+' / '+(b6p.avg_retrace_pct_of_stretch??'—')+'%' : '—'}</td>`;
      tbody.appendChild(tr);
    }
  }catch(e){ statusEl.innerText = 'Ошибка загрузки'; }
}

async function clearStretchHistory(){
  if(!confirm('Точно очистить всю историю "Отрыв от EMA"? Это нельзя отменить.')) return;
  try{
    const r = await fetch('/ema_stretch_history_clear', {method:'POST'});
    const d = await r.json();
    if(d.ok){ loadSummary(); loadRaw(); }
    else { alert('Не получилось очистить'); }
  }catch(e){ alert('Ошибка запроса'); }
}

const STRETCH_PERIODS = [5,10,20];
const STRETCH_TFS = ["15m","30m","1h","4h"];
const STRETCH_STRATEGIES = [["fade","Фейд (SHORT, возврат к EMA)"],["continuation","Продолжение (LONG, на импульс)"]];

function buildScanFilterUI(){
  const box = document.getElementById('scanFilterBox');
  let html = '<div><div style="font-size:12px;color:#8b949e;margin-bottom:4px">ЕМА периоды</div>';
  for(const p of STRETCH_PERIODS){
    html += `<label style="display:flex;gap:6px;align-items:center;font-size:13px;color:#c9d1d9;cursor:pointer;margin-bottom:4px"><input type="checkbox" id="scanPeriod_${p}" style="width:16px;height:16px">EMA${p}</label>`;
  }
  html += '</div><div><div style="font-size:12px;color:#8b949e;margin-bottom:4px">Таймфреймы</div>';
  for(const tf of STRETCH_TFS){
    html += `<label style="display:flex;gap:6px;align-items:center;font-size:13px;color:#c9d1d9;cursor:pointer;margin-bottom:4px"><input type="checkbox" id="scanTf_${tf}" style="width:16px;height:16px">${tf}</label>`;
  }
  html += '</div>';
  box.innerHTML = html;
}
buildScanFilterUI();

async function loadScanSettings(){
  const statusEl = document.getElementById('scanFilterStatus');
  try{
    const r = await fetch('/stretch_scan_settings'); const d = await r.json();
    const periods = (d.periods && d.periods.length) ? d.periods : (d.all_periods || STRETCH_PERIODS);
    const tfs = (d.timeframes && d.timeframes.length) ? d.timeframes : (d.all_timeframes || STRETCH_TFS);
    for(const p of STRETCH_PERIODS){ const el = document.getElementById(`scanPeriod_${p}`); if(el) el.checked = periods.includes(p); }
    for(const tf of STRETCH_TFS){ const el = document.getElementById(`scanTf_${tf}`); if(el) el.checked = tfs.includes(tf); }
    statusEl.innerText = '';
  }catch(e){ statusEl.innerText = 'Ошибка загрузки настроек скана'; }
}

async function saveScanSettings(){
  const statusEl = document.getElementById('scanFilterStatus');
  const periods = STRETCH_PERIODS.filter(p => document.getElementById(`scanPeriod_${p}`).checked);
  const timeframes = STRETCH_TFS.filter(tf => document.getElementById(`scanTf_${tf}`).checked);
  try{
    const r = await fetch('/stretch_scan_settings', {method:'POST', headers:{'Content-Type':'application/json'},
      body: JSON.stringify({periods, timeframes})});
    const d = await r.json();
    statusEl.style.color = d.ok ? '#3fb950' : '#f85149';
    statusEl.innerText = d.ok ? 'Настройки скана сохранены' : (d.msg || 'Ошибка сохранения');
    if(d.ok) loadScanSettings();
  }catch(e){ statusEl.style.color = '#f85149'; statusEl.innerText = 'Ошибка запроса'; }
}
loadScanSettings();

function buildAutoTradeConfigsUI(){
  const box = document.getElementById('autoTradeConfigsBox');
  let html = '';
  for(const [strat, label] of STRETCH_STRATEGIES){
    html += `<div><div style="font-size:12px;color:#8b949e;margin-bottom:4px">${label}</div>`;
    html += '<table style="border-collapse:collapse"><thead><tr><th></th>' + STRETCH_TFS.map(tf=>`<th style="font-size:11px;color:#8b949e;padding:2px 6px">${tf}</th>`).join('') + '</tr></thead><tbody>';
    for(const p of STRETCH_PERIODS){
      html += `<tr><td style="font-size:11px;color:#8b949e;padding:2px 6px">EMA${p}</td>`;
      for(const tf of STRETCH_TFS){
        html += `<td style="text-align:center;padding:2px 6px"><input type="checkbox" id="autoCfg_${tf}_${p}_${strat}" style="width:14px;height:14px"></td>`;
      }
      html += '</tr>';
    }
    html += '</tbody></table></div>';
  }
  box.innerHTML = html;
}
buildAutoTradeConfigsUI();

async function loadAutoTradeSettings(){
  const statusEl = document.getElementById('autoTradeStatus');
  const liveEl = document.getElementById('autoTradeLive');
  const activeEl = document.getElementById('autoTradeActiveConfigs');
  try{
    const r = await fetch('/stretch_autotrade_settings'); const d = await r.json();
    document.getElementById('autoTradeMode').value = d.mode || 'off';
    document.getElementById('autoTradePct').value = d.position_pct ?? 3.0;
    document.getElementById('autoTradeLev').value = d.leverage ?? 10;
    document.getElementById('autoTradeEnabled').checked = !!d.enabled;
    const configs = d.configs || [];
    for(const [strat] of STRETCH_STRATEGIES){
      for(const p of STRETCH_PERIODS){
        for(const tf of STRETCH_TFS){
          const el = document.getElementById(`autoCfg_${tf}_${p}_${strat}`);
          if(el) el.checked = configs.includes(`${tf}|${p}|${strat}`);
        }
      }
    }
    activeEl.innerText = configs.length
      ? `Автоторговля ведётся по: ${configs.join(', ')}`
      : 'Ни один конфиг не выбран — автоторговля не откроет ни одной позиции, даже если включена';
    statusEl.style.color = d.gate_configured ? '#3fb950' : '#f85149';
    statusEl.innerText = d.gate_configured ? 'Ключи Gate.io настроены' : 'Ключи Gate.io НЕ настроены (страница /gate_cfg) — включить нельзя';
    const live = d.live_positions || [];
    liveEl.innerHTML = live.length
      ? '<b>Открытые live-позиции:</b><br>' + live.map(p =>
          `${p.direction==='long'?'🟢':'🔴'} ${p.symbol} (${p.strategy}) вход ${p.entry}, ${p.size} контр, ${p.leverage}×, ~${p.notional}U`
        ).join('<br>')
      : 'Live-позиций сейчас нет';
  }catch(e){ statusEl.innerText = 'Ошибка загрузки настроек'; }
}

async function saveAutoTradeSettings(){
  const statusEl = document.getElementById('autoTradeStatus');
  const mode = document.getElementById('autoTradeMode').value;
  const pct = parseFloat(document.getElementById('autoTradePct').value);
  const lev = parseInt(document.getElementById('autoTradeLev').value);
  const enabled = document.getElementById('autoTradeEnabled').checked;
  const configs = [];
  for(const [strat] of STRETCH_STRATEGIES){
    for(const p of STRETCH_PERIODS){
      for(const tf of STRETCH_TFS){
        const el = document.getElementById(`autoCfg_${tf}_${p}_${strat}`);
        if(el && el.checked) configs.push(`${tf}|${p}|${strat}`);
      }
    }
  }
  if(enabled && !configs.length && !confirm('Ни один конфиг не выбран — автоторговля не откроет ни одной позиции. Всё равно включить?')) return;
  if(enabled && !confirm(`Точно включить автоторговлю? Режим: ${mode}, ${pct}% депозита на сделку, плечо ${lev}×, конфигов выбрано: ${configs.length}. Это реальные деньги на Gate.io.`)) return;
  try{
    const r = await fetch('/stretch_autotrade_settings', {method:'POST', headers:{'Content-Type':'application/json'},
      body: JSON.stringify({mode, position_pct: pct, leverage: lev, enabled, configs})});
    const d = await r.json();
    statusEl.style.color = d.ok ? '#3fb950' : '#f85149';
    statusEl.innerText = d.ok ? 'Настройки сохранены' : (d.msg || 'Ошибка сохранения');
    if(d.ok) loadAutoTradeSettings();
  }catch(e){ statusEl.style.color = '#f85149'; statusEl.innerText = 'Ошибка запроса'; }
}
loadAutoTradeSettings();

async function loadRaw(){
  const statusEl = document.getElementById('rawStatus');
  try{
    const r = await fetch('/ema_stretch_status'); const d = await r.json();
    const tf = document.getElementById('filterTf').value;
    const period = document.getElementById('filterPeriod').value;
    let items = (d.recent || []).slice().reverse();
    if(tf) items = items.filter(x => x.tf === tf);
    if(period) items = items.filter(x => String(x.period) === period);
    statusEl.innerText = `Показано ${items.length} из ${d.count}`;
    const tbody = document.querySelector('#rawTable tbody'); tbody.innerHTML = '';
    for(const it of items.slice(0,100)){
      const tr = document.createElement('tr');
      const timeToTouch = it.time_to_touch_sec ? Math.round(it.time_to_touch_sec/60)+' мин' : '—';
      const outcomeColors = {take:'#3fb950', stop:'#f85149', timeout:'#8b949e', open:'#d29922'};
      const oc = it.signal_outcome || (it.touched ? 'take' : (it.track_done ? 'timeout' : 'open'));
      const status = `<span style="color:${outcomeColors[oc]||'#8b949e'}">${oc}</span>`;
      tr.innerHTML = `<td>${it.strategy === 'continuation' ? '📈' : '📉'}</td><td>${it.symbol}</td><td>${it.tf}</td><td>EMA${it.period}</td>`+
        `<td>${it.entry ?? '—'}</td><td>${it.stop ?? '—'}</td><td>${it.take ?? '—'}</td>`+
        `<td>${it.stretch_pct>0?'+':''}${it.stretch_pct}%</td><td>${it.closest_stretch_pct}%</td>`+
        `<td>${it.max_adverse_stretch_pct ?? it.stretch_pct}%</td>`+
        `<td>${it.max_retrace_pct_of_stretch}%</td><td>${status}</td>`+
        `<td>${timeToTouch}</td><td>${fmtAgo(it.ts)}</td>`;
      tbody.appendChild(tr);
    }
  }catch(e){ statusEl.innerText = 'Ошибка загрузки'; }
}

loadSummary();
loadRaw();
setInterval(loadRaw, 30000);
</script></body></html>"""


WEEKLY_EMA_BACKTEST_HTML_PAGE = """<!DOCTYPE html>
<html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Weekly EMA Backtest</title>
<style>
body{background:#0d1117;color:#c9d1d9;font-family:system-ui,sans-serif;margin:0;padding:16px}
h1{font-size:20px}
label{display:block;margin-top:12px;font-size:13px;color:#8b949e}
input,select{width:100%;background:#161b22;color:#c9d1d9;border:1px solid #30363d;border-radius:6px;padding:8px;margin-top:4px;font-size:14px;box-sizing:border-box}
button{background:#238636;color:#fff;border:0;padding:10px 16px;border-radius:6px;font-size:14px;margin-top:14px;cursor:pointer}
.card{background:#161b22;border:1px solid #30363d;border-radius:10px;padding:16px;margin-top:14px;max-width:480px}
#status{color:#8b949e;font-size:13px;margin-top:10px}
table{width:100%;border-collapse:collapse;margin-top:14px;font-size:13px}
th,td{padding:6px 8px;text-align:left;border-bottom:1px solid #21262d;white-space:nowrap}
th{color:#8b949e;position:sticky;top:0;background:#161b22;cursor:pointer}
tbody tr:hover{background:#1c2128}
.wr-hi{color:#3fb950;font-weight:bold}
.wr-lo{color:#f85149;font-weight:bold}
.table-scroll{overflow-x:auto}
.section-card{background:#161b22;border:1px solid #30363d;border-radius:10px;padding:14px;margin-top:14px}
</style></head><body>
<div style="background:#161b22;border-bottom:1px solid #30363d;padding:8px 12px;display:flex;gap:6px;flex-wrap:wrap;font-size:12px">
  <a href="/ema" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#127968; Главная</a>
  <a href="/manual_scan" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128269; Ручной скан</a>
  <a href="/ema_diag" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128202; EMA Диагностика</a>
  <a href="/ema_stretch" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128201; Отрыв от EMA</a>
  <a href="/pump_match" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#127919; Pump Match</a>
  <a href="/weekly_ema_backtest" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128200; Бэктест EMA</a>
</div>

<h1>&#128202; Weekly EMA Backtest — рейтинг угадывания</h1>
<p style="color:#8b949e;font-size:13px;max-width:480px">Сигнал ищется на НЕДЕЛЬНОМ графике (касание EMA7/14/28), но оценивается БЫСТРАЯ реакция — по 1-минутным свечам в первые часы после касания: достигла ли цена цели (3/4/6/10%) в сторону классического bounce-bias, и за сколько минут. Так, как реально торгуется сигнал — сделки на минуты-час, а не удержание неделями.</p>
<p style="color:#d29922;font-size:12px;max-width:480px">⚠️ У биржи почти наверняка нет 1m-истории на годы назад — реакцию можно проверить только для сравнительно недавних касаний (см. "макс. давность сигнала" ниже). Для более старых сигналов факт касания всё равно посчитается (total_signals), но в винрейт по реакции они не попадут — это видно по числу "evaluable" в скобках у каждой ячейки.</p>

<div class="card">
  <label>Таймфрейм для поиска касаний</label>
  <select id="btTf">
    <option value="1w" selected>Недельный</option>
    <option value="1d">Дневной</option>
  </select>
  <label>Сколько монет по объёму (топ-N)</label>
  <input type="number" id="btTopN" value="100" min="1" max="500">
  <label>Глубина истории для поиска касаний, дней</label>
  <input type="number" id="btDays" value="1500" min="200" max="5000">
  <label>Макс. давность сигнала для проверки реакции, дней (старше — 1m почти наверняка недоступны)</label>
  <input type="number" id="btMaxAgeDays" value="90" min="1" max="1000">
  <label>Окно проверки реакции после касания, минут</label>
  <input type="number" id="btWindowMin" value="180" min="5" max="1440">
  <label>Пороги цели, % (через запятую)</label>
  <input type="text" id="btThresholds" value="3,4,6,10">
  <label>Минимум сигналов у монеты, чтобы попасть в таблицу (отсеивает шум от 1-2 случайных касаний)</label>
  <input type="number" id="btMinSignals" value="5" min="1" max="100">
  <label>Сортировать по hit-rate на пороге</label>
  <select id="btSortThreshold"></select>
  <button onclick="runBacktest()">&#128202; Запустить бэктест</button>
  <div id="status"></div>
</div>

<div class="section-card">
  <div class="table-scroll">
    <table id="resultsTable">
      <thead><tr id="theadRow"><th>Монета</th><th>Сигналов</th></tr></thead>
      <tbody></tbody>
    </table>
  </div>
</div>

<script>
let lastResults = null;

function wrClass(wr){
  if(wr === null || wr === undefined) return '';
  if(wr >= 60) return 'wr-hi';
  if(wr <= 25) return 'wr-lo';
  return '';
}

function currentThresholds(){
  return lastResults && lastResults.thresholds ? lastResults.thresholds
       : document.getElementById('btThresholds').value.split(',').map(s => parseFloat(s.trim())).filter(v => !isNaN(v));
}

function rebuildHeadAndSortOptions(){
  const thresholds = currentThresholds();
  const thead = document.getElementById('theadRow');
  thead.innerHTML = '<th>Монета</th><th>Сигналов</th>';
  const sel = document.getElementById('btSortThreshold');
  sel.innerHTML = '';
  for(const t of thresholds){
    thead.innerHTML += `<th>${t}%</th>`;
    const opt = document.createElement('option');
    opt.value = t; opt.text = t + '%';
    sel.appendChild(opt);
  }
}

function renderTable(){
  if(!lastResults) return;
  rebuildHeadAndSortOptions();
  const thresholds = currentThresholds();
  const minSignals = parseInt(document.getElementById('btMinSignals').value) || 1;
  const sortT = document.getElementById('btSortThreshold').value || thresholds[0];
  const rows = Object.entries(lastResults.results || {})
    .filter(([sym, r]) => r.total_signals >= minSignals)
    .sort((a, b) => {
      const wa = (a[1].by_threshold[sortT] || {}).hit_rate;
      const wb = (b[1].by_threshold[sortT] || {}).hit_rate;
      if(wa === null || wa === undefined) return 1;
      if(wb === null || wb === undefined) return -1;
      return wb - wa;
    });
  const tbody = document.querySelector('#resultsTable tbody');
  tbody.innerHTML = '';
  for(const [sym, r] of rows){
    const tr = document.createElement('tr');
    let cells = `<td>${sym}</td><td>${r.total_signals}</td>`;
    for(const t of thresholds){
      const stat = r.by_threshold[t] || {};
      const hr = stat.hit_rate;
      const timeNote = stat.avg_time_min != null ? `, ~${stat.avg_time_min}м` : '';
      cells += `<td class="${wrClass(hr)}">${hr!=null ? hr+'%' : '—'}${timeNote} <span style="color:#6e7681;font-size:11px">(n=${stat.total||0})</span></td>`;
    }
    tr.innerHTML = cells;
    tbody.appendChild(tr);
  }
  document.getElementById('status').innerHTML = `Монет в таблице: <b>${rows.length}</b> (из ${Object.keys(lastResults.results||{}).length} с сигналами) — обновлено ${new Date(lastResults.generated_at*1000).toLocaleString()}`;
}

async function runBacktest(){
  const statusEl = document.getElementById('status');
  const tf = document.getElementById('btTf').value;
  const topN = document.getElementById('btTopN').value;
  const days = document.getElementById('btDays').value;
  const maxAgeDays = document.getElementById('btMaxAgeDays').value;
  const windowMin = document.getElementById('btWindowMin').value;
  const thresholds = document.getElementById('btThresholds').value.split(',').map(s => parseFloat(s.trim())).filter(v => !isNaN(v));
  statusEl.innerText = 'Запускаю бэктест...';
  try{
    const r = await fetch('/weekly_ema_backtest_run', {method:'POST', headers:{'Content-Type':'application/json'},
      body: JSON.stringify({tf: tf, top_n: parseInt(topN), days: parseInt(days), max_age_days: parseInt(maxAgeDays),
                             window_min: parseInt(windowMin), thresholds: thresholds})});
    const d = await r.json();
    if(!d.ok){ statusEl.innerText = '❌ ' + d.msg; return; }
    statusEl.innerText = `Запущено (${d.symbols_count} монет), жду...`;
    pollStatus(d.job_id);
  }catch(e){
    statusEl.innerText = '❌ ошибка запроса: ' + e;
  }
}

async function pollStatus(jobId){
  const statusEl = document.getElementById('status');
  try{
    const r = await fetch('/weekly_ema_backtest_status?job_id=' + jobId, {cache:'no-store'});
    const d = await r.json();
    if(!d.ok){ statusEl.innerText = '❌ ' + d.msg; return; }
    if(d.status === 'running'){
      statusEl.innerText = `Прогоняю монеты: ${d.done}/${d.total}...`;
      setTimeout(() => pollStatus(jobId), 2000);
      return;
    }
    if(d.status === 'error'){
      statusEl.innerText = '❌ ' + (d.result && d.result.msg ? d.result.msg : 'ошибка выполнения');
      return;
    }
    lastResults = d.result;
    renderTable();
  }catch(e){
    statusEl.innerText = '❌ ошибка опроса статуса: ' + e;
  }
}

document.getElementById('btMinSignals').addEventListener('change', renderTable);
document.getElementById('btSortThreshold').addEventListener('change', renderTable);

// подхватываем последний сохранённый результат ДЛЯ ВЫБРАННОГО ТФ
async function loadLastForCurrentTf(){
  const tf = document.getElementById('btTf').value;
  try{
    const r = await fetch('/weekly_ema_backtest_last?tf=' + tf);
    const d = await r.json();
    if(d.ok !== false && d.results){
      lastResults = d;
      renderTable();
    } else {
      lastResults = null;
      document.querySelector('#resultsTable tbody').innerHTML = '';
      document.getElementById('status').innerText = d.msg || 'Бэктест для этого ТФ ещё не запускался';
      rebuildHeadAndSortOptions();
    }
  }catch(e){ rebuildHeadAndSortOptions(); }
}
document.getElementById('btTf').addEventListener('change', loadLastForCurrentTf);
loadLastForCurrentTf();
</script></body></html>"""


PUMP_MATCH_HTML_PAGE = """<!DOCTYPE html>
<html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Pump Match</title>
<style>
body{background:#0d1117;color:#c9d1d9;font-family:system-ui,sans-serif;margin:0;padding:16px}
h1{font-size:20px}
label{display:block;margin-top:12px;font-size:13px;color:#8b949e}
input{width:100%;background:#161b22;color:#c9d1d9;border:1px solid #30363d;border-radius:6px;padding:8px;margin-top:4px;font-size:14px;box-sizing:border-box}
button{background:#238636;color:#fff;border:0;padding:10px 16px;border-radius:6px;font-size:14px;margin-top:14px;cursor:pointer}
.card{background:#161b22;border:1px solid #30363d;border-radius:10px;padding:16px;margin-top:14px;max-width:480px}
.cand{background:#161b22;border:1px solid #30363d;border-radius:10px;padding:10px;margin-top:12px}
.cand.best{border-color:#3fb950;box-shadow:0 0 0 1px #3fb950}
.cand img{width:100%;border-radius:6px;margin-top:6px}
.cand .params{font-size:12px;color:#8b949e;margin-top:6px}
.score{font-size:16px;font-weight:bold;color:#3fb950}
#status{color:#8b949e;font-size:13px;margin-top:10px}
#previewCard img{max-width:100%;border-radius:6px}
details summary{cursor:pointer;font-size:13px;color:#8b949e;margin-top:12px}
</style></head><body>
<div style="background:#161b22;border-bottom:1px solid #30363d;padding:8px 12px;display:flex;gap:6px;flex-wrap:wrap;font-size:12px">
  <a href="/ema" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#127968; Главная</a>
  <a href="/manual_scan" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128269; Ручной скан</a>
  <a href="/ema_diag" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128202; EMA Диагностика</a>
  <a href="/ema_stretch" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128201; Отрыв от EMA</a>
  <a href="/pump_match" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#127919; Pump Match</a>
  <a href="/weekly_ema_backtest" style="color:#c9d1d9;text-decoration:none;padding:5px 10px;border-radius:6px;background:#21262d;border:1px solid #30363d">&#128200; Бэктест EMA</a>
</div>

<h1>&#127919; Pump Match — авто-подбор параметров по скрину</h1>
<p style="color:#8b949e;font-size:13px;max-width:480px">Загрузи скрин чужого бота (пампа), укажи монету и дату — время окна искать НЕ нужно, бот сам сравнит форму ценовой линии со скрина с реальными свечами Gate.io за день и найдёт совпадение. Дальше перебирает варианты отрисовки и подбирает ближайший. Лучший вариант сразу становится рабочим для всех новых алертов.</p>

<div class="card">
  <label>Скрин-эталон</label>
  <input type="file" id="pmImage" accept="image/*">
  <label>Символ (контракт Gate.io)</label>
  <input type="text" id="pmSymbol" placeholder="T_USDT" value="T_USDT">
  <label>Дата (ГГГГ-ММ-ДД)</label>
  <input type="date" id="pmDate">
  <label>Длительность окна на скрине, минут (посчитай по подписям оси времени)</label>
  <input type="number" id="pmDuration" value="120" min="5" max="1000">
  <label>Базовая цена (необязательно — по умолчанию минимум найденного окна)</label>
  <input type="text" id="pmBase" placeholder="напр. 5.801e-6">
  <details>
    <summary>Сузить диапазон поиска по времени (необязательно, ускоряет поиск)</summary>
    <label>Искать не раньше</label>
    <input type="time" id="pmSearchStart" value="00:00">
    <label>Искать не позже</label>
    <input type="time" id="pmSearchEnd" value="23:59">
  </details>
  <button onclick="runMatch()">&#128269; Подобрать время и параметры</button>
  <div id="status"></div>
</div>

<div id="previewCard" class="card" style="display:none">
  <b>Область скрина, с которой сравниваем</b>
  <img id="previewImg">
</div>

<div id="results"></div>

<script>
document.getElementById('pmDate').valueAsDate = new Date();

async function runMatch(){
  const statusEl = document.getElementById('status');
  const fileInput = document.getElementById('pmImage');
  if(!fileInput.files.length){ statusEl.innerText = 'Выбери файл скрина'; return; }
  const symbol = document.getElementById('pmSymbol').value.trim();
  const date = document.getElementById('pmDate').value;
  const duration = document.getElementById('pmDuration').value;
  const searchStart = document.getElementById('pmSearchStart').value || '00:00';
  const searchEnd = document.getElementById('pmSearchEnd').value || '23:59';
  const base = document.getElementById('pmBase').value.trim();
  if(!symbol || !date || !duration){ statusEl.innerText = 'Заполни символ/дату/длительность'; return; }

  const fd = new FormData();
  fd.append('image', fileInput.files[0]);
  fd.append('symbol', symbol);
  fd.append('date', date);
  fd.append('duration_min', duration);
  fd.append('search_start', searchStart);
  fd.append('search_end', searchEnd);
  if(base) fd.append('base_price', base);

  statusEl.innerText = 'Запускаю подбор...';
  document.getElementById('results').innerHTML = '';
  document.getElementById('previewCard').style.display = 'none';

  try{
    const r = await fetch('/pump_match_run', {method:'POST', body: fd});
    const d = await r.json();
    if(!d.ok){ statusEl.innerText = '❌ ' + d.msg; return; }
    pollMatchStatus(d.job_id);
  }catch(e){
    statusEl.innerText = '❌ ошибка запроса: ' + e;
  }
}

async function pollMatchStatus(jobId){
  const statusEl = document.getElementById('status');
  try{
    const r = await fetch('/pump_match_status?job_id=' + jobId, {cache:'no-store'});
    const d = await r.json();
    if(!d.ok){ statusEl.innerText = '❌ ' + d.msg; return; }
    if(d.status === 'running'){
      const pct = d.total ? Math.round(100*d.done/d.total) : 0;
      statusEl.innerText = `Перебираю варианты: ${d.done}/${d.total} (${pct}%)...`;
      setTimeout(() => pollMatchStatus(jobId), 2000);
      return;
    }
    if(d.status === 'error'){
      statusEl.innerText = '❌ ' + (d.result && d.result.msg ? d.result.msg : 'ошибка выполнения');
      return;
    }
    // status === 'done'
    renderMatchResult(d.result);
  }catch(e){
    statusEl.innerText = '❌ ошибка опроса статуса: ' + e;
  }
}

function renderMatchResult(d){
  const statusEl = document.getElementById('status');
  if(!d.ok){ statusEl.innerText = '❌ ' + d.msg; return; }

  statusEl.innerHTML = `Просмотрено свечей: ${d.candles_scanned} | Найденное окно: <b>${d.window_start} — ${d.window_end}</b> (совпадение формы цены: ${d.time_match_score}) | База: ${d.base_price} → Последняя: ${d.last_price} (${d.pct>=0?'+':''}${d.pct}%)`;

  document.getElementById('previewImg').src = d.cropped_preview_b64;
  document.getElementById('previewCard').style.display = 'block';

  const resDiv = document.getElementById('results');
  d.top.forEach((c, i) => {
    const div = document.createElement('div');
    div.className = 'cand' + (i===0 ? ' best' : '');
    const methodNames = {none:'без сглаживания', sma:'SMA', ema:'EMA', wma:'WMA', rma:'Уайлдер (RMA)'};
    const srcNames = {volume:'объём', price_change:'изм.цены', range:'диапазон бара',
                       upper_wick:'верхняя тень', lower_wick:'нижняя тень', body:'тело свечи'};
    const mLabel = methodNames[c.smoothing_method] || c.smoothing_method;
    const srcLabel = srcNames[c.source] || c.source;
    let paramsLine;
    if(c.volume_mode === 'dual_source'){
      const srcSlowLabel = srcNames[c.source_slow] || c.source_slow;
      paramsLine = `dual_source: зелёная=${srcLabel} / красная=${srcSlowLabel}, ${mLabel} период ${c.ema_period} | floor_frac=${c.floor_frac}`;
    } else if(c.volume_mode === 'dual_period'){
      paramsLine = `dual_period (${srcLabel}), ${mLabel}: период ${c.ema_period}/${c.ema_period_slow} | floor_frac=${c.floor_frac}`;
    } else {
      paramsLine = `directional (${srcLabel}), ${mLabel}${c.smoothing_method!=='none' ? ' период '+c.ema_period : ''} | floor_frac=${c.floor_frac}`;
    }
    div.innerHTML = `<div class="score">${i===0?'★ ЛУЧШИЙ — ':''}итог ${c.combined_score} (стиль ${c.score}, баланс ${c.balance_score})</div>`
      + `<div class="params">${paramsLine} | окно ${c.window_start}-${c.window_end} | (g=${c.score_g} r=${c.score_r}) | доля красного: эталон ${c.ref_balance} / кандидат ${c.cand_balance}</div>`
      + `<img src="${c.png_b64}">`;
    resDiv.appendChild(div);
  });
  if(d.top.length){
    statusEl.innerHTML += ' — лучшие параметры сохранены и уже применяются в реальных алертах';
  }
}
</script></body></html>"""



class Handler(http.server.BaseHTTPRequestHandler):
    def log_message(self, *a): pass

    def _json(self, obj, code=200):
        body = json.dumps(obj, ensure_ascii=False).encode()
        self.send_response(code)
        self.send_header("Content-Type","application/json")
        self.send_header("Content-Length",len(body))
        self.send_header("Cache-Control","no-store, no-cache, must-revalidate, max-age=0")
        self.send_header("Pragma","no-cache")
        self.end_headers()
        self.wfile.write(body)

    def _json_download(self, obj, filename_prefix):
        # то же, что _json, но с Content-Disposition: attachment — браузер
        # на телефоне сохранит файл вместо того, чтобы показать JSON текстом
        body = json.dumps(obj, ensure_ascii=False, indent=2).encode()
        fname = f"{filename_prefix}_{time.strftime('%Y%m%d_%H%M')}.json"
        self.send_response(200)
        self.send_header("Content-Type", "application/json; charset=utf-8")
        self.send_header("Content-Disposition", f'attachment; filename="{fname}"')
        self.send_header("Content-Length", len(body))
        self.end_headers()
        self.wfile.write(body)

    def do_GET(self):
      try:
        if self.path == "/" or self.path == "/index.html":
            self.send_response(302)
            self.send_header("Location", "/ema")
            self.end_headers()
        elif self.path == "/ema" or self.path == "/ema.html":
            body = PUMP_MAIN_HTML_PAGE.replace("__APP_VERSION__", APP_VERSION).encode()
            self.send_response(200)
            self.send_header("Content-Type","text/html; charset=utf-8")
            self.send_header("Content-Length",len(body))
            self.send_header("Cache-Control","no-store, no-cache, must-revalidate, max-age=0")
            self.end_headers()
            self.wfile.write(body)
        elif self.path == "/ema_full":
            # старая полная страница EMA-инверт движка (досье/сигналы/авто-
            # трейд) — сам движок сейчас не запущен (см. main()), поэтому
            # таблицы тут будут пустые; оставлено на случай, если понадобится
            # вернуться к этой фиче — правкой одной строки в main().
            body = EMA_HTML_PAGE.replace("__APP_VERSION__", APP_VERSION).encode()
            self.send_response(200)
            self.send_header("Content-Type","text/html; charset=utf-8")
            self.send_header("Content-Length",len(body))
            self.send_header("Cache-Control","no-store, no-cache, must-revalidate, max-age=0")
            self.end_headers()
            self.wfile.write(body)
        elif self.path == "/ema_logs":
            with opt_lock:
                self._json({"logs": opt_state["logs"], "logs_dropped": opt_state.get("logs_dropped", 0)})
        elif self.path == "/ema_dossier_status":
            self._json(_load_ema_dossier_state())
        elif self.path == "/ema_live_signals":
            with _ema_live_lock:
                self._json(dict(_ema_live_state))
        elif self.path == "/ema_signal_history":
            with _ema_history_lock:
                state = _load_ema_history()
            items = sorted(state["items"].values(), key=lambda v: v["opened_at"], reverse=True)
            closed = [v for v in items if v["status"] in ("tp", "sl")]
            tp_n = sum(1 for v in closed if v["status"] == "tp")
            winrate = round(tp_n / len(closed) * 100, 1) if closed else None
            self._json({"items": items, "closed": len(closed), "tp": tp_n,
                         "sl": len(closed) - tp_n, "winrate": winrate})
        elif self.path == "/ema_auto_trade_status":
            with ema_auto_trade_lock:
                cfg = dict(ema_auto_trade_state)
            with _ema_history_lock:
                state = _load_ema_history()
            live_items = [v for v in state["items"].values()
                          if v.get("status") == "open" and v.get("live")]
            balance = None
            try:
                balance = _gate_get_balance()
            except Exception:
                pass
            cfg["live_count"] = len(live_items)
            cfg["live_positions"] = [
                {"symbol": v["symbol"], "dir": v["dir"], "entry": v["price"],
                 "sl": v["sl"], "tp": v["tp"], "size": v.get("live_size"),
                 "leverage": v.get("live_leverage"), "notional": v.get("live_notional"),
                 "opened_at": v["opened_at"]}
                for v in live_items
            ]
            cfg["balance"] = balance
            cfg["gate_configured"] = bool(GATE_KEY and GATE_SECRET)
            self._json(cfg)
        elif self.path == "/gate_cfg":
            self._json({"gate_key": GATE_KEY[:4]+"***" if GATE_KEY else "",
                        "gate_secret": "***" if GATE_SECRET else "",
                        "has_key": bool(GATE_KEY and GATE_SECRET)})
        elif self.path == "/pump_status":
            # v0.1.0: статус Live Pump Detector — сколько монет отслеживается
            # прямо сейчас и последние сработавшие пампы (из истории на диске).
            with _pump_detect_lock:
                tracked = len(_pump_price_history)
            recent = []
            try:
                if os.path.exists(PUMP_DETECT_HISTORY_FILE):
                    with open(PUMP_DETECT_HISTORY_FILE) as f:
                        recent = json.load(f)
            except Exception:
                recent = []
            self._json({"tracked": tracked, "recent": recent[-100:]})
        elif self.path == "/dump_status":
            # v0.13.0: зеркало /pump_status для дампов — общая история цен
            # с пампом (_pump_price_history), отдельный лог срабатываний.
            with _pump_detect_lock:
                tracked = len(_pump_price_history)
            recent = []
            try:
                if os.path.exists(DUMP_DETECT_HISTORY_FILE):
                    with open(DUMP_DETECT_HISTORY_FILE) as f:
                        recent = json.load(f)
            except Exception:
                recent = []
            self._json({"tracked": tracked, "recent": recent[-100:]})
        elif self.path == "/vol_anomaly_status":
            recent = []
            try:
                if os.path.exists(VOL_ANOMALY_HISTORY_FILE):
                    with open(VOL_ANOMALY_HISTORY_FILE) as f:
                        recent = json.load(f)
            except Exception:
                recent = []
            self._json({"recent": recent[-100:]})
        elif self.path == "/vol_peak_watchlist_status":
            with _vol_peak_lock:
                snapshot = {sym: len(peaks) for sym, peaks in _vol_peak_watchlist.items() if peaks}
            total = sum(snapshot.values())
            self._json({"total_watched": total, "coins": len(snapshot), "by_symbol": snapshot})
        elif self.path == "/weekly_ema_status":
            with _weekly_ema_touch_lock:
                recent = list(_weekly_ema_recent[-100:])
            self._json({"recent": recent})
        elif self.path == "/weekly_ema_source_stats":
            self._json({"stats": _weekly_ema_source_stats()})
        elif self.path == "/pump_export":
            recent = []
            try:
                if os.path.exists(PUMP_DETECT_HISTORY_FILE):
                    with open(PUMP_DETECT_HISTORY_FILE) as f:
                        recent = json.load(f)
            except Exception:
                recent = []
            self._json_download({"exported_at": int(time.time()), "count": len(recent),
                                 "records": recent}, "pump_history")
        elif self.path == "/dump_export":
            recent = []
            try:
                if os.path.exists(DUMP_DETECT_HISTORY_FILE):
                    with open(DUMP_DETECT_HISTORY_FILE) as f:
                        recent = json.load(f)
            except Exception:
                recent = []
            self._json_download({"exported_at": int(time.time()), "count": len(recent),
                                 "records": recent}, "dump_history")
        elif self.path == "/vol_anomaly_export":
            recent = []
            try:
                if os.path.exists(VOL_ANOMALY_HISTORY_FILE):
                    with open(VOL_ANOMALY_HISTORY_FILE) as f:
                        recent = json.load(f)
            except Exception:
                recent = []
            self._json_download({"exported_at": int(time.time()), "count": len(recent),
                                 "records": recent}, "vol_anomaly_history")
        elif self.path == "/ema28w_touch_export":
            recent = []
            try:
                if os.path.exists(EMA28W_TOUCH_HISTORY_FILE):
                    with open(EMA28W_TOUCH_HISTORY_FILE) as f:
                        recent = json.load(f)
            except Exception:
                recent = []
            self._json_download({"exported_at": int(time.time()), "count": len(recent),
                                 "records": recent}, "ema28w_touch_history")
        elif self.path == "/weekly_ema_export":
            with _weekly_ema_touch_lock:
                recent = list(_weekly_ema_recent) + list(_weekly_ema_skipped)
            recent.sort(key=lambda r: r.get("ts", 0))
            self._json_download({"exported_at": int(time.time()), "count": len(recent),
                                 "records": recent}, "weekly_ema_signals")
        elif self.path == "/alert_cfg":
            self._json({"tg_token": TG_TOKEN, "tg_chat": TG_CHAT, "ntfy_url": NTFY_URL,
                        "hc_url": HC_URL,
                        "watchdog_enabled": WATCHDOG_ENABLED,
                        "watchdog_timeout_min": WATCHDOG_TIMEOUT_MIN,
                        "pump_dump_alerts_enabled": PUMP_DUMP_ALERTS_ENABLED})
        elif self.path == "/scan_settings":
            with _scan_settings_lock:
                self._json(dict(_scan_settings))
        elif self.path == "/pump_match" or self.path == "/pump_match.html":
            body = PUMP_MATCH_HTML_PAGE.encode()
            self.send_response(200)
            self.send_header("Content-Type", "text/html; charset=utf-8")
            self.send_header("Content-Length", len(body))
            self.send_header("Cache-Control", "no-store, no-cache, must-revalidate, max-age=0")
            self.end_headers()
            self.wfile.write(body)
        elif self.path.startswith("/pump_match_status"):
            qs = urllib.parse.urlparse(self.path).query
            params = urllib.parse.parse_qs(qs)
            job_id = (params.get("job_id") or [""])[0]
            with _pump_match_jobs_lock:
                job = _pump_match_jobs.get(job_id)
            if not job:
                self._json({"ok": False, "msg": "Задача не найдена (истекла или неверный job_id)"})
            else:
                self._json({"ok": True, "status": job["status"], "done": job["done"],
                            "total": job["total"], "result": job["result"]})
        elif self.path == "/weekly_ema_backtest" or self.path == "/weekly_ema_backtest.html":
            body = WEEKLY_EMA_BACKTEST_HTML_PAGE.encode()
            self.send_response(200)
            self.send_header("Content-Type", "text/html; charset=utf-8")
            self.send_header("Content-Length", len(body))
            self.send_header("Cache-Control", "no-store, no-cache, must-revalidate, max-age=0")
            self.end_headers()
            self.wfile.write(body)
        elif self.path.startswith("/weekly_ema_backtest_status"):
            qs = urllib.parse.urlparse(self.path).query
            params = urllib.parse.parse_qs(qs)
            job_id = (params.get("job_id") or [""])[0]
            with _weekly_ema_backtest_jobs_lock:
                job = _weekly_ema_backtest_jobs.get(job_id)
            if not job:
                self._json({"ok": False, "msg": "Задача не найдена (истекла или неверный job_id)"})
            else:
                self._json({"ok": True, "status": job["status"], "done": job["done"],
                            "total": job["total"], "result": job["result"]})
        elif self.path.startswith("/weekly_ema_backtest_last"):
            # последний сохранённый результат бэктеста (переживает рестарт процесса)
            qs = urllib.parse.urlparse(self.path).query
            params = urllib.parse.parse_qs(qs)
            tf = (params.get("tf") or ["1w"])[0]
            try:
                fpath = _weekly_ema_backtest_file(tf)
                if os.path.exists(fpath):
                    with open(fpath) as f:
                        self._json(json.load(f))
                else:
                    self._json({"ok": False, "msg": f"Бэктест для {tf} ещё не запускался"})
            except Exception as e:
                self._json({"ok": False, "msg": str(e)})
        elif self.path.startswith("/ema_diag_status"):
            qs = urllib.parse.urlparse(self.path).query
            params = urllib.parse.parse_qs(qs)
            tf_filter = (params.get("tf") or [None])[0]
            period_filter = (params.get("period") or [None])[0]
            version_filter = (params.get("diag_version") or [None])[0]
            limit = int((params.get("limit") or [200])[0])
            with _diag_lock:
                items = list(_diag_records)
            if tf_filter:
                items = [r for r in items if r["tf"] == tf_filter]
            if period_filter:
                items = [r for r in items if str(r["period"]) == str(period_filter)]
            if version_filter:
                items = [r for r in items if str(r.get("diag_version", 0)) == str(version_filter)]
            items.sort(key=lambda r: r["ts"], reverse=True)
            self._json({"total": len(items), "recent": items[:limit]})
        elif self.path == "/ema_diag_summary":
            self._json({"summary": _diag_summary()})
        elif self.path == "/ema_diag_export":
            # скачивание ВСЕГО сырого набора (не через _json — та не
            # проставляет Content-Disposition, браузер показал бы JSON
            # текстом вместо предложения сохранить файл)
            with _diag_lock:
                items = list(_diag_records)
            payload = {"exported_at": int(time.time()), "count": len(items),
                       "summary": _diag_summary(), "records": items}
            body = json.dumps(payload, ensure_ascii=False, indent=2).encode()
            fname = f"ema_diagnostics_{time.strftime('%Y%m%d_%H%M')}.json"
            self.send_response(200)
            self.send_header("Content-Type", "application/json; charset=utf-8")
            self.send_header("Content-Disposition", f'attachment; filename="{fname}"')
            self.send_header("Content-Length", len(body))
            self.end_headers()
            self.wfile.write(body)
        elif self.path == "/ema_stretch" or self.path == "/ema_stretch.html":
            body = EMA_STRETCH_HTML_PAGE.encode()
            self.send_response(200)
            self.send_header("Content-Type", "text/html; charset=utf-8")
            self.send_header("Content-Length", len(body))
            self.send_header("Cache-Control", "no-store, no-cache, must-revalidate, max-age=0")
            self.end_headers()
            self.wfile.write(body)
        elif self.path == "/ema_stretch_status":
            with _stretch_diag_lock:
                items = list(_stretch_diag_records)
            self._json({"summary": _stretch_diag_summary(), "count": len(items),
                        "last_cycle_sec": _stretch_diag_last_cycle_sec,
                        "recent": items[-100:]})
        elif self.path == "/stretch_autotrade_settings":
            with _stretch_autotrade_lock:
                cfg = dict(_stretch_autotrade_state)
            with _stretch_diag_lock:
                live_open = [r for r in _stretch_diag_records if r.get("live") and not r.get("live_closed")]
            cfg["gate_configured"] = bool(GATE_KEY and GATE_SECRET)
            cfg["live_positions"] = [{"symbol": r["symbol"], "direction": r["direction"],
                                       "strategy": r.get("strategy"), "entry": r["entry"],
                                       "size": r.get("live_size"), "leverage": r.get("live_leverage"),
                                       "notional": r.get("live_notional"), "opened_at": r.get("live_opened_at")}
                                      for r in live_open]
            self._json(cfg)
        elif self.path == "/stretch_scan_settings":
            # v0.30.95: по прямому запросу — какие ЕМА+ТФ сейчас реально
            # выбраны для скана "Отрыва от EMA" (галочки на странице)
            with _stretch_scan_cfg_lock:
                cfg = dict(_stretch_scan_cfg_state)
            cfg["all_periods"] = list(STRETCH_DIAG_PERIODS)
            cfg["all_timeframes"] = list(STRETCH_DIAG_TIMEFRAMES)
            self._json(cfg)
        elif self.path == "/ema_stretch_export":
            with _stretch_diag_lock:
                items = list(_stretch_diag_records)
            payload = {"exported_at": int(time.time()), "count": len(items),
                       "summary": _stretch_diag_summary(), "records": items}
            body = json.dumps(payload, ensure_ascii=False, indent=2).encode()
            fname = f"ema_stretch_diag_{time.strftime('%Y%m%d_%H%M')}.json"
            self.send_response(200)
            self.send_header("Content-Type", "application/json; charset=utf-8")
            self.send_header("Content-Disposition", f'attachment; filename="{fname}"')
            self.send_header("Content-Length", len(body))
            self.end_headers()
            self.wfile.write(body)
        elif self.path == "/manual_scan" or self.path == "/manual_scan.html":
            body = MANUAL_SCAN_HTML_PAGE.encode()
            self.send_response(200)
            self.send_header("Content-Type", "text/html; charset=utf-8")
            self.send_header("Content-Length", len(body))
            self.send_header("Cache-Control", "no-store, no-cache, must-revalidate, max-age=0")
            self.end_headers()
            self.wfile.write(body)

        elif self.path == "/manual_scan_status":
            recent = []
            try:
                if os.path.exists(MANUAL_SCAN_HISTORY_FILE):
                    with open(MANUAL_SCAN_HISTORY_FILE) as f:
                        recent = json.load(f)
            except Exception:
                recent = []
            self._json({"count": len(recent), "records": recent})

        elif self.path == "/manual_scan_export":
            recent = []
            try:
                if os.path.exists(MANUAL_SCAN_HISTORY_FILE):
                    with open(MANUAL_SCAN_HISTORY_FILE) as f:
                        recent = json.load(f)
            except Exception:
                recent = []
            self._json_download({"exported_at": int(time.time()), "count": len(recent),
                                 "records": recent}, "manual_scans")

        elif self.path == "/manual_scan_chronology_export":
            recent = []
            try:
                if os.path.exists(MANUAL_SCAN_CHRONOLOGY_FILE):
                    with open(MANUAL_SCAN_CHRONOLOGY_FILE) as f:
                        recent = json.load(f)
            except Exception:
                recent = []
            self._json_download({"exported_at": int(time.time()), "count": len(recent),
                                 "records": recent}, "manual_scan_chronology")

        elif self.path == "/ema_diag" or self.path == "/ema_diag.html":
            body = EMA_DIAG_HTML_PAGE.encode()
            self.send_response(200)
            self.send_header("Content-Type", "text/html; charset=utf-8")
            self.send_header("Content-Length", len(body))
            self.send_header("Cache-Control", "no-store, no-cache, must-revalidate, max-age=0")
            self.end_headers()
            self.wfile.write(body)
        else:
            self.send_response(404); self.end_headers()
      except Exception as e:
        try:
            self.send_response(500); self.end_headers()
        except Exception: pass

    def do_POST(self):
        global GATE_KEY, GATE_SECRET   # v0.30.92: реальный синтаксис-баг, найден при компиляции — global должен идти раньше ЛЮБОГО упоминания имени по всей функции, не только в своём блоке; было раскидано по нескольким elif-веткам, теперь один раз в начале
        if self.path == "/pump_match_run":
            self._handle_pump_match_run()
            return
        try:
            length = int(self.headers.get("Content-Length",0))
            body   = json.loads(self.rfile.read(length)) if length else {}
        except Exception as e:
            self._json({"ok":False,"msg":f"bad request: {_explain_error(e)}"}); return

        if self.path == "/alert_cfg":
            global TG_TOKEN, TG_CHAT, NTFY_URL, WATCHDOG_ENABLED, WATCHDOG_TIMEOUT_MIN, HC_URL
            global PUMP_DUMP_ALERTS_ENABLED
            TG_TOKEN = (body.get("tg_token") or "").strip()
            TG_CHAT  = (body.get("tg_chat")  or "").strip()
            NTFY_URL = (body.get("ntfy_url") or "").strip()
            if "hc_url" in body:
                HC_URL = (body.get("hc_url") or "").strip()
            if "watchdog_enabled" in body:
                WATCHDOG_ENABLED = bool(body.get("watchdog_enabled"))
            if "watchdog_timeout_min" in body:
                try:
                    WATCHDOG_TIMEOUT_MIN = max(5, min(1440, int(body.get("watchdog_timeout_min"))))
                except (TypeError, ValueError):
                    pass
            if "pump_dump_alerts_enabled" in body:
                PUMP_DUMP_ALERTS_ENABLED = bool(body.get("pump_dump_alerts_enabled"))
            _save_alert_cfg()
            self._json({"ok": True})

        elif self.path == "/scan_settings":
            kwargs = {}
            errors = []
            if "min_volume_usd" in body:
                try:
                    v = float(body.get("min_volume_usd"))
                    if v < 0:
                        raise ValueError("должно быть ≥ 0")
                    kwargs["min_volume_usd"] = v
                except (TypeError, ValueError) as e:
                    errors.append(f"min_volume_usd: {_explain_error(e)}")
            if "max_spread_pct" in body:
                try:
                    v = float(body.get("max_spread_pct"))
                    if not (0.01 <= v <= 10.0):
                        raise ValueError("вне диапазона 0.01-10")
                    kwargs["max_spread_pct"] = v
                except (TypeError, ValueError) as e:
                    errors.append(f"max_spread_pct: {_explain_error(e)}")
            if "max_symbols_cap" in body:
                try:
                    v = int(body.get("max_symbols_cap"))
                    if not (10 <= v <= 2000):
                        raise ValueError("вне диапазона 10-2000")
                    kwargs["max_symbols_cap"] = v
                except (TypeError, ValueError) as e:
                    errors.append(f"max_symbols_cap: {_explain_error(e)}")
            if "slow_impulse_enabled" in body:
                kwargs["slow_impulse_enabled"] = bool(body.get("slow_impulse_enabled"))
            if "stretch_top_alert_enabled" in body:
                kwargs["stretch_top_alert_enabled"] = bool(body.get("stretch_top_alert_enabled"))
            if errors:
                self._json({"ok": False, "msg": "; ".join(errors)}); return
            _save_scan_settings(**kwargs)
            with _scan_settings_lock:
                self._json({"ok": True, **_scan_settings})

        elif self.path == "/alert_test":
            ok, err = _test_alert()
            self._json({"ok": ok, "error": err})

        elif self.path == "/ema_signal_history_clear":
            with _ema_history_lock:
                _save_ema_history({"items": {}})
            olog("[ema_history] история сигналов очищена вручную")
            self._json({"ok": True})

        elif self.path == "/weekly_ema_history_clear":
            global _weekly_ema_recent
            with _weekly_ema_touch_lock:
                _weekly_ema_recent = []
                _save_weekly_ema_history()
            olog("[weekly_ema] история EMA-сигналов очищена вручную")
            self._json({"ok": True})

        elif self.path == "/ema_stretch_history_clear":
            # v0.30.75: по прямому запросу — старая история "Отрыв от EMA"
            # копилась в период, когда трек-луп был сломан (v0.30.74) и
            # не проверял открытые записи вообще — их реальный исход
            # (взяла ли цена стоп/тейк за то время, пока не проверялась)
            # неизвестен, статистика по ним ненадёжна. Чистим начисто,
            # дальше копим честно, с рабочим трек-лупом.
            # v0.30.76: реальный дедлок, найден по прямому сообщению
            # "нажал удалить, подвисло" — тут держали _stretch_diag_lock и
            # ВНУТРИ него звали _stretch_diag_save(), которая САМА пытается
            # взять тот же лок (threading.Lock не реентерабелен — поток
            # блокируется сам на себе намертво). Запрос никогда не
            # завершался, файл на диске не перезаписывался, после
            # перезапуска подхватывалась старая история с диска.
            global _stretch_diag_records
            with _stretch_diag_lock:
                _stretch_diag_records = []
            _stretch_diag_save()
            olog("[ema_stretch] история отрывов от EMA очищена вручную")
            self._json({"ok": True})

        elif self.path == "/stretch_autotrade_settings":
            # v0.30.92: по прямому запросу — "автоторговля для Long или
            # Short на выбор, % депозита на сделку, тейки/стоп из сигнала".
            # Валидация нарочно строгая — это реальные деньги, ошибка в
            # диапазоне значения может стоить дорого.
            try:
                errors = []
                kwargs = {}
                if "mode" in body:
                    mode = str(body["mode"]).strip().lower()
                    if mode not in ("off", "long", "short"):
                        errors.append(f"mode: должно быть off/long/short, получено {mode!r}")
                    else:
                        kwargs["mode"] = mode
                if "position_pct" in body:
                    pct = float(body["position_pct"])
                    if not (0.1 <= pct <= 20.0):
                        errors.append(f"position_pct: {pct} вне разумного диапазона 0.1-20% (это % ДЕПОЗИТА на одну сделку)")
                    else:
                        kwargs["position_pct"] = pct
                if "leverage" in body:
                    lev = int(body["leverage"])
                    if not (1 <= lev <= MAX_LEVERAGE):
                        errors.append(f"leverage: {lev} вне диапазона 1-{MAX_LEVERAGE}")
                    else:
                        kwargs["leverage"] = lev
                if "max_concurrent" in body:
                    mc = body["max_concurrent"]
                    kwargs["max_concurrent"] = int(mc) if mc not in (None, "") else None
                if "enabled" in body:
                    enabled = bool(body["enabled"])
                    if enabled and not (GATE_KEY and GATE_SECRET):
                        errors.append("нельзя включить — не настроены ключи Gate.io (страница /gate_cfg)")
                    else:
                        kwargs["enabled"] = enabled
                if "configs" in body:
                    # v0.30.95: по прямому запросу — "по какому конфигу
                    # осуществлять автоторговлю, а не по топ 1". Список
                    # строк "tf|period|strategy" — галочки на странице.
                    # Валидация: каждый элемент должен разбираться на
                    # известные tf/period/strategy, иначе опечатка в теле
                    # запроса тихо создала бы "мёртвый" конфиг, который
                    # никогда ни с чем не совпадёт.
                    raw_configs = body["configs"]
                    if not isinstance(raw_configs, list):
                        errors.append("configs: должен быть списком строк вида 'tf|period|strategy'")
                    else:
                        valid_tfs = set(STRETCH_DIAG_TIMEFRAMES)
                        valid_periods = {str(p) for p in STRETCH_DIAG_PERIODS}
                        valid_strategies = {"fade", "continuation"}
                        cleaned = []
                        for c in raw_configs:
                            parts = str(c).split("|")
                            if len(parts) != 3 or parts[0] not in valid_tfs or parts[1] not in valid_periods or parts[2] not in valid_strategies:
                                errors.append(f"configs: неизвестная связка {c!r}")
                                continue
                            cleaned.append(f"{parts[0]}|{parts[1]}|{parts[2]}")
                        if not errors:
                            kwargs["configs"] = cleaned
                if errors:
                    self._json({"ok": False, "msg": "; ".join(errors)}); return
                with _stretch_autotrade_lock:
                    _stretch_autotrade_state.update(kwargs)
                _save_stretch_autotrade_cfg()
                olog(f"[stretch_autotrade] настройки обновлены: {kwargs}")
                with _stretch_autotrade_lock:
                    self._json({"ok": True, **_stretch_autotrade_state})
            except (TypeError, ValueError) as e:
                self._json({"ok": False, "msg": f"ошибка в значении: {_explain_error(e)}"})

        elif self.path == "/stretch_scan_settings":
            # v0.30.95: по прямому запросу — скан "Отрыва от EMA" только по
            # выбранным параметрам ЕМА+ТФ (галочки на странице).
            try:
                errors = []
                kwargs = {}
                if "periods" in body:
                    raw = body["periods"]
                    if not isinstance(raw, list):
                        errors.append("periods: должен быть списком чисел")
                    else:
                        try:
                            periods = [int(p) for p in raw]
                        except (TypeError, ValueError):
                            errors.append("periods: все значения должны быть числами")
                        else:
                            bad = [p for p in periods if p not in STRETCH_DIAG_PERIODS]
                            if bad:
                                errors.append(f"periods: неизвестные значения {bad} (доступны {STRETCH_DIAG_PERIODS})")
                            else:
                                kwargs["periods"] = periods
                if "timeframes" in body:
                    raw = body["timeframes"]
                    if not isinstance(raw, list):
                        errors.append("timeframes: должен быть списком строк")
                    else:
                        bad = [tf for tf in raw if tf not in STRETCH_DIAG_TIMEFRAMES]
                        if bad:
                            errors.append(f"timeframes: неизвестные значения {bad} (доступны {STRETCH_DIAG_TIMEFRAMES})")
                        else:
                            kwargs["timeframes"] = list(raw)
                if errors:
                    self._json({"ok": False, "msg": "; ".join(errors)}); return
                with _stretch_scan_cfg_lock:
                    _stretch_scan_cfg_state.update(kwargs)
                _save_stretch_scan_cfg()
                olog(f"[ema_stretch] настройки скана обновлены: {kwargs}")
                with _stretch_scan_cfg_lock:
                    self._json({"ok": True, **_stretch_scan_cfg_state})
            except (TypeError, ValueError) as e:
                self._json({"ok": False, "msg": f"ошибка в значении: {_explain_error(e)}"})

        elif self.path == "/ema_diag_backfill_extended":
            # v0.30.16: по прямому вопросу — досчитать 12-часовые чекпоинты
            # (v0.30.15) для СТАРЫХ, уже завершённых записей диагностики
            # задним числом, по историческим свечам. Может занять время
            # (сетевой запрос на каждую монету) — запускаем в фоне, не
            # блокируя ответ; итог смотреть в логах ([ema_diag] backfill...).
            threading.Thread(target=_diag_backfill_extended_checkpoints, daemon=True).start()
            olog("[ema_diag] backfill 12ч чекпоинтов запущен вручную — итог появится в логах")
            self._json({"ok": True, "msg": "Запущено в фоне, итог смотри в логах через пару минут"})

        elif self.path == "/manual_scan":
            symbol = (body.get("symbol") or "").strip().upper() if isinstance(body, dict) else ""
            if not symbol:
                self._json({"ok": False, "msg": "Не указан symbol"}); return
            if not symbol.endswith("_USDT"):
                symbol = symbol + "_USDT"
            at_ts = None
            if isinstance(body, dict) and body.get("at_ts"):
                try:
                    at_ts = float(body["at_ts"])
                    if at_ts > time.time():
                        self._json({"ok": False, "msg": "Дата/время в будущем — так не получится"}); return
                except (TypeError, ValueError) as e:
                    self._json({"ok": False, "msg": f"Некорректная дата/время: {_explain_error(e)}"}); return
            side = None
            if isinstance(body, dict) and body.get("side") in ("LONG", "SHORT"):
                side = body["side"]
            ref_entry = ref_stop = ref_take = None
            try:
                if isinstance(body, dict):
                    ref_entry = float(body["ref_entry"]) if body.get("ref_entry") not in (None, "") else None
                    ref_stop = float(body["ref_stop"]) if body.get("ref_stop") not in (None, "") else None
                    ref_take = float(body["ref_take"]) if body.get("ref_take") not in (None, "") else None
            except (TypeError, ValueError) as e:
                self._json({"ok": False, "msg": f"Некорректный вход/стоп/тейк: {_explain_error(e)}"}); return
            try:
                result = _manual_symbol_scan(symbol, at_ts=at_ts, side=side,
                                              ref_entry=ref_entry, ref_stop=ref_stop, ref_take=ref_take)
                self._json({"ok": True, "result": result})
            except Exception as e:
                self._json({"ok": False, "msg": _explain_error(e)})

        elif self.path == "/recheck_history":
            # v0.30.2: по прямому вопросу — "недостаточно истории" кэшируется
            # на 3 дня (EMA_TOUCH_INSUFFICIENT_HISTORY_RECHECK_SEC), чтобы не
            # дёргать биржу зря на заведомо молодых монетах. Если монета
            # только что перевалила за нужный возраст, а ждать до 3 дней не
            # хочется — сбрасываем кэш здесь, следующий скан перепроверит
            # заново. symbol=XXX — конкретная монета; без параметра — все
            # записи с insufficient=True разом.
            symbol = body.get("symbol") if isinstance(body, dict) else None
            with _ema_touch_ctx_lock:
                if symbol:
                    removed = 0
                    for tf in ("1d", "1w"):
                        key = f"{symbol}|{tf}"
                        if key in _ema_touch_ctx_cache and _ema_touch_ctx_cache[key].get("insufficient"):
                            del _ema_touch_ctx_cache[key]
                            removed += 1
                else:
                    stale_keys = [k for k, v in _ema_touch_ctx_cache.items() if v.get("insufficient")]
                    for k in stale_keys:
                        del _ema_touch_ctx_cache[k]
                    removed = len(stale_keys)
            olog(f"[weekly_ema] перепроверка истории запрошена вручную "
                 f"({'монета ' + symbol if symbol else 'все монеты'}) — сброшено записей: {removed}")
            self._json({"ok": True, "reset": removed})

        elif self.path == "/pump_history_clear":
            with _get_history_file_lock(PUMP_DETECT_HISTORY_FILE):
                _pump_dump_history_save_all(PUMP_DETECT_HISTORY_FILE, [])
            olog("[pump_detect] история пампов очищена вручную")
            self._json({"ok": True})

        elif self.path == "/dump_history_clear":
            with _get_history_file_lock(DUMP_DETECT_HISTORY_FILE):
                _pump_dump_history_save_all(DUMP_DETECT_HISTORY_FILE, [])
            olog("[dump_detect] история дампов очищена вручную")
            self._json({"ok": True})

        elif self.path == "/vol_anomaly_history_clear":
            with _get_history_file_lock(VOL_ANOMALY_HISTORY_FILE):
                _pump_dump_history_save_all(VOL_ANOMALY_HISTORY_FILE, [])
            olog("[vol_anomaly] история аномалий объёма очищена вручную")
            self._json({"ok": True})

        elif self.path == "/ema_diag_clear":
            global _diag_records
            with _diag_lock:
                _diag_records = []
            _diag_save()
            olog("[ema_diag] диагностическая история очищена вручную")
            self._json({"ok": True})

        elif self.path == "/ema_dossier_start":
            with _ema_dossier_lock:
                if _ema_dossier_running["v"]:
                    self._json({"ok": False, "msg": "Скан уже идёт"})
                else:
                    _ema_dossier_running["v"] = True
                    def _run():
                        try:
                            _run_ema_dossier_scan(50)
                        finally:
                            with _ema_dossier_lock:
                                _ema_dossier_running["v"] = False
                    threading.Thread(target=_run, daemon=True).start()
                    self._json({"ok": True, "msg": "Скан запущен (топ-50, 3 ТФ x 4 EMA) — статус на /ema_dossier_status"})

        elif self.path == "/gate_cfg":
            GATE_KEY    = (body.get("gate_key")    or "").strip()
            GATE_SECRET = (body.get("gate_secret") or "").strip()
            _save_gate_cfg()
            self._json({"ok": True})

        elif self.path == "/ema_auto_trade_settings":
            try:
                if "enabled" in body:
                    enabled = bool(body["enabled"])
                    if enabled and not (GATE_KEY and GATE_SECRET):
                        self._json({"ok": False, "msg": "Не настроены Gate.io ключи (/gate_cfg)"}); return
                    with ema_auto_trade_lock:
                        ema_auto_trade_state["enabled"] = enabled
                if "position_pct" in body:
                    pct = float(body["position_pct"])
                    assert 0.1 <= pct <= 100.0, f"position_pct вне диапазона: {pct}"
                    with ema_auto_trade_lock:
                        ema_auto_trade_state["position_pct"] = pct
                if "risk_pct" in body:
                    rp = float(body["risk_pct"])
                    assert 0.1 <= rp <= 100.0, f"risk_pct вне диапазона: {rp}"
                    with ema_auto_trade_lock:
                        ema_auto_trade_state["risk_pct"] = rp
                if "max_concurrent" in body:
                    mc = body["max_concurrent"]
                    mc = int(mc) if mc not in (None, "", "null") else None
                    with ema_auto_trade_lock:
                        ema_auto_trade_state["max_concurrent"] = mc
                if "max_forced_margin_pct" in body:
                    mfp = float(body["max_forced_margin_pct"])
                    assert 0.1 <= mfp <= 100.0, f"max_forced_margin_pct вне диапазона: {mfp}"
                    with ema_auto_trade_lock:
                        ema_auto_trade_state["max_forced_margin_pct"] = mfp
                if "forced_size_max_multiple" in body:
                    fsm = float(body["forced_size_max_multiple"])
                    assert 1.0 <= fsm <= 20.0, f"forced_size_max_multiple вне диапазона: {fsm}"
                    with ema_auto_trade_lock:
                        ema_auto_trade_state["forced_size_max_multiple"] = fsm
            except Exception as e:
                self._json({"ok": False, "msg": f"Некорректные параметры: {_explain_error(e)}"}); return
            _save_ema_auto_trade_cfg()
            with ema_auto_trade_lock:
                olog(f"[ema_auto_trade] настройки обновлены: {dict(ema_auto_trade_state)}")
            self._json({"ok": True})

        elif self.path == "/ema_auto_trade_close":
            sym = (body.get("symbol") or "").strip()
            if not sym:
                self._json({"ok": False, "msg": "Не указан symbol"}); return
            try:
                _gate_close_position(sym)
                self._json({"ok": True})
            except Exception as e:
                self._json({"ok": False, "msg": str(e)})

        elif self.path == "/weekly_ema_backtest_run":
            try:
                tf = (body.get("tf") or "1w").strip()
                if tf not in ("1w", "1d"):
                    self._json({"ok": False, "msg": "tf должен быть '1w' или '1d'"}); return
                top_n = int(body.get("top_n") or WEEKLY_EMA_BACKTEST_TOP_N)
                days = int(body.get("days") or WEEKLY_EMA_BACKTEST_DAYS)
                window_min = int(body.get("window_min") or WEEKLY_EMA_REACTION_WINDOW_MIN)
                max_age_days = int(body.get("max_age_days") or WEEKLY_EMA_REACTION_MAX_AGE_DAYS)
                thresholds_raw = body.get("thresholds") or WEEKLY_EMA_REACTION_THRESHOLDS
                thresholds = sorted(float(t) for t in thresholds_raw)
                custom_symbols = body.get("symbols")  # необязательный явный список вместо топ-N
            except (TypeError, ValueError) as e:
                self._json({"ok": False, "msg": f"Некорректные параметры: {_explain_error(e)}"}); return
            if custom_symbols and isinstance(custom_symbols, list):
                symbols = [s.strip().upper() for s in custom_symbols if s.strip()]
            else:
                symbols = _fetch_all_symbols()[:top_n]
            if not symbols:
                self._json({"ok": False, "msg": "Не удалось получить список монет"}); return
            job_id = _weekly_ema_backtest_new_job()
            threading.Thread(
                target=_weekly_ema_backtest_run_background,
                args=(job_id, symbols, tf, days, window_min, thresholds, max_age_days),
                daemon=True,
            ).start()
            self._json({"ok": True, "job_id": job_id, "symbols_count": len(symbols)})

        else:
            self.send_response(404); self.end_headers()

    def _handle_pump_match_run(self):
        """multipart/form-data: image + symbol + date + duration_min +
        необязательные search_start/search_end (сузить диапазон поиска
        времени, по умолчанию весь день) + необязательный base_price.
        Отдельный путь от общего do_POST, т.к. тело — не JSON, а файл с
        полями."""
        try:
            content_type = self.headers.get("Content-Type", "")
            length = int(self.headers.get("Content-Length", 0))
            raw_body = self.rfile.read(length) if length else b""
            if "multipart/form-data" not in content_type:
                self._json({"ok": False, "msg": "Ожидался multipart/form-data"}); return
            fields = _pm_parse_multipart(raw_body, content_type)
            if "image" not in fields or not fields["image"][0]:
                self._json({"ok": False, "msg": "Файл картинки не пришёл"}); return
            image_bytes = fields["image"][0]

            def _f(name, default=""):
                v = fields.get(name)
                return v[0].decode("utf-8", "ignore").strip() if v else default

            symbol = _f("symbol")
            date_s = _f("date")
            duration_s = _f("duration_min", "120")
            search_start_s = _f("search_start", "00:00")
            search_end_s = _f("search_end", "23:59")
            base_s = _f("base_price")
            if not (symbol and date_s):
                self._json({"ok": False, "msg": "Не хватает полей (symbol/date)"}); return

            try:
                duration_min = max(3, int(float(duration_s)))
            except ValueError:
                self._json({"ok": False, "msg": "duration_min должен быть числом"}); return

            import datetime as _dt
            try:
                search_start_dt = _dt.datetime.strptime(f"{date_s} {search_start_s}", "%Y-%m-%d %H:%M")
                search_end_dt = _dt.datetime.strptime(f"{date_s} {search_end_s}", "%Y-%m-%d %H:%M")
                search_start_ts = time.mktime(search_start_dt.timetuple())
                search_end_ts = time.mktime(search_end_dt.timetuple())
            except Exception as e:
                self._json({"ok": False, "msg": f"Не смог разобрать дату/диапазон поиска: {_explain_error(e)}"}); return
            if search_end_ts <= search_start_ts:
                self._json({"ok": False, "msg": "Конец диапазона поиска должен быть позже начала"}); return

            base_price_override = None
            if base_s:
                try:
                    base_price_override = float(base_s)
                except ValueError:
                    pass

            job_id = _pump_match_new_job()
            threading.Thread(
                target=_pump_match_run_background,
                args=(job_id, image_bytes, symbol.upper(), search_start_ts, search_end_ts,
                      duration_min, base_price_override),
                daemon=True,
            ).start()
            self._json({"ok": True, "job_id": job_id})
        except Exception as e:
            olog(f"[pump_match] ⚠ ошибка обработки запроса: {_explain_error(e)}")
            try:
                self._json({"ok": False, "msg": f"Внутренняя ошибка: {_explain_error(e)}"})
            except Exception:
                pass

def main():
    global TG_TOKEN, TG_CHAT, NTFY_URL
    TG_TOKEN  = os.environ.get("TG_TOKEN", TG_TOKEN)
    TG_CHAT   = os.environ.get("TG_CHAT",  TG_CHAT)
    NTFY_URL  = os.environ.get("NTFY_URL", NTFY_URL)
    _load_alert_cfg()
    _load_gate_cfg()
    _load_ema_auto_trade_cfg()
    _load_stretch_autotrade_cfg()   # v0.30.92: по прямому запросу — автоторговля для отрывов от EMA, по умолчанию enabled=False/mode=off, не активна без явного включения через UI
    _load_stretch_scan_cfg()   # v0.30.95: по прямому запросу — выбранные ЕМА+ТФ для скана "Отрыва от EMA", по умолчанию (нет файла) — выбрано всё
    _load_pump_render_params()
    # v0.15.0: КРИТИЧНЫЙ фикс — раньше история EMA-сигналов подхватывалась
    # только внутри _weekly_ema_touch_loop; когда в v0.13.0 этот цикл
    # остановили (заменили на триггер от пампа/дампа), подгрузка истории
    # при старте молча перестала происходить. После любого перезапуска
    # процесса (падение Termux, потеря сессии, OOM) все ОТКРЫТЫЕ сигналы
    # теряли отслеживание — на диске оставались, но в памяти пропадали, и
    # _weekly_ema_resolve_loop их больше не видел. Теперь вызывается здесь,
    # не зависит от того, какие циклы включены.
    _load_weekly_ema_history()
    _migrate_open_signals_to_fixed_take()
    _load_scan_settings()
    _diag_load()
    _stretch_diag_load()
    threading.Thread(target=_watchdog_loop, daemon=True).start()
    threading.Thread(target=_telegram_sender_worker, daemon=True).start()
    threading.Thread(target=_heartbeat_loop, daemon=True).start()
    # v0.5.0: остановлен по запросу — уходим от EMA-инверт как основной
    # фичи, главная страница теперь только про пампы. Код движка остался в
    # файле нетронутым (можно вернуть одной строкой), просто больше не
    # крутится в фоне — не дёргает Gate.io и не может случайно открыть
    # реальную сделку через _ema_maybe_open_live_trade без видимого UI.
    # threading.Thread(target=_ema_signal_loop, daemon=True).start()
    threading.Thread(target=_weekly_ema_resolve_loop, daemon=True).start()
    threading.Thread(target=_slow_impulse_scan_loop, daemon=True).start()
    threading.Thread(target=_diag_scan_loop, daemon=True).start()
    threading.Thread(target=_diag_track_loop, daemon=True).start()
    threading.Thread(target=_stretch_diag_scan_loop, daemon=True).start()
    threading.Thread(target=_stretch_diag_track_loop, daemon=True).start()
    threading.Thread(target=_stretch_autotrade_watch_loop, daemon=True).start()   # v0.30.92: наблюдает за реально открытыми live-позициями, шлёт алерт с реальным PnL при закрытии
    # v0.30.47: по прямому запросу — убрано всё лишнее относительно цели
    # "скопировать его бота". Проверено по ВСЕМ собранным сравнениям: ни
    # один из его 22+ сигналов ни разу не пришёл через памп/дамп или
    # аномалию объёма (Volume Peak Watcher) — только через slow_impulse.
    # EMA28W-трекинг зависел ИСКЛЮЧИТЕЛЬНО от пампа/дампа/аномалии как
    # источников (свой отдельный, независимый триггер) — без них ему
    # неоткуда брать события. Код всех функций остался в файле нетронутым
    # (как и раньше поступали с _ema_signal_loop/_weekly_ema_touch_loop) —
    # просто не запускается, ничего не потребляет и не может сломать
    # slow_impulse, ручной скан или бэктест, которые от этого не зависят.
    # threading.Thread(target=_pump_detect_loop, daemon=True).start()
    # threading.Thread(target=_pump_dump_track_loop, daemon=True).start()
    # threading.Thread(target=_vol_peak_find_loop, daemon=True).start()
    # threading.Thread(target=_vol_peak_watch_loop, daemon=True).start()
    # threading.Thread(target=_vol_anomaly_track_loop, daemon=True).start()
    # threading.Thread(target=_ema28w_touch_track_loop, daemon=True).start()
    # v0.13.0: полностью заменено на триггер от пампа/дампа (см.
    # _pump_or_dump_maybe_trigger_ema_signal, вызывается изнутри
    # _pump_fire_alert/_dump_fire_alert) — постоянный опрос топ-100 монет
    # независимо от того, происходит там что-то или нет, больше не нужен.
    # Функция _weekly_ema_touch_loop осталась в файле нетронутой на случай
    # отката — просто не запускается.
    # threading.Thread(target=_weekly_ema_touch_loop, daemon=True).start()

    server = http.server.ThreadingHTTPServer(("0.0.0.0", PORT), Handler)
    print(f"{_C_GRN}Pump Radar v{APP_VERSION} — http://0.0.0.0:{PORT}{_C_RST}", flush=True)
    try:
        server.serve_forever()
    except KeyboardInterrupt:
        print("\nЗавершено"); server.shutdown()
    except Exception as e:
        # v0.15.0: раньше ловили только KeyboardInterrupt — любая другая
        # неожиданная ошибка в serve_forever() убивала процесс без единой
        # строчки лога о причине. Теперь хотя бы напечатаем и пробросим
        # дальше — внешний while-цикл ниже перезапустит main() сам.
        print(f"{_C_RED}main() упал: {_explain_error(e)}{_C_RST}", flush=True)
        try:
            server.shutdown()
        except Exception:
            pass
        raise

if __name__ == "__main__":
    multiprocessing.freeze_support()
    try:
        multiprocessing.set_start_method("spawn", force=True)
    except RuntimeError:
        pass
    # v0.15.0: защита от падения — "интернет пропал/термукс упал" снаружи
    # процесса не починить (для этого нужен внешний супервизор, см.
    # run_pump_radar.sh), но ЕСЛИ упадёт сам процесс Python (а не вся
    # Termux-сессия) — например временный сетевой сбой уронит поток внутри
    # main() — здесь он не просто умирает молча, а перезапускается сам,
    # с паузой и логом каждой попытки.
    _restart_count = 0
    while True:
        try:
            main()
            break  # server.serve_forever() вышел штатно (KeyboardInterrupt) — не перезапускаем
        except Exception as e:
            _restart_count += 1
            print(f"{_C_RED}Процесс упал ({_restart_count}-й раз): {_explain_error(e)} — "
                  f"перезапуск через 10с{_C_RST}", flush=True)
            time.sleep(10)
